[project]
name = "omnia-api"
version = "0.0.1"
description = "Omnia.AI — Backend Core (FastAPI)"
readme = "README.md"
requires-python = ">=3.12"
authors = [{ name = "Omnia.AI" }]
license = { text = "Proprietary" }

dependencies = [
    "fastapi>=0.136,<0.137",
    "uvicorn[standard]>=0.30,<1.0",
    "sqlalchemy[asyncio]>=2.0,<3.0",
    "asyncpg>=0.29,<1.0",
    "alembic>=1.13,<2.0",
    "redis>=5.0,<6.0",
    "rq>=1.16,<2.0",
    "minio>=7.2,<8.0",
    "pygit2>=1.15,<2.0",
    "playwright>=1.47,<2.0",
    "pyjwt>=2.10,<3.0",
    "cryptography>=48.0.1,<49.0",
    "passlib[bcrypt]>=1.7,<2.0",
    # passlib 1.7.x detect_wrap_bug check fails on bcrypt>=4.1 — pin to 4.0.x.
    "bcrypt>=4.0,<4.1",
    "pydantic>=2.7,<3.0",
    "pydantic-settings>=2.14.2,<3.0",
    "email-validator>=2.2,<3.0",
    "structlog>=24.1,<25.0",
    "httpx>=0.27,<1.0",
    "slowapi>=0.1.9,<1.0",
    "python-slugify>=8.0,<9.0",
    "jinja2>=3.1,<4.0",
    "pillow>=12.3,<13",
    "langdetect>=1.0.9",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3,<10.0",
    "pytest-asyncio>=1.3,<2.0",
    "ruff>=0.8,<1.0",
    "mypy>=1.13,<2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/omnia_api"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
asyncio_mode = "auto"
# Every async fixture and test runs on a per-function event loop. The DB engine
# fixture is function-scoped too (see conftest), so its asyncpg pool is created
# and used on the same loop — avoids "another operation is in progress" that a
# session-scoped pool hits when reused from a later test's loop (pytest-asyncio
# defaults to per-function loops since 0.24). Setting this explicitly also
# silences the unset-default deprecation warning.
asyncio_default_fixture_loop_scope = "function"
addopts = "-q"
filterwarnings = [
    "ignore::DeprecationWarning:passlib.*",
]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E", "F", "W",
    "I",
    "B",
    "UP",
    "RUF",
    "ASYNC",
]
ignore = [
    "B008",  # FastAPI Depends() idiom
    # Продукт намеренно русскоязычный: проверки «неоднозначного юникода»
    # (кириллические с/о/е…, en-dash) — чистый шум на промптах и UI-строках.
    "RUF001",
    "RUF002",
    "RUF003",
]

[tool.ruff.lint.per-file-ignores]
"migrations/**" = ["E501"]
"tests/**" = ["B"]

[tool.ruff.lint.isort]
known-first-party = ["omnia_api"]

[tool.mypy]
python_version = "3.12"
strict = true
plugins = ["pydantic.mypy"]
exclude = ["migrations/"]

[[tool.mypy.overrides]]
module = ["passlib.*", "rq.*", "pygit2.*", "minio.*", "slugify.*"]
ignore_missing_imports = true
