# Vendored AI Generation Skills

External design / UX knowledge bundles vendored into the API repo so prod
LLM-gateway nodes ship with deterministic, version-pinned content.

Loaders live at `apps/api/src/omnia_api/services/skill_library.py`.

## `ui-ux-pro-max/`

Source: Claude Code skill `ui-ux-pro-max` from `~/.claude/skills/ui-ux-pro-max/`
(`SKILL.md` frontmatter `name: ui-ux-pro-max`).

Snapshot date: 2026-05-25. Content is CSV-backed, regenerated by upstream's
own `data/_sync_all.py`.

What's inside (`data/`):

| File | Rows | Use |
|---|---:|---|
| `colors.csv` | 161 | WCAG-safe palettes by product type (SaaS, e-com, healthcare, fintech, gaming, …). Each row: primary / accent / background / foreground / card / muted / border / destructive / ring. |
| `typography.csv` | 57 | Font pairings (heading + body) with Google-Fonts import URL + Tailwind config snippet + mood/best-for keywords. |
| `ux-guidelines.csv` | 99 | UX rules across Navigation / Animation / Forms / Loading / Mobile / Accessibility / Performance. Each row: do / don't + code example + severity. |
| `styles.csv` | ~50 | Visual style presets (glassmorphism, brutalism, bento, neumorphism, …). |
| `products.csv` | 161 | Product-type reasoning rules. |
| `charts.csv` | 25 | Chart-type recommendations by data-shape. |
| `google-fonts.csv` | ~1k | Full Google Fonts catalogue (largest file at 728 KB). |
| `icons.csv`, `landing.csv`, `app-interface.csv`, `design.csv`, `ui-reasoning.csv`, `react-performance.csv` | — | Supplementary tables. |

`scripts/` ships upstream's own helpers (`core.py`, `design_system.py`,
`search.py`). They aren't imported by the API — `skill_library.py` reads
CSVs directly so the surface stays narrow.

## License

`ui-ux-pro-max` is distributed under the upstream skill's license (see
`SKILL.md` frontmatter; check before redistribution outside this private
repo). The snapshot is included for runtime use inside Omnia.AI; do not
re-publish externally without verifying terms.

## Integration status

- **Vendored** — files are in the repo, deploy artefacts include them.
- **Loader live** — `skill_library.lookup_palette`, `lookup_font_pairing`,
  `random_ux_guidelines`, `format_design_brief` ready to call.
- **Prompt-builder wiring** — NOT YET. Upstream `services/prompt_builder.py`
  has a rich `_DESIGN_KIT` + `_STYLE_KIT` + preset system already. Plugging
  the larger ui-ux-pro-max library in requires extending the preset flow
  rather than replacing it. See `format_design_brief` for the suggested
  injection shape; a follow-up PR should hook it in.
