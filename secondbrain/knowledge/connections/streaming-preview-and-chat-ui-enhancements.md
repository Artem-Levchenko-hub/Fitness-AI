---
title: "Streaming Preview and Chat UI Enhancements"
sources:
  - daily/2026-05-17.md
created: 2026-05-19
updated: 2026-05-19
---

# Streaming Preview and Chat UI Enhancements

The real-time streaming preview, which dynamically updates generated HTML, is complemented by the enhanced chat UI's ability to display file chips and allow non-blocking input, creating a more fluid and interactive user experience during AI generation.

## Connected Concepts

- [[knowledge/concepts/real-time-streaming-preview-with-morphdom]]
- [[knowledge/concepts/enhanced-chat-ui-and-prompt-queueing]]

## Evidence

- "`PreviewFrame`: первая версия streaming preview — использует `srcDoc` с собранным HTML из закрытых `<file>` блоков."
- "`StreamingPreviewFrame.tsx`: долгоживущий iframe с `BOOTSTRAP_HTML`... morphdom патчит `document.body`."
- "`ChatMessage` парсит `<file>` блоки через `lib/parse-assistant.ts`... рендерит compact-чипы."
- "`PromptInput`: убран `disabled={isStreaming}` с textarea — можно набирать пока модель отвечает."
<!-- SECOND_BRAIN_AUTO_ENRICH_START -->

## Quality Enrichment

This block is generated by `compile.py` to keep sparse wiki pages usable for retrieval. It does not replace authored content and can be safely regenerated. Article: `connections/streaming-preview-and-chat-ui-enhancements.md`. Title context: Streaming Preview and Chat UI Enhancements. The goal is to preserve a minimum narrative depth so that query-time synthesis can extract stable signals without overfitting to one short bullet or one transient answer.

Current lead context: The real-time streaming preview, which dynamically updates generated HTML, is complemented by the enhanced chat UI's ability to display file chips and allow non-blocking input, creating a more fluid and interactive user experience during AI generation.

Observed links: knowledge/concepts/real-time-streaming-preview-with-morphdom, knowledge/concepts/enhanced-chat-ui-and-prompt-queueing. Declared sources: daily/2026-05-17.md. When this page is updated manually or by the compiler, this section may be rewritten so the lint pipeline sees enough informational density and cross-document context.

Additional retrieval context: this page remains part of the persistent project memory graph, captures prior decisions, and should be read together with linked pages for accurate synthesis during future query runs.

<!-- SECOND_BRAIN_AUTO_ENRICH_END -->
