# Daily — 2026-05-17

## Sessions

### Session: Haiku via proxyapi.ru + T1 fix + Streaming Preview + CodeView + Compact Cards + SecondBrain port

**Контекст:** Один длинный chat (Sun 17 May 2026), Claude Opus 4.7 (1M context). Цели накапливались по ходу — каждая решённая задача рождала следующую.

#### Что сделано (хронологически, с коммитами):

1. **`84a8986` `feat: add Claude Haiku 4.5 via proxyapi.ru + surface 0-files failure`**
   - Добавил `claude-haiku-4-5` в каталог моделей (`apps/llm-gateway/.../pricing.py`).
   - Routing: `anthropic/claude-haiku-4-5` slug + `api_base=https://api.proxyapi.ru/anthropic` (без `/v1` — LiteLLM сам добавит `/v1/messages`). Через override-механизм `_PROXY_ROUTES` в `services/litellm_router.py`.
   - Per-model availability в `routers/models.py`: `_MODEL_KEY_OVERRIDE` для haiku (читает PROXYAPI_API_KEY, не ANTHROPIC).
   - `core/config.py`: новые поля `proxyapi_api_key`, `proxyapi_base_url`.
   - **T1-fix:** `apps/api/.../routers/messages.py:308` — теперь когда `extract_files()` возвращает {}, шлём `llm.error` с подсказкой про Haiku/Sonnet. До этого был silent fail: только `llm.done`, без snapshot — UI выглядел «всё ок» но preview не обновлялся.
   - `conftest.py`: использовать `setenv("")` вместо `delenv` — pydantic-settings читает `.env` файл независимо от `os.environ`.
   - Обновил `docs/01-api-contract.md` и `agents/AGENT-C-LLM-GATEWAY.md`.
   - Деплой: prod `.env` обновлён (`DEFAULT_MODEL=claude-haiku-4-5`, `PROXYAPI_API_KEY`+`PROXYAPI_BASE_URL`), `docker compose up --build gateway api worker`.
   - Верификация: `/v1/models` показывает `claude-haiku-4-5 ON`, реальный chat-completion проходит (PROD-OK, 0.0075 ₽).

2. **`9c80dfe` `feat(web): compact chat with file chips + non-blocking input + live srcDoc preview`**
   - `ChatMessage` парсит `<file>` блоки через `lib/parse-assistant.ts` (новый файл) и рендерит compact-чипы с expand-on-click + размер в KB.
   - `PromptInput`: убран `disabled={isStreaming}` с textarea — можно набирать пока модель отвечает.
   - `PreviewFrame`: первая версия streaming preview — использует `srcDoc` с собранным HTML из закрытых `<file>` блоков. Inline-ит CSS из `<link rel=stylesheet>` тегов.

3. **`a49a1d9` `fix(web): Stop actually stops + Enter queues during streaming`**
   - `cancel()` в `usePromptStream`: теперь не только закрывает WS, но и помечает текущее ассистентское сообщение завершённым в React Query кэше (`tokens_out=0` + `[Отменено пользователем]`) — UI разлочивается мгновенно.
   - **Очередь промптов:** `submit()` во время стрима кладёт в `pendingRef` (single slot). На `llm.done` / `llm.error` автоматом fires через `setTimeout` (для разрыва recursion).
   - UI: одновременно видны **Стоп** + **Send/В очередь**. Баннер «В очереди: …» с × для удаления.
   - `llm.error` тоже теперь ставит `tokens_out: 0` — UI разлочивается на ошибке.

4. **`01943b5` `feat(web): real-time per-element preview via long-lived iframe + morphdom`**
   - **Новый `StreamingPreviewFrame.tsx`:** долгоживущий iframe с `BOOTSTRAP_HTML` (Tailwind CDN + morphdom UMD CDN + анимационный CSS keyframe + `<style id="omnia-css">` слот + skeleton placeholder).
   - **Bootstrap script:** слушает `window.message` для `{type:'omnia:render', bodyHtml, cssText}`. `DOMParser` строит новый body, top-level children без существующего совпадения получают `data-omnia-new` → CSS keyframe fade+slide-up 250ms cubic-bezier(0.16,1,0.3,1). morphdom патчит `document.body` с `childrenOnly + skip-if-equal`. CSS обновляется через `document.getElementById('omnia-css').textContent = cssText` — без флекера.
   - **PreviewFrame** показывает StreamingPreviewFrame пока `isStreaming && !selectedSnapshotId`, потом AnimatePresence flip на committed iframe `/p/<slug>?snapshot=<id>`.
   - **parse-assistant.ts**: добавил `collectStreamingFilesPartial` (включает closed:false) и `extractStreamingBody` (последний `<file path="index.html">`, открытый или закрытый).
   - Дебаунс 150ms через `useEffect` cleanup, memo по `content.length`.

5. **`016dca4` `feat(web): explicit Code view + "viewing old version" banner`**
   - **CodeView.tsx (новый):** файл-дерево слева (отсортирован, размеры в KB), `<pre>` справа с Copy/Download buttons. Тянет `GET /api/projects/:pid/snapshots/:sid` (`SnapshotWithFiles`).
   - **PreviewFrame:** таб-переключатель **Preview** / **Код**. Device toggle и reload-кнопка скрываются в режиме «Код».
   - **Янтарный баннер** «Просматриваете старую версию (X назад) — sha» с линком «Вернуться к текущей →» когда `selectedSnapshotId && visible.id !== snapshots[0].id`.
   - `workspace.ts` store: новое поле `viewMode: "preview" | "code"`, `setViewMode` action. Дефолтная модель сменена с `gigachat-2` на `claude-haiku-4-5` (совпадает с прод `DEFAULT_MODEL`).
   - `lib/api/snapshots.ts`: новая функция `getSnapshotWithFiles(projectId, snapshotId)`.

6. **`9e232b2` `feat(web): compact snapshot cards with hover-scale`**
   - SnapshotCard уменьшил: prompt-текст 2 строки → 1, badges 11px → 9px, кнопка «Откатить» h-9 → h-6.
   - `whileHover={{ scale: 1.04 }}` с cubic-bezier(0.16,1,0.3,1) — карточка приподнимается при наведении.
   - `hover:z-10 + shadow-xl` — magnified card визуально приподнимается над соседями без клиппинга.

7. **Этот коммит:** port SecondBrain из CorporateMessanger.
   - Bulk-копирование `scripts/` (16 файлов) + `hooks/` (5 файлов) + `templates/` через bash.
   - Adapt: `sync_project_docs.py` (DOC_DIRS под `docs/`, `agents/`, `apps/*/CLAUDE.md`), `update_context.py` (path split `omnia-mvp/`), `hooks/post-tool-use.py` (CONTEXT_TRIGGERS под Omnia-структуру).
   - Написал Omnia-specific README, AGENTS, project-context, index, log.
   - `.claude/settings.json` с 5 хуками (SessionStart/SessionEnd/PreCompact/PostToolUse/Stop).
   - `.gitignore` для state-файлов.
   - Seed concept-страницы (см. Memory Maintenance ниже).

## Docs Ingest

(empty — `sync_project_docs.py` ещё не запускался; первый запуск создаст блок с хэшами всех md в docs/ + agents/)

## Memory Maintenance

(empty — `sync_memory.py` запустится автоматически при следующем session-start)

## Seeded concepts

- `[[knowledge/concepts/proxyapi-anthropic-route]]`
- `[[knowledge/concepts/realtime-streaming-preview]]`
- `[[knowledge/concepts/file-extractor-pipeline]]`
- `[[knowledge/concepts/zero-files-silent-failure]]`
- `[[knowledge/concepts/secondbrain-runtime]]` (port из мессенджера)
- `[[knowledge/concepts/auto-memory-bridge]]` (port из мессенджера)
- `[[knowledge/concepts/wiki-taxonomy-and-link-conventions]]` (port)
