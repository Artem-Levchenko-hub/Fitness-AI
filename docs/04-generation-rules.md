# 04. Свод правил генерации — как AI пишет сайты в Omnia.AI

> **Цель документа.** Зафиксировать единый стандарт того, что Omnia.AI выдаёт
> пользователю по промпту. Принцип один: **«готовое решение, а не страничка»** —
> один промпт обязан давать законченный продукт enterprise-уровня.
>
> **Операционный источник правил** — система-промпт в
> [`apps/api/src/omnia_api/services/prompt_builder.py`](../apps/api/src/omnia_api/services/prompt_builder.py).
> Этот документ — человекочитаемая версия с обоснованием. Меняешь правила —
> синхронизируй оба.

## 1. Static-first: почему статика по умолчанию

Превью обязано показывать сайт **без исключений и мгновенно**. Этого даёт только
статика:

- **Статический сайт** (`index.html` + Tailwind CDN + vanilla JS) рендерится
  напрямую из git через `/p/<slug>` за миллисекунды. Контейнер не нужен.
- **Next.js/full-stack** рендерится только в живом dev-контейнере (оркестратор +
  Docker + nginx), с холодным стартом 30–60 c. Пока контейнер не поднят —
  показать нечего. Это и есть источник «белого экрана на TypeScript».

Поэтому **режим определяется типом проекта** (`projects.template`), а не желанием
модели писать модный стек:

| Тип проекта (`template`) | Режим | Чем рендерится превью |
|---|---|---|
| `blank`, `landing`, `portfolio`, `blog` | **Статика** | `/p/<slug>` (git → файл) |
| `fullstack` | **Next.js + Postgres + Drizzle** | dev-контейнер оркестратора |

**Следствие для модели:** в статическом проекте НИКОГДА не генерируй `.tsx`,
`package.json` и прочий Next.js — его нечем показать. Если задача реально требует
сервера (личный кабинет, оплата, хранение заявок в БД) — сделай лучший
статический вариант и одной строкой предложи создать full-stack проект.

## 2. Стандарт качества (применяется к обоим режимам)

### 2.1 Полнота
Лендинг — это **7–9 осмысленных секций**, а не один экран: sticky-шапка → hero →
блок доверия → ценность/возможности → как это работает → отзывы → цены → FAQ →
финальный CTA → футер. Набор адаптируется под продукт (портфолио, блог, меню,
мероприятие), но страница всегда цельная и «прокручиваемая».

### 2.2 Контент
Настоящий и конкретный, по-русски, в тоне бренда: реальные офферы, цифры, цены в
₽, имена, города. **Запрещено:** lorem ipsum, «Заголовок 1», «ваш текст здесь»,
повтор одного абзаца, пустые `<a href="#">`. Нет данных — придумай правдоподобные.

### 2.3 Дизайн-система (Refactoring UI)
- Одна палитра: фирменный/акцентный цвет + нейтральная шкала (slate/zinc/stone,
  **не** чистый `#000` на `#fff`) + семантика. Контраст текста ≥ **4.5:1**.
- Максимум 2 шрифта (заголовочный + текстовый), Google Fonts с `display=swap`.
  База 16px, line-height 1.5–1.7, заголовки — крупные, `tracking-tight`.
- Типографическая шкала (14/16/18/20/24/30/36/48/60), не случайные размеры.
- Ритм отступов кратен **4/8**. Секции просторные (`py-16…py-28`), контейнер
  `max-w-6xl/7xl mx-auto px-6`. Глубина — слоями (фон → карточка → мягкая тень),
  а не рамками везде. Консистентные скругления.

### 2.3.1 Дизайн-кит — готовые пресеты (ui-ux-pro-max)

Чтобы сайт не выглядел «дефолтным», модель определяет индустрию и берёт ближайший
пресет как основу (палитра → семантические токены, пара шрифтов → Google Fonts).
Если ничего не подходит — собирает свой цельный набор, но **никогда** не оставляет
«голый» Tailwind. Все палитры WCAG-безопасны (контраст ≥ 4.5:1).

| Индустрия | primary / accent / нейтраль | Шрифты (заголовок + текст) |
|---|---|---|
| Бизнес-услуги, B2B | `#0F172A` / `#0369A1` / slate | Poppins + Open Sans |
| SaaS, IT, стартап | `#2563EB` / `#EA580C` / slate | Space Grotesk + DM Sans |
| Бьюти, спа, салон | `#EC4899` / `#8B5CF6` / stone | Playfair Display + Inter |
| Ресторан, доставка | `#DC2626` / `#A16207` / stone | Playfair Display SC + Karla |
| Фитнес, спорт (тёмный фон) | `#F97316` / `#16A34A` | Barlow Condensed + Barlow |
| Недвижимость | `#0F766E` / `#0369A1` / slate | Cinzel + Josefin Sans |
| Медицина, клиника | `#0891B2` / `#16A34A` / slate | Figtree + Noto Sans |
| Юристы, финансы | `#1E3A8A` / `#B45309` / slate | EB Garamond + Lato |
| Интернет-магазин | `#059669` / `#EA580C` / slate | Rubik + Nunito Sans |
| Образование, курсы | `#0D9488` / `#EA580C` / slate | Lexend + Source Sans 3 |
| Премиум / luxury | `#1C1917` / `#A16207` / stone | Cormorant + Montserrat |
| Портфолио, креатив | `#18181B` / `#2563EB` / zinc | Space Grotesk + Archivo |

Источник — skill `ui-ux-pro-max` (палитры `colors.csv`, пары шрифтов
`typography.csv`); в промпте зашиты в `_DESIGN_KIT`.

**Коммерческие шрифты с референсов → бесплатные аналоги** (генерация = только Google
Fonts / Fontshare): Aeonik · Neue Montreal · Neubau → **Hanken Grotesk / General Sans /
Schibsted Grotesk**; GT Flexa → **Bricolage Grotesque / Anybody**; URW DIN → **Saira /
Oswald**; FS Kim · Perpetua · Stelvio → **Fraunces / Instrument Serif / EB Garamond**;
Peak → **Fredoka / Quicksand**; Helvetica Neue → **Inter / Geist**.

### 2.3.2 Визуальные пресеты (стиль) — `_STYLE_KIT`

Поверх палитры модель выбирает **один** визуальный пресет под нишу — это снимает с
неё «изобретение» вида. Стиль адаптивен: где-то смело, где-то строго.

| Пресет | Приём (классы кита) | Тема | Для ниш |
|---|---|---|---|
| Apple Tech | макс. воздух, `.display-hero`, ч/б + 1 акцент, `.grain`, `.reveal--blur`, `data-parallax` | свет/тёмный | электроника, премиум-продукт |
| Fintech Trust | navy + электрик, `.glass-dark`, KPI `data-count-to`, `.gradient-border` | тёмный | банк, финтех, инвестиции |
| Linear Dark | `.bg-mesh`, `.gradient-text`, `.glow`, `.tilt`, неон-акцент | тёмный | SaaS, IT, dev-tools |
| Editorial Luxury | serif, чёрный + золото, `.img-zoom`, `.divider-fade`, `.grain` | тёмный/свет | люкс, мода, ювелирка |
| Vibrant Consumer | `.bg-aurora`, `rounded-3xl`, `.hover-lift`, `.shine`, `.magnetic` | светлый | еда, доставка, ивенты, D2C |
| Clinical Trust | teal/blue, чистая сетка, `.card-soft`, KPI `data-count-to` | светлый | медицина, клиники |
| Bold Studio | гигантские буквы `.display-hero`, асимметрия, `.marquee`, `.tilt` | свет/тёмный | агентство, портфолио, креатив |
| Kinetic Type | тип как герой `.display-fill`+`.kinetic-type`, монохром + электрик-акцент | чаще тёмный | афиша, манифест, дизайн-студия |
| Refined Minimal | один грайтеск + сдержанная палитра (одно поле / ч-б) + крупный тип `.display-fill`, воздух | свет/тёмный | **дефолт**: бизнес, услуги, корпоратив |
| Nordic Minimal | швейцарская сетка + природная приглушённая палитра + грайтеск ± serif, hairline, ноль теней | светлый | эко, дом/мебель, велнес, крафт |

Один стиль на весь сайт; палитра берётся из §2.3.1 (пресеты её переиспользуют). Если
ниша не считывается — **Refined Minimal по умолчанию** (референсы: carterco.us, studiosentempo.com).

### 2.3.3 Сигнатурные приёмы из референсов — `_SIGNATURE_MOVES`

«Изюминки» уровня топ-студий (Awwwards/Godly), дистиллированные из реальных
референс-сайтов: повторяем **приём**, не вёрстку. Модель берёт 1–2 под нишу и
характер бренда. Общий принцип — **сдержанность + уверенность**: монохром/почти-моно
+ один акцент, крупный шрифт, воздух, движение по делу.

| Приём | Что делает | Классы кита | Референс-источник |
|---|---|---|---|
| Тип как герой | заголовок во весь экран, иерархия масштабом, игра осей вариативного шрифта | `.display-fill`, `.kinetic-type` | gt-flexa.com |
| Архитектурный кроп | фото получает осмысленный силуэт (горы ↔ skyline) на плоском цветном поле, заголовок внахлёст | `.clip-arch`, `.text-protect` | brooksengland.com/scape |
| Безрамочная инверсия | ритм через чередование светлая→тёмная секций с инверсией текста, без линий/рамок | — (Tailwind `bg`/`text`) | efesto.studio |
| Тихие кейсы / лента доверия | крупное фото-обложка с минимумом текста; бегущая лента лого | `.img-zoom`+`.reveal`, `.marquee` | efesto.studio |
| Whitespace как материал | сдержанность: одно заявление-заголовок, разреженная курируемая сетка | — (воздух, `.reveal`) | k95.it |
| Глянцевая графика без модели | кластер морфящихся гради-сфер + grain = «3D-ish» арт-герой без картинок (дёшево) | `.blob`, `.bg-mesh`, `.grain` | studiosentempo.com, polybion.bio |
| Премиум смус-скролл | инерционная прокрутка (Lenis по CDN), reduced-motion off; «дорогое» ощущение | — (Lenis CDN + init) | studiofreight.com |
| Крупные арт-фото | одно большое художественно-кадрированное фото вместо россыпи мелких | `.img-zoom` | brooksengland.com, k95.it |

**Как добавить новый референс:** распознать приём → дать класс(ы) кита (или новый) →
одна строка-описание здесь и в `_SIGNATURE_MOVES`. Приёмы статического режима.

### 2.4 Иерархия и полировка
Одна главная CTA на экран, вторичные приглушены. У всех кликабельных элементов —
`hover`/`focus` состояния и `transition` 150–300 мс. Иконки — **инлайновый SVG**
(Heroicons/Lucide, stroke 1.5–2), **никогда не эмодзи**. Микро-анимации появления
секций — ненавязчиво и с уважением к `prefers-reduced-motion`.

### 2.5 Адаптив (mobile-first)
Корректно на 375 / 768 / 1024 / 1440. Без горизонтального скролла. На мобиле —
рабочее бургер-меню. Сетки перестраиваются (`grid-cols-1 → md:grid-cols-3`).

### 2.6 Доступность
Семантика (`header/nav/main/section/footer`), заголовки `h1→h6` без пропусков,
ровно один `<h1>`. `alt` у картинок, `aria-label` у иконочных кнопок, видимый
focus-ring. Смысл — не только цветом.

### 2.7 SEO и мета
Уникальный `<title>`, `<meta name="description">`, `<html lang="ru">`, Open Graph
(`og:title/description/type/image`), favicon. Осмысленная иерархия заголовков.

### 2.8 Интерактив и контракт «ноль тупиков»
Готовый продукт значит, что **каждый** кликабельный элемент куда-то ведёт и
что-то делает (промпт — `_FUNCTIONAL_CONTRACT`):
- **Якоря:** секциям заранее заданы осмысленные `id`; пункты меню и кнопки-якоря
  ссылаются только на существующие `id` этой же страницы; плавный скролл.
- **CTA → реальная цель:** скролл к секции, `tel:`, `mailto:`, `https://wa.me/…`,
  `https://t.me/…`, карта/соцсеть. Конечная цель главной CTA — заявка/контакт.
- **JS реально работает:** бургер, аккордеон FAQ, «наверх», табы, слайдер.
- **Форма:** `preventDefault` + валидация (required/email/телефон) + видимый
  success-state. `action` — `mailto:` или formspree-плейсхолдер.
- **Внешние ссылки** — `target="_blank" rel="noopener"`; соцсети — реальные URL.
- **Запрещено:** `href="#"`, `href=""`, `javascript:void(0)`, пустой `onclick`,
  кнопка-обманка, пункт меню на несуществующий `id`.

### 2.9 Цельность
Один визуальный язык на весь сайт: единые кнопки, карточки, ритм секций.

### 2.10 Финальная самопроверка
Перед выдачей модель мысленно проходит чек-лист (`_SELF_CHECK`): выбран пресет и
шрифты; 7–9 секций с реальным контентом и ценами в ₽; **каждая** `<a>`/`<button>`
имеет существующую цель (якорь → есть секция с этим `id`); ничего из запрещённого
списка; адаптив на 375px; корневой `index.html` и семантика на месте. Любой
невыполненный пункт исправляется до ответа.

## 3. Статический стек (режим по умолчанию)

- Чистый HTML5 + CSS + vanilla JS, без сборщиков и npm.
- Tailwind через CDN; кастомизация — инлайн `tailwind.config = {…}` перед
  использованием. Шрифты — Google Fonts через `<link>`.
- **Главная — всегда `index.html` в корне репозитория.** Это то, что рендерит
  превью; без него сайт не покажется. В любом ответе, меняющем вёрстку, корневой
  `index.html` обязан присутствовать.
- Доп. страницы — отдельные `.html` в корне, относительные ссылки.
- Картинки — `picsum.photos` / `placehold.co` с `alt` и заданными размерами,
  либо инлайновый SVG / CSS-градиент.
- Формы — `mailto:` или внешний приёмник (formspree) + клиентская валидация.

### 3.1 Встроенный кит анимаций — `assets/omnia-kit.{css,js}`

Чтобы даже слабая модель (Claude Haiku) давала плавный, рабочий сайт, анимации и
интерактив вынесены в готовый кит, который **лежит в каждом статическом шаблоне** и
подключается генерацией. Модель НЕ пишет свой CSS/JS для этого и НЕ переписывает
kit-файлы — только проставляет классы и хуки (документировано в `_ANIMATION_KIT`).

- **CSS-классы:** `.reveal` (+`--left/--right/--scale`, `data-reveal-delay="1..8"`),
  `.fade-in/.fade-up/.scale-in`, `.hover-lift/.hover-glow/.press`, `.glass/.glass-dark`,
  `.card-soft`, `.bg-aurora` (`--a1..a3`) / `.bg-mesh` (`--m1..m3`), `.gradient-text`
  (`--g1/--g2`), `.divider-fade`.
- **v2 эффекты:** `.display-hero/.display-xl`, `.gradient-border`, `.glow/.glow-pulse`,
  `.magnetic/.tilt`, `.img-zoom`, `.shine`, `.marquee`, `.grain`, `.sticky-top`,
  `data-parallax`, `data-count-to`; детали `.eyebrow/.orb/.dot-grid/.badge/.avatar/.stars/.bento/.connector/.has-tip`.
- **v2.3 сигнатурные:** `.display-fill` (тип во весь экран), `.kinetic-type` (морф осей
  вариативного шрифта на hover; `--wght-from/-to`, `--wdth-from/-to`), `.clip-arch`
  (силуэт-кроп фото; `--clip`), `.text-protect` (читаемость текста поверх фото).
- **v2.4 графика:** `.blob` — глянцевая морфящаяся гради-сфера (`--b1` основа / `--b2`
  блик); кластер + `.grain` = «3D-ish» арт-герой без картинок (бесплатно).
- **JS-хуки:** бургер `#menu-toggle`+`#mobile-menu`, FAQ `.faq>.faq-item>(.faq-question,
  .faq-answer)`, `#back-to-top`, плавный скролл по `a[href^="#"]`. Всё за
  `prefers-reduced-motion`; при отсутствии хука — тихий no-op.
- **Подключение:** в `<head>` каждой страницы `<link rel="stylesheet" href="assets/omnia-kit.css">`
  + `<script src="assets/omnia-kit.js" defer></script>`.
- **Управляется Omnia:** `KIT_FILES` фильтруется из контекста и из ответа модели в
  `messages.py`; если модель уронила теги — `_ensure_kit_linked` переинжектит их.
  Шаблонные `index.html` — готовые анимированные экземпляры (worked-example).

## 4. Full-stack стек (только для `fullstack`-проектов)

Next.js 15 App Router + React 19 + TypeScript + Tailwind v4 + Drizzle ORM. Полный
свод конвенций (импорты Drizzle, server actions, что нельзя трогать) — в
[`apps/orchestrator/templates/nextjs-postgres-drizzle/SYSTEM_PROMPT.md`](../apps/orchestrator/templates/nextjs-postgres-drizzle/SYSTEM_PROMPT.md)
и в `_FULLSTACK_STACK` промпта. Стандарт качества из §2 применяется и здесь.

## 5. Анти-примеры: «страничка» vs «готовое решение»

| ❌ Страничка (так НЕ надо) | ✅ Готовое решение |
|---|---|
| Hero + 3 пустые карточки + «Цены» без цен | 8 секций с реальным контентом и ценами в ₽ |
| «Заголовок, бьющий в боль клиента» | «Запишитесь на стрижку за 60 секунд — мастера с опытом 8+ лет» |
| `#000` на `#fff`, один размер шрифта | палитра slate + акцент, типошкала, ритм 4/8 |
| Эмодзи вместо иконок | инлайновый SVG |
| Нет адаптива, нет focus-состояний | mobile-first, бургер-меню, hover/focus |
| `.tsx` в статическом проекте → белый экран | корневой `index.html`, рендерится сразу |

## 6. Где это включено в коде

- **Промпт:** `prompt_builder.py` → `_IDENTITY` + `_QUALITY_BAR` + (статика: `_TASTE`) +
  `_DESIGN_KIT` + (статика: `_STYLE_KIT` + `_DETAILS_KIT` + `_SIGNATURE_MOVES`) +
  `_FUNCTIONAL_CONTRACT` + (`_STATIC_STACK` + `_ANIMATION_KIT` \| `_FULLSTACK_STACK`) +
  `_SELF_CHECK` + `_RESPONSE`; режим выбирает `build_system_prompt(template)`.
- **Встроенный кит:** `assets/omnia-kit.{css,js}` в каждом статическом шаблоне (готовые
  анимированные `index.html`-экземпляры); `KIT_FILES` защищён в `messages.py` — фильтр
  из контекста, дроп из ответа модели, `_ensure_kit_linked` переинжектит теги.
- **Серверная страховка от мёртвых ссылок:** `services/link_validator.py`
  (`find_dead_links`) + one-shot repair-pass в `_process_prompt`: если в статике
  нашлись `href="#"`/битые якоря, модель один раз перегенерирует с конкретным
  списком проблем (коммит при этом никогда не блокируется).
- **Передача типа проекта:** `routers/messages.py` читает `project.template` и
  отдаёт его в `build_messages(...)`.
- **Надёжность превью:** `routers/public.py` никогда не отдаёт пустой экран (см.
  [`docs/07-v2-architecture.md`](07-v2-architecture.md) про full-stack runtime).
