# Fitness SaaS — Epic: AI-разбор v3 + UX + соц (2026-06-09)

**Branch:** `epic/fitness-upgrades` (от `origin/master` @ b8c2eaf). Режим: автономно, один поток.
**Это живой файл** — чеклист внизу. Любая новая сессия: прочитать этот файл → продолжить с первого незакрытого пункта.

## 🔴 НОВЫЙ ЭПИК G — багфиксы + UX-передел (запрос владельца 2026-06-10, утро) — **ВЫСШИЙ ПРИОРИТЕТ, начинать отсюда**
Владелец вернулся со списком РЕАЛЬНЫХ багов (данные считаются неверно) + UX-передел. **Багфиксы важнее фич — корректность данных в первую очередь.** Каждый баг: воспроизвести репро на проде → починить → доказать что репро больше не воспроизводится. Порядок приоритета: G1 → G2 → G4 → G5 → G3 → G6 → G7 (G7 крупный, бить на много ранов).

- **G1 — БАГ: графики на `/stats` считают НЕ только мои завершённые тренировки** (включают активные/незавершённые/«удалённые»). Владелец: «графики по 30 тренировкам, а не по моим». **Root cause найден:** `lib/repos/stats.repo.ts` — `dailyVolume`/`weeklyVolume` (и аналогичные) джойнят `workouts` БЕЗ фильтра `status='completed'` → активные и брошенные сессии попадают в объём/графики. Усугубляется G2 (вечно-активная сессия копит объём). Фикс: добавить `eq(schema.workouts.status, "completed")` во ВСЕ stats-запросы (dailyVolume, weeklyVolume, e1RM-тренды, любые агрегаты). Проверить: delete = хард-удаление (нет `deletedAt`, FK cascade) → удалённые реально исчезают; если владелец всё равно видит удалённые — проверить что кнопка удаления реально вызывает `deleteWorkout`, а не просто скрывает. **Графики должны строиться РОВНО по тем тренировкам, что видны в истории `/workouts` (completed).**
- **G2 — БАГ: активную тренировку нельзя убрать, висит всегда** даже после «завершить»/прокликивания. `finishWorkout` (strength, `workouts.repo.ts:276`) и `getActiveWorkoutId` (:534) КОРРЕКТНЫ (flip active→completed, фильтр status='active'). → баг в КРУГОВОЙ или КАРДИО finish-пути (владелец про круговую). Проверить `finishCircuit`/`finishCardio` (или эквивалент) в `circuits.repo`/`cardio.repo` + соответствующие server actions + кнопки в `active-circuit.tsx`/`active-cardio.tsx`: реально ли flip `status` в `completed`, нет ли пути где сессия создаётся но не финишится. Также добавить пользователю способ ОТМЕНИТЬ/удалить активную сессию (`deleteCircuit`/`deleteCardio` + кнопка на active-экране и в ResumeBanner). Репро: создать круговую, завершить, убедиться что ResumeBanner исчез И она completed.
- **G4 — БАГ: в круговой вес сохраняется не к тому упражнению.** Владелец: на приседаниях ввёл вес приседаний, на подтягиваниях — свой; но вес записался не туда → фантомный прогресс +50 кг. Каждое упражнение круга должно хранить СВОЙ вес/повторы по своему `circuitExerciseId`, не по индексу раунда/позиции. Investigate: `active-circuit.tsx` (как собирается ввод по упражнениям × раундам), `server/actions/circuits` (saveCircuitSet/аналог), `circuits.repo` (куда пишется weight). Вероятно перепутан ключ (раунд vs упражнение). Репро: круговая с присед+подтягивания, разный вес → проверить в БД что вес лёг к правильному упражнению.
- **G5 — БАГ/защита: невозможный прогресс (+50 кг за неделю)** — добавить sanity-guard. Человек не прогрессирует на 50 кг/неделю → это признак ошибки ввода (как в G4) ИЛИ опечатки. В PR-детекции (`lib/domain/...` 1RM/PR) и в AI-разборе: при дельте веса/e1RM выше правдоподобного порога (напр. >20% или >X кг за <Y дней) — НЕ считать рекордом молча; помечать как «подозрительный скачок, проверь ввод» (бейдж/предупреждение), AI должен оговаривать это, а не хвалить за нереальный прогресс. Не блокировать сохранение, но не врать в статистике/разборе.
- **G3 — ФИЧА: моментальный + КРАСИВЫЙ AI-разбор после завершения.** Сейчас стрим есть (F8), но результат выглядит как сплошной текст без цвета. Хочет: завершил → сразу красивая анимация «тренер думает» → текст появляется живо → итог = КРАСИВАЯ карточка с цветом, форматированием, и сравнением с ПРОШЛОЙ такой же тренировкой (тот же шаблон/формат). Investigate почему показывается plain markdown а не `TrainerResultCard` с цветными дельтами (F4) — возможно сохранённый разбор рендерит markdown `content` вместо `resultJson`, или дельты пустые. Убедиться: (а) разбор стартует моментально при завершении (не нужен отдельный заход на /trainer); (б) анимация «думает» (скелетон уже есть — F8 run-3 — но владелец хочет красивее); (в) финал = цветная карточка с per-exercise сравнением vs предыдущая сессия того же типа. Это полировка цепочки F4+F8, а не новый бэкенд.
- **G6 — UX: графики понятнее людям** (не визуал — смысл). Сейчас объём/e1RM абстрактны. Сделать понятным: подписи человеческим языком, «что это значит», явный вывод (растёшь/стоишь/падаешь по конкретному упражнению), сравнение период-к-периоду словами. Меньше жаргона (kg·reps), больше «ты поднял на X% больше чем месяц назад». Можно: топ-движения с трендом, простые карточки-инсайты над графиком.
- **G7 — UX-передел архитектуры (КРУПНЫЙ, бить на много ранов).** Целевой флоу владельца: (1) «Создать шаблон» → выбрал формат (круговая / Tabata / обычная силовая / …) → конструктор шаблона (сам процесс создания СЕЙЧАС норм, не трогать). (2) В следующий раз заходит в «Тренировки» → выбирает нужный СЕГОДНЯ шаблон и просто выполняет. (3) Прямо в тренировке/шаблоне указать ПО КАКИМ ДНЯМ её делать → на главной/в тренировках подсвечивается «сегодня надо такую». (Сейчас расписание живёт отдельно в `/schedule` + ведёт на `/create`-пикер, а не на конкретный шаблон — связать `workout_schedules` с templateId/circuitId/cardioId = миграция, pg_dump!). (4) Комментарий после завершения тренировки (F3 feeling-note есть для силовой — распространить на круговую/кардио, и показывать его в истории/разборе). Бить: G7a = расписание привязать к конкретному шаблону (миграция) + «сегодня» подсветка; G7b = быстрый запуск шаблона из «сегодня»; G7c = комментарий после завершения для всех форматов. Подтверждение направления = прямой запрос владельца здесь.
- **G1·G2·G4·G5·G3·G6(ПОЛНОСТЬЮ: core-объём + per-упражнение mover)·G7a·G7b·G7c ВСЕ ПОЛНОСТЬЮ ЗАКРЫТЫ** (см. RESUME-блоки + чеклист). **ЭПИК G ПОЛНОСТЬЮ ЗАКРЫТ.** Запрос владельца «один поток по тренировкам, а не три вкладки» — проверено в этом ране: уже реализовано полностью (F9, master `afb5b56`; nav=1 таб «Тренировки», `/workouts` сливает 3 формата через `buildHistory`, `/circuits`·`/cardio`→redirect на `/workouts`, дашборд «Недавние»+«Последняя»+единый ResumeBanner) — переделывать нечего, регрессий нет. **NEXT-ACTION = ждать новый запрос владельца** (эпики F+G закрыты; см. опц. хвосты G2b ниже).
- **G2b ✅ ЗАКРЫТ (master `e05e1c8`):** кнопка «Убрать» прямо на ResumeBanner (дашборд + /workouts) для зависшей активной круговой/кардио — отменяет сессию без захода в неё, с обязательным подтверждением (ConfirmDeleteButton → случайный тап не теряет сессию). Силовая «Убрать» не несёт (её finish корректен, safe `cancel*Action` есть только у круговой/кардио). Verify: оба формата cancelled end-to-end на проде (банер исчез + БД status=cancelled). См. RESUME ниже.

## ⏯ RESUME (2026-06-11, F4 verify ЗАКРЫТ на реальной истории) — **последний реальный `[ ]` эпика закрыт; F+G ПОЛНОСТЬЮ закрыты, NEXT=ждать запрос владельца**
- **Слайс выбран осознанно:** R-37-хвост детальных роутов с LIST-скелетом в родителе исчерпан (прошлые раны). Единственный оставшийся реальный `[ ]` = **F4 «verify дельт на реальной истории»** (line 429). Прошлые ~6 ранов откладывали его как «не ночной слайс — нужен реальный DeepSeek-вызов». Но это verify-ONLY (нет правок кода/деплоя/миграции), полностью обратимо (throwaway-юзер + FK cascade), Step-6-санкционировано, и напрямую валидирует ГЛАВНУЮ фичу владельца G3 (красивый цветной AI-разбор со сравнением). Запрос владельца «один поток, не три вкладки» — перепроверен по коду в этом ране (nav=1 таб «Тренировки»→`/workouts`, `/circuits`+`/cardio`→redirect, `/create`=единый пикер) — давно реализован (F9), переделывать нечего.
- **Что доказано (прод, реальный DeepSeek):** `exerciseComparisons` — **модель-генерируемые** (DeepSeek structured output по контексту с прошлыми сессиями set-by-set), не детерминированные. Засеял 2 completed силовые одного упр. (Жим лёжа: A 7д назад 60×5, B сегодня 60×6) → ai_job(on_demand,B) → trigger cron `process-ai-jobs` → job `succeeded`. `result_json.exerciseComparisons` = `[{name:"Жим лёжа со штангой", prevTopSet:"60×5", curTopSet:"60×6", deltaReps:1, deltaWeightKg:0, status:"improved"}]` — модель корректно прочитала прошлую сессию и посчитала дельту. Runtime `/workouts/B/trainer` (authed throwaway puppeteer): `TrainerResultCard` рисует цветную `ComparisonRow` «60×5 → 60×6 ↑», row `bg-success/10`, span `text-success` (computed green) = цветная карточка, НЕ plain markdown (page рендерит `resultJson`, не `content`). **Закрывает F4 + подтверждает G3.**
- **Без правок кода:** только plan-update. Тест-юзер + сид-скрипт удалены (cascade: analyses=0, workouts=0), прод git-tree чист (`git status --porcelain` пуст), деплой не нужен (нет code-дельты; prod HEAD `f742108`). Прод здоров.
- **NEXT-ACTION: ждать новый запрос владельца.** **Эпики F и G ПОЛНОСТЬЮ закрыты — НИ ОДНОГО `[ ]` не осталось.** Опц. остаток низкой ценности (по мере касания, не приоритет): R-37 edit-роуты (`/templates/[id]/edit`, `/exercises/[id]/edit`) и sub-роуты (`/workouts/[id]/coach|trainer`) — у их родителей уже detail-скелет, промах формы минимален.

## ⏯ RESUME (2026-06-11, R-37 detail-skeleton для /friends/[friendId]) — **продолжение R-37-хвоста на детальных роутах; эпики F+G закрыты, NEXT=ждать запрос владельца**
- **Слайс выбран осознанно:** единственный `[ ]` в плане = F4 «verify дельт на реальной истории» (нужен реальный DeepSeek-разбор, не ночной слайс). Запрос владельца «один поток, не три вкладки» (повторён в задании рана) — давно реализован (F9), многократно re-verified — переделывать нечего. Прошлые раны закрыли детальные роуты, **родитель которых имеет LIST-скелет**: `/workouts/[id]`, `/templates/[id]`, `/exercises/[id]`. Оставшийся такой роут = **`/friends/[friendId]`** — async (`getFriendProfile` + `listRecentWorkouts(30)`), без своего `loading.tsx` → при навигации `/friends → /friends/[friendId]` вспыхивал родительский `/friends/loading.tsx` = скелет СПИСКА друзей («Добавить друга» + список друзей) — не та форма для профиля друга с карточками тренировок (R-37: loading совпадает с контентом).
- **Сделано (master `f742108`):** создан `app/(app)/friends/[friendId]/loading.tsx` — детальный скелет: назад + шапка (аватар `size-12` + «Только просмотр» + имя) + 3 read-only карточки тренировок (дата + название + 3-кол KPI-грид), повторяет структуру `page.tsx`. Чистый server-компонент, `sr-only role=status`, `aria-hidden` на визуале. Только новый файл, 0 правок логики/page/схемы — additive Suspense-fallback, рантайм-риск нулевой.
- **Gate:** typecheck ✅ lint ✅ test (57) ✅ build ✅; lefthook pre-commit зелёный.
- **Deploy+verify:** prod HEAD `f742108`, deployed `loading.tsx` совпал с исходником; build-форма роута идентична known-good `/workouts/[id]` (Next 16 инлайнит loading-границу — отдельного `loading.js` нет даже у рабочих роутов → не регрессия); `/friends` рендерится 200 с новым сиблингом (дерево сегмента не сломано). Тест-юзер подчищен.
- **Честное ограничение verify:** наблюдать сам вспыхивающий скелет требует подтверждённой дружбы (у throwaway-юзера друзей нет) — не сидил. Корректность формы доказана code-review против `page.tsx` + зелёным билдом; риск нулевой (additive fallback).
- **NEXT-ACTION:** R-37-хвост детальных роутов с LIST-скелетом в родителе **исчерпан** (`/workouts/[id]`,`/templates/[id]`,`/exercises/[id]`,`/friends/[friendId]` закрыты). Эпики F+G закрыты, единственный `[ ]`=F4 (нужен реальный DeepSeek-разбор). **Ждать запрос владельца.** Опц. остаток (низкая ценность): edit-роуты (`/templates/[id]/edit`, `/exercises/[id]/edit`) и sub-роуты (`/workouts/[id]/coach|trainer`) — у их родителей УЖЕ detail-скелет, промах формы минимален.

## ⏯ RESUME (2026-06-11, R-37 detail-skeleton для /exercises/[id]) — **продолжение R-37-хвоста на детальных роутах; эпики F+G закрыты, NEXT=ждать запрос владельца**
- **Слайс выбран осознанно:** единственный `[ ]` в плане = F4 «verify дельт на реальной истории» (нужен реальный DeepSeek-разбор, не ночной слайс). Запрос владельца «один поток по тренировкам, не три вкладки» (повторён в задании рана) — давно реализован (F9, memory `fitness-unified-workout-feed`), многократно re-verified — переделывать нечего. Прошлые раны закрыли `/workouts/[id]` + `/templates/[id]`. Продолжён хвост на детальных роутах, **родитель которых имеет LIST-скелет** (там промах формы реальный): `/exercises/[id]` (NEXT-ACTION прошлого рана). `exercises/[id]/page.tsx` — async (`getExerciseById`), без своего `loading.tsx` → при навигации `/exercises → /exercises/[id]` вспыхивал родительский `/exercises/loading.tsx` = скелет СПИСКА упражнений (поиск + фильтр-чипы + сетка карточек) — не та форма для детали (R-37: loading совпадает с контентом).
- **Реализация (master `ce7c7e3`, 1 НОВЫЙ файл, аддитивно, без правок логики, без миграции, обратимо):** `app/(app)/exercises/[id]/loading.tsx` (Server Component, `Skeleton`). Повторяет layout детали: кнопка «назад» (h-8 w-24 «Каталог») + header (h1 h-8/9 w-2/3 + nameEn sub h-4 w-1/3) + карточка `bg-card border rounded-2xl p-5 space-y-4` (3 блока: Основные мышцы — label h-3 + 3 badge-скелета `rounded-full`; Вторичные — label + 2 badge; Описание — label + 2 text-строки) + ряд edit/delete (2× `h-10 flex-1`). `aria-hidden` на визуале + `sr-only role=status`. **error.tsx НЕ нужен** — родительский `exercises/error.tsx` бабблится на `[id]` (error-границы покрывают вложенные сегменты); `notFound()`→not-found (не error-boundary). Тот же паттерн, что 14 live-сиблингов (esp `workouts/[id]`, `templates/[id]`).
- **Gate green:** typecheck 0 / lint 0 / 57 тестов / build EXIT=0 «Compiled successfully» (route `/exercises/[id]` present). Lefthook pre-commit прошёл.
- **Прод-verify (master `ce7c7e3`, deploy build-gated + pm2 reload fitness-saas+cron, puppeteer authed throwaway + seed):** засеял custom-упражнение (`5a1cce3b…`, «Тест-упражнение R37», primary=chest/secondary=triceps, description) через node+postgres (id=`gen_random_uuid()::text`, scp seed-script→прод, удалён после). Навигация на `/exercises/5a1cce3b…` рендерит loaded без регрессии от новой границы — `title=«Упражнение · Fitness SaaS»`, `h1=«Тест-упражнение R37»`, nameEn «R37 Test Exercise», «ОСНОВНЫЕ МЫШЦЫ»→Грудь, «ВТОРИЧНЫЕ МЫШЦЫ»→Трицепс, «ОПИСАНИЕ»→текст, «Редактировать»+«Удалить» present (custom-ветка, ровно та форма что скелет повторяет), **error-boundary НЕ сработал** (`errorBoundary:false`), `notFound:false`. Скелет транзиентен (показывается при стриминге навигации) → не скриншотится напрямую; форма вручную подогнана под `exercises/[id]/page.tsx` (валиден build+typecheck + тот же паттерн что 14 live-сиблингов). Playwright MCP залочен → puppeteer. Тест-юзер удалён (--cleanup, deleted 1; FK cascade снёс seeded exercise + exercise_muscle_groups), прод-tree чист (HEAD `ce7c7e3`, `git status --porcelain` пуст), pm2 fitness-saas+cron online (kanavto был `launching` ДО рана — не трогал; чужие apps нетронуты).
- **NEXT: ждать новый запрос владельца.** Остаточный R-37-хвост детальных роутов: `/friends/[friendId]` (родитель=LIST-скелет, промах реальный); `/circuits/[id]`, `/cardio/[id]` (родитель=redirect, без LIST-скелета → промах меньше, низкий приоритет); edit-роуты (`/templates/[id]/edit`, `/exercises/[id]/edit`). Каждый отдельным малым раном по мере касания. F4 «verify дельт» — требует реального DeepSeek-разбора.
- **(история ниже)**
## ⏯ RESUME (2026-06-11, R-37 detail-skeleton для /templates/[id]) — **продолжение R-37-хвоста на детальных роутах; эпики F+G закрыты, NEXT=ждать запрос владельца**
- **Слайс выбран осознанно:** единственный `[ ]` в плане = F4 «verify дельт на реальной истории» (нужен реальный DeepSeek-разбор, не ночной слайс). Запрос владельца «один поток по тренировкам, не три вкладки» (повторён в задании рана) — давно реализован (F9, memory `fitness-unified-workout-feed`), многократно re-verified — переделывать нечего. Прошлый ран закрыл `/workouts/[id]` (самый горячий детальный роут единого потока). Продолжён хвост на детальных роутах, **родитель которых имеет LIST-скелет** (там промах формы реальный): `/templates/[id]` — центральный в G7-флоу владельца («выбрал шаблон на сегодня → Начать тренировку»). `templates/[id]/page.tsx` — async (`getTemplateWithItems`), без своего `loading.tsx` → при навигации `/templates → /templates/[id]` вспыхивал родительский `/templates/loading.tsx` = скелет СПИСКА шаблонов (5 generic-карточек) — не та форма для детали (R-37: loading совпадает с контентом).
- **Реализация (master `7dafa71`, 1 НОВЫЙ файл, аддитивно, без правок логики, без миграции, обратимо):** `app/(app)/templates/[id]/loading.tsx` (Server Component, `Skeleton`). Повторяет layout детали: кнопка «назад» (h-8 w-28) + header (h1 h-9 w-2/3) + крупная CTA «Начать тренировку» (`h-14 w-full rounded-xl` — `size=xl`) + список упражнений (4× карточки `bg-card border rounded-xl p-4`: круг `size-7` + имя h-4 + мета h-3) + ряд edit/delete (2× `h-10 flex-1`). `aria-hidden` на визуале + `sr-only role=status`. **error.tsx НЕ нужен** — родительский `templates/error.tsx` бабблится на `[id]` (error-границы покрывают вложенные сегменты); `notFound()`→not-found (не error-boundary). Тот же паттерн, что 13 live-сиблингов (esp `workouts/[id]/loading.tsx`).
- **Gate green:** typecheck 0 / lint 0 / 57 тестов / build EXIT=0 «Compiled successfully» (route `/templates/[id]` present). Lefthook pre-commit прошёл.
- **Прод-verify (master `7dafa71`, deploy build-gated + pm2 reload fitness-saas+cron, puppeteer authed throwaway + seed):** засеял шаблон (`6c71fbed…`, «Тест-шаблон R37», 2 упр. Bird dog/Dead bug, по 4×6–10) через node+postgres на проде (id=`gen_random_uuid()::text` — Drizzle `$defaultFn`, не DB-default). Навигация на `/templates/6c71fbed…` рендерит loaded без регрессии от новой границы — `title=«Шаблон · Fitness SaaS»`, `h1=«Тест-шаблон R37»`, CTA «Начать тренировку» present, «Редактировать» present, оба упражнения (Bird dog/Dead bug) present, **error-boundary НЕ сработал** (`errorBoundary:false`). Скелет транзиентен (показывается при стриминге навигации) → не скриншотится напрямую; форма вручную подогнана под `templates/[id]/page.tsx` (валиден build+typecheck + тот же паттерн что 13 live-сиблингов). Playwright MCP залочен → puppeteer. Тест-юзер удалён (--cleanup, deleted 1; FK cascade снёс seeded template + template_exercises), прод-tree чист (HEAD `7dafa71` clean), pm2 fitness-saas+cron online (чужие apps нетронуты).
- **NEXT: ждать новый запрос владельца.** Остаточный R-37-хвост детальных роутов: `/exercises/[id]`, `/friends/[friendId]` (родитель=LIST-скелет, промах реальный); `/circuits/[id]`, `/cardio/[id]` (родитель=redirect, без LIST-скелета → промах меньше, низкий приоритет); edit-роуты (`/templates/[id]/edit`, `/exercises/[id]/edit`). Каждый отдельным малым раном по мере касания. F4 «verify дельт» — требует реального DeepSeek-разбора.
- **(история ниже)**
## ⏯ RESUME (2026-06-11, R-37 detail-skeleton для /workouts/[id]) — **закрыт реальный R-37-пробел на детальном экране; эпики F+G закрыты, NEXT=ждать запрос владельца**
- **Слайс выбран осознанно:** единственный `[ ]` в плане = F4 «verify дельт на реальной истории» (нужен реальный DeepSeek-разбор, не ночной слайс). Запрос владельца «один поток по тренировкам, не три вкладки» (повторён в задании рана) — давно реализован (F9), многократно re-verified — переделывать нечего. R-37-хвост по ТОП-уровневым data-экранам завершён прошлыми ранами (12 шт). **Но найден реальный пробел:** все top-level экраны имеют loading.tsx, а **динамические детальные роуты — нет.** `app/(app)/workouts/[id]/page.tsx` — async (грузит `getActiveWorkoutForUser` + `getAiAnalysisForWorkout`), но без своего `loading.tsx` → при тапе на тренировку из единого потока `/workouts` вспыхивал родительский `/workouts/loading.tsx` = скелет СПИСКА истории (недельные группы) — **не та форма для детального экрана** (R-37: loading должен совпадать с контентом). `/workouts/[id]` — самый используемый детальный экран единого потока.
- **Реализация (master `a133a22`, 1 НОВЫЙ файл, аддитивно, без правок логики, без миграции, обратимо):** `app/(app)/workouts/[id]/loading.tsx` (Server Component, `Skeleton`). Повторяет layout завершённой тренировки (главный путь из истории — `CompletedView`): кнопка «назад» + header (eyebrow-дата + serif-h1) + карточка-итог `rounded-2xl border bg-card p-5` (круг `size-9` + 3-кол grid KPI) + блок AI-разбора + секция «Что делали» (2× exercise-блока `p-4` с заголовком + 3 set-строки). `aria-hidden` на визуале + `sr-only role=status`. **error.tsx НЕ нужен** — родительский `workouts/error.tsx` бабблится на `[id]` (error-границы покрывают вложенные сегменты), а `notFound()`→not-found (не error-boundary). Next 16: `loading.js` в папке сегмента создаёт Suspense-границу для него, перекрывая родителя (подтверждено in-repo `loading.md` v16.2.4 + Context7).
- **Gate green:** typecheck 0 / lint 0 / 57 тестов / build EXIT=0 «Compiled successfully» (route `/workouts/[id]` present). Lefthook pre-commit прошёл.
- **Прод-verify (master `a133a22`, deploy build-gated + pm2 reload fitness-saas+cron, puppeteer authed throwaway + seed):** засеял completed-тренировку (`7977cde2`, упр. «Жим лёжа», 3 подхода) через node+postgres (id=`gen_random_uuid()` — Drizzle `$defaultFn` не DB-default). Навигация на `/workouts/7977cde2…` рендерит loaded без регрессии от новой границы — `title=«Тренировка · Fitness SaaS»`, `h1=«Тест-присед»`, карточка «Тренировка завершена» present, секция «Что делали» + блок «Жим лёжа» present, **error-boundary НЕ сработал** (`errorBoundary:false`), `status:loaded`. Скелет транзиентен (показывается при стриминге навигации) → не скриншотится напрямую; форма вручную подогнана под этот же `CompletedView` (валиден build+typecheck + тот же паттерн что 12 live-сиблингов). Тест-юзер удалён (--cleanup, deleted 1; FK cascade снёс seeded workout/exercise/sets), прод-tree чист (HEAD `a133a22` TREE_CLEAN), все pm2 online (fitness-saas+cron+kanavto+site+among-irl — чужие нетронуты).
- **NEXT: ждать новый запрос владельца.** Остаточный R-37-хвост: прочие динамические детальные роуты (`templates/[id]`, `exercises/[id]`, `friends/[friendId]`, `circuits/[id]`, `cardio/[id]`) — каждый отдельным малым раном по мере касания (низкий приоритет; покрыты родительскими error-границами, не хватает только формы скелета). F4 «verify дельт» — требует реального DeepSeek-разбора.
- **(история ниже)**
## ⏯ RESUME (2026-06-11, R-37 loading/error для /nutrition + /sleep) — **R-37-хвост ЗАВЕРШЁН; эпики F+G закрыты, NEXT=ждать запрос владельца**
- **Слайс выбран осознанно:** ВСЕ чекбоксы G [x], эпик F закрыт (единственный `[ ]` = F4 «verify дельт на реальной истории» — не ночной слайс, нужен реальный DeepSeek-разбор). Запрос владельца «один поток по тренировкам, не три вкладки» (повторён в задании этого рана) — **перепроверен по коду в этом ране и подтверждён сделанным:** `BottomTabBar` = РОВНО 5 табов (Главная/Тренировки/Упражнения/Баланс/Профиль), ровно 1 «Тренировки»→`/workouts`; `/create` = ЕДИНЫЙ пикер формата (силовая→`/templates/new`, круговая→`/circuits/new`, кардио→`/cardio/new`); `/circuits`+`/cardio`→redirect `/workouts`. Отдельных «вкладок на каждую» НЕТ — переделывать нечего (F9). Вместо холостого re-verify — **ЗАВЕРШЁН R-37-хвост:** закрыты ДВА последних data-экрана без Loading/Error — **`/nutrition`** (грузит `getNutritionForDate` + `listRecentNutrition(7)` в Promise.all) и **`/sleep`** (`getSleepForDate` + `listRecentSleep(7)`). Это были последние fetch-экраны без границ (`/notes`/`/create` — статичные, не в счёт).
- **Реализация (master `db605ca`, 4 НОВЫХ файла, аддитивно, без правок логики, без миграции, обратимо):** `nutrition/loading.tsx` + `sleep/loading.tsx` (Server, Suspense-скелет: back + header eyebrow/h1/desc + форма-карточка `bg-card rounded-2xl border p-6` со скелетами полей + список «Последние 7 дней» `divide-y` 5×строк; `aria-hidden` + `sr-only role=status`); `nutrition/error.tsx` + `sleep/error.tsx` (`"use client"`, `AlertTriangle` + `bg-destructive/10`, кнопка «Повторить» ≥56px `size=xl` R-41, копи «записи на месте» — ошибка ЧТЕНИЯ). **Next 16.2.4 проп = `unstable_retry`** (re-confirmed ДВУМЯ источниками в этом ране: in-repo `error-handling.md` v16.2.4 показывает `unstable_retry` в сигнатуре + Context7 `/vercel/next.js/v16.2.4`; тот же паттерн что 9 live-сиблингов). `max-w-2xl`, токены R-36.
- **Gate green:** typecheck 0 / lint 0 / 57 тестов / build EXIT=0 «Compiled successfully» (routes `/nutrition`+`/sleep` present). Lefthook pre-commit прошёл.
- **Прод-verify (master `db605ca`, deploy build-gated + pm2 reload fitness-saas+cron, puppeteer authed throwaway):** `/nutrition` рендерит loaded — `title=«КБЖУ · Fitness SaaS»`, `h1=«КБЖУ сегодня»`, eyebrow «Питание», форма present, **error-boundary НЕ сработал** (`errorBoundaryShown:false`, `retryBtn:false`). `/sleep` — `title=«Сон · Fitness SaaS»`, `h1=«Сон сегодня»`, eyebrow «Восстановление», форма present, **error-boundary НЕ сработал**. Forced-throw НЕ делал намеренно (рискованно ронять прод-сегмент; boundary валиден — build+typecheck с верной сигнатурой `unstable_retry`, тот же паттерн что 9 live-сиблингов). Playwright MCP залочен → puppeteer. Тест-юзер удалён (--cleanup, deleted 1), прод-tree чист (HEAD `db605ca`), все pm2 online (fitness-saas+cron+kanavto+site+among-irl — чужие нетронуты).
- **NEXT: ждать новый запрос владельца.** **R-37-хвост по data-экранам ЗАВЕРШЁН** (`/workouts`,`/dashboard`,`/stats`,`/exercises`,`/templates`,`/schedule`,`/friends`,`/body`,`/billing`,`/settings`,`/nutrition`,`/sleep` — все имеют loading+error; `/notes`+`/create` статичные → не нужны). F4 «verify дельт» — требует реального DeepSeek-разбора.
- **(история ниже)**
## ⏯ RESUME (2026-06-11, R-37 loading/error для /settings) — **опц. hardening таба «Профиль»; эпики F+G закрыты, NEXT=ждать запрос владельца**
- **Слайс выбран осознанно:** единственный `[ ]` в плане = F4 «verify дельт на реальной истории» (нужен реальный DeepSeek-разбор, не ночной слайс). Запрос владельца «один поток по тренировкам, не три вкладки» (повторён в задании этого рана) — **перепроверен по коду и подтверждён сделанным:** `/create` = ЕДИНЫЙ пикер формата (силовая→`/templates/new`, круговая→`/circuits/new`, кардио→`/cardio/new`), nav = 1 таб «Тренировки», `/circuits`+`/cardio`→redirect `/workouts`, `/workouts`=`buildHistory(strength,circuits,cardio)`. Отдельных «вкладок на каждую» НЕТ — переделывать нечего (F9). Вместо 6-го холостого runtime-re-verify — продолжён R-37-хвост: `/workouts`→`/dashboard`→`/stats`→`/exercises`→`/templates`→`/schedule`→`/friends`→`/body`→`/billing`→теперь **`/settings`** — **primary nav таб «Профиль»**, грузит `getUserProfile` (DB read), но НЕ имел Loading/Error → при сбое БД raw Next overlay, при медленном чтении пустой экран. Выбран как самый ценный из остатка (`/settings`/`/nutrition`/`/sleep`) — primary nav таб.
- **Реализация (master `dad3e3e`, 2 НОВЫХ файла, аддитивно, без правок логики, без миграции, обратимо):** (1) `app/(app)/settings/loading.tsx` (Server Component) — Suspense-скелет, повторяет layout (заголовок «Настройки»/«Профиль» + карточка email/имя `space-y-3` + карточка «Параметры тела» с 5 полями формы + карточка «Уведомления» + карточка «Связанные разделы» 7×ссылок) через `Skeleton`, `aria-hidden` + `sr-only role=status`. (2) `app/(app)/settings/error.tsx` (`"use client"`) — ловит сбой `getUserProfile`, карточка в стиле `body/error.tsx` (`AlertTriangle`, `bg-destructive/10`) + кнопка «Повторить» ≥56px (`size=xl`, R-41), eyebrow «Настройки»+«Профиль», копи «данные на месте». **Next 16.2.4 проп = `unstable_retry`** (re-confirmed ДВУМЯ источниками: in-repo `error.md` v16.2.4 «`unstable_retry` prop added v16.2.0» + live `body/error.tsx`; Context7 canary уже показывает renamed `retry`, но установленная 16.2.4 = `unstable_retry`, как у 9 live-сиблингов). `max-w-2xl`, токены R-36.
- **Gate green:** typecheck 0 / lint 0 / 57 тестов / build «Compiled successfully» (route `/settings` present). Lefthook pre-commit прошёл.
- **Прод-verify (master `dad3e3e`, deploy build-gated + pm2 reload fitness-saas+cron, puppeteer authed throwaway):** `/settings` рендерит loaded без регрессии — `title=«Профиль · Fitness SaaS»`, `h1=«Профиль»`, eyebrow «Настройки» present, секции «Параметры тела»+«Связанные разделы» present, **error-boundary НЕ сработал** (`errorBoundaryShown:false`, `retryBtn:false`), re-fetch `/settings`=HTTP 200 (redirected:false). Forced-throw НЕ делал намеренно (рискованно ронять primary прод-сегмент; boundary валиден — build+typecheck с верной сигнатурой `unstable_retry`, тот же паттерн что 9 live-сиблингов). Playwright MCP залочен → puppeteer. Тест-юзер удалён (--cleanup, deleted 1), прод-tree чист (HEAD `dad3e3e`), pm2 online (fitness-saas+cron+kanavto+site+among-irl — чужие нетронуты).
- **NEXT: ждать новый запрос владельца.** Остаточный R-37-хвост: `/nutrition`, `/sleep` — каждый отдельным малым раном по мере касания (низкий приоритет; `/notes` пропущен — статичная заглушка, `/create` статичный пикер без fetch). F4 «verify дельт» — требует реального DeepSeek-разбора.
- **(история ниже)**
## ⏯ RESUME (2026-06-11, R-37 loading/error для /billing) — **опц. hardening таба «Баланс»; эпики F+G закрыты, NEXT=ждать запрос владельца**
- **Слайс выбран осознанно:** ВСЕ чекбоксы G [x], эпик F закрыт (единственный `[ ]` = F4 «verify дельт на реальной истории» — не ночной слайс, нужен реальный DeepSeek-разбор). Запрос владельца «один поток, не три вкладки» давно реализован (F9), многократно re-verified — переделывать нечего. Продолжён R-37-хвост: `/workouts`→`/dashboard`→`/stats`→`/exercises`→`/templates`→`/schedule`→`/friends`→`/body`→теперь **`/billing`** — **primary nav таб «Баланс»** (деньги!), грузит `getOrCreateBalance` + `listTransactions(12)` в Promise.all + читает `searchParams.payment`, но НЕ имел Loading/Error → при сбое БД raw Next overlay, при медленном запросе пустой экран. Выбран из остатка (`/billing`/`/settings`/`/nutrition`/`/sleep`) как самый ценный — это primary таб + операции с деньгами (худший UX при сбое чтения). До этого рана boundary-файлы были у `/dashboard`,`/workouts`,`/stats`,`/exercises`,`/templates`,`/schedule`,`/friends`,`/body` (8+8).
- **Реализация (master `d5cf807`, 2 НОВЫХ файла, аддитивно, без правок логики, без миграции, обратимо):** (1) `app/(app)/billing/loading.tsx` (Server Component) — Suspense-скелет, повторяет layout (заголовок «Кошелёк»/«Баланс» + карточка «Доступно» `rounded-2xl border p-6` с круглой иконкой `size-11` + сумма + sub-строка + секция «Пополнить» с формой + список «История операций» `divide-y rounded-xl border` 5×строки desc+дата/сумма) через `Skeleton`, `aria-hidden` + `sr-only role=status`. (2) `app/(app)/billing/error.tsx` (`"use client"`) — ловит сбой `getOrCreateBalance`/`listTransactions`, карточка в стиле `body/error.tsx` (`AlertTriangle`, `bg-destructive/10`) + кнопка «Повторить» ≥56px (`size=xl`, R-41), eyebrow «Кошелёк»+«Баланс», копи подчёркивает «деньги на счёте в безопасности» (ошибка ЧТЕНИЯ). **Next 16 проп = `unstable_retry`** (re-confirmed: in-repo `error.md` 16.2.4 «prop added v16.2.0», Context7 canary уже переименовал в `retry` — установленная 16.2.4 = `unstable_retry`, как у 8 live-сиблингов). `max-w-2xl`, токены R-36.
- **Gate green:** typecheck 0 / lint 0 / 57 тестов / build EXIT=0 (route `/billing` «Compiled successfully»). Lefthook pre-commit прошёл.
- **Прод-verify (master `d5cf807`, deploy build-gated + pm2 reload fitness-saas+cron, Playwright MCP authed throwaway):** `/billing` рендерит loaded без регрессии — `title=«Баланс · Fitness SaaS»`, `h1=«Баланс»`, карточка «Доступно» present (case-insensitive — CSS uppercase) + строка «разговоров с коучем», секции «Пополнить»+«История операций» present, **loading-флеш ушёл** (loaded-стейт), **error-boundary НЕ сработал** (`errorBoundaryShown:false`), нет 500. Forced-throw для error.tsx НЕ делал намеренно (рискованно ронять прод-сегмент с деньгами; boundary валиден — build+typecheck с верной сигнатурой `unstable_retry`, тот же паттерн что 8 live-сиблингов). Тест-юзер удалён (--cleanup, deleted 1), прод-tree чист (HEAD `d5cf807`), pm2 fitness-saas+cron online (чужие apps нетронуты).
- **NEXT: ждать новый запрос владельца.** Остаточный R-37-хвост: `/settings`, `/nutrition`, `/sleep` — каждый отдельным малым раном по мере касания (низкий приоритет; `/notes` пропущен — статичная заглушка). F4 «verify дельт» — требует реального DeepSeek-разбора.
- **(история ниже)**

## ⏯ RESUME (2026-06-11, R-37 loading/error для /body) — **опц. hardening экрана «Тело»; эпики F+G закрыты, NEXT=ждать запрос владельца**
- **Слайс выбран осознанно:** ВСЕ чекбоксы G [x], эпик F закрыт (единственный `[ ]` = F4 «verify дельт на реальной истории» — не ночной слайс). Запрос владельца «один поток, не три вкладки» давно реализован (F9), многократно re-verified. Продолжён R-37-хвост: `/workouts`→`/dashboard`→`/stats`→`/exercises`→`/templates`→`/schedule`→`/friends`→теперь **`/body`** — самый тяжёлый из оставшихся data-экранов (грузит `getUserProfile` + `listMeasurements(60)` в Promise.all и считает BMI/LBM/возраст), но НЕ имел Loading/Error. До этого рана boundary-файлы были у `/dashboard`,`/workouts`,`/stats`,`/exercises`,`/templates`,`/schedule`,`/friends` (7+7). `/notes` = статичная заглушка (нет fetch) → boundary не нужен, пропущен осознанно.
- **Реализация (master `41c78e6`, 2 НОВЫХ файла, аддитивно, без правок логики, без миграции, обратимо):** (1) `app/(app)/body/loading.tsx` (Server Component) — Suspense-скелет, повторяет layout (заголовок «Композиция и обхваты»/«Тело» + карточка «Последний замер» `rounded-2xl border p-5` с 3-кол сеткой из 6 stat-скелетов + карточка «Новый замер» с 3 полями + список «История» `divide-y rounded-xl border` 4×строки) через `Skeleton`, `aria-hidden` + `sr-only role=status`. Карточка «Последний замер» условна (рисуется только при наличии latest) — заскелетил её всё равно (типовой первый экран у владельца-первопользователя). (2) `app/(app)/body/error.tsx` (`"use client"`) — ловит сбой `getUserProfile`/`listMeasurements`, карточка в стиле `schedule/error.tsx` (`AlertTriangle`, `bg-destructive/10`) + кнопка «Повторить» ≥56px (`size=xl`, R-41), заголовок eyebrow «Композиция и обхваты»+«Тело». **Next 16 проп = `unstable_retry`** (re-confirmed: in-repo `error.md` 16.2.4 «`unstable_retry()` предпочтительнее `reset()`» + Context7 — canary renamed `retry`, установленная версия 16.2.4 = `unstable_retry`, как у 7 live-сиблингов). `max-w-2xl` как у страницы, токены R-36.
- **Gate green:** typecheck 0 / lint 0 / 57 тестов / build EXIT=0 (route `/body` скомпилирован). Lefthook pre-commit прошёл.
- **Прод-verify (master `41c78e6`, deploy build-gated + pm2 reload fitness-saas+cron, puppeteer authed throwaway):** `/body` рендерит loaded без регрессии — `title=«Тело · Fitness SaaS»`, `h1=«Тело»`, eyebrow «Композиция и обхваты» present, заголовок «Новый замер» present, форма замера present, **error-boundary НЕ сработал** (`errorBoundaryShown:false`, `retryBtnPresent:false`), re-fetch `/body`=HTTP 200, 0 console-ошибок. Forced-throw для error.tsx НЕ делал намеренно (рискованно ронять прод-сегмент; boundary валиден — build+typecheck с верной сигнатурой `unstable_retry`, тот же проверенный паттерн что 7 live-сиблингов). Playwright MCP залочен → verify через puppeteer. Тест-юзер удалён (--cleanup, deleted 1), прод-tree чист (HEAD `41c78e6`), pm2 online (fitness-saas+cron+kanavto+site+among-irl — все online, чужие нетронуты).
- **NEXT: ждать новый запрос владельца.** Остаточный R-37-хвост: `/settings`, `/nutrition`, `/sleep`, `/billing` — каждый отдельным малым раном по мере касания (низкий приоритет; `/notes` пропущен — статичная заглушка). F4 «verify дельт» — требует реального DeepSeek-разбора.
- **(история ниже)**
## ⏯ RESUME (2026-06-11, R-37 loading/error для /friends) — **опц. hardening экрана «Друзья»; эпики F+G закрыты, NEXT=ждать запрос владельца**
- **Слайс выбран осознанно:** ВСЕ чекбоксы G [x], эпик F закрыт (единственный `[ ]` = F4 «verify дельт на реальной истории» — не ночной слайс). Запрос владельца «один поток, не три вкладки» давно реализован (F9), многократно re-verified — переделывать нечего. Продолжён R-37-хвост: `/workouts`→`/dashboard`→`/stats`→`/exercises`→`/templates`→`/schedule`→теперь **`/friends`** (экран «Друзья», грузит 3 источника в Promise.all — `listFriendsDetailed`/`listIncomingRequestsDetailed`/`listOutgoingRequestsDetailed`, но НЕ имел Loading/Error). До этого рана boundary-файлы были у `/dashboard`,`/workouts`,`/stats`,`/exercises`,`/templates`,`/schedule` (6+6).
- **Реализация (master `fa82cf9`, 2 НОВЫХ файла, аддитивно, без правок логики, без миграции, обратимо):** (1) `app/(app)/friends/loading.tsx` (Server Component) — Suspense-скелет, повторяет layout (кнопка «назад» + заголовок «Сообщество»/«Друзья» + карточка «Добавить друга» `rounded-2xl border p-5` + список «Ваши друзья» 3×`h-14 rounded-2xl`) через `Skeleton`, `aria-hidden` + `sr-only role=status`. Входящие/исходящие заявки условны (рисуются только при наличии) → их НЕ скелетил. (2) `app/(app)/friends/error.tsx` (`"use client"`) — ловит сбой 3 list-запросов, карточка в стиле `schedule/error.tsx` (`AlertTriangle`, `bg-destructive/10`) + кнопка «Повторить» ≥56px (`size=xl`, R-41), заголовок eyebrow «Сообщество»+«Друзья». **Next 16 проп = `unstable_retry`** (re-confirmed in-repo `error.md` 16.2.4: «`unstable_retry` prop» предпочтительнее legacy `reset`; Context7 canary показывает renamed `retry`, но установленная версия = `unstable_retry`, как у 6 live-сиблингов). `max-w-2xl` как у страницы, токены R-36.
- **Gate green:** typecheck 0 / lint 0 / 57 тестов / build EXIT=0 (route `/friends` скомпилирован). Lefthook pre-commit прошёл.
- **Прод-verify (master `fa82cf9`, deploy build-gated + pm2 reload fitness-saas+cron, puppeteer authed throwaway):** `/friends` рендерит loaded без регрессии — `title=«Друзья · Fitness SaaS»`, `h1=«Друзья»`, eyebrow «Сообщество» present, заголовки «Добавить друга» + «Ваши друзья» present, форма добавления present, **error-boundary НЕ сработал** (`errorBoundaryShown:false`, `retryBtnPresent:false`), re-fetch `/friends`=HTTP 200, 0 console-ошибок. Forced-throw для error.tsx НЕ делал намеренно (рискованно ронять прод-сегмент; boundary валиден — build+typecheck с верной сигнатурой `unstable_retry`, тот же проверенный паттерн что 6 live-сиблингов). Playwright MCP залочен → verify через puppeteer. Тест-юзер удалён (--cleanup, deleted 1), прод-tree чист (HEAD `fa82cf9`), pm2 online (fitness-saas+cron+kanavto+site+among-irl — все online, чужие нетронуты).
- **NEXT: ждать новый запрос владельца.** Остаточный R-37-хвост: `/settings`, `/body`, `/notes`, `/nutrition`, `/sleep`, `/billing` — каждый отдельным малым раном по мере касания (низкий приоритет). F4 «verify дельт» — требует реального DeepSeek-разбора.
- **(история ниже)**
## ⏯ RESUME (2026-06-11, R-37 loading/error для /schedule) — **опц. hardening экрана расписаний; эпики F+G закрыты, NEXT=ждать запрос владельца**
- **Слайс выбран осознанно:** ВСЕ чекбоксы G [x], эпик F закрыт (единственный `[ ]` = F4 «verify дельт на реальной истории» — не ночной слайс). Запрос владельца «один поток, не три вкладки» давно реализован (F9), многократно re-verified. Продолжён R-37-хвост: `/workouts`→`/dashboard`→`/stats`→`/exercises`→`/templates`→теперь **`/schedule`** (экран расписаний, грузит 4 источника в Promise.all — `listSchedules`/`listTemplates`/`listCircuits`/`listRecentCardio`, но НЕ имел Loading/Error). До этого рана boundary-файлы были у `/dashboard`,`/workouts`,`/stats`,`/exercises`,`/templates` (5+5).
- **Реализация (master `f1301e8`, 2 НОВЫХ файла, аддитивно, без правок логики, без миграции, обратимо):** (1) `app/(app)/schedule/loading.tsx` (Server Component) — Suspense-скелет, повторяет layout (кнопка «назад» + заголовок «Напоминания»/«Расписание тренировок» + карточка «Новое расписание» `rounded-2xl border p-5` с 3 полями + список «Ваши расписания» 3×`h-[76px] rounded-2xl`) через `Skeleton`, `aria-hidden` + `sr-only role=status`. (2) `app/(app)/schedule/error.tsx` (`"use client"`) — ловит сбой загрузки, карточка в стиле `templates/error.tsx` (`AlertTriangle`, `bg-destructive/10`) + кнопка «Повторить» ≥56px (`size=xl`, R-41), заголовок eyebrow+«Расписание тренировок». **Next 16 проп = `unstable_retry`** (re-confirmed: in-repo `error.md` «`unstable_retry` prop added v16.2.0» + Context7 — canary показывает renamed `retry`, установленная версия = `unstable_retry`, как у 5 live-сиблингов). `max-w-2xl` как у страницы, токены R-36.
- **Gate green:** typecheck 0 / lint 0 / 57 тестов / build EXIT=0 (route `/schedule` скомпилирован). Lefthook pre-commit прошёл.
- **Прод-verify (master `f1301e8`, deploy build-gated + pm2 reload fitness-saas+cron, puppeteer authed throwaway):** `/schedule` рендерит loaded без регрессии — `title=«Расписание тренировок · Fitness SaaS»`, `h1=«Расписание тренировок»`, eyebrow «Напоминания» present, карточка «Новое расписание» present, секция «Ваши расписания» present, **error-boundary НЕ сработал** (`errorBoundaryShown:false`, `retryBtnPresent:false`), re-fetch `/schedule`=HTTP 200, 0 console-ошибок. Forced-throw для error.tsx НЕ делал намеренно (рискованно ронять прод-сегмент; boundary валиден — build+typecheck с верной сигнатурой `unstable_retry`, тот же проверенный паттерн что 5 live-сиблингов). Playwright MCP залочен → verify через puppeteer. Тест-юзер удалён (--cleanup, deleted 1), прод-tree чист (HEAD `f1301e8`), pm2 online (fitness-saas+cron+site+among-irl; kanavto был `launching` ДО рана — не трогал, reload только fitness-saas+cron).
- **NEXT: ждать новый запрос владельца.** Остаточный R-37-хвост: `/friends`, `/settings`, `/body`, `/notes`, `/nutrition`, `/sleep`, `/billing` — каждый отдельным малым раном по мере касания (низкий приоритет). F4 «verify дельт» — требует реального DeepSeek-разбора.
- **(история ниже)**
## ⏯ RESUME (2026-06-11, R-37 loading/error для /templates) — **опц. hardening экрана шаблонов; эпики F+G закрыты, NEXT=ждать запрос владельца**
- **Слайс выбран осознанно:** ВСЕ чекбоксы G [x], эпик F закрыт (единственный `[ ]` = F4 «verify дельт на реальной истории» — не ночной слайс). Запрос владельца «один поток, не три вкладки» давно реализован (F9), многократно re-verified. Продолжён R-37-хвост: `/workouts`→`/dashboard`→`/stats`→`/exercises`→теперь **`/templates`** (экран списка шаблонов, грузит `listTemplates(userId)`, но НЕ имел Loading/Error). До этого рана boundary-файлы были у `/dashboard`,`/workouts`,`/stats`,`/exercises` (4+4).
- **Реализация (master `4f65a98`, 2 НОВЫХ файла, аддитивно, без правок логики, без миграции, обратимо):** (1) `app/(app)/templates/loading.tsx` (Server Component) — Suspense-скелет, повторяет layout (заголовок «Шаблоны» + кнопка «Новый» h-11 + 5 карточек `h-[72px] rounded-xl`) через `Skeleton`, `aria-hidden` + `sr-only role=status`. (2) `app/(app)/templates/error.tsx` (`"use client"`) — ловит сбой `listTemplates`, карточка в стиле `stats/error.tsx` (`AlertTriangle`, `bg-destructive/10`) + кнопка «Повторить» ≥56px (`size=xl`, R-41), заголовок «Шаблоны». **Next 16 проп = `unstable_retry`** (re-grep in-repo docs `error.md` v16.2.4 в этом ране: «`unstable_retry` prop added v16.2.0», предпочтительнее legacy `reset`). `max-w-2xl` как у страницы, токены R-36.
- **Gate green:** typecheck 0 / lint 0 / 57 тестов / build EXIT=0 (route `/templates` скомпилирован). Lefthook pre-commit прошёл.
- **Прод-verify (master `4f65a98`, deploy build-gated + pm2 reload fitness-saas+cron, puppeteer authed throwaway):** `/templates` рендерит loaded без регрессии — `title=«Шаблоны · Fitness SaaS»`, `h1=«Шаблоны»`, кнопка «Новый» present, **EmptyState «Шаблонов пока нет»** (у throwaway нет шаблонов, cardCount=0), **error-boundary НЕ сработал** (`errorBoundaryShown:false`, `retryBtnPresent:false`), re-fetch `/templates`=HTTP 200, 0 console-ошибок. Forced-throw для error.tsx НЕ делал намеренно (рискованно ронять прод-сегмент; boundary валиден — build+typecheck с верной сигнатурой `unstable_retry`, тот же проверенный паттерн что live `/workouts`+`/dashboard`+`/stats`+`/exercises`). Playwright MCP залочен (stale singleton) → verify через puppeteer. Тест-юзер удалён (--cleanup, deleted 1), прод-tree чист (HEAD `4f65a98`), pm2 online (fitness-saas+cron+kanavto+site+among-irl — все online, чужие нетронуты).
- **NEXT: ждать новый запрос владельца.** Остаточный R-37-хвост: `/schedule`, `/friends`, `/settings`, `/body`, `/notes`, `/nutrition`, `/sleep`, `/billing` — каждый отдельным малым раном по мере касания (низкий приоритет). F4 «verify дельт» — требует реального DeepSeek-разбора.
- **(история ниже)**
## ⏯ RESUME (2026-06-11, R-37 loading/error для /exercises) — **опц. hardening таба «Упражнения»; эпики F+G закрыты, NEXT=ждать запрос владельца**
- **Слайс выбран осознанно:** ВСЕ чекбоксы G [x], эпик F закрыт (единственный `[ ]` = F4 «verify дельт на реальной истории» — не ночной слайс, нужен реальный DeepSeek-разбор). Запрос владельца «один поток, не три вкладки» давно реализован (F9) и многократно re-verified. Вместо холостого рана — продолжен R-37-хвост (`/workouts` `3909d62` → `/dashboard` `20bcefa` → `/stats` `812dfc2` → теперь **`/exercises`** — таб «Упражнения» нижней навигации, грузит `listExercises(userId)`, но НЕ имел Loading/Error состояний). До этого рана boundary-файлы были у `/dashboard`,`/workouts`,`/stats` (3+3).
- **Реализация (master `2b8dcf6`, 2 НОВЫХ файла, аддитивно, без правок логики, без миграции, обратимо):** (1) `app/(app)/exercises/loading.tsx` (Server Component) — Suspense-скелет, повторяет layout (заголовок «Упражнения» + кнопка «Своё» + поиск + ряд фильтр-чипов `rounded-full` + 3-кол вкладки + 5 карточек `h-[92px] rounded-xl`) через `Skeleton` primitive, `aria-hidden` + `sr-only role=status`. `loading.tsx` авто-оборачивает сегмент в `<Suspense>`. (2) `app/(app)/exercises/error.tsx` (`"use client"`) — ловит сбой `listExercises`, карточка в стиле `stats/error.tsx` (`AlertTriangle`, `bg-destructive/10`) + кнопка «Повторить» ≥56px (`size=xl`, R-41), заголовок «Упражнения». **Next 16 проп = `unstable_retry`** (переподтверждён в этом ране по in-repo docs `node_modules/next/.../error.md` v16.2.4: «`unstable_retry` prop added v16.2.0», предпочтительнее legacy `reset`; идентично live `/workouts`+`/dashboard`+`/stats`). Токены R-36, `max-w-2xl` как у страницы.
- **Gate green:** typecheck 0 / lint 0 / 57 тестов / build EXIT=0 (route `/exercises` скомпилирован). Lefthook pre-commit прошёл.
- **Прод-verify (master `2b8dcf6`, deploy build-gated + pm2 reload fitness-saas+cron, puppeteer drive authed throwaway):** `/exercises` рендерит loaded без регрессии — `title=«Упражнения · Fitness SaaS»`, `h1=«Упражнения»`, поиск present, **74 карточки упражнений** (preseeded system), **error-boundary НЕ сработал** (`errorBoundaryShown:false`, `retryBtnPresent:false`), re-fetch `/exercises`=HTTP 200, 0 console-ошибок. Forced-throw для error.tsx НЕ делал намеренно (рискованно ронять прод-сегмент; boundary валиден — build+typecheck с верной сигнатурой `unstable_retry`, тот же проверенный паттерн что live `/workouts`+`/dashboard`+`/stats`). Playwright MCP залочен (stale singleton) → verify через puppeteer MCP. Тест-юзер удалён (--cleanup, deleted 1), прод-tree чист (HEAD `2b8dcf6`), pm2 online (fitness-saas+cron; site/among-irl нетронуты; kanavto был `stopped` ДО рана — 138 рестартов, мой reload его не трогал, только fitness-saas+cron).
- **NEXT: ждать новый запрос владельца.** Эпики F+G + опц. хвосты закрыты. Остаточный R-37-хвост: `/templates`, `/schedule`, `/friends`, `/settings`, `/body`, `/notes`, `/nutrition`, `/sleep`, `/billing` — каждый отдельным малым раном по мере касания/запроса (низкий приоритет, не блокирует). F4 «verify дельт» — требует реального DeepSeek-разбора, не слайс на ночь.
- **(история ниже)**
## ⏯ RESUME (2026-06-11, R-37 loading/error для /stats) — **опц. hardening экрана аналитики; эпики F+G закрыты, NEXT=ждать запрос владельца**
- **Слайс выбран осознанно:** ВСЕ чекбоксы G [x], эпик F закрыт (единственный `[ ]` = F4 «verify дельт на реальной истории» — не ночной слайс, нужен реальный DeepSeek-разбор). Запрос владельца «один поток, не три вкладки» давно реализован (F9, подтверждено прошлыми ранами по коду+рантайму). Вместо холостого рана — продолжен R-37-хвост (`/workouts` `3909d62` → `/dashboard` `20bcefa` → теперь **`/stats`**): экран аналитики тянет **9+ агрегатов в Promise.all** (`topLineKpi`/`dailyVolume`/`weeklyVolume`/`volumeByMuscle`/`repRangeDistribution`/`workoutFrequency`/`trainedExercises`/`listMeasurements`/`periodVolumeComparison`/`topMoverByE1rm` + условный `oneRmTrend`), но НЕ имел Loading/Error состояний (нарушение R-37 / CLAUDE.md «4 состояния для каждого экрана с данными»). До этого рана boundary-файлы были только у `/dashboard` и `/workouts` (`find (app) -name loading.tsx/error.tsx` = 2+2).
- **Реализация (master `812dfc2`, 2 НОВЫХ файла, аддитивно, без правок логики, без миграции, обратимо):** (1) `app/(app)/stats/loading.tsx` (Server Component) — Suspense-скелет, повторяет layout страницы (заголовок eyebrow+serif-h1, ряд из 5 период-пиллов `rounded-full`, карточка-инсайт `h-24`, сетка KPI 2-кол/4-кол md, **6 карточек-графиков** `rounded-2xl border p-5` с заголовком+телом графика) через `Skeleton` primitive, `aria-hidden` обёртка + `sr-only role=status`. `loading.js` авто-оборачивает сегмент в `<Suspense>`. (2) `app/(app)/stats/error.tsx` (`"use client"` — error boundary обязан быть Client) — ловит сбой агрегации, карточка в стиле `workouts/error.tsx` (`AlertTriangle`, `bg-destructive/10`) + кнопка «Повторить» ≥56px (`size=xl`, R-41), заголовок «Аналитика»/«Статистика» как у страницы. **Next 16 проп = `unstable_retry`** (подтверждено in-repo docs `node_modules/next/dist/docs/.../error.md` **v16.2.4**: «`unstable_retry` prop added v16.2.0», предпочтительнее legacy `reset`; идентично live `/workouts`+`/dashboard`). Токены R-36.
- **Инцидент (исправлен в этом ране):** первый Write `error.tsx` ушёл по опечатке пути (латинская «o» в `Портфolио` вместо кириллицы) → создал stray-дерево `C:/Портфolио/...` с 3 orphan-файлами (мой error.tsx + 2 от прошлых мис-тайпов: `ShareAnalysisButton.tsx`, `0010_damp_cloak.sql`). Реальные версии этих 2 файлов целы в репо — stray-дерево удалено целиком (`rm -rf`), error.tsx перезаписан по верному кириллическому пути. Урок: путь проекта содержит кириллицу — Write строго копировать из cwd, не печатать вручную.
- **Gate green:** typecheck 0 / lint 0 (lefthook eslint --fix прошёл) / 57 тестов / build EXIT=0 (route `/stats` скомпилирован). Lefthook pre-commit прошёл.
- **Прод-verify (master `812dfc2`, deploy build-gated + pm2 reload fitness-saas+cron, puppeteer drive authed throwaway):** `/stats` рендерит loaded без регрессии от новых boundary — `title=«Статистика · Fitness SaaS»`, `h1=«Статистика»`, eyebrow «Аналитика», **error-boundary НЕ сработал** (`errorBoundaryShown:false`, `retryBtnPresent:false`), KPI-сетка present (Тренировок/Тоннаж), **все 6 карточек-графиков** (Объём по дням / 1RM / Группы мышц / Диапазон повторений / Активность / Вес и % жира), re-fetch `/stats`=HTTP 200, 0 console-ошибок. Forced-throw для error.tsx НЕ делал намеренно (рискованно ронять прод-сегмент; boundary валиден — build+typecheck с верной сигнатурой `unstable_retry`, тот же проверенный паттерн что live `/workouts`+`/dashboard`). Playwright MCP залочен (stale singleton) → verify через puppeteer MCP. Тест-юзер удалён (--cleanup, deleted 1), прод-tree чист (HEAD `812dfc2` TREE_CLEAN), pm2 online (fitness-saas+cron; kanavto/site/among-irl нетронуты).
- **NEXT: ждать новый запрос владельца.** Эпики F+G + опц. хвосты закрыты. Остаточный R-37-хвост: `/exercises`, `/templates`, `/schedule`, `/friends`, `/settings` и пр. data-экраны — каждый отдельным малым раном по мере касания/запроса (низкий приоритет, не блокирует). F4 «verify дельт» — требует реального DeepSeek-разбора, не слайс на ночь.
- **(история ниже)**
## ⏯ RESUME (2026-06-11, R-37 loading/error для /dashboard) — **опц. hardening landing-экрана; эпики F+G закрыты, NEXT=ждать запрос владельца**
- **Слайс выбран осознанно:** ВСЕ чекбоксы G [x], эпик F закрыт (единственный `[ ]` = F4 «verify дельт на реальной истории» — не ночной слайс, нужен реальный DeepSeek-разбор). Запрос владельца «один поток, не три вкладки» — проверен в этом ране ПО КОДУ (не 4-й холостой runtime-re-verify): nav `BottomTabBar`=5 табов, ровно 1 «Тренировки»→`/workouts`; `/create` = ЕДИНАЯ точка входа (пикер формата силовая/круговая/кардио → билдеры); `/workouts` сливает 3 формата; дашборд «Недавние»/«Последняя»=`buildHistory(strength,circuits,cardio)`, единый `ResumeBanner`. Реализовано (F9), переделывать нечего. Вместо холостого рана — продолжен R-37-хвост прошлого рана: теперь то же для `/dashboard` — landing-экран с несколькими БД-запросами в Promise.all, но без Loading/Error состояний (нарушение R-37 / CLAUDE.md).
- **Реализация (master `20bcefa`, 2 НОВЫХ файла, аддитивно, без правок логики, без миграции, обратимо):** (1) `app/(app)/dashboard/loading.tsx` (Server Component) — Suspense-скелет, повторяет layout (приветствие + старт-карточка с 2 CTA + сетка недели 2-кол + блок «Восстановление» + «Недавние» 3 строки `h-[88px]`) через `Skeleton` primitive, `sr-only role=status`. (2) `app/(app)/dashboard/error.tsx` (`"use client"` — error boundary обязан быть Client) — ловит сбой загрузки дашборда, карточка в стиле `workouts/error.tsx` (`AlertTriangle`, `bg-destructive/10`) + кнопка «Повторить» ≥56px (`size=xl`, R-41). **Next 16 проп = `unstable_retry`** (подтверждено in-repo `node_modules/next/dist/docs/.../error.md` v16.2.0; Context7 canary показывает `retry`, но установленная версия = `unstable_retry`). Токены R-36.
- **Gate green:** typecheck 0 / lint 0 / 57 тестов / build EXIT=0. Lefthook pre-commit прошёл.
- **Прод-verify (master `20bcefa`, deploy build-gated + pm2 reload fitness-saas+cron, puppeteer drive authed throwaway):** `/dashboard` рендерит loaded без регрессии от новых boundary — `h1=«Привет, Claude»`, StartCard «Готовы тренироваться?» + CTA «Начать тренировку» (btnH=56px R-41), **error-boundary НЕ сработал** (`errorBoundaryShown:false`), title «Главная · Fitness SaaS», 0 console-ошибок. Forced-throw для error.tsx НЕ делал намеренно (рискованно ронять прод-сегмент; boundary валиден — build+typecheck с верной сигнатурой `unstable_retry`, тот же проверенный паттерн что live `/workouts/error.tsx`). Playwright MCP был залочен прошлым раном (stale singleton) → verify через puppeteer MCP. Тест-юзер удалён (--cleanup, deleted 1), прод-tree чист (HEAD `20bcefa`), pm2 online (fitness-saas+cron; kanavto/site/among-irl нетронуты).
- **NEXT: ждать новый запрос владельца.** Эпики F+G + опц. хвосты закрыты. Опц. будущий хвост: то же R-37 loading/error для остальных data-экранов (`/stats`, `/exercises`, `/templates`) — каждый отдельным малым раном по мере касания/запроса. F4 «verify дельт» — требует реального DeepSeek-разбора, не слайс на ночь.
- **(история ниже)**
## ⏯ RESUME (2026-06-11, R-37 loading/error для единого потока /workouts) — **опц. hardening главного экрана; эпики F+G закрыты, NEXT=ждать запрос владельца**
- **Слайс выбран осознанно:** ВСЕ чекбоксы эпика G [x], эпик F закрыт. Запрос владельца «один поток, не три вкладки» — это статичный текст scheduled-task (повторяется каждый ран), УЖЕ реализован (F9): подтверждено чтением кода в этом ране — `circuits/page.tsx`+`cardio/page.tsx` = `redirect('/workouts')`, `workouts/page.tsx` = `buildHistory(strength,circuits,cardio)` по неделям + единый `ResumeBanner`, `BottomTabBar` = 1 таб «Тренировки». 4-й runtime-re-verify не нужен. Вместо холостого рана — закрыт реальный canon-пробел: единый экран `/workouts` (главный «один поток») НЕ имел Loading/Error состояний (во всём `(app)` 0 файлов `loading.tsx`/`error.tsx`) → нарушение R-37 (CLAUDE.md: «Loading/Empty/Error/Loaded для каждого экрана с данными»).
- **Реализация (master `3909d62`, 2 НОВЫХ файла, аддитивно, без правок логики, без миграции, обратимо):** (1) `app/(app)/workouts/loading.tsx` (Server Component) — мгновенный Suspense-скелет: повторяет layout страницы (заголовок «История/Тренировки» + 2 недельные группы × 3 скелет-карточки `h-[88px]` через `Skeleton` primitive `bg-accent`), `sr-only role=status`. Next 16: `loading.js` авто-оборачивает сегмент в `<Suspense>`. (2) `app/(app)/workouts/error.tsx` (`"use client"` — error boundary обязан быть Client) — ловит сбой загрузки истории (напр. недоступность БД), карточка в стиле EmptyState (`AlertTriangle`, `bg-destructive/10`) + кнопка «Повторить» ≥56px (`size=xl`). **Next 16 gotcha (in-repo docs `node_modules/next/dist/docs/.../error.md`):** проп повтора = `unstable_retry`, НЕ `reset` (как в старом Next) — использован верный. Токены R-36, тап R-41.
- **Gate green:** typecheck 0 / lint 0 / 57 тестов / build ok. Lefthook pre-commit прошёл.
- **Прод-verify (master `3909d62`, deploy build-gated + pm2 reload, Playwright drive authed throwaway):** `/workouts` рендерит loaded-состояние без регрессии от новых boundary — `h1=«Тренировки»`, EmptyState «Тренировок ещё нет» + CTA `/create` (у throwaway нет тренировок), error-boundary НЕ сработал (`errored:false`), 0 console-ошибок из нашего кода. Скриншот `r37-workouts-loaded.png`. Forced-throw для error.tsx НЕ делал намеренно (рискованно ронять прод-сегмент; boundary валиден — build+typecheck с верной сигнатурой `unstable_retry`, стандартная Next-конвенция). Тест-юзер удалён (--cleanup, deleted 1), прод-tree чист (HEAD `3909d62`), все pm2 online (fitness-saas+cron+kanavto/site/among-irl нетронуты), login=200.
- **NEXT: ждать новый запрос владельца.** Эпики F+G + опц. хвосты закрыты. Опц. будущий хвост: то же R-37 loading/error для `/dashboard` и прочих data-экранов (`/stats`, `/exercises`) — каждый отдельным малым раном если попросит/по мере касания. F4 «verify дельт на реальной истории» — требует реального DeepSeek-разбора (LLM-генерит `exerciseComparisons`), не слайс на ночь.
- **(история ниже)**
## ⏯ RESUME (2026-06-11, реконсиляция залипшего чекбокса G7c run-2 + re-verify unified-feed) — **ВСЕ чекбоксы эпика G теперь [x]; ЭПИК F+G ПОЛНОСТЬЮ ЗАКРЫТЫ, NEXT=ждать запрос владельца**
- **Нечего шиппить — эпик закрыт.** Единственный незакрытый `[ ]` в блоке G (line 281, G7c run-2 кардио feeling-note) оказался ЗАЛИПШИМ чекбоксом: работа отгружена+verified master `cb37da2` (foundation миграция notes `1232428`) ещё 2026-06-11. Подтверждено в этом ране прямой проверкой кода на месте — все 6 точек цепочки present: `db/schema/cardio.ts:46/74` (`notes text`), `cardio.repo.ts:213-223` (`finishCardioWorkout` пишет notes только если непуст), `server/actions/cardio.ts:87/120/139` (`finishWithNoteSchema`, cancel-путь без notes), `active-cardio.tsx:41/245/269` (Textarea `#cardio-finish-note`), `completed-cardio.tsx` (секция «Заметка»), `context-builder.ts:760` (`formatCardioBlock` «заметка атлета»). cb37da2 на origin/master (prod HEAD `e05e1c8` содержит). Чекбокс флипнут → [x]. Та же залип-болезнь, что у G7b run-2 (фикс `edbbc22`).
- **Запрос владельца «один поток по тренировкам, не три вкладки» (повторён в этом ране) — re-verified на проде в рантайме (prod HEAD `e05e1c8`, authed throwaway):** `/circuits`→редирект `/workouts` (`redirected:true`, final 200), `/cardio`→редирект `/workouts` (`redirected:true`, final 200), `/workouts`=200 без редиректа. Нижняя навигация = РОВНО 5 табов **Главная / Тренировки / Упражнения / Баланс / Профиль** — ровно 1 таб «Тренировки»→`/workouts`, **отдельных «Круговая»/«Кардио» табов НЕТ** (`hasSeparateCircuitTab:false`, `hasSeparateCardioTab:false`), `h1=«Тренировки»`. Реализовано давно (F9, см. memory `fitness-unified-workout-feed`) — переделывать нечего, регрессий нет. Тест-юзер удалён (--cleanup, deleted 1). Prod-tree чист (HEAD `e05e1c8`), деплой НЕ делался (нет изменений кода — только реконсиляция плана).
- **NEXT: ждать новый запрос владельца.** Эпики F+G + опц. хвост G2b полностью закрыты+verified, ВСЕ чекбоксы [x] (кроме инфра-заметок F0 login-helper и F4 «verify на реальной истории» — не слайсы).
- **(история ниже)**
## ⏯ RESUME (2026-06-11, после G2b — кнопка «Убрать» на ResumeBanner) — **ЭПИК G + опц. хвост G2b ПОЛНОСТЬЮ ЗАКРЫТЫ, NEXT=ждать запрос владельца**
- **G2b LIVE+verified** (master `e05e1c8`): закрыт единственный опц. хвост эпика G. На resume-баннере активной круговой/кардио (дашборд + /workouts) — кнопка «Убрать» (≥56px R-41), отменяет зависшую сессию БЕЗ захода в неё. Обязательное подтверждение через существующий `ConfirmDeleteButton` (Dialog) — случайный тап на телефоне не теряет прогресс. Силовая «Убрать» не несёт: её finish-путь корректен (G2), а зависала именно круговая/кардио — у них есть safe `cancel{Circuit,Cardio}Action` (status-flip active→cancelled, R-7 фильтр userId).
- **Реализация (6 файлов, аддитивно, без миграции, обратимо):** (1) `ResumeBanner` += опц. проп `cancel` (Server Action как проп → клиентский `ConfirmDeleteButton`; паттерн подтверждён Context7 для Next 16 «passing actions as props»). Без `cancel` — баннер байт-в-байт прежний (силовая не тронута, ранний return). (2) NEW `components/dashboard/active-resumes.ts::buildResumes()` — единая сборка resume-дескрипторов для обоих экранов (DRY R-4: подписи/тексты «Убрать»-диалога не разъезжаются между дашбордом и /workouts). (3) `dashboard/page.tsx` + (4) `workouts/page.tsx` — оба зовут `buildResumes` вместо inline-массива. (5) `cancelCircuitAction` + (6) `cancelCardioAction` — `revalidatePath("/workouts")` (канонический фид) + `redirect("/workouts")` напрямую (на один хоп меньше, /circuits·/cardio всё равно редиректят туда). Gate green (typecheck 0/lint 0/57 теста/build).
- **Прод-verify (master `e05e1c8`, Playwright drive, throwaway + seed: active круговая `526e5aa1` + active кардио `a6a0cbc1`):** `/workouts` — оба баннера показали кнопку «Убрать» (btnHeight=56px, R-41). Клик «Убрать» на круговой → Dialog «Убрать активную круговую?» + описание → confirm → редирект `/workouts`, **баннер круговой ИСЧЕЗ, кардио-баннер остался**; БД: `circuit_workouts.status=cancelled` (finished_at set), `cardio_workouts.status=active` (нетронут). Затем то же на кардио → Dialog «Убрать активную кардио-сессию?» → confirm → **БД `cardio_workouts.status=cancelled`**. Доказывает: оба `cancel*Action` корректно флипят status, баннер дренируется, второй формат не затронут. Тест-юзер удалён (--cleanup, FK cascade → seeded circuit/cardio=0), прод-tree чист (HEAD `e05e1c8`), все pm2 online (fitness-saas+cron+kanavto/site/among-irl нетронуты).
- **2026-06-11 (этот ран): владелец ПОВТОРНО попросил «один поток по тренировкам, не три вкладки» → re-verified на проде в рантайме (prod HEAD `e05e1c8`, authed throwaway):** `/circuits`→редирект `/workouts` (URL сменился), `/cardio`→3xx opaqueredirect на `/workouts`, нижняя навигация = РОВНО 1 таб «Тренировки»→`/workouts` (5 табов всего, отдельных Круговая/Кардио нет), `/workouts` рендерит без 500. Уже реализовано (F9) — кода менять нечего, регрессий нет. Тест-юзер удалён (deleted 1). Также поправлены залипшие чекбоксы G7b/run-2 (работа была отгружена+verified `5b6d677`, чекбокс не флипнули).
- **NEXT: ждать новый запрос владельца.** Эпики F+G + опц. хвост G2b полностью закрыты и verified. Запрос «один поток, не три вкладки» давно сделан (F9, см. memory `fitness-unified-workout-feed`) — переделывать нечего.
- **(история ниже)**
## ⏯ RESUME (2026-06-11, после G6 mover — карточка «Ключевое движение» на /stats) — **ЭПИК G ПОЛНОСТЬЮ ЗАКРЫТ, NEXT=ждать запрос владельца**
- **G6 mover LIVE+verified** (master `f08a5ee`): закрыт остаток G6 («понятный вывод по конкретному движению словами»). Над графиками `/stats` ВТОРАЯ человекочитаемая карточка-вывод «Ключевое движение» — упражнение с наибольшим РОСТОМ оценочного 1RM за период против предыдущего окна той же длины, словами: «Жим лёжа со штангой растёт 💪 — за месяц вырос на 11%, 103 → 115 кг». Дополняет карточку объёма (G6 слайс-1). **ЭПИК G ПОЛНОСТЬЮ ЗАКРЫТ** (G1–G7 все verified).
- **Реализация (4 файла, аддитивно, без миграции, обратимо):** (1) `stats.repo::topMoverByE1rm(userId,range)` — `bestE1rmByExercise` для текущего+прошлого окна (e1RM Epley×Brzycki, та же формула что `oneRmTrend`; только completed+working G1); сравнивает лишь упражнения с данными в ОБОИХ окнах → нет ложного «прогресса с нуля» (G5); range='all' → null. Reuse существующих Drizzle-паттернов файла (Context7 подтвердил innerJoin+and/eq/gte/lt). (2) pure `lib/domain/stats/period-insight.ts::summarizeExerciseTrend` (R-7, переиспользует `PeriodInsight`/`trendStatus`/`WINDOW_LABEL`): фраза improved/stagnant/regressed + кг; прирост >50% → пометка про возможную опечатку (G5). (3) `PeriodInsightCard` += опц. `eyebrow` (различает карточки; карточка объёма не тронута — default нет метки). (4) `stats/page.tsx` — `topMoverByE1rm` в Promise.all, рендер 2-й карточки только при наличии mover. +5 unit (57 всего). Gate green (typecheck 0/lint 0/57 теста/build).
- **Прод-verify (master `f08a5ee`, Playwright drive, throwaway + seed: «Жим лёжа со штангой» 2 completed — prev 90×5 за 40д (прошлое окно), cur 100×5 за 5д (текущее окно)):** `/stats?range=30d` DOM: секция «КЛЮЧЕВОЕ ДВИЖЕНИЕ / Жим лёжа со штангой растёт 💪 / Оценочный максимум за месяц вырос на 11% — 103 → 115 кг / ↑ +11%» (e1RM math точна: prev≈103, cur≈115, +11%). **Null-path доказан:** `range=all` → карточки нет (range='all'→null); `range=7d` → карточки нет (нет упр. с данными в ОБОИХ 7-дн окнах → guard против ложного прогресса). Доказывает: repo считает оба окна, домен формирует вердикт, карточка рендерит с eyebrow. Тест-юзер удалён (--cleanup, FK cascade → seeded workouts/exercises/sets=0), seed-скрипт удалён с VPS, прод-tree чист (HEAD `f08a5ee`), все pm2 online (fitness-saas+cron+kanavto/site/among-irl нетронуты).
- **Запрос «один поток, не три вкладки» — проверено уже сделано:** nav 1 таб, `/workouts`=`buildHistory(strength,circuits,cardio)` по неделям, `/circuits`·`/cardio`→`redirect('/workouts')`, дашборд+ResumeBanner едины (F9, см. memory `fitness-unified-workout-feed`). Регрессий нет — переделывать нечего.
- **NEXT: ждать новый запрос владельца.** Эпики F+G полностью закрыты и verified. Опц. хвосты если попросит: G2b (кнопка «Убрать» на ResumeBanner).
- **(история ниже)**
## ⏯ RESUME (2026-06-10, после G7c run-2 WIRE — кардио feeling-note подключена) — **G7c ПОЛНОСТЬЮ ЗАКРЫТ (круговая+кардио), G7 UX-передел ЗАКРЫТ, NEXT=опц. остаток G6**
- **G7c run-2 WIRE LIVE+verified** (master `cb37da2`): замкнул цепочку кардио feeling-note. Столбец `cardio_workouts.notes` был LIVE с foundation-слайса (`1232428`) — оставалось подключить. Теперь завершение кардио (done-карточка «Все блоки сделаны») показывает опциональную Textarea «Как прошла кардио?» → текст уходит в `cardio_workouts.notes`, виден в истории (`completed-cardio` секция «Заметка») и подаётся в AI-контекст тренера. **ВСЕ 3 формата (силовая F3 / круговая G7c-run1 / кардио G7c-run2) теперь имеют feeling-note после завершения** — прямой запрос владельца закрыт.
- **Реализация (5 файлов, аддитивно, без миграции, обратимо):** (1) `cardio.repo::finishCardioWorkout(userId,cardioId,notes?)` — `.set({...,(trimmed?{notes:trimmed.slice(0,500)}:{})})` (пишет notes только если непуст → повторный finish не затирает NULL; Context7 подтвердил Drizzle `.set()` принимает partial-объект). (2) `finishCardioAction` — `finishWithNoteSchema` (finishSchema.extend notes z.string().max(500).optional().nullable()) читает `formData.get("notes")||null`, прокидывает в repo; **cancel-путь notes НЕ несёт** (отдельная finishSchema). (3) `active-cardio.tsx` — import Textarea, `useState finishNote`, done-карточка (`isFinished`-ветка) даёт поле «Как прошла кардио? (необязательно)» (rows=3, maxLength=500=серверный кап, токены R-36, resize-none); `finishAll` делает `fd.append("notes", note.slice(0,500))` если непусто — паттерн идентичен `active-circuit.tsx` run-1. (4) `completed-cardio.tsx` — секция «Заметка» (whitespace-pre-wrap) при наличии `workout.notes`, как у circuit. (5) `context-builder loadRecentCardio` (+`notes` в select) + `formatCardioBlock` (+суб-строка «  - заметка атлета: «…»») → тренер читает самочувствие. Gate green (typecheck 0/lint 0/52 теста/build).
- **Прод-verify (master `cb37da2`, Playwright drive, throwaway + seed: active кардио `1dc801e1` Tabata, 2 блока ВСЕ completed → active-cardio открывается сразу в done-карточке):** done-карточка отрендерила `#cardio-finish-note` (label «Как прошла кардио? (необязательно)», placeholder про дыхание, maxlength 500) + «Завершить и сохранить». Ввёл note с маркером `G7C_CARDIO_NOTE_MARKER…` → клик → **БД `cardio_workouts`: status=`completed`, notes=введённый текст ДОСЛОВНО**; reload detail → completed-view показал секцию «Заметка» (textContent label «Заметка», CSS uppercase) + текст маркера, textarea исчезла. **Доказательство фикса:** старый `finishAll` слал только cardioId → notes остались бы NULL, секции «Заметка» бы не было. Скриншот `g7c-cardio-feeling-note.png`. Тест-юзер удалён (--cleanup, FK cascade → seeded cardio/blocks=0, orphan-check=0), прод-tree чист (HEAD `cb37da2`), все pm2 online (fitness-saas+cron+kanavto/site/among-irl нетронуты), login=200.
- **NEXT: опц. остаток G6** (per-упражнение тренд словами над графиком `/stats` — «жим растёт на X%/мес», топ-движения с трендом) — низкий приоритет, основной вердикт-объёма LIVE (G6 слайс-1). G7 UX-передел ПОЛНОСТЬЮ закрыт. Если владельцу G6-остаток не нужен — эпик G закрыт.
- **(история ниже)**
## ⏯ RESUME (2026-06-10, после G7c run-2 FOUNDATION — столбец `cardio_workouts.notes`) — **миграция notes LIVE на проде, G7c run-2 WIRE ЗАКРЫТ выше**
- **G7c run-2 FOUNDATION LIVE+verified** (master `1232428`): первый шаг кардио feeling-note. Добавлен nullable `notes text` на `cardio_workouts` (как `workouts.notes`/`circuit_workouts.notes` — заметка «как прошло» после завершения; AI читает целиком при разборе). **Foundation-слайс как 0011/0012: НИЧЕГО пока не читает/не пишет столбец → нулевое изменение поведения.** Аддитивно, обратимо (DROP COLUMN). Wire (action+repo+active-cardio finish-форма+completed-display) — следующим раном.
- **Реализация (4 файла, без db:generate — circuit snapshot-дрифт):** (1) schema `db/schema/cardio.ts` — `notes: text("notes")` в `cardioWorkouts` (nullable, `text` уже импортирован; Context7 подтвердил `text()` без `.notNull()` = `string|null`). (2) ручная идемпотентная `0013_cardio_workouts_notes.sql` (`ALTER TABLE … ADD COLUMN IF NOT EXISTS "notes" text`). (3) journal idx 13. (4) `0013_snapshot.json` (клон 0012 + notes-столбец в cardio_workouts-блок, prevId→0012 id) вручную. Применено на прод через **psql** после `pg_dump` (бэкап `~/fitness-backup.sql` 13M). Gate green (typecheck 0/lint 0/52 теста/build).
- **Прод-verify (master `1232428`):** (1) `\d cardio_workouts` → `notes | text | nullable` (NOT NULL отсутствует). (2) ROLLBACK-txn round-trip (прод-данные НЕ тронуты): `INSERT … notes='kak proshla kardio verify'` → `INSERT 0 1`, `SELECT name,notes` вернул заметку дословно, `ROLLBACK` → доказывает столбец пишется+читается (контракт для run-3 wire). (3) authed `/workouts` (сливает кардио) = HTTP 200 + рендерит → deployed-код SELECT'ит `cardio_workouts` с новым столбцом без 500. Тест-юзер удалён (--cleanup, deleted 1), temp `/tmp/g7c-verify.sql` удалён с VPS, local tmp удалён, прод-tree чист (HEAD `1232428`), все pm2 online (fitness-saas+cron+kanavto/site/among-irl нетронуты).
- **NEXT: G7c run-2 WIRE** — столбец уже на проде. Слайс: (1) `cardio.repo::finishCardioWorkout` (+ возможный `setCardioNotes`) принимает/пишет `notes`; (2) `finishCardioAction` читает `formData.get("notes")` (cap 500, trim) и прокидывает в repo; (3) `active-cardio.tsx` finish-форма (фаза «done») даёт `<Textarea>` «Как прошла кардио? (необязательно)» — паттерн идентичен `active-circuit.tsx` из run-1; (4) показать в completed-cardio detail + cardio AI-контекст (`buildCardioFocusedContext` или аналог — проверить, подаёт ли `w.notes`). Затем опц. остаток G6 (per-упражнение тренд словами) — низкий приоритет.
- **(история ниже)**
## ⏯ RESUME (2026-06-10, после G7c run-1 — feeling-note на завершении круговой) — **G7c круговая ЗАКРЫТА, NEXT=G7c run-2 (кардио, нужна миграция notes)**
- **G7c run-1 LIVE+verified** (master `7d1207a`): распространил F3 feeling-note («как прошло») на круговую. Активный экран круговой в фазе «done» теперь показывает необязательную Textarea «Как прошла круговая?» — текст уходит в `circuit_workouts.notes`, виден в истории и в AI-разборе. **Один UI-файл замкнул всю цепочку** — бэкенд был готов: `finishCircuitAction`/`finishCircuit` принимали/писали `notes` ещё с G2-эры, `completed-circuit.tsx` рендерит «Заметка», `buildCircuitFocusedContext` подаёт `w.notes` в контекст тренера. Не хватало только сбора текста в UI.
- **Реализация (1 файл `active-circuit.tsx`, аддитивно, без миграции, обратимо):** import `Textarea`; `useState finishNote`; в done-карточке (фаза «done») — поле «Как прошла круговая? (необязательно)» (rows=3, maxLength=500=серверный кап, токены R-36, resize-none); `finishAll` делает `fd.append("notes", finishNote.trim().slice(0,500))` если непусто. Library-check: вызов server-action из onClick с FormData — подтверждён Context7 (Next.js 16 «Invoke Server Actions via Event Handlers»), идентичен существующему `finishAll`. Gate green (typecheck 0/lint 0/52 теста/build).
- **Прод-verify (master `7d1207a`, Playwright drive, throwaway + seed: active круговая 1упр×1раунд с 1 round-log → `findFirstIncomplete`=done → экран открывается сразу в фазе «done»):** done-карточка отрендерила `#circuit-finish-note` (label «Как прошла круговая? (необязательно)», placeholder про самочувствие, maxlength 500) + «Завершить и сохранить». Ввёл note с маркером (native setter+input event на controlled textarea) → клик → **БД `circuit_workouts`: status=`completed`, notes=введённый текст дословно**; reload detail → completed-view показал секцию «ЗАМЕТКА» + текст, textarea исчез. **Доказательство фикса:** старый `finishAll` слал только circuitId → notes остались бы NULL, «Заметка»-секции бы не было. Скриншот `g7c-circuit-feeling-note.png`. Тест-юзер удалён (--cleanup, FK cascade → seeded circuit/exercises/logs=0), temp seed/check-mjs удалены с VPS, прод-tree чист, fitness-saas+cron online, login=200.
- **NEXT: G7c run-2** — кардио feeling-note. `cardio_workouts` (см. `db/schema/cardio.ts`) НЕ имеет `notes`; `finishCardioAction`/`finishCardioWorkout` не принимают текст. Слайс: (1) миграция +колонка `notes text` (pg_dump ОБЯЗАТЕЛЬНО, идемпотентная через psql как 0011/0012, journal+snapshot вручную — circuit snapshot-дрифт) → (2) `finishCardioAction`+repo принимают/пишут notes → (3) `active-cardio.tsx` finish-форма даёт поле «как прошло» → (4) показать в completed-cardio (+ cardio AI-контекст если есть). Бить на foundation(миграция)+wire если крупно. Затем опц. остаток G6 (per-упражнение тренд словами) — низкий приоритет.
- **(история ниже)**
## ⏯ RESUME (2026-06-10, после G7b run-2 — прямой re-run круговой/кардио из «сегодня») — **G7b ПОЛНОСТЬЮ ЗАКРЫТ (все 3 формата стартуют из «сегодня» одним кликом), NEXT=G7c**
- **G7b run-2 LIVE+verified** (master `5b6d677`): завершает прямой старт из «Сегодня по расписанию» для круговой/кардио. Строки, привязанные к круговой/кардио-заготовке, стали `<form>`+кнопкой (как силовой шаблон в run-1) вместо `<Link>` на detail — клик = re-run = клон плана заготовки в НОВЫЙ активный сеанс + редирект на него. Теперь ВСЕ 3 формата (силовой шаблон / круговая / кардио) стартуют из «сегодня» одним кликом; свободное расписание (нет привязки) остаётся `<Link>` на /create-пикер.
- **Реализация (4 файла, аддитивно, без миграции, обратимо):** (1) `circuits.repo::cloneCircuit(userId,sourceId)` — читает прошлую круговую (R-7 фильтр userId через getCircuitForUser), делегирует в `startCircuit` → копирует имя/раунды/отдыхи/упражнения с целями, НЕ логи раундов; G2-инвариант (startCircuit отменяет прошлую active-круговую) сохранён бесплатно. (2) `cardio.repo::cloneCardio(userId,sourceId)` — отдельная tx: копирует БЛОКИ напрямую (имя/preset/planJson/плановые длительности; сбрасывает фактические), точнее пере-вывода из preset для custom-параметров; отменяет прошлую active-кардио (G2); guard на 0 блоков. (3) `reRunCircuitAction`/`reRunCardioAction` (requireUser→clone→revalidate→redirect на новый /circuits|/cardio/[id]). (4) `TodayScheduleCard`: `PresetTarget` union расширен (template|circuit|cardio = прямой старт; link = свободное); DRY `START`-мапа {action,field} по формату; рендер — link→`<Link>`, иначе→`<form>`+hidden+кнопка. R-4 общий ROW_CLASS+RowInner, R-41 ≥56px. Gate green (typecheck 0/lint 0/52/build).
- **Прод-verify (master `5b6d677`, Playwright drive, throwaway + seed: completed круговая `9d13a9ae`(присед 1упр×3раунда) + completed Tabata `a54b1945` + 2 расписания daysOfWeek=[3]=сегодня-среда, привязанные к ним):** `/dashboard` блок «Сегодня по расписанию» — обе строки `<form>`+`<button type=submit>` (НЕ `<a>` — `isLink:false`), hidden `circuitId=9d13a9ae`/`cardioId=a54b1945`. `requestSubmit()` круговой → редирект `/circuits/7a7ccceb…` (новый id≠source); кардио → `/cardio/f8097ba9…` (новый id≠source). **БД:** клон-круговая `status=active`+name копирован+3 раунда+1 упр+**0 round_logs** (свежий старт); клон-кардио `active`+«G7b Tabata»+tabata+**16 блоков, 0 с actual** (план скопирован, факты сброшены); ОБА source остались `completed` (не тронуты). Скриншот `g7b-run2-circuit-cardio-rerun.png`. Тест-юзер удалён (--cleanup, cascade → orphan_clones=0), temp `scripts/seed-g7b.ts` удалён с VPS, прод-tree чист, все pm2 online (fitness-saas+cron+чужие), login=200.
- **NEXT: G7c** — комментарий после завершения для круговой/кардио (распространить F3 feeling-note силовой). Сейчас круговая `finishCircuitAction` уже принимает `notes`, кардио `finishCardioAction` — нет. Дать обоим finish-формам поле «как прошло» → сохранить (circuit.notes уже есть; cardio нужен notes-столбец ИЛИ заметка — проверить, возможна миграция) → показать в истории/разборе. Затем опц. остаток G6 (per-упражнение тренд словами) — низкий приоритет.
- **(история ниже)**
## ⏯ RESUME (2026-06-10, после G7b run-1 — прямой старт силового шаблона из «сегодня») — **G7b силовой путь ЗАКРЫТ, run-2 (круговая/кардио re-run) ТОЖЕ ЗАКРЫТ выше**
- **G7b run-1 LIVE+verified** (master `15875e3`): прямой старт привязанного СИЛОВОГО шаблона из блока «Сегодня по расписанию» одним кликом. Строка стала `<form action={startWorkoutFromTemplateAction}>` (hidden `templateId`) вместо `<Link>` на detail — «Начать» сразу создаёт активный сеанс из шаблона и редиректит на `/workouts/[id]`. Круговая/кардио/свободные — ссылкой как раньше (re-run круговой/кардио = клон session-instance, run-2). `resolvePreset`→discriminated `{kind}`; R-4 общий `ROW_CLASS`+`RowInner`; R-41 71px; G2-инвариант (старт отменяет прошлый active-силовой); R-7 (action ре-валидирует владение). Аддитивно, 1 файл, без миграции. Gate green (typecheck 0/lint 0/52/build).
- **Прод-verify (master `15875e3`, Playwright drive, throwaway + seed template[UUID]+`g7b-sch` daysOfWeek=[3]=сегодня):** `/dashboard` строка = `<form>`+`<button>` (НЕ `<a>` — старый Link ушёл, `anyPlainLink:null`), hidden templateId=UUID, 71px; `requestSubmit()` → редирект `/workouts/3c68f679…` active; БД новый workout `status=active`+`template_id`=UUID+клон-name. Поймал баг seed-данных: `startSchema` требует `.uuid()` → slug-id дал «Invalid templateId» 500 → re-seed реальным UUID (код корректен). Скриншот `g7b-direct-start-template.png`. Тест-юзер удалён (cascade→0/0/0), прод-tree чист, все pm2 online.
- **NEXT: G7b run-2** — круговая/кардио re-run (клон session-instance в новый active): repo `cloneCircuit`/`cloneCardio` + `TodayScheduleCard` сделать их строки тоже `<form>`-кнопкой. Затем G7c (коммент после завершения для круговой/кардио — F3 feeling-note). Опц. остаток G6 (per-упражнение тренд словами) — низкий приоритет.
- **(история ниже)**
## ⏯ RESUME (2026-06-10, после G7a подсветки «сегодня» — дашборд показывает «сегодня надо вот эту») — **G7a ПОЛНОСТЬЮ ЗАКРЫТ, NEXT=G7b**
- **G7a подсветка «сегодня» LIVE+verified** (master `f8e31ed`): финальный шаг UX-передела расписаний. Дашборд показывает блок «Сегодня по расписанию» — включённые расписания, чей день недели = сегодня (локальная TZ юзера), ссылкой ведут прямо на привязанную заготовку (или /create-пикер если свободное). Нет расписаний на сегодня → блок не рисуется.
- **Реализация (3 файла + 1 edit, аддитивно, без миграции):** (1) NEW `lib/datetime/local-day.ts` — pure `localDateIso`/`localIsoDay` (1=Пн..7=Вс, DST-safe через UTC-полдень) выделены из inline-копии cron push-reminders (помечено к объединению — cron НЕ трогали в одном слайсе с фичей); +7 unit-тестов (Пн/Вс, сдвиг через полночь ±, кривая TZ→фолбэк). (2) NEW `components/dashboard/TodayScheduleCard.tsx` — async server-компонент: `getUserProfile(tz)` + `listEnabledSchedulesForUser` + индекс id→имя (templates/circuits/cardio), фильтр `daysOfWeek.includes(todayIso)`, `resolvePreset`→href+имя (фолбэк-метка по формату если заготовка вне выборки/удалена FK SET NULL), ссылки ≥56px R-41, статус текстом не только цветом. (3) edit `dashboard/page.tsx` — рендер блока над StartCard. Gate green (typecheck 0/lint 0/52 теста/build).
- **Прод-verify (master `f8e31ed`, Playwright drive, throwaway-юзер):** seed template `g7a_today_tmpl` + расписание `sch_today` (daysOfWeek=[3]=сегодня-среда, привязано к template) + контроль `sch_other` (daysOfWeek=[1,5]=Пн/Пт). `/dashboard` → блок «Сегодня по расписанию» виден, строка «Segodnya test / G7a Today Tmpl / Начать», ссылка `href=/templates/g7a_today_tmpl`; контроль «Ne segodnya» ОТСУТСТВУЕТ → день-фильтр работает (сломанный показал бы оба). Скриншот `g7a-today-highlight-dashboard.png`. Тест-юзер удалён (--cleanup, cascade → schedules=0/template=0, orphans=0), temp SQL удалён, прод-tree чист, fitness-saas+cron online, login=200.
- **NEXT: G7b** — быстрый запуск заготовки из «сегодня» БЕЗ пикера. Сейчас ссылка ведёт на detail-страницу заготовки (`/templates|/circuits|/cardio/[id]`), оттуда юзер жмёт «Начать». G7b = прямой старт: re-run заготовки = клон в новый активный сеанс (circuit/cardio_workouts — session-instance, несут name+params+exercises → клонируются; template → `startWorkoutFromTemplate`). Учесть G2-инвариант: старт закрывает прошлую active этого формата в 'cancelled'. Затем G7c (коммент после завершения для круговой/кардио). Опц. остаток G6 (per-упражнение тренд словами) — низкий приоритет.
- **(история ниже)**
## ⏯ RESUME (2026-06-10, после G7a repo+UI — расписание привязывается к конкретной заготовке) — **G1·G2·G4·G5·G3·G6(core)·G7a-foundation·G7a-repo+UI ЗАКРЫТЫ, NEXT=G7a подсветка «сегодня»**
- **G7a repo+UI LIVE+verified** (master `8b2ec11`): второй шаг UX-передела расписаний. Теперь при создании расписания на `/schedule` можно опционально привязать КОНКРЕТНУЮ заготовку (силовой шаблон / круговую / кардио) через сгруппированный пикер; список расписаний показывает имя привязанной заготовки. NULL во всех 3 FK = «свободное» (как раньше, ведёт на /create-пикер).
- **Реализация (аддитивно, без миграции — столбцы добавлены foundation-слайсом `0ad963b`; обратимо; 5 файлов = 1 вертикальный слайс):** (1) `server/schemas/schedule.ts` += опц. `preset` (regex `^(template|circuit|cardio):.+`). (2) `lib/repos/schedule.repo.ts`: `SchedulePresetLink` тип, `NewScheduleInput += preset?`, приватный `assertPresetOwned` (R-7 — SELECT по id+userId из нужной из 3 таблиц, бросает если не владелец; FK без RLS владельца не проверяет) + `presetColumns` (раскладка линка в 3 nullable-столбца, ровно один). (3) `server/actions/schedule.ts`: `formData.get("preset")||undefined`, `parsePresetLink` строка→линк, try/catch вокруг createSchedule. (4) форма — нативный `<select name="preset">` с `<optgroup>` на формат (пустая опция = свободное; блок рендерится только если есть хоть одна заготовка). (5) `page.tsx` — `Promise.all` грузит templates/circuits/cardio, строит опции пикера + индекс id→имя; `resolvePresetLabel` рисует имя привязки под расписанием (фолбэк-метка по формату если заготовка вне recent-выборки / удалена→SET NULL). Gate green (typecheck 0/lint 0/45 тестов/build).
- **Прод-verify (master `8b2ec11`, Playwright drive, throwaway-юзер + seeded template `g7a-verify-tmpl`):** `/schedule` пикер показал шаблон под «Силовые шаблоны», circuit/cardio-optgroup СКРЫТЫ (не сидил → conditional рендер). Драйв формы (label+день Пн+select template) → submit → **БД `workout_schedules`: `template_id=g7a-verify-tmpl`, circuit_workout_id/cardio_workout_id NULL** (ровно один FK); список отрендерил строку «G7a Verify Shablon» под расписанием. Контроль «свободное» (preset="") → новая строка со ВСЕМИ 3 FK NULL (старое поведение цело). Скриншот `g7a-schedule-preset-binding.png`. Тест-юзер удалён (--cleanup, FK cascade → schedules=0), seed-template+temp SQL удалены, прод-tree чист, все pm2 online, login=200.
- **NEXT: G7a подсветка «сегодня»** — на главной/`/workouts` подсветить расписание, чьё `daysOfWeek` включает сегодняшний ISO-день, со ссылкой на привязанную заготовку («сегодня надо вот эту»). `listSchedules` уже отдаёт FK-привязку + enabled; нужен helper «расписания на сегодня» (фильтр по дню недели в локальной TZ юзера — см. cron push-reminders за паттерном вычисления локального дня) + UI-блок. Затем G7b (быстрый запуск без пикера — re-run заготовки = клон), G7c (коммент после завершения для круговой/кардио). Опц. остаток G6 (per-упражнение тренд словами) — низкий приоритет.
- **(история ниже)**
## ⏯ RESUME (2026-06-10, после G7a foundation — 3 nullable FK расписание→заготовка) — **G1·G2·G4·G5·G3·G6(core)·G7a-foundation ЗАКРЫТЫ, NEXT=G7a repo+UI**
- **G7a foundation LIVE+verified** (master `0ad963b`): первый шаг UX-передела расписаний (запрос владельца «расписание → конкретный шаблон → сегодня надо вот эту»). Добавлены 3 nullable FK на `workout_schedules`: `template_id`→workout_templates (силовая) / `circuit_workout_id`→circuit_workouts (круговая) / `cardio_workout_id`→cardio_workouts (кардио). **R-29:** 3 отдельных FK вместо полиморфного type+id. Все nullable, `ON DELETE SET NULL` (удалили заготовку → расписание остаётся «свободным», само напоминание валидно). Семантика: NULL во всех = свободное расписание (ведёт на /create-пикер как раньше); ровно одна заполнена = «сегодня надо эту» → прямой запуск (G7b). **circuit_workouts/cardio_workouts — session-instance (есть status), но несут name+params+exercises → дублируют роль reusable-заготовки; re-run = клон (G7b). Отдельные reusable circuit/cardio-template таблицы — YAGNI до запроса.**
- **Реализация (foundation-слайс, как F4-incr.2b/F6/F7 foundation — НИЧТО пока не читает столбцы → нулевое изменение поведения; repo-запись и подсветка «сегодня» следующими ранами):** schema `db/schema/schedule.ts` (+3 `.references(onDelete:'set null')`); ручная идемпотентная миграция `0012_workout_schedules_template_link.sql` (`ADD COLUMN IF NOT EXISTS` ×3 + `DO $$ … EXCEPTION WHEN duplicate_object $$` FK-гарды ×3); journal idx 12 + `0012_snapshot.json` (клон 0011 + 3 столбца/FK в workout_schedules-блок) вручную — **НЕ db:generate** (circuit snapshot-дрифт пере-создал бы circuit-таблицы). Применено на прод через **psql** после `pg_dump` (бэкап 13.5M). Аддитивно, обратимо (DROP COLUMN). Gate green (typecheck 0/lint 0/45 тестов/build).
- **Прод-verify (master `0ad963b`):** `\d workout_schedules` → `template_id`/`circuit_workout_id`/`cardio_workout_id` text nullable + 3 FK `ON DELETE SET NULL` к верным таблицам (+ старый user FK cascade цел). FK round-trip в **ROLLBACK-txn** (прод-данные НЕ тронуты): (1) валидная привязка `template_id='g7a-tmpl'` ПРИНЯТА, SELECT показал хранится, два др. столбца NULL; (2) богус `template_id='__nonexistent__'` ОТКЛОНЁН — `ERROR … violates foreign key constraint workout_schedules_template_id_workout_templates_id_fk` (констрейнт энфорсит); (3) DELETE привязанного шаблона → расписание выжило, `template_id` стал NULL (доказывает ON DELETE SET NULL). Authed `/schedule`=200 рендерит список (select* над новой формой таблицы — deployed-код читает миграцию без 500; «500» в HTML = `fontWeight:500`, не ошибка). Тест-юзер удалён (--cleanup), temp `/tmp/g7a.sql` удалён с VPS, все pm2 online (fitness-saas+cron+чужие нетронуты), login=200.
- **NEXT: G7a repo+UI** — `createSchedule`/форма `/schedule` принимают опц. привязку к заготовке (пикер «к какой заготовке привязать»); `listSchedules` отдаёт + UI показывает связь. Затем G7a-подсветка «сегодня» (расписание с сегодняшним днём недели подсвечено на главной/`/workouts` со ссылкой на заготовку), G7b (быстрый запуск без пикера), G7c (коммент после завершения для всех форматов). Опц. остаток G6 (per-упражнение тренд словами) — низкий приоритет.
- **(история ниже)**
## ⏯ RESUME (2026-06-10, после G6 слайс-1 — карточка-вывод объёма словами на /stats) — **G1·G2·G4·G5·G3·G6(core) ЗАКРЫТЫ, NEXT=G7a**
- **G6 слайс-1 LIVE+verified** (master `7a235d9`): главная боль владельца «графики абстрактны (kg·повт), хочу понятный вывод словами». Над графиками `/stats` теперь ведёт цветная карточка-вердикт: сравнивает тренировочный объём текущего периода с предыдущим окном той же длины и говорит человеческим языком «Объём за месяц на 100% больше, чем за прошлый — так держать». Цифры (KPI/графики) идут СЛЕДОМ.
- **Реализация (аддитивно, без миграции, обратимо, 5 файлов = 1 вертикальный слайс):** (1) `lib/repos/stats.repo.ts::periodVolumeComparison(userId,range)`→`{current,previous}` working-тоннаж текущего+предыдущего окна (приватный `workingTonnage(from,to)` helper, только completed G1; range='all'→previous=null). (2) pure `lib/domain/stats/period-insight.ts::summarizeVolumeChange(current,previous,range)`→`{status,headline,detail,pct}` (переиспользует `trendStatus` F4/F7; epsilon=5% от prev→«держишь уровень»; previous null/≤0→«копим данные», без ложного прогресса; `InsightRange`-union продублирован локально вместо импорта из repos — домен чист R-7). (3) `components/stats/PeriodInsightCard.tsx` — карточка на `TREND_TONE` (DRY с графиками/разбором), статус текстом+иконкой не только цветом (R-41). (4) page wire: `periodVolumeComparison` в Promise.all, карточка после `PeriodPills` перед KPI. +8 unit-тестов. Gate green (typecheck 0/lint 0/45 тестов/build).
- **Прод-verify (master `7a235d9`, throwaway-юзер + seed 2 периода: прошлый месяц workout 100×5=500, этот месяц 100×10=1000):** Playwright authed `/stats?range=30d` → DOM: headline «Ты растёшь 💪», detail «Объём тренировок за месяц на 100% больше, чем за прошлый месяц. Так держать!» (1000 vs 500=+100% верно), бейдж «↑ +100%» `aria-label=Рост`, `insightBeforeKpi=true` (карточка ведёт над KPI-сеткой). `/stats?range=7d` (предыдущее 7-дн окно пусто → previous=0) → «Пока копим данные», бейджа нет (ветка no-baseline сработала). Доказывает: repo считает оба окна, домен формирует вердикт, карточка рендерит. Seed удалён (DELETE 2, leftover=0), тест-юзер удалён (--cleanup), все pm2 online (fitness-saas+cron+чужие нетронуты), login=200.
- **NEXT: G7a** — расписание привязать к КОНКРЕТНОМУ шаблону (templateId/circuitId/cardioId → `workout_schedules`, **МИГРАЦИЯ + pg_dump ОБЯЗАТЕЛЬНО**) + подсветка «сегодня надо такую» на главной/в тренировках. Сейчас `/schedule` хранит только label+дни+час, ведёт на `/create`-пикер, а не на конкретный шаблон. Бить: схема+миграция (FK на шаблон, nullable — расписание может быть «свободным») → repo → подсветка «сегодня». Опц. остаток G6 (per-упражнение тренд словами, «что это значит» под графиком) — низкий приоритет, основной вердикт уже LIVE.
- **(история ниже)**
## ⏯ RESUME (2026-06-10, после G3-followup — круговая показывает цветную карточку) — **G1·G2·G4·G5·G3(strength+circuit) ЗАКРЫТЫ, NEXT=G6**
- **G3-followup LIVE+verified** (master `f941c69`): круговая `completed-circuit.tsx` `AnalysisCard` при валидном `resultJson` рендерит цветную `TrainerResultCard` (как силовая G3), а не plain markdown `{analysis.content}`; `trainerSchema.safeParse` страхует legacy → markdown-фолбэк. Круговые job'ы идут через тот же `generateTrainerResponse` → пишут trainerSchema-совместимый resultJson → карточка применима. **Share-страница `/a/[token]:55` НЕ трогалась — там уже safeParse→card→фолбэк; «осколок» был ложным (фолбэк-ветка, не баг).** Аддитивно, один файл, без миграции. Gate green (typecheck 0/lint 0/37 тестов/build).
- **Прод-verify (master `f941c69`, throwaway + 2 seeded completed круговые):** struct (resultJson) → DOM «Прогресс по упражнениям»+score 78+«Бёрпи», raw `STRUCT_MD_LEAK` и старый заголовок «Разбор тренера» ОТСУТСТВУЮТ (старый код показал бы markdown = доказательство фикса); legacy (resultJson NULL) → markdown-фолбэк `LEGACY_MD_VISIBLE`+«Разбор тренера», карточки нет (backward-compat). Скриншот `g3-followup-circuit-colored-card.png`. Тест-юзер удалён (cascade → 0|0), temp SQL удалён VPS+локально, прод-tree чист, все pm2 online, login=200.
- **NEXT: G6** — графики на `/stats` понятнее людям: человеческие подписи, явный вывод «растёшь/стоишь/падаешь по упражнению», сравнение период-к-периоду словами, меньше жаргона (kg·reps) — текстовые карточки-инсайты над графиком. Затем G7a/b/c (UX расписания → конкретный шаблон + «сегодня» + комментарий для всех форматов; КРУПНЫЙ + миграция pg_dump). Бить G6 на слайсы (напр. один инсайт-блок за ран).
- **(история ниже)**
## ⏯ RESUME (2026-06-10, после G3 силовой — история показывает цветную карточку, не plain markdown) — **G1·G2·G4·G5·G3(strength) ЗАКРЫТЫ**
- **G3 силовой путь LIVE+verified** (master `034ab62`): главная боль владельца «сохранённый разбор = сплошной текст без цвета». **Root cause:** страница завершённой силовой `/workouts/[id]` (CompletedView) тянула разбор через `getAiAnalysisForWorkout` (только `content`=markdown) → рендерила plain-text `AnalysisCard`. Цветная `TrainerResultCard` (resultJson, per-exercise дельты F4) жила ТОЛЬКО на отдельной `/trainer`-странице. Владелец из истории попадал на детальную → видел сплошной текст.
- **Фикс (аддитивно, без миграции, обратимо, 2 файла):** (1) `getAiAnalysisForWorkout` дотягивает `resultJson` (тот же select-паттерн что `getLatestTrainerResult`); (2) CompletedView: `resultJson` есть → цветная `TrainerResultCard` + кнопка «Открыть разговор»; legacy без resultJson → markdown-фолбэк `AnalysisCard`; нет разбора → `CoachCta`. Анимация «думает» + живой текст уже были (F8 stream-консьюмер + скелетон F8 run-3) — не трогал. Gate green (typecheck 0/lint 0/37 тестов/build).
- **Прод-verify (master `034ab62`, throwaway-юзер + seeded completed силовая `b1c98979` с ai_analyses.resultJson structured):** authed `/workouts/b1c98979…` → DOM: header «ОЦЕНКА ТРЕНЕРА» + `82/100`, секция «Прогресс по упражнениям» (эксклюзив `TrainerResultCard`, в `AnalysisCard` её нет), цветная строка-дельта «Жим лёжа со штангой · 80×5 → 80×6 ↑» (tone-bg `rounded-lg`), кнопка «Открыть разговор». **Доказательство фикса:** raw markdown `# Разбор тренировки (82/100)` ОТСУТСТВУЕТ (старый `AnalysisCard` отрендерил бы его как `whitespace-pre-wrap`). Скриншот `g3-colored-card-in-history.png`. Тест-юзер удалён (--cleanup, FK cascade → workout/exercises/sets/analysis = 0), seed-скрипт удалён с VPS+локально, прод-tree чист, оба pm2 online, login=200.
- **NEXT: G3-followup** — те же plain-`{analysis.content}` осколки на ДРУГИХ поверхностях: `completed-circuit.tsx:174` (круговая) + share-страница `app/a/[token]/page.tsx:55`. Перевести на `TrainerResultCard` где есть resultJson (для круговой проверить, что resultJson вообще пишется — у круговой свой формат). Затем G6 (графики понятнее людям: текстовые инсайты над графиком), G7a/b/c (UX расписания, КРУПНЫЙ + миграция pg_dump).

## ⏯ RESUME (2026-06-10, после G5 — sanity-guard на невозможный прогресс) — **G1·G2·G4·G5 ЗАКРЫТЫ, NEXT=G3**
- **G5 LIVE+verified** (master `6f56348`): защита от фантомного прогресса (+50кг/нед как в G4). Pure domain `assessProgressJump(prevKg,curKg)`→`{suspicious,deltaKg,pct}`; подозрительно = `prev>0 && deltaKg≥10 && pct≥0.2` (реальный недельный прирост силы — единицы %; e1RM-скачок >20% при заметной абсолютной дельте ≈ ошибка ввода/опечатка, не рекорд). Абсолютный floor 10кг гасит шум на лёгких упражнениях (5→8кг=+60% безобиден). **Не блокирует сохранение** — только помечает. 8 unit-тестов.
- **Две точки подключения:** (1) `checkPersonalRecord` += `suspicious` (additive optional — функция пока dormant/без UI-потребителя, но теперь корректна для будущего PR-бейджа: «не показывать зелёный рекорд молча на подозрительном скачке»); (2) AI-контекст `formatOneRmTrend` (`lib/ai/context-builder.ts`): подозрительная e1RM-строка аннотируется «⚠️ ПОДОЗРИТЕЛЬНЫЙ СКАЧОК (+X% за сессию) — вероятно ошибка ввода; НЕ считать рекордом, не хвалить, попросить проверить ввод» → AI больше не врёт/не хвалит за нереальный прогресс (прямая боль владельца). Аддитивно, без миграции, обратимо. Gate green (typecheck 0/lint 0 err/37 тестов (+8)/build).
- **Прод-verify (master `6f56348`, tsx `scripts/verify-g5.ts` на VPS):** throwaway-юзер + seed прошлая(2 упр по 100кг, e1RM 100)+сегодня(Жим штанга 160кг=e1RM 160 +60% / Жим гантели 103кг=+3%, reps=1 → e1RM=вес). `buildTrainerContext(post_workout)` блок «# Прогресс 1RM»: строка штанги = `100.0 → 160.0 kg (+60.0) ⚠️ ПОДОЗРИТЕЛЬНЫЙ СКАЧОК (+60% за сессию)…`, строка гантелей = `100.0 → 103.0 kg (+3.0)` БЕЗ маркера. **Доказательство:** guard селективен (флагует только неправдоподобный скачок, не blanket). Тест-юзер удалён (--cleanup, FK cascade → workouts/exercises/sets), temp-скрипт удалён с VPS+локально, прод-tree чист, оба pm2 online, login=200.
- **NEXT: G3** — моментальный + КРАСИВЫЙ AI-разбор. Investigate почему сохранённый разбор = plain markdown, а не цветная `TrainerResultCard` (F4) со сравнением vs прошлая сессия того же типа. Вероятно: рендерится markdown `content` вместо `resultJson`, ИЛИ `exerciseComparisons`/дельты пустые. Цель: завершил → моментальный старт + анимация «думает» (скелетон F8 run-3 уже есть, владелец хочет красивее) → финал = цветная карточка с per-exercise дельтами. Полировка цепочки F4+F8, не новый бэкенд. Затем G6 (графики людям понятнее), G7a/b/c (UX расписания, КРУПНЫЙ + миграция).

## ⏯ RESUME (2026-06-10, после F9 run-4 — тайл «Последняя» на дашборде = последняя сессия ЛЮБОГО формата) — **единый поток укреплён ещё на один уровень**
- **F9 run-4 LIVE+verified** (master `81fece5`): владелец снова попросил «по тренировкам один поток, а не три разные вкладки». Аудит подтвердил: nav/feed/start/resume/history уже едины. Найден ПОСЛЕДНИЙ остаточный strength-only осколок: тайл «Последняя» на дашборде (`LastWorkoutMini`) читал только `listRecentWorkouts` (силовые) → свежая круговая/кардио-сессия в нём «пропадала», показывалась устаревшая силовая. Форматы жили в разных мирах. **Аддитивно, без миграции, обратимо, один shared-экспорт + дашборд.**
- **Что сделано:** (1) новый презентационный `LastSessionMini({item})` в общем `components/workouts/workout-history.tsx` (R-4 DRY — переиспользует `FORMAT_HREF`/`CARDIO_FORMAT_LABEL`/`formatShortDate`/`FormatPill`; одна заглавная метрика на формат через `lastSessionHeadline`: силовая=N подходов, круговая=N кругов·M упр., кардио=N мин; AI-бейдж для силовых с разбором). (2) Дашборд: `latestSession = buildHistory(strength,circuits,cardio)[0]` (тот же общий билдер что «Недавние» и `/workouts`) → тайл «Последняя» теперь = реальная последняя сессия любого формата с корректным detail-роутом. Strength-only `LastWorkoutMini` удалён. `last` (последняя СИЛОВАЯ) сохранён — нужен `TrainerTrigger` (AI-тренер анализирует силовые). Токены R-36. Gate green (typecheck 0/lint 0 err/29 тестов/build).
- **Прод-verify (master `81fece5`, throwaway-юзер + seeded силовая 2 дня назад + кардио-Tabata 1ч назад, обе completed):** authed `/dashboard` → тайл «Последняя» = `href=/cardio/last-cardio-verify`, текст «Tabata · Tabata (свежая) · Сегодня 11:05 · 4 мин» (свежая кардио, НЕ 2-дневная силовая). **Доказательство фикса:** старый strength-only код показал бы силовую; новый показал кардио. Силовая `/workouts/last-str-verify` присутствует только в «Недавние» (оба формата сосуществуют, тайл корректно выбирает последний по времени). Скриншот `f9-last-session-any-format.png`. Тест-юзер удалён (--cleanup, FK cascade → seeded workout/cardio/blocks = 0|0|0), temp SQL удалён, прод-tree чист, оба pm2 online, login=200.
- **NEXT: эпик ПОЛНОСТЬЮ ЗАКРЫТ (F1–F9 + F4-incr.2b + F8 run-3 + F9 run-4).** Единый поток выполнен на ВСЕХ уровнях: nav (1 вкладка) · feed (`/workouts`) · start (`/create`) · resume (`ResumeBanner`) · history (редиректы) · dashboard «Недавние» И «Последняя» (любой формат). Глубоко-опц. остатки (требуют миграцию + продуктовое решение владельца, YAGNI v1): объединение 3 формат-билдеров `/create` в единый конструктор (крупный UX-редизайн — разные модели данных: силовые подходы / круговые раунды / кардио-интервалы; merge спорен по UX); унификация detail/active-экранов; WeekCard-тоннаж силовой (намеренно — у кардио нет тоннажа). **Если авто-ран запущен и нечего делать — записать, что эпик закрыт, и остановиться.**

## ⏯ RESUME (2026-06-10, после F8 run-3 — скелетон карточки разбора при стриме) — **ЭПИК F1–F9 + F4-incr.2b + F8 run-3 ПОЛНОСТЬЮ ЗАКРЫТ**
- **F8 run-3 LIVE+verified** (master `6cb50b5`): UX-полиш ожидания AI-разбора. Раньше во время стрима (~30с генерации DeepSeek) показывался одностраничный спиннер «Тренер анализирует…». Теперь — **скелетон в форме будущей `TrainerResultCard`** (оценка/цитата/3 аспекта/2 сравнения/рекомендации), чтобы было видно ЧТО грузится. **Один client-файл `components/trainer/TrainerStreamConsumer.tsx`, аддитивно, без миграции, обратимо.**
- **Что сделано:** `LoadingState` (bare-спиннер) → `TrainerSkeleton`: контейнер как у карточки (`bg-card border-border rounded-2xl p-6`), сверху реальный статус-текст со спиннером (меняется по факту байт стрима: анализирует→пишет→собираю — поведение сохранено), ниже `animate-pulse`-каркас из ~16 `bg-muted`-блоков (helper `Bar`, DRY). Честный индикатор: стрим `streamText` НЕ отдаёт Content-Length → каркас ожидаемого, НЕ проценты. A11y: `role=status`+`aria-busy`+`aria-live` на контейнере (статус-текст озвучивается), скелетон-блоки `aria-hidden`. `motion-reduce:animate-none` — уважает prefers-reduced-motion (concern #7). Токены R-36. Gate green (typecheck 0/lint 0 err/29 тестов/build).
- **Прод-verify (master `6cb50b5`, throwaway-юзер + seeded completed workout `26b7925f` без сохранённого разбора → фолк роутит на стрим-консьюмер):** authed заход на `/workouts/26b7925f/trainer` ВО ВРЕМЯ стрима → `[role=status]` присутствует (`aria-busy=true`), статус-текст «Тренер пишет разбор…» (bytes>0 фаза — доказывает что прогресс-текст обновляется по байтам), **16 скелетон-баров** `.bg-muted.rounded` (vs старый 1 спиннер-строка), pulse-обёртка `aria-hidden=true`; карточки ещё нет. Скриншот `f8-run3-skeleton.png`. Через ~50с дождался → «Оценка тренера»-карточка появилась, `[role=status]`+скелетон ушли (`statusGone:true, skeletonBars:0, cardPresent:true`) → стрим-пайплайн цел, скелетон корректно заменяется реальным разбором. Seed-скрипт удалён с VPS, тест-юзер удалён (--cleanup, FK cascade → orphan workout=0), прод-tree чист, оба pm2 online.
- **NEXT: эпик ПОЛНОСТЬЮ ЗАКРЫТ (F1–F9 + F4-incr.2b + F8 run-3).** Реальных незакрытых слайсов нет (чеклист весь `[x]`, кроме F0 login-helper = инфра-заметка про локальный Docker, и F4 «verify на реальной истории» = опц. прогон). Глубоко-опц. остатки (все требуют миграцию + продуктовое решение владельца, YAGNI v1): прямой запуск конкретного шаблона из workout-напоминания (templateId→`workout_schedules`); privacy opt-out видимости тренировок для друзей (`users.share_workouts`); объединение 3 формат-билдеров `/create` в единый конструктор (крупный UX-редизайн). Запрос владельца «один поток, не три вкладки» выполнен на всех уровнях (nav 1 вкладка · feed `/workouts` · start `/create` · resume `ResumeBanner` · history редиректы). **Если авто-ран запущен и нечего делать — записать в план, что эпик закрыт, и остановиться (так и сделано).**

## ⏯ RESUME (2026-06-10, после F4-incr.2b run-2 — численная эффект.нагрузка bodyweight в trainer-контексте) — **ЭПИК F1–F9 + F4-incr.2b ПОЛНОСТЬЮ ЗАКРЫТ**
- **F4-incr.2b run-2 LIVE+verified** (master `20837c5`): завершает bodyweight-эффект.нагрузку. Раньше `is_bodyweight`-столбец жил, но ничто его не читало (foundation-слайс). Теперь `buildTrainerContext` (контекст для AI-тренера) даёт AI ЧИСЛЕННУЮ эффективную нагрузку для bodyweight-упражнений (подтягивания/брусья/отжимания): введённый `weightKg` = добавка → реальная нагрузка = вес тела + добавка, и доля добавки в тотале. Дополняет качественную подсказку из «Профиля атлета» (run-2a) численно. **Аддитивно, без миграции, один продакшн-файл + новый pure-helper + тест.**
- **Что сделано:** (1) pure domain `lib/domain/progression/bodyweight-load.ts::bodyweightEffectiveLoad(bodyweightKg, sets)` → `{effectiveKg, addedKg, pct}` по top-set (макс. добавка); null при нет-подходов / невалидном весе тела; clamp добавки ≥0; 5 unit-тестов. (2) `WorkoutSummary.exercises += isBodyweight`; `loadWorkoutSummary` и `loadPastWorkouts` селектят `schema.exercises.isBodyweight`. (3) `loadLatestBodyweight(userId)` (последний `body_measurements.weightKg`); `buildCoachContext` грузит его раз и прокидывает в `formatWorkout(w, bodyweightKg)`. (4) `formatWorkout` для bodyweight-упр. с известным весом тела добавляет строку «вес тела N кг → эфф. нагрузка top-set ≈ M кг (добавка X кг = Y% тотала)». Нет веса тела ИЛИ не-bodyweight упр. → строка не добавляется = поведение как раньше. Прошлым тренировкам применяется текущий вес тела (историю веса по дате не храним; аппроксимация документирована в комментарии). Gate green (typecheck 0/lint 0 err/29 тестов/build).
- **Прод-verify (master `20837c5`):** на VPS через `tsx` напрямую вызвал `buildTrainerContext` для seeded throwaway-юзера: completed workout с bodyweight-упр. (dips-chest, `is_bodyweight=true`, 1 working-подход 20кг×6) + `body_measurement` 90кг. Вывод содержал ровно `вес тела 90 кг → эфф. нагрузка top-set ≈ 110 кг (добавка 20 кг = 18% тотала)` — численно верно (90+20=110, 20/110=18%). Прямая функциональная проверка нового кода (а не «страница грузится»): доказывает что флаг читается из БД, вес тела подтягивается, helper считает, строка попадает в AI-контекст. Seed (workout+measurement) удалён скриптом, temp `verify-bw.ts` удалён с VPS, тест-юзер удалён (`issue-session --cleanup`, FK cascade), прод-tree чист, все pm2 online (fitness-saas+cron), login=200.
- **NEXT: эпик ПОЛНОСТЬЮ ЗАКРЫТ (F1–F9 + F4-incr.2b).** Реальных незакрытых слайсов нет. Глубоко-опц. низкоценный полиш, можно не делать: F8 run-3 (скелетон/прогресс-бар по байтам стрима вместо статичного «Тренер анализирует…»); прямой запуск конкретного шаблона из workout-напоминания (требует привязку templateId к `workout_schedules` = миграция, YAGNI); privacy opt-out видимости тренировок для друзей (колонка `users.share_workouts` = миграция, YAGNI v1). Если авто-ран запущен и нечего делать — выбрать F8 run-3 (чистый UX-полиш, без миграции) ИЛИ записать в план, что эпик закрыт, и остановиться.

## ⏯ RESUME (2026-06-10, после F4-incr.2b foundation — `exercises.is_bodyweight` столбец+сид)
- **F4-incr.2b foundation LIVE+verified** (master `6517a48`): аддитивный столбец `exercises.is_bodyweight` (boolean default false NOT NULL) помечает упражнения, где базовая нагрузка = вес тела, а введённый вес = ДОБАВКА сверху (подтягивания/брусья/отжимания). Сид-флаг на 5 системных: `pull-up`·`chin-up`·`dips-chest`·`dips-triceps`·`push-up` (кастомные = false по умолчанию). **Ничего пока НЕ читает столбец → нулевое изменение поведения** (foundation-слайс как F6 run-1 / F7 run-1). Seed-runner пишет флаг в insert+onConflictUpdate (re-seed обновит). Gate green (typecheck 0/lint 0 err/24 теста/build).
- **МИГРАЦИЯ `0011`:** ручная идемпотентная `0011_exercises_is_bodyweight.sql` (`ADD COLUMN IF NOT EXISTS` + `UPDATE ... WHERE slug IN(5)`) — **НЕ db:generate** (circuit snapshot-дрифт пере-создал бы circuit-таблицы, см. ниже про 0007). Применена на прод через **psql** после `pg_dump` (бэкап `~/fitness-backup.sql` 13M). Journal idx 11 + `0011_snapshot.json` (копия 0010 + is_bodyweight в exercises-блоке) добавлены вручную. Обратимо (`DROP COLUMN`).
- **Прод-verify (master `6517a48`):** psql применил (ALTER TABLE ok, `UPDATE 5`); `\d exercises` → `is_bodyweight | boolean | not null | default false`; ровно 5 ожидаемых слугов flagged (chin-up·dips-chest·dips-triceps·pull-up·push-up); `bw_true=5, nulls=0` (NOT NULL держит на существующих строках); миграция применена ДО деплоя кода (новая схема селектит столбец) → build+reload; authed `/exercises`=200 рендерит список упражнений (Жим лёжа·Отжимания·Подтягивания) = live SELECT с новым столбцом работает, нет 500. Тест-юзер удалён (--cleanup), оба pm2 online, login=200.
- **NEXT: F4-incr.2b run-2 (context-builder численная эффект.нагрузка).** В `lib/ai/context-builder.ts` `buildTrainerContext`: при формировании истории/профиля для bodyweight-упражнений (флаг `is_bodyweight`) считать эффективную нагрузку = вес тела (из body_measurements) + введённый вес (добавка), и подавать AI в % тотала (падение добавки 20→10 при 90кг = <10% тотала — prompt уже рассуждает качественно, теперь будет и численно). Репо `exercises` уже отдаёт `isBodyweight` через `$inferSelect`. Аддитивно, без миграции. После — **эпик F1–F9 + F4-incr.2b ПОЛНОСТЬЮ ЗАКРЫТ.**

## ⏯ RESUME (2026-06-10, после F9 run-3 — ретайр дублирующих формат-вкладок `/circuits`·`/cardio`) — **запрос владельца «один поток» выполнен на ВСЕХ уровнях**
- **F9 run-3 LIVE+verified** (master `a3d3dcc`): владелец снова попросил «по тренировкам один поток, а не три разные вкладки на каждую». Последняя фрагментация = две дублирующие формат-специфичные history-страницы `/circuits` и `/cardio` (показывали ту же историю формата, что уже слита в `/workouts`, + свой active-resume + свою кнопку «Новая»). **Аддитивно, без схемы/миграции, обратимо.**
- **Что сделано:** (1) `/circuits` и `/cardio` (bare-list) → `redirect('/workouts')` (Next 16 `redirect` из `next/navigation`). (2) `/workouts` стал ПОЛНЫМ единым экраном истории — теперь surface active-сессию ЛЮБОГО формата через общий `ResumeBanner` (DRY с дашбордом; раньше `/workouts` показывал resume ТОЛЬКО силовой — круговая/кардио-resume жили лишь на удаляемых вкладках). (3) Back-ссылки 4 страниц (`/circuits/new`·`/circuits/[id]`·`/cardio/new`·`/cardio/[id]`) репойнтнуты на `/workouts`. Detail-роуты и билдеры `/new` нетронуты; создание — через `/create`; `/templates` (силовые шаблоны, иной концепт) сохранена. Gate green (typecheck 0/lint 0 err/24 теста/build).
- **Прод-verify (master `a3d3dcc`, throwaway-юзер + seeded active circuit `c1a78da4`):** authed `/circuits`→`/workouts`, `/cardio`→`/workouts` (редиректы LIVE); `/workouts` показал `ResumeBanner` «Активная круговая»→`/circuits/c1a78da4` (доказывает новый all-format resume — старый strength-only код круговую НЕ показал бы); `/circuits/c1a78da4`=200 (не проглочен редиректом), back=«К истории»→`/workouts`. Тест-юзер удалён (--cleanup, FK cascade → orphan circuit=0), temp SQL удалён, оба pm2 online, login=200.
- **Единый поток выполнен на всех уровнях:** nav (1 вкладка «Тренировки») · feed (1 экран `/workouts` со всеми форматами) · start (1 вход `/create`) · resume (1 `ResumeBanner` на формат на дашборде И в фиде) · history (`/circuits`·`/cardio` редиректят в `/workouts`).
- **NEXT: эпик F1–F9 ЗАКРЫТ.** Остаётся только глубоко-опц. низкоценный полиш: **F4-инкр.2b** (`exercises.is_bodyweight` миграция — deferred, ИИ уже рассуждает качественно) ИЛИ **F8 run-3** (скелетон/прогресс-бар стрима). Оба — низкий приоритет, можно не делать.

## ⏯ RESUME (2026-06-10, после F9 run-2 — единый вход «Начать тренировку» на дашборде) — **по запросу владельца «один поток, не три разные вкладки на каждую»**
- **F9 run-2 LIVE+verified** (master `d13eb23`): владелец повторно попросил «по тренировкам один поток, а не три разные вкладки на каждую». Дашборд показывал **три формат-специфичных тайла** — `StartCard` (силовая→/templates,/create) + локальный `CardioTile` (кардио→/cardio/new) + `CircuitTile` (круговая→/circuits) — каждый со своим стартом И своим активным-resume. Это и была фрагментация. **Аддитивно, без схемы/миграции.**
- **Что сделано:** (1) единый CTA «Начать тренировку»→`/create` (пикер формата, уже live) вместо трёх раздельных стартов; вторичная «Мои шаблоны»→/templates сохранена. (2) Новый общий `components/workouts/../dashboard/ResumeBanner.tsx` (R-4 DRY — три почти одинаковых active-блока схлопнуты в один; формат различает только подпись `label`); дашборд строит `resumes[]` = filter активных {силовая, круговая, кардио} → map ResumeBanner (несколько активных → несколько баннеров, детерминир. порядок). (3) Удалён orphaned `CircuitTile.tsx` (grep: только дашборд), локальные `ResumeCard`/`CardioTile` удалены, `getActiveCircuitId` поднят в Promise.all дашборда (тот же запрос, без N+1). Per-format «последняя сессия» info — теперь в едином «Недавние» (F9 run-1), не потеряна. Gate green (typecheck 0/lint 0 err/24 теста/build).
- **Прод-verify (master `d13eb23`, throwaway-юзер):** (A) без активных сессий `/dashboard` (authed curl, raw SSR) = StartCard «Начать тренировку»→`/create` + «Мои шаблоны»→/templates; **0** старых CTA `/cardio/new`, **0** `/circuits` (доказывает удаление формат-тайлов); 0 «Продолжить». (B) seeded active силовая(`7bcf7b91`)+active кардио(`583731cc`) → дашборд показал **2 ResumeBanner**: `/workouts/7bcf7b91`+«Активная тренировка» и `/cardio/583731cc`+«Активная кардио-сессия»; circuit-баннер «Активная круговая»=0 (не сидил → доказывает conditional per-format filtering); порядок strength@4013 < cardio@5201 (детерминир.); StartCard сохранён. Тест-юзер удалён (--cleanup, FK cascade → 0 orphan workouts/cardio_workouts), login 200, оба pm2 online.
- **NEXT (опц., оставшаяся консолидация — продуктовое):** старт+resume на дашборде теперь единый. Остаётся: (b) убрать отдельные list-страницы `/templates`·`/circuits`·`/cardio` из bottom-nav в пользу `/workouts`+`/create` (останутся достижимы из билдеров) — но bottom-nav уже = одна вкладка «Тренировки», так что это мелочь. Эпик F1–F9 практически закрыт. Низкоценный опц. полиш: F8 run-3 (скелетон стрима) / F4-инкр.2b (is_bodyweight миграция). Можно не делать.

## ⏯ RESUME (2026-06-10, после F9 run-1 — единый поток «Недавние» на дашборде) — **по запросу владельца «один поток, не три вкладки»**
- **F9 run-1 LIVE+verified** (master `c80070b`): владелец попросил «по тренировкам один поток, а не три разные вкладки на каждую». Аудит: bottom-nav уже одна вкладка «Тренировки»→`/workouts`, и `/workouts` уже сливал силовые+круговые+кардио (F5 run-2). НО **дашборд «Недавние» показывал только силовые** (`listRecentWorkouts`) — круговая/кардио-сессия НЕ появлялась на главной → ощущение фрагментации. Этот слайс чинит именно это. **Аддитивно, без схемы/миграции.**
- **Что сделано:** вынес `buildHistory`+`HistoryItem`+`HistoryCard` (+format-pill/metrics/helpers) из `app/(app)/workouts/page.tsx` в общий `components/workouts/workout-history.tsx` (R-4 DRY — 2-й потребитель = дашборд; знание «что такое единый элемент истории и как его рисовать» теперь single-source; недельная группировка осталась в `/workouts`). Дашборд: `listCircuits` добавлен в `Promise.all`, «Недавние» = `buildHistory(strength,circuits,cardio).slice(0,3)` с `HistoryCard` (формат-pill + свой detail-роут на каждый формат); удалён strength-only `RecentRow`. Недельная сводка тоннажа осталась силовой (отдельная метрика, не трогал). Gate green (typecheck 0/lint 0 err/24 теста/build, `/workouts` рендерит идентично).
- **Прод-verify (master `c80070b`, 1 минтнутый юзер + seeded 1 силовая(60×8) + 1 кардио-Tabata(1мин/160bpm)):** `/dashboard` «Недавние» = **2 карточки разных форматов** — Tabata→`/cardio/{id}` (1мин·160bpm) + Силовая→`/workouts/{id}` (1подход·480kg·reps·30мин), отсортированы по времени. **Доказательство фикса:** старый strength-only код НЕ показал бы кардио-карточку; новый показывает оба. `/workouts` после рефактора рендерит те же 2 карточки под «Эта неделя» (поведение идентично). Тест-юзер удалён (--cleanup, FK cascade → 0 orphan cardio_workouts), оба pm2 online.
- **NEXT (опц., более крупная консолидация — ПРОДУКТОВОЕ РЕШЕНИЕ владельца, не делать big-bang):** если «три вкладки» означало больше, чем дашборд-фид, кандидаты: (a) объединить 3 стартовых тайла дашборда (StartCard силовая / CardioTile / CircuitTile) в один CTA «Начать тренировку»→`/create`; (b) убрать отдельные list-страницы `/templates`·`/circuits`·`/cardio` из навигации в пользу единого `/workouts`+`/create` (они останутся достижимы по прямым ссылкам из билдеров); (c) единый «активный» баннер вместо раздельных ResumeCard(силовая)+CardioTile(кардио). Все три — UX-редизайн, требуют подтверждения направления у владельца. Текущий слайс уже даёт «один поток» на главной и в истории. ИЛИ вернуться к глубоко-опц. полишу F8 run-3 / F4-инкр.2b.

## ⏯ RESUME (2026-06-10, после F5-полиш — несколько напоминаний о тренировке в день) — **F5 ПОЛНОСТЬЮ ЗАКРЫТ**
- **F5-полиш LIVE+verified** (master `1100e40`): `push-reminders` cron теперь дедупит `workout_reminder` по **kind+ЧАС** (раньше kind+день) → юзер с расписаниями на разные часы получает отдельные напоминания в один день (Ноги 18:00 + Кардио 20:00 больше не съедают друг друга). Все расписания на ТЕКУЩИЙ час объединяются в **одно** уведомление (label через запятую, без спама). **Аддитивно, без миграции, один файл.** Реализация: `alreadySentToday` рефакторнут в `alreadySentSince(userId,kind,since)` (день-окно делегирует туда же — сон/КБЖУ без изменений, одно/сутки); `startOfHour(now)` = граница часового окна; `.find`→`.filter` (шлём все расписания, совпавшие с часом+ISO-днём). Gate green (typecheck 0/lint 0 err/24 теста/build).
- **Прод-verify (master `1100e40`, throwaway-юзер + fake push_subscription + 2 enabled-расписания на текущий MSK час/день + сид-строка delivered `workout_reminder` в 08:05 ранее сегодня delivered_count=1):** дёрнул cron с CRON_SECRET → `workoutSent:1`. **Доказательство hour-window:** старый day-window заблокировал бы (есть delivered-строка за сегодня → alreadySentToday=true), но новый код проверяет окно с startOfHour(06:00 UTC) → строка 05:05 UTC вне окна → послал. **Доказательство combine:** новая `notifications_log`-строка этого часа имеет `payload.title` = «🏋️ Пора тренироваться: **Ноги·тест, Кардио·тест**» (оба расписания в одном пуше через `.filter`+join), delivered_count=0 (fake sub). Всего 2 строки workout_reminder. Тест-юзер удалён (cascade → 0 строк), temp-скрипты удалены с VPS и локально, оба pm2 online, login=200.
- **NEXT: эпик F1–F8 ЗАКРЫТ.** Остаётся только глубоко-опц. низкоценный полиш: **F4-инкр.2b** (`exercises.is_bodyweight` миграция для численной эффект.нагрузки — deferred, ИИ уже рассуждает качественно) ИЛИ **F8 run-3** (полиш стрима: прогресс-бар по байтам/скелетон) ИЛИ **F5** прямой запуск конкретного шаблона из напоминания (сейчас ведёт на `/create`-пикер — требует привязку templateId к расписанию = миграция). Все — низкий приоритет, можно не делать. Если ран запущен — выбрать F8 run-3 (чистый UX-полиш, без миграции).

## ⏯ RESUME (2026-06-10, после F6 run-4b — actions + публичный роут + кнопка «Поделиться») — **F6 ЗАКРЫТ**
- **F6 run-4b LIVE+verified** (master `a7f9c8c`): завершён публичный шеринг AI-разбора (фундамент в run-4a). **Аддитивно, без миграции.** Добавлено: server actions `enableAnalysisSharingAction`/`disableAnalysisSharingAction` (requireUser→repo, R-7 фильтр по userId, возвращают token/ok — клиент сам обновляет state, без revalidate); публичный роут `app/a/[token]/page.tsx` (Next 16 async `params`; БЕЗ авторизации — `getSharedAnalysis(token)`→null=`notFound()`; `trainerSchema.safeParse(resultJson)`→`TrainerResultCard` с цветными дельтами F4, fallback на markdown `content` при провале; `metadata.robots = noindex,nofollow` чтобы чужие разборы не индексировались); client `ShareAnalysisButton` (enable→ссылка `/a/{token}` + copy-to-clipboard, disable→приватный; тапы ≥56px R-41, токены R-36; `origin` через `useSyncExternalStore` — hydration-safe, lint-clean вместо setState-in-effect). `trainerSchema` экспортнут из `trainer-structured.ts` для валидации на публичной странице; `getLatestTrainerResult` += `shareToken` (начальное состояние кнопки). Роут `/a/[token]` НЕ в `PROTECTED_PREFIXES` (proxy.ts) → публично доступен. Gate green (typecheck 0/lint 0 err/24 теста/build, `/a/[token]` в route-list).
- **Прод-verify (master `a7f9c8c`, 1 минтнутый юзер + seeded completed workout `wk-verify4b` + ai_analyses с валидным resultJson):** как владелец `/workouts/wk-verify4b/trainer` → `TrainerResultCard` (72/100, «Жим лёжа 60×5→60×6 ↑») + кнопка «Поделиться разбором»; клик → токен создан, ссылка `app.lead-generator.ru/a/{token}` показана; открыл `/a/{token}` → разбор виден read-only (БЕЗ кнопки шеринга, CTA «Узнать больше»→/); **unauth-доказательство:** `fetch(credentials:'omit')` на `/a/{token}` = HTTP 200 (НЕ редирект на /login), `<meta name=robots>`=`noindex, nofollow`; reload trainer-страницы → ссылка персистнула (доказывает `shareToken`-плумбинг через getLatestTrainerResult); клик «Сделать приватным» → UI вернулся к «Поделиться разбором», `/a/{token}` → **404** (disable→notFound). Тест-данные удалены (--cleanup, FK cascade → ai_analyses+workout=0), temp seed-скрипт удалён с VPS, прод-tree чист, оба pm2 online.
- **NEXT: эпик F1–F8 практически закрыт.** Остаётся только опц. полиш: **F5** (несколько напоминаний/день — снять дедуп по kind, дедупить по schedule.id+час; прямой запуск конкретного шаблона из напоминания вместо `/create`) ИЛИ **F4-инкр.2b** (`exercises.is_bodyweight` миграция для численной эффект.нагрузки — deferred/low-value, ИИ уже рассуждает качественно) ИЛИ **F8 run-3** (полиш стрима: прогресс-бар по байтам/скелетон). Все три — низкий приоритет. Рекомендую при следующем ране: F5-полиш (несколько напоминаний/день) как самый осязаемый для владельца.

## ⏯ RESUME (2026-06-10, после F6 run-4a — schema+repo шеринга AI-разбора, без UI)
- **F6 run-4a LIVE+verified** (master `f605e67`): фундамент публичного шеринга AI-разбора. **Миграция `0010` (pg_dump бэкап 13.6MB сделан ПЕРЕД).** Добавлено: `ai_analyses.share_token` (nullable text + `uniqueIndex` NULLS DISTINCT → много нерасшаренных NULL-строк сосуществуют; capability-токен: непустой = доступен по публичной ссылке); repo `enableAnalysisSharing` (R-7, идемпотентно — НЕ перевыпускает токен, старая ссылка живёт), `disableAnalysisSharing` (R-7), `getSharedAnalysis(token)` — **СОЗНАТЕЛЬНОЕ R-7-исключение** (сам токен = право доступа, без userId-фильтра), отдаёт ТОЛЬКО non-PII (content/resultJson/modelVersion/createdAt — без userId/email). Миграция идемпотентна (`ADD COLUMN IF NOT EXISTS` + `CREATE UNIQUE INDEX IF NOT EXISTS`; snapshot-дрифт circuit — meta-снапшот `0010` пересоздан, лечит дрифт). Gate green (typecheck 0/lint 0 err/24 теста/build).
- **Прод-verify (master `f605e67`):** psql применил миграцию (ALTER+CREATE INDEX ok); `\d ai_analyses` → `share_token` + `ai_analyses_share_token_unq UNIQUE`; index создался над таблицей с существующими NULL-строками = доказывает coexist NULLs (иначе упал бы). **Functional ROLLBACK-роундтрип:** в txn set token на реальную строку → SELECT by token вернул content/model_version/created_at БЕЗ userId (= getSharedAnalysis-проекция) → ROLLBACK → 0 строк с тест-токеном (прод-данные не тронуты, минтить юзеров не пришлось). Прод здоров (login 200, fitness-saas+cron online).
- **NEXT: F6 run-4b — actions + публичный роут + кнопка «Поделиться».** `enableAnalysisSharingAction`/`disableAnalysisSharingAction` (server actions, requireUser→repo, revalidate); публичный роут `/a/[token]` (Next 16 async `params`; БЕЗ requireUser — `getSharedAnalysis(token)`→null=`notFound()`; рендер read-only разбора, переиспользовать `TrainerResultCard` для resultJson ИЛИ markdown content; R-37 empty; R-36 токены; meta `noindex` чтобы не индексировать чужие разборы); кнопка «Поделиться»/«Скрыть» на `TrainerResultCard` (показывает ссылку `app.lead-generator.ru/a/{token}`, copy-to-clipboard). После — verify (enable→открыть /a/token в инкогнито→виден разбор; disable→404) + закрыть F6. Затем эпик практически закрыт (F1-F8 done); опц. полиш F5 (несколько напоминаний/день).
- **(история ниже)** F6 run-3 LIVE+verified (master `b10c1b1`): аддитивный read-only просмотр чужих тренировок по дружбе. **Без миграции.** Добавлено: repo `areFriends(userId, otherId)` (accepted-дружба в любом направлении, себя другом не считаем) + `getFriendProfile(userId, otherId)` (публичная карточка ТОЛЬКО если друзья, иначе null — единый R-7-гейт); новая страница `/friends/[friendId]` (Next 16 async `params`: `const {friendId} = await params`; requireUser → `getFriendProfile` → null=`notFound()`; затем `listRecentWorkouts(friendId,30)` completed — R-7: запрос тренировок ДРУГА только после подтверждения дружбы); read-only карточки (дата·название·KPI подходов/kg·reps/мин, БЕЗ ссылок в detail-роуты, БЕЗ действий; R-37 empty-state; R-36 токены); карточки друзей на `/friends` теперь Link на `/friends/{id}` (chevron, min-h-14 R-41). Gate green (typecheck 0/lint 0 ошибок/24 теста/build, `/friends/[friendId]` в route-list).
- **Прод-verify (master `b10c1b1`, 2 минтнутых юзера A=claude-verify, B=claude-friend, дружба accepted + B засеян completed-тренировкой 2×80кг×5):** как A — `/friends` показал карточку-Link на `/friends/{B.id}`; переход → h1=имя B, бейдж «ТОЛЬКО ПРОСМОТР», тренировка B read-only (2 подхода · 800 kg·reps · 60 мин · «(friend-view)»), единственная ссылка = назад на `/friends` (никаких detail-ссылок → read-only ✓). **R-7-гейт:** `/friends/{stranger-uuid}` → 404, `/friends/{A.id}` (свой id, не друг себе) → 404. Тест-данные удалены (2 юзера, FK cascade → дружба+тренировка B исчезли), temp seed/cleanup-скрипты удалены с VPS, прод-tree чист, оба pm2 online.
- **NEXT: F6 run-4 — шеринг AI-разбора.** share-флаг на `ai_analyses` (МИГРАЦИЯ → pg_dump!) ИЛИ публичная ссылка-токен на разбор; страница просмотра общего разбора (read-only, как friend-view). ЛИБО privacy-настройка видимости тренировок (колонка `users.share_workouts` bool default true, МИГРАЦИЯ) — сейчас accepted-друзья видят тренировки без opt-out (YAGNI v1). Repo `removeFriend`/`declineRequest` + кнопки — при необходимости UI (пока нет). После F6 — эпик практически закрыт (F1-F8 done); опц. полиш F5 (несколько напоминаний/день).

## ⏯ RESUME (2026-06-10, после F6 run-2 — add/accept flow + /friends UI; ЗАВЕРШЕНИЕ in-flight слайса)
- **F6 run-2 LIVE+verified** (master `220fbfd`): код был закоммичен+запушен+задеплоен ПРЕДЫДУЩИМ раном (prod уже на `220fbfd`, BUILD_ID 03:27, после коммита 03:26), но ран умер ДО verify+обновления плана. Этот ран **довёл слайс до конца**: gate green (typecheck/lint 0 ошибок/24 теста/build), полный рантайм-verify на проде, очистка тест-данных, обновление плана. **Аддитивно, без миграции** (схема friendships из run-1 уже живёт).
- **Что в run-2:** repo `findUserByEmail` + `listFriendsDetailed`/`listIncoming|OutgoingDetailed` (обогащают каждую дружбу публичной карточкой ДРУГОГО юзера одним `inArray`-запросом — без N+1, R-7: партнёр = сторона ≠ userId); Zod `friendRequestSchema` (email trim+lowercase+email()); actions `sendFriendRequestAction` (найти по email→`sendFriendRequest`; self-email + not-found как error-state, не 500) + `acceptFriendRequestAction`; страница `/friends` (R-37: входящие с кнопкой Принять ≥56px / форма add-by-email / исходящие «Ожидает» / список друзей с empty-state; токены R-36); ссылка из `/settings` («Друзья (сообщество)»).
- **Прод-verify (master `220fbfd`, 2 минтнутых юзера A=claude-verify, B=claude-friend):** A submit B-email → success «Заявка отправлена» + B в исходящих «Ожидает»; switch на B → входящая заявка от A, кнопка Принять `min-height:56px` (R-41 ✓), accept; reload → B видит A в «Ваши друзья», входящие пусты; switch назад на A → B переехал из исходящих в «Ваши друзья» (pending→accepted на обеих сторонах). Settings→/friends ссылка есть. **Бонус:** обнаружил leftover тест-данные от мёртвого рана (bob-ftest/carol-ftest показывались в incoming/outgoing — доказывает, что рендер заявок уже работал live). **Очистка:** удалил все 4 тест-юзера (claude-verify/claude-friend/bob-ftest/carol-ftest) — `friendships` table теперь 0 строк (FK cascade, без сирот). Прод здоров (login 200, оба pm2 online).
- **NEXT: F6 run-3 — просмотр тренировок друга (read-only, privacy).** Нужен роут `/friends/[friendId]` (или `/u/[id]`): проверить, что target — accepted-друг текущего юзера (новый repo `areFriends(userId, otherId)` или переиспользовать `listFriends`), затем показать его недавние тренировки read-only (переиспользовать `listRecentWorkouts`/`buildHistory` с friendId, R-7: явный friend-userId, НЕ свой). Privacy-настройка (share workouts on/off) — колонка на users ИЛИ отдельная таблица (МИГРАЦИЯ → pg_dump!); для v1 можно default-видимость для accepted-друзей без настройки (YAGNI), приватность отдельным run. Потом run-4: шеринг разбора (share-флаг на ai_analyses + публичная ссылка). Repo `removeFriend`/`declineRequest` добавить при появлении UI кнопок (пока нет — YAGNI).

## ⏯ RESUME (2026-06-10, после F6 run-1 — фундамент друзей: схема+миграция+repo, без UI)
- **F6 run-1: friendships схема + repo LIVE+verified** (master `5e6ac65`): greenfield-фундамент соц-части, ЧИСТО АДДИТИВНО (без UI, без рантайм-пути — ничего пока не запрашивает таблицу). Добавлено: enum `friendship_status` (pending|accepted; отклонение/блок = удаление строки, отложено YAGNI); таблица `friendships(requesterId→users, addresseeId→users, status, created/updatedAt)` с `UNIQUE(requester,addressee)` + индексы по обеим сторонам + FK `ON DELETE CASCADE`; repo `lib/repos/friends.repo.ts` (R-7, все берут acting userId): `sendFriendRequest` (self-guard + авто-accept встречной pending-заявки + `onConflictDoNothing` идемпотентно), `acceptFriendRequest` (только адресат своей pending), `listFriends` (обе стороны), `listIncoming/OutgoingRequests`. Naming requester/addressee (не userId/friendId) — направленность заявки явная (R-6). Gate green (typecheck/lint/24 теста/build).
- **МИГРАЦИЯ:** ручная идемпотентная `0009_friendships.sql` (`DO $$ CREATE TYPE ... EXCEPTION duplicate_object` + `CREATE TABLE IF NOT EXISTS` + FK через DO/EXCEPTION + `CREATE INDEX IF NOT EXISTS`). НЕ `db:generate` (circuit snapshot-дрифт пере-создал бы circuit-таблицы). Применено на прод через **psql** после `pg_dump` (бэкап `~/fitness-backup.sql` 13M). Журнал idx 9 + `0009_snapshot.json` (копия 0008 + enum + таблица + uniqueConstraint) добавлены вручную.
- **Прод-verify (master `5e6ac65`):** `enum_range` = `{pending,accepted}`; `\d friendships` = все колонки/2 индекса/UNIQUE/2 FK cascade точны. Транзакционный roundtrip (ROLLBACK, без остаточных данных): insert pending → дубль `ON CONFLICT DO NOTHING` = 0 строк (count остался 1, UNIQUE держит) → accept (фильтр addressee+pending) = status `accepted` → `DELETE user` → friendship каскадно удалён (count 0). Прод здоров (login 200, оба pm2 online).
- **NEXT: F6 run-2 — server actions + Zod + UI заявок.** Action `sendFriendRequestAction` (найти юзера по email → `sendFriendRequest`), `acceptFriendRequestAction`; страница `/friends` (R-37: список друзей / входящие / исходящие / форма add-by-email; тапы ≥56px R-41; токены R-36). Без миграции (схема готова). Добавить ссылку из `/settings`. Потом run-3: просмотр тренировок друга (read-only, privacy), run-4: шеринг разбора. Repo для удаления/отклонения (`removeFriend`/`declineRequest`) добавить в run-2 при необходимости UI.

## ⏯ RESUME (2026-06-10, после F5 run-3b — cron-доставка push-напоминаний о тренировке) — **F5 ПРАКТИЧЕСКИ ЗАКРЫТ**
- **F5 run-3b: cron push-доставка LIVE+verified** (master `4b0a139`): почасовой cron `push-reminders` теперь шлёт напоминание о тренировке, когда у юзера есть включённое расписание, совпадающее с сегодняшним ISO-днём недели И локальным часом. Добавлено: enum value `push_kind += workout_reminder` (идемпотентная миграция `0008`, `ALTER TYPE ... ADD VALUE IF NOT EXISTS`); repo `listEnabledSchedulesForUser` (R-7: userId+enabled); хелпер `localIsoDay(now,tz)` (1=Пн..7=Вс через UTC-полдень — DST/locale-safe, DRY с `localDateIso`); ветка в cron после subs-проверки → `sendPushToUser` (title «🏋️ Пора тренироваться: <label>», url `/create`, tag `workout-reminder`). **Дедуп по kind+день** → одно напоминание о тренировке в сутки (даже если несколько расписаний совпали — достаточно для v1). Tokens-only, без UI-изменений. Gate green (typecheck/lint/24 теста/build).
- **МИГРАЦИЯ (enum):** `pg_dump` (бэкап `~/fitness-backup.sql` 13M) → применил `ALTER TYPE push_kind ADD VALUE IF NOT EXISTS 'workout_reminder'` через **psql** (НЕ db:migrate — circuit snapshot-дрифт из run-3a остаётся). Журнал idx 8 + `0008_snapshot.json` (копия 0007 + enum value) добавлены вручную. Проверил `enum_range` на проде = содержит `workout_reminder`.
- **Прод-verify (master `4b0a139`):** засеял test-юзеру fake push_subscription + enabled-расписание на ТЕКУЩИЙ MSK час+ISO-день (Ср 03:00 `{3}`), дёрнул cron-эндпоинт с `CRON_SECRET` → ответ `workoutSent: 1`, в `notifications_log` появилась строка `workout_reminder` (delivered_count=0 — fake endpoint, реальная браузер-подписка доставила бы). Полный путь (schedule query→day+hour match→sendPushToUser→logNotification) отработал. Тест-данные удалены (--cleanup, FK cascade → 0 остаточных расписаний). Прод здоров (login 200, оба pm2 online).
- **NEXT: F6 — друзья/соц** (greenfield, schema `friendships(userId, friendId, status: pending|accepted)` + privacy). ИЛИ полиш F5 (несколько напоминаний/день — снять дедуп по kind, дедупить по schedule.id+час; прямой запуск конкретного шаблона из напоминания вместо `/create`). F4-инкр.2b (is_bodyweight) остаётся deferred. **Рекомендую F6 run-1: схема friendships + миграция (pg_dump!) + repo add/accept (R-7), без UI — аддитивно.**

## ⏯ RESUME (2026-06-10, после F5 run-3a — расписание тренировок: схема+UI, без пуша)
- **F5 run-3a: `/schedule` LIVE+verified** (master `ace93ac`): аддитивная таблица `workout_schedules` (userId, label, daysOfWeek `integer[]` ISO 1–7, hour 0–23, enabled, timestamps) + repo (R-7 userId-filter: list/create/delete/setEnabled) + Zod (`daysOfWeek` через `formData.getAll("days")`, min 1 день, sort+uniq) + server actions + страница `/schedule` (форма создания: day-toggles ≥56px, hour-Select default 18, R-37 empty/error/loaded; список с toggle Включить/Выключить + delete) + ссылка из `/settings` («Связанные разделы»). **Модель совпадает с почасовым cron `push-reminders`** (день недели + час, БЕЗ persisted next_run — cron сам посчитает «сегодня∈daysOfWeek И localHour===hour»). **Push-доставка отложена на run-3b.** Tokens-only (R-36). Gate green (typecheck/lint/24 теста/build, `/schedule` в route-list).
- **⚠️ МИГРАЦИЯ — особый случай (важно для будущих миграций):** `pnpm db:generate` выдал ЗАГРЯЗНЁННЫЙ `0007` (пере-создавал circuit-таблицы + `ai_job_kind` enum value + circuit-колонки) — **снапшот-дрифт ПРЕ-СУЩЕСТВУЕТ**: `0005_circuit_workouts.sql` есть, но `meta/0006_snapshot.json` НЕ содержит circuit-таблиц (circuits применялись hand-written/push без обновления снапшота). Поэтому `db:migrate` на проде УГРОБИЛ БЫ (CREATE TABLE circuit_workouts уже существует). **Решение:** откатил generate-артефакты, написал РУЧНУЮ идемпотентную `0007_workout_schedules.sql` (CREATE TABLE IF NOT EXISTS + FK через DO/EXCEPTION + CREATE INDEX IF NOT EXISTS), применил на прод через `psql` (НЕ `db:migrate`) после `pg_dump` (бэкап `~/fitness-backup.sql` 13.6MB). Журнал+снапшот 0007 добавлены вручную (снапшот = 0006+таблица; circuit-дрифт остаётся — пре-существующий тех-долг, НЕ трогал чтобы не переписать историю circuits). **Будущим ранам с миграцией: НЕ доверять `db:generate` вслепую — проверять diff; применять только нужную таблицу идемпотентно через psql.**
- **Прод-verify (master `ace93ac`, logged-in):** создал расписание «Ноги·тест» Пн·Ср·Пт 18:00 через РЕАЛЬНЫЙ server action → персист после reload, `formatDays`/`formatHour` верны; toggle → «ВЫКЛЮЧЕНО»+«Включить»; delete → empty-state «Пока нет расписаний». Тест-юзер удалён (--cleanup, FK cascade).
- **NEXT: F5 run-3b — cron-доставка пуша.** Добавить enum value `push_kind` += `workout_reminder` (МИГРАЦИЯ — отдельный `ALTER TYPE ... ADD VALUE`, идемпотентно через psql+pg_dump) → в `app/api/cron/push-reminders/route.ts` добавить ветку: для каждого юзера получить enabled-расписания (новый repo `listEnabledSchedulesForUser` или join), если `localISODay(now,tz)∈daysOfWeek И localHour===hour` И не слали сегодня (`alreadySentToday(u.id,"workout_reminder")`) → `sendPushToUser` (title «Пора тренироваться: <label>», url `/create`). `localISODay` хелпер (1=Пн..7=Вс по tz). Verify: seed расписание на текущий локальный час+день, дёрнуть cron-эндпоинт, проверить notifications_log. ИЛИ F6 (друзья, greenfield schema friendships).

## ⏯ RESUME (2026-06-10, после F5 run-1 — единый пикер форматов)
- **F5 run-1: единая точка входа `/create` LIVE** (master `6b501a2`): новая аддитивная серверная страница — пикер формата с короткими explainer'ами, роутит в СУЩЕСТВУЮЩИЕ билдеры. 3 карточки: **Силовая**→`/templates/new`, **Круговая**→`/circuits/new`, **Кардио · HIIT** (Tabata · EMOM · Norwegian 4×4 · custom)→`/cardio/new`. EMOM/Tabata уже живут как cardio-пресеты (`PRESET_META`, `startCardioAction` POST-форма) → новых билдеров НЕ нужно. Дашборд `StartCard` вторичный CTA перенаправлен `/templates/new`→`/create` («Создать тренировку»). Существующие билдер-роуты НЕ тронуты, напрямую достижимы. Tokens-only (R-36), тапы ≥56px (R-41, факт. 106–125px), паттерн карточек как `cardio/new`. **Без схемы/миграции — чисто аддитивно.** Gate green (typecheck/lint/24 теста/build, `/create` в route-list). **Прод-verify (logged-in):** `/create` → h1 «Выбери формат» + 3 карточки с правильными href; `/templates/new` резолвится; дашборд содержит `a[href="/create"]`=«Создать тренировку». Тест-юзер удалён (--cleanup). **NEXT: F5 run-2 — EMOM/Tabata как самостоятельные форматы шаблонов ЛИБО подписи формата у названий в списках ЛИБО расписание/напоминания (schema: schedule + push, cron уже есть). Рекомендую: подписи формата (мелкий аддитивный слой) ИЛИ schedule-схема (требует миграцию + pg_dump).** ИЛИ F6 (друзья).

## ⏯ RESUME (2026-06-10, после F7 run-3 — F7 ЗАКРЫТ)
- **F7 run-3: тренд-цвет + бейдж на баре «Объём по неделям» LIVE** (master `7dddf8a`): `VolumeBarChart` красит бары по тренду периода (первая→последняя точка через общий `trendStatus`, higherIsBetter — больше объёма=прогресс) + бейдж Рост/Регресс/Стагнация/Новое + дельта тоннажа (формат k/M). **Относительный epsilon 5% от старта** (объём — крупная величина в тысячах, фикс-epsilon как у e1RM бессмыслен → доля). Цвет бара — `tone.stroke` CSS-var токен (как stroke линии в run-2). Полностью DRY: `trendStatus`+`TREND_TONE`+`TREND_LABEL`, та же семантика что e1RM-график (run-2) и карточка разбора (F4). **Один файл `components/charts/VolumeChart.tsx`**, аддитивно, без миграции, `stats/page.tsx` НЕ тронут (тренд считается внутри компонента из `data`). Gate green (typecheck/lint/24 теста/build). **Прод-verify:** seeded 2 completed workouts (Жим лёжа, объём 300→480, +60% в 2 разные ISO-недели) → `/stats?g=week` показал 2 зелёных бара `fill=var(--success)`, бейдж «↑Рост» (text-color === `--success`), дельту «+180». Тест-данные удалены (--cleanup, FK cascade), seed-скрипт удалён с VPS. **Вес тела (линия) уже был** на `/stats` (`BodyTrendChart` — вес + % жира) — этот пункт чеклиста закрыт вместе с объёмом. **F7 ПОЛНОСТЬЮ ЗАКРЫТ.** NEXT: **F5** (унифицированный пикер форматов обычная/круговая/EMOM/Tabata/кардио + explainers + расписание/напоминания) — следующий приоритет. ИЛИ F6 (друзья). F4-инкр.2b (is_bodyweight миграция) остаётся deferred/опционал.

## ⏯ RESUME (2026-06-10, после F7 run-2)
- **F7 run-2: тренд-цвет + бейдж на e1RM-графике LIVE** (master `2d318b1`): `OneRmTrendChart` красит линию e1RM по тренду периода (первая→последняя точка через `trendStatus`, epsilon 0.5кг от float-шума) + бейдж Рост/Регресс/Стагнация/Новое + дельта в кг. Цвет линии/точек — CSS-var токен (Recharts не принимает Tailwind-класс). **Вынесен общий `TREND_TONE` в `lib/ui/trend-tone.ts`** (presentation, отдельно от чистого домена R-7): `{text,bg,icon,stroke}`. DRY — `TrainerResultCard` (F4) теперь импортит его вместо локальной копии (поведение карточки идентично); домен `trend.ts` остаётся pure. Аддитивно, без миграции, `stats/page.tsx` НЕ тронут (тренд считается внутри компонента из `data`). Gate green (typecheck/lint/24 теста/build). **Прод-verify:** seeded 2 completed workouts (Bird dog 60×5→70×5) → `/stats` показал бейдж «Рост», дельту «+11.5 кг», `line stroke=var(--success)` (зелёный), селектор=Bird dog. Тест-данные удалены (--cleanup, FK cascade). **NEXT: F7 run-3 — объём по неделям (бар, уже есть `VolumeBarChart` — добавить тренд-окраску?) + вес тела линия; ИЛИ перейти к F5 (форматы EMOM/Tabata + расписание).** Рекомендую: F7 run-3 опционален (объём-бар уже работает), можно сразу F5.

## ⏯ RESUME (2026-06-10, после F7 run-1)
- **F7 run-1: общий `trendStatus()` хелпер LIVE** (master `0100b52`): `lib/domain/progression/trend.ts` — pure `TrendStatus` тип + `trendStatus(prev,cur,{epsilon,higherIsBetter})` → improved|regressed|stagnant|new (null prev→new; epsilon dead-zone для float-шума; higherIsBetter для метрик где направление≠прогресс, напр. вес тела) + `TREND_LABEL` (Рост/Регресс/Стагнация/Новое). 12 unit-тестов. DRY: канонический `TrendStatus` теперь single-source — `trainer-structured.ts` + `TrainerResultCard.tsx` импортят его (убрали дубль union-литерала). Аддитивно, без миграции, БЕЗ рантайм-изменений UI (тип стирается). Деплой green, прод здоров (login 200, /stats 307 auth-gate). **Цвет-токены (TREND_TONE) ОТЛОЖЕНЫ** до run-2 (YAGNI: 2-й потребитель появится при графиках; F4 пока держит свою копию). **NEXT: F7 run-2 — `/stats` e1RM-линии (Recharts) + бейдж Рост/Стагнация/Регресс через `trendStatus`+общий TREND_TONE.**
- Runtime-verify хелпера отложен на run-2 (хелпер ещё не подключён ни к одному UI — нечего кликать; typecheck+build+24 теста зелёные = доказательство корректности).
- **Recharts API (Context7, v3):** `ResponsiveContainer width="100%" height={N}` + `LineChart`/`ComposedChart`; `accessibilityLayer` уже true по умолчанию; цвет линии через `stroke` (SVG — нельзя Tailwind-класс напрямую; читать значение CSS-var токена в JS, R-36); split-color по значению через `linearGradient gradientUnits="userSpaceOnUse"`.

## ⏯ RESUME (2026-06-10, после F8-B run-2)
- **F8-B стрим разбора ПОЛНОСТЬЮ LIVE** (master `ab8fcd6`): run-1 endpoint + run-2 клиент. On_demand-заход на `/workouts/[id]/trainer` теперь генерит разбор inline+стримом (без cron/poll/перезахода), цветные дельты F4 сохранены. Verified на проде.

## ⏯ RESUME (2026-06-09, после деплоя F1–F3)
- **origin/master `b360d3f` задеплоен на прод (live).** F1 (удаление) + F2 (числовой ввод) + F3 (самочувствие) + прод-хотфиксы (trainer prompt v2, RAG в cron) — на `app.lead-generator.ru`.
- **F2 проверен на проде в рантайме:** `06→6`, `60→60`, инпуты = NumberField (`type=text`+`inputMode`). PWA на реальном телефоне — за владельцем.
- **F1/F3 задеплоены + зелёные** (typecheck/lint/build/lefthook). Полный клик-тест — в раунде F4 (там всё равно нужен workout-flow).
- **Доступ:** GitHub-токен зашит в `origin` URL → `git push origin <branch>:master` работает. Деплой: `ssh kanavto-vps` → `cd /opt/fitness-saas` → `git fetch` → `git reset --hard origin/master` → `pnpm build` → `pm2 reload ecosystem.config.cjs`. **Docker ЛОКАЛЬНО не стартует (WSL)** → verify только на проде: `scripts/issue-session.mjs` (gitignored, на VPS) даёт REFRESH_TOKEN → playwright POST `/api/auth/restore` → залогинен; `--cleanup` удаляет тест-юзера.
- **Готово + LIVE на проде** (master `850506d`): F1 удаление · F2 числовой ввод (полный sweep, 0 `type=number`, verified) · F3 самочувствие · **F4-инкр.1** (exerciseComparisons + цветные дельты `ComparisonRow`) · **F4-инкр.2a** (профиль атлета вес/рост/возраст + история 10 трен ЦЕЛИКОМ set-by-set в `buildTrainerContext`). ИИ уже знает вес 90кг и рассуждает про bodyweight (подтягивания = тело+добавка) из prompt+профиля.
- **NEXT ACTION (продолжить отсюда, июнь 2026):** **F5 run-3 — расписание/напоминания** (по дням недели ИЛИ интервалу → push через существующий cron). ТРЕБУЕТ миграцию (`pg_dump` ПЕРЕД!): новая таблица расписаний (templateId/circuitId/cardioId + days/interval + next_run) ИЛИ колонки на сущностях. Разбить: run-3a = схема+репо+UI настройки расписания (без отправки); run-3b = cron-проверка → push. ИЛИ, если расписание слишком крупное для авто-ранов — перейти к **F6 друзья** (greenfield schema friendships). F5 run-1 (пикер `/create`) + run-2 (единый список с формат-подписями) LIVE+verified на проде (master `d9fb345`). F7 ЗАКРЫТ; F8 run-1+2 LIVE.
- **(старый NEXT, выполнено):** ~~F8 run-3 полиш стрима / F7 графики~~ — F7 закрыт, F8 базовый UX работает.
- **(пред. NEXT):** F8 run-3 — полиш стрима (опц.) ИЛИ **F7 графики** (`/stats` + Recharts, `trendStatus` DRY с F4). F8 run-1+run-2 LIVE+verified на проде (master `ab8fcd6`). Возможный полиш run-3: прогресс-бар/скелетон по байтам стрима вместо статичного спиннера; анимация появления карточки; обработка очень долгой генерации (>45с timeout) с явным сообщением. Низкий приоритет — базовый UX уже работает. Рекомендую начать **F7**.
- **Опционально (deferred, low-value):** F4-инкр.2b — `exercises.is_bodyweight` миграция (с `pg_dump`!) для ЧИСЛЕННОЙ эффект.нагрузки. Не критично: ИИ уже рассуждает качественно.
- **Потом:** F7 графики (`/stats` + Recharts, `trendStatus` DRY с F4) · F5 форматы (унифицированный пикер обычная/круговая/EMOM/Tabata/кардио + explainers + расписание/напоминания) · F6 друзья.
- **Компакт-правило владельца:** держать <600к, коммит+деплой каждый инкремент (lossless при авто-компакте). Деплой: `ssh kanavto-vps` → `cd /opt/fitness-saas` → `git fetch && git reset --hard origin/master && pnpm build && pm2 reload ecosystem.config.cjs`.

## Доступ / Деплой (факты)

- **VPS:** `ssh kanavto-vps` → `i48ptgvnis@170.168.72.200`. App: `/opt/fitness-saas`. pm2: `fitness-saas` (порт 3001), `fitness-saas-cron`.
- **Deploy:** ssh → `cd /opt/fitness-saas` → `git pull` → `pnpm install` → `pnpm build` → `pnpm db:migrate` (если новые миграции) → `pm2 reload ecosystem.config.cjs`.
- **НЕ трогать:** pm2-аппы `kanavto`/`site`/`among-irl`, Innertalk на 80/443, чужой nginx. Только наш `app.lead-generator.ru`.
- **GitHub:** `origin` = https://github.com/Artem-Levchenko-hub/Fitness-AI . Push требует свежий токен (`gh auth login`). Fetch — анонимно ок.
- **Login для тестов:** `session.strategy = "database"` → логинимся инъекцией строки в таблицу `session` + cookie в браузере (`__Secure-authjs.session-token` на https). Mailbox НЕ нужен. AUTH_SECRET в `.env.production` на VPS.
- **Local:** docker-compose Postgres + `.env.local`. `pnpm dev` → :3000.

## Цикл на каждую фичу (автономно)

`sequential-thinking` дизайн → Context7 (Next 16 / Drizzle / либы) → код (TDD где логика; 10 правил code-canon; R-7 чистый domain; R-36 токены; R-37 4 состояния; R-41 тапы ≥56px) → `pnpm typecheck && pnpm lint && pnpm build && pnpm test` → commit → push GitHub → deploy VPS → проверка в браузере на аккаунте владельца → удалить тест-данные → отметка в чеклисте.
**Перед каждой prod-миграцией:** `pg_dump` бэкап.

## Фичи и дизайн

### F1 — Удаление тренировок и шаблонов [S]
Server actions `deleteWorkoutAction` / `deleteTemplateAction` → repo с фильтром по `userId` (R-7). Каскад детей (sets/exercises/ai_jobs/notes) — через FK ON DELETE CASCADE или явно. Удаление шаблона НЕ удаляет связанные тренировки (FK → null). UI: destructive-кнопка + confirm-диалог на detail + в списке. После → redirect к списку + toast. Это же — инструмент уборки тест-данных.

### F2 — Числовой ввод [S]
Баг: `<Input type="number">` контролируемый → ведущий `0`, нельзя стереть (`06`/`60`), курсор. Фикс: примитив `NumberField` (`type="text"` + `inputMode="decimal|numeric"` + select-on-focus + санитайз + clamp на blur). Заменить в `SetInput` + ~7 формах (sleep/nutrition/body/profile/topup/template-builder/exercises/cardio). Телефонная цифровая клава гарантированно.

### F3 — Самочувствие перед завершением [S]
Необязательное поле «Как прошло? Самочувствие, боли, энергия» на шаге завершения. Хранить (колонка `feeling_note` на `workouts` ИЛИ WorkoutNote source=manual). Прокинуть в `context-builder` → AI учитывает. Миграция: +колонка.

### F4 — AI-разбор v3 [M] (главная)
- **context-builder:** последние **10 тренировок целиком** (set-by-set), PR, **вес тела + рост** из body_measurements, feeling-note (F3).
- **Взвешенные упражнения:** считать эффективную нагрузку = вес тела + добавка (подтягивания/брусья). Падение добавки 20→10 кг при 90 кг = <10% тотала — prompt/контекст должны рассуждать в % тотала. Нужен флаг bodyweight у exercise.
- **Promt:** проще язык, глубже «почему», ссылки на канон **И на прошлые цифры атлета**.
- **UI сравнения:** структурный вывод per-exercise `{prevTopSet, curTopSet, deltaReps, deltaWeight, status: improved|regressed|stagnant|new}` → цветовое кодирование: рост — зелёным, регресс — серо-красным (мягко), стагнация — серым. «5→6» подсвечено.

### F5 — Кардио + единый поток форматов [L]
Master уже имеет cardio + circuits (круговые). Объединить вход «создать шаблон» → пикер формата: **обычная / круговая / EMOM / Tabata / кардио** (+ короткий explainer per формат до создания). EMOM/Tabata — интервальные (расширить circuit schema enum `format`). Кардио-типы: бег/вел/плавание/… Расписание: дни недели ИЛИ интервал → push-напоминания (cron уже есть). Неяркие подписи формата у названий. Всё в одном списке.

### F6 — Друзья/соц [L]
Greenfield. Schema `friendships(userId, friendId, status: pending|accepted)`. Добавить по email/коду. Смотреть тренировки друзей (read-only, приватность). Делиться разборами (share-флаг/ссылка на AiAnalysis). Privacy-настройки. UI: страница друзей, профиль друга, share на разборе.

### F7 — Наглядные графики прогресса [M]
Дашборд на `/stats` (Recharts уже есть). Очень наглядно: рост — зелёным, стагнация — серым, регресс — серо-красным (мягко). Графики: e1RM по ключевым упражнениям во времени (линия + цвет тренда + бейдж Рост/Стагнация/Регресс), объём по неделям (бар), вес тела (линия). Общий хелпер `trendStatus(prev, cur)` + цвет-токены — DRY с F4 (та же семантика тренда).

### F8 — Стриминг разбора в реальном времени [M]
Сейчас trainer-разбор async (ai_jobs + cron) → надо перезаходить, чтобы увидеть (плохо чувствуется). Сделать live: на `/workouts/[id]/trainer` стримить генерацию (coach route уже умеет `toTextStreamResponse`). При заходе без готового анализа — запустить стрим прямо в запросе (text-stream) + сохранить результат в конце. Без перезахода.

## Чеклист (живой)

### 🔴 G — Багфиксы + UX-передел (запрос владельца 2026-06-10) — НАЧИНАТЬ ОТСЮДА
- [x] **G1** — stats только по completed (master `761a7eb`, prod-verified): добавлен `eq(workouts.status,'completed')` во все 8 запросов `lib/repos/stats.repo.ts` (dailyVolume/weeklyVolume/volumeByMuscle/repRangeDistribution/oneRmTrend/workoutFrequency/topLineKpi/trainedExercises). **Прод-verify (tsx):** seed completed(100×5)+active(999×9) → `topLineKpi`=`{workouts:1,sets:1,reps:5,tonnage:500}`, volume=500 (активная 8991 ИСКЛЮЧЕНА; до фикса было бы workouts:2/tonnage:9491). Тест-данные удалены (cascade, orphans=0). Графики = ровно completed-история `/workouts`.
- [x] **G2** — один активный сеанс на формат (master `6377386`, prod-verified). Root cause: `start*` (startWorkoutFromTemplate/startCircuit/startCardioFromPreset) НИКОГДА не закрывали прошлую active-сессию → брошенные копились, getActive* возвращал старую, баннер «висел в любом случае». Фикс: в начале транзакции каждого start-репо закрываем все active юзера этого формата в 'cancelled', потом вставляем новую. Cancelled вне истории/статистики (G1). **Прод-verify (tsx):** seed 2 active (pile-up) → новый startCircuit → active=1 (только новая)+cancelled=2 → finish → active=0. Старый код оставил бы 3 active. Завал дренируется при следующем старте. Тест-данные удалены (cascade, orphans=0).
- [x] **G4** — круговая: вес пишется к СВОЕМУ упражнению (master `4037a73`, prod-verified). **Root cause:** стейл-замыкание в `active-circuit.tsx` — `startWorking()` заполнял поле веса из `currentExercise`, но вызывался СРАЗУ после `setCurrentExerciseIdx/Round` (React: next-state доступен только со след. рендера) → вес ПРОШЛОГО упражнения прелился в новое и сохранялся под его `circuitExerciseId` (вес приседа лип к подтягиваниям → фантомный +50кг). Сервер/репо уже писали вес по явному `circuitExerciseId` — правился только клиентский прелил. **Фикс:** `startWorking(forExercise)` принимает упражнение перехода ЯВНО; все 5 переходов (старт / no-rest упр / no-rest круг / rest-упр / rest-круг) передают целевое упр.; «Старт»-кнопка обёрнута `() => startWorking()` чтобы MouseEvent не попал в параметр. **Прод-verify (Playwright drive на проде):** seed круговая 2 reps-упр (Толчок target=100кг, Тяга target=NULL), 1 круг, rest=0. Драйв БЕЗ ручного ввода: Старт→ex1 поле веса прелито «100» (свой target ✓)→Готово→ex2 поле веса ПУСТОЕ (placeholder «—», НЕ «100» — стейл-баг устранён)→Готово. БД `circuit_round_logs`: ex0 Толчок logged=100 (свой), ex1 Тяга logged=**NULL** (НЕ 100). Старый код залогировал бы 100 на Тягу. Тест-юзер удалён (--cleanup, FK cascade → orphan circuits=0), temp SQL удалён, оба pm2 online, login=200.
- [x] **G5** — sanity-guard на невозможный прогресс (+50 кг/нед) (master `6f56348`, prod-verified). Pure domain `assessProgressJump(prevKg,curKg)`→`{suspicious,deltaKg,pct}` (порог `SUSPICIOUS_JUMP_PCT=0.2` + `SUSPICIOUS_JUMP_MIN_KG=10` — реальный недельный прирост силы единицы %, скачок e1RM >20% при дельте ≥10кг ≈ ошибка ввода/опечатка). 8 unit-тестов (порог, малая-дельта-большой-%, регресс, нет-базы, NaN). `checkPersonalRecord` += `suspicious` (additive — будущий PR-бейдж не покажет зелёный рекорд молча). AI-контекст `formatOneRmTrend`: подозрительная строка аннотируется «⚠️ ПОДОЗРИТЕЛЬНЫЙ СКАЧОК (+X% за сессию) … НЕ считать рекордом, не хвалить, попросить проверить ввод» → AI не врёт про нереальный прогресс. Не блокирует сохранение. **Прод-verify (tsx `verify-g5.ts`):** seed прошлая(оба 100кг e1RM)+сегодня(Жим штанга 160кг=+60% / Жим гантели 103кг=+3%) → `buildTrainerContext` блок «Прогресс 1RM»: строка штанги несёт маркер «ПОДОЗРИТЕЛЬНЫЙ СКАЧОК (+60%)», строка гантелей — НЕТ (селективно, не blanket). Тест-юзер удалён (cascade), temp-скрипт удалён с VPS+локально, прод-tree чист, оба pm2 online, login=200.
- [x] **G3** (силовой путь) — история `/workouts/[id]` теперь рендерит цветную `TrainerResultCard` (resultJson, per-exercise дельты F4), а не plain markdown (master `034ab62`, prod-verified). Анимация «думает» + живой текст уже были (F8 stream-консьюмер + скелетон F8 run-3).
- [x] **G3-followup** (круговая) — `completed-circuit.tsx` `AnalysisCard` теперь при валидном `resultJson` рендерит цветную `TrainerResultCard` (как силовая G3), а не plain markdown `{analysis.content}` (master `f941c69`, prod-verified). `trainerSchema.safeParse` страхует legacy-записи без resultJson → markdown-фолбэк. Круговые AI-job'ы идут через тот же `generateTrainerResponse` (process-ai-jobs route) → пишут trainerSchema-совместимый resultJson → карточка применима. **Share-страница `/a/[token]` НЕ трогалась — там уже был safeParse→card→markdown-фолбэк; «осколок на :55» был ложным (это и есть фолбэк-ветка, не баг).** Аддитивно, один файл, без миграции. Gate green (typecheck 0/lint 0/37 тестов/build). **Прод-verify (master `f941c69`, throwaway-юзер + 2 seeded completed круговые):** (A) struct (resultJson валидный) `/circuits/cw-g3f-struct` → DOM: «Прогресс по упражнениям» (эксклюзив `TrainerResultCard`) + score 78 + строка-дельта «Бёрпи»; raw markdown `STRUCT_MD_LEAK` ОТСУТСТВУЕТ, старый заголовок «Разбор тренера» ОТСУТСТВУЕТ → **доказательство фикса** (старый код показал бы plain markdown). (B) legacy (resultJson NULL) `/circuits/cw-g3f-legacy` → markdown-фолбэк виден (`LEGACY_MD_VISIBLE` + «Разбор тренера»), карточки нет → safeParse-фолбэк цел (backward-compat). Скриншот `g3-followup-circuit-colored-card.png`. Тест-юзер удалён (--cleanup, FK cascade → circuits/analyses=0|0), temp SQL удалён с VPS+локально, прод-tree чист, все pm2 online, login=200.
- [x] **G6 (слайс 1: сводка объёма словами)** — карточка-вывод над графиками `/stats` (master `7a235d9`, prod-verified). `periodVolumeComparison(userId,range)` (repo) сравнивает working-тоннаж текущего окна с предыдущим окном той же длины (только completed, G1; range='all'→previous=null). Pure `summarizeVolumeChange`→`{status,headline,detail,pct}` (переиспользует `trendStatus` F4/F7; <5% дрейфа=«держишь уровень»; нет базы→«копим данные»). `PeriodInsightCard` — цветная карточка (`TREND_TONE`, статус текстом+иконкой не только цветом R-41), ведёт НАД KPI/графиками. Аддитивно, без миграции. +8 unit-тестов. **Прод-verify (master `7a235d9`, throwaway + seed 2 периода: прошлый месяц 100×5=500, этот 100×10=1000):** `/stats?range=30d` → headline «Ты растёшь 💪», detail «Объём тренировок за месяц на 100% больше, чем за прошлый месяц», бейдж «↑ +100%» aria=«Рост», карточка ПЕРЕД KPI-сеткой (insightBeforeKpi=true). `/stats?range=7d` (прошлое 7д-окно пусто→previous=0) → «Пока копим данные», без бейджа (ветка no-baseline). Тест-юзер удалён (--cleanup, leftover workouts=0), seed удалён, все pm2 online, login=200.
- [x] **G6 (слайс 2: per-упражнение mover)** — карточка «Ключевое движение» на `/stats` (master `f08a5ee`, prod-verified). `topMoverByE1rm(userId,range)` (repo) — лучший e1RM по упражнению в текущем+прошлом окне (Epley×Brzycki как `oneRmTrend`, completed+working G1), выбирает движение с наибольшим РОСТОМ среди тех, что есть в ОБОИХ окнах (нет ложного прогресса с нуля — G5); range='all'→null. Pure `summarizeExerciseTrend` (R-7, reuse `PeriodInsight`/`trendStatus`): «Жим растёт на X% — 103→115 кг»; прирост >50% → пометка про опечатку (G5). `PeriodInsightCard` += опц. `eyebrow` (карточка объёма не тронута). +5 unit (57 всего). **Прод-verify (Playwright, throwaway + seed «Жим лёжа со штангой» prev 90×5@40д / cur 100×5@5д):** `/stats?range=30d` → «КЛЮЧЕВОЕ ДВИЖЕНИЕ / Жим лёжа со штангой растёт 💪 / за месяц вырос на 11% — 103 → 115 кг / ↑ +11%» (e1RM-math точна). Null-path: `range=all` нет карточки (null), `range=7d` нет (нет упр. в обоих 7д-окнах → guard). Тест-юзер+seed удалены, прод-tree чист, все pm2 online. **G6 ПОЛНОСТЬЮ ЗАКРЫТ; ЭПИК G ЗАКРЫТ.**
- [x] **G7a** — расписание привязать к КОНКРЕТНОМУ шаблону (templateId/circuitId/cardioId → `workout_schedules`, МИГРАЦИЯ+pg_dump) + подсветка «сегодня надо такую» на главной/в тренировках. **ЗАКРЫТ** (foundation + repo+UI + подсветка «сегодня»).
  - [x] **foundation: схема+миграция 3 nullable FK** (master `0ad963b`, prod-verified). `workout_schedules` += `template_id`→workout_templates / `circuit_workout_id`→circuit_workouts / `cardio_workout_id`→cardio_workouts, все nullable, `ON DELETE SET NULL` (R-29: 3 отдельных FK, не полиморфный type+id; удалили заготовку → расписание остаётся «свободным»). NULL во всех = свободное (на /create-пикер как раньше); ровно одна = «сегодня вот эту» (для G7b). Миграция `0012` ручная идемпотентная (`ADD COLUMN IF NOT EXISTS` + `DO/EXCEPTION` FK-гарды) через psql после pg_dump (бэкап 13.5M), journal idx 12 + `0012_snapshot.json` вручную (НЕ db:generate — circuit snapshot-дрифт). Foundation-слайс: НИЧТО пока не читает столбцы → нулевое изменение поведения. Аддитивно, обратимо (DROP COLUMN). Gate green (typecheck 0/lint 0/45 тестов/build). **Прод-verify (master `0ad963b`):** `\d workout_schedules` → 3 nullable text-столбца + 3 FK `ON DELETE SET NULL` к верным таблицам. FK round-trip в ROLLBACK-txn (прод-данные не тронуты): валидная привязка template_id ПРИНЯТА (хранится, два др. NULL); богус template_id ОТКЛОНЁН (FK violation `…_template_id_…_fk`); DELETE шаблона → расписание выжило, `template_id` стал NULL (ON DELETE SET NULL работает). Authed `/schedule`=200 рендерит (select* над новой формой таблицы без 500). Тест-юзер удалён (--cleanup), temp SQL удалён с VPS, оба pm2 online, login=200.
  - [x] **repo+UI: запись привязки** (master `8b2ec11`, prod-verified). Опц. поле `preset` в `scheduleSchema` (regex `kind:id`, kind∈template|circuit|cardio); `NewScheduleInput += preset?: SchedulePresetLink`; `createSchedule` проверяет владение заготовкой (R-7 — FK без RLS не энфорсит владельца) и пишет ровно один из 3 FK-столбцов (`presetColumns` helper). Action нормализует "" → undefined, парсит строку в линк. Форма `/schedule` = нативный сгруппированный `<select name="preset">` (optgroup на формат, пустая опция = свободное, рендерится только при наличии заготовок). Список `/schedule` показывает имя связанной заготовки под расписанием (`resolvePresetLabel` + индекс id→имя; фолбэк-метка по формату если заготовка вне выборки). Аддитивно, без миграции, обратимо. Gate green (typecheck 0/lint 0/45 тестов/build). **Прод-verify (Playwright drive, throwaway-юзер + seeded template `g7a-verify-tmpl`):** пикер показал шаблон под «Силовые шаблоны» (круговая/кардио-группы скрыты — не сидил); создал расписание с привязкой → БД `template_id=g7a-verify-tmpl`, circuit/cardio NULL; список отрендерил «G7a Verify Shablon». Контроль «свободное» (preset="") → все 3 FK NULL (поведение как раньше). Скриншот `g7a-schedule-preset-binding.png`. Тест-юзер удалён (--cleanup, FK cascade → schedules=0), seed-template + temp SQL удалены, прод-tree чист, все pm2 online (fitness-saas+cron+чужие), login=200.
  - [x] **подсветка «сегодня»** (master `f8e31ed`, prod-verified). Блок «Сегодня по расписанию» на дашборде (`TodayScheduleCard` async server-компонент): берёт включённые расписания юзера, чьи `daysOfWeek` включают сегодняшний ISO-день в его локальной TZ (`getUserProfile.timezone`), ведёт прямо на привязанную заготовку (`/templates|/circuits|/cardio/[id]`) или на `/create`-пикер если расписание свободное; нет расписаний на сегодня → блок не рендерится. Выделен pure-хелпер `lib/datetime/local-day.ts` (`localDateIso`/`localIsoDay`, 1=Пн..7=Вс, DST-safe — байт-в-байт эквивалент inline-копии в cron push-reminders, помечен к объединению; cron не трогали), +7 unit-тестов. Фолбэк-метка по формату если заготовка вне выборки/удалена (FK SET NULL). Аддитивно, без миграции, ссылки ≥56px (R-41). Gate green (typecheck 0/lint 0/52 теста/build). **Прод-verify (master `f8e31ed`, Playwright drive, throwaway-юзер + seed: template `g7a_today_tmpl` + расписание `sch_today` daysOfWeek=[3]=сегодня(ср) привязано к template + контроль `sch_other` daysOfWeek=[1,5]):** `/dashboard` → блок «Сегодня по расписанию» виден, строка «Segodnya test / G7a Today Tmpl / Начать», ссылка `href=/templates/g7a_today_tmpl` (ведёт на привязанную заготовку); контроль «Ne segodnya» (Пн/Пт, не сегодня) ОТСУТСТВУЕТ → **день-фильтр работает** (сломанный показал бы оба). Скриншот `g7a-today-highlight-dashboard.png`. Тест-юзер удалён (--cleanup, FK cascade → schedules=0/template=0, orphans=0), temp SQL удалён, прод-tree чист, fitness-saas+cron online, login=200.
- [x] **G7b** — быстрый запуск конкретной заготовки из «сегодня» (без пикера). **ПОЛНОСТЬЮ ЗАКРЫТ:** силовой шаблон (run-1 `15875e3`) + круговая/кардио re-run (run-2 `5b6d677`) — все 3 формата стартуют из «сегодня» одним кликом.
  - [x] **run-1: силовой шаблон — прямой старт** (master `15875e3`, prod-verified). Блок «Сегодня по расписанию» на дашборде: строка с привязанным СИЛОВЫМ шаблоном теперь рендерится как `<form action={startWorkoutFromTemplateAction}>` (hidden `templateId`) вместо `<Link>` на detail-страницу. Один клик «Начать» = новый активный сеанс из шаблона → редирект `/workouts/[id]` (active), без detail-детура. Круговая/кардио (session-instance, re-run = клон) и свободные расписания (нет привязки → `/create`-пикер) ведут ссылкой как раньше. `resolvePreset` → discriminated `{kind:'template'|'link'}`; R-4 общий `ROW_CLASS`+`RowInner` для form/link; R-41 тап 71px; G2-инвариант соблюдён (`startWorkoutFromTemplate` отменяет прошлый active-силовой); R-7 (action ре-валидирует владение шаблоном). Аддитивно, один файл, без миграции, обратимо. Gate green (typecheck 0/lint 0/52 теста/build). **Прод-verify (Playwright drive, throwaway + seed template[UUID]+`g7b-sch` daysOfWeek=[3]=сегодня(ср)):** `/dashboard` → строка = `<form>`+`<button>` (НЕ `<a>`, старый Link ушёл — `anyPlainLink:null`), hidden `templateId`=UUID, текст «Начать», 71px. `form.requestSubmit()` → редирект на `/workouts/3c68f679…` (active, title «Тренировка»); БД: новый workout `status=active`, `template_id`=seed-UUID, `name='G7b Strength Tmpl'` (клон из шаблона). **Поймал баг тест-данных:** `startSchema` требует `templateId.uuid()` — seed со slug-id `g7b-tmpl` дал «Invalid templateId» 500 → re-seed реальным UUID (код корректен, у настоящих шаблонов id=`crypto.randomUUID`). Скриншот `g7b-direct-start-template.png`. Тест-юзер удалён (--cleanup, cascade → workouts/schedules/templates=0/0/0), прод-tree чист, все pm2 online.
  - [x] **run-2: круговая/кардио re-run** (master `5b6d677`, prod-verified — чекбокс был залип, работа отгружена+verified). `cloneCircuit`(из `getCircuitForUser`→`startCircuit`) / `cloneCardio`(из cardio session, копирует блоки, сбрасывает факты) + `reRunCircuitAction`/`reRunCardioAction` + `TodayScheduleCard` рендерит круговую/кардио строки `<form>`-кнопкой «Начать» (как template). Прод-verify см. RESUME 2026-06-10 G7b run-2: клон-круговая active+0 round_logs, клон-кардио active+16 блоков 0 actual, оба source остались completed.
- [x] **G7c** — комментарий после завершения для ВСЕХ форматов (распространить F3 feeling-note на круговую/кардио) + показ в истории/разборе. **ЗАКРЫТ: круговая (run-1 `7d1207a`) + кардио (foundation `1232428` миграция notes → wire `cb37da2`). Все 3 формата имеют feeling-note + показ в истории + подачу в AI-контекст.**
  - [x] **run-1: круговая — feeling-note на завершении** (master `7d1207a`, prod-verified). Активный экран круговой в фазе «done» теперь показывает необязательное поле «Как прошла круговая?» (Textarea, maxLength 500, токены R-36). `finishAll` аппендит `notes` в FormData → `finishCircuitAction`+`finishCircuit` уже принимали/писали `circuit_workouts.notes` (бэкенд готов с G2-эры), `completed-circuit.tsx` уже рендерит «Заметка»-секцию, `buildCircuitFocusedContext` уже подаёт `w.notes` в AI-разбор (строка `Заметка: …`). Итог: одним UI-файлом замкнулась вся цепочка collect→save→история→разбор. Аддитивно, один файл, без миграции, обратимо. Gate green (typecheck 0/lint 0/52 теста/build). **Прод-verify (master `7d1207a`, Playwright drive, throwaway + seed: active круговая 1упр×1раунд с 1 логом → открывается сразу в фазе «done»):** done-карточка показала textarea `#circuit-finish-note` (label «Как прошла круговая? (необязательно)», placeholder, maxlength 500) + кнопку «Завершить и сохранить»; ввёл note с маркером → клик → **БД `circuit_workouts`: status=`completed`, notes=введённый текст дословно**; reload → completed-view рендерит секцию «ЗАМЕТКА» + текст заметки, textarea исчез (доказывает collect→save→display; старый код слал только circuitId → notes остались бы NULL, секции бы не было). Скриншот `g7c-circuit-feeling-note.png`. Тест-юзер удалён (--cleanup, cascade → seeded circuit/exercises/logs=0), temp seed/check-скрипты удалены с VPS, прод-tree чист, fitness-saas+cron online, login=200.
  - [x] **run-2: кардио — feeling-note на завершении** (master `cb37da2`, foundation миграция notes `1232428`, prod-verified — **чекбокс был залип, отгружено+verified**). `cardio_workouts.notes` (nullable text, миграция 0013 через psql после pg_dump) → `finishCardioWorkout(userId,cardioId,notes?)` пишет notes только если непуст → `finishCardioAction` (отдельная `finishWithNoteSchema`, cancel-путь notes НЕ несёт) → `active-cardio.tsx` done-карточка даёт Textarea `#cardio-finish-note` «Как прошла кардио? (необязательно)» (maxLength 500, токены R-36) → `completed-cardio.tsx` секция «Заметка» + `context-builder formatCardioBlock` «заметка атлета: «…»» в AI-контекст. **Все 3 формата (силовая F3 / круговая run-1 / кардио run-2) имеют feeling-note.** Прод-verify см. RESUME 2026-06-11 (master `cb37da2`): done-карточка отрендерила поле, ввод note → БД `cardio_workouts.status=completed, notes=текст дословно`, completed-view показал «Заметка». Код подтверждён в этом ране на месте (schema/repo/action/active-cardio/completed-cardio/context-builder — все present, cb37da2 на master).

### F0 — Foundation
- [x] fetch + база = origin/master; epic-ветка; stash эксперимента
- [x] .gitignore hardening (.claude/.omc/dev-login)
- [x] spec/checklist (этот файл)
- [x] pnpm install ok; build/typecheck/lint/test зелёные на master (b8c2eaf)
- [x] vitest настроен (config + первые тесты)
- [x] GitHub push-токен (юзер дал PAT) → gh настроен + push
- [x] login-helper (session JWT) — **устарел/закрыт:** заменён `scripts/issue-session.mjs` (mint throwaway REFRESH_TOKEN на проде + `--cleanup`), работает каждый ран (подтверждено 2026-06-11). Docker local не нужен — verify идёт на проде.

### F1 — Удаление [done, prod-verify pending]
- [x] repo: deleteWorkout (userId-filter, каскад); deleteTemplate уже был
- [x] actions + revalidate (deleteWorkoutAction)
- [x] UI: ConfirmDeleteButton (диалог) на workout(active+completed) + template
- [x] typecheck/lint/build зелёные (integration-тесты отложены — нет test-DB)
- [x] push (b360d3f) → deploy на прод → F1 UI live (полный клик-тест — в раунде F4)

### F2 — Числовой ввод [core done]
- [x] NumberField + sanitizeNumeric/clampNumber + 12 unit-тестов
- [x] замена в SetInput (вес/повторы/RPE) + template-builder (NumField/RangeField)
- [x] sweep ВСЕХ форм: circuit-builder («Кругов»!), cardio (custom/emom/active), sleep, nutrition, body, topup, profile, active-circuit — **0 type=number осталось** (grep=0)
- [x] NumberField теперь и uncontrolled (FormData-формы) + общий LabeledNumberField (DRY)
- [x] verify на проде runtime (06→6, 60→60) ✓ · PWA на телефоне — за владельцем

### F3 — Самочувствие [done, prod-verify pending]
- [x] хранение: workout_note source=manual (миграция не нужна — таблица есть)
- [x] UI textarea на завершении + saveManualWorkoutNote
- [x] context-builder уже читает recentWorkoutNotes → AI увидит
- [x] задеплоено на прод · полный клик-тест заметки → в раунде F4

### F4 — AI-разбор v3 [инкрементами]
- [x] инкр.1: `exerciseComparisons` в structured (trainer-structured + JSON_SHAPE + prompt) + UI цветовых дельт (TrainerResultCard `ComparisonRow`: рост зелёный↑ / стагнация серый= / регресс мягко-красный↓, «60×5 → 60×6» с подсветкой). Backward-compat (optional/default([])). typecheck+test зелёные
- [x] инкр.2a (БЕЗ миграции): context-builder += «Профиль атлета» (вес тела/рост/возраст + bodyweight-подсказка) в buildTrainerContext; история 8→10; прошлые трен set-by-set целиком (formatWorkout вместо compact)
- [x] инкр.2b — **foundation + run-2 DONE**. **foundation** (master `6517a48`): `exercises.is_bodyweight` boolean (default false, NOT NULL) + сид-флаг 5 системных (pull-up·chin-up·dips-chest·dips-triceps·push-up). Ручная идемпотентная миграция `0011` через psql (pg_dump бэкап 13M ПЕРЕД), journal+snapshot 0011 вручную (НЕ db:generate — circuit snapshot-дрифт). **run-2** (master `20837c5`): `buildTrainerContext` теперь даёт AI ЧИСЛЕННУЮ эффект.нагрузку для bodyweight-упражнений. Pure domain `bodyweightEffectiveLoad(bw,sets)`→{effectiveKg,addedKg,pct} по top-set (5 unit-тестов); `WorkoutSummary += isBodyweight`, оба лоадера (`loadWorkoutSummary`/`loadPastWorkouts`) селектят флаг; `loadLatestBodyweight(userId)`; `formatWorkout` аннотирует bodyweight-упр. «вес тела N кг → эфф. нагрузка top-set ≈ M кг (добавка X кг = Y% тотала)». Аддитивно, без миграции; нет веса тела → нулевое изменение. Gate green (typecheck 0/lint 0 err/29 тестов/build). **Прод-verify (master `20837c5`):** seeded completed workout (dips-chest 20кг×6, is_bodyweight=true) + body_measurement 90кг → tsx-вызов `buildTrainerContext` на проде вернул строку «вес тела 90 кг → эфф. нагрузка top-set ≈ 110 кг (добавка 20 кг = 18% тотала)» (численно верно 90+20=110, 18%); seed+temp-скрипт+тест-юзер удалены (cascade), прод-tree чист, оба pm2 online, login=200.
- [x] verify на реальной истории — **ЗАКРЫТО (2026-06-11, прод, реальный DeepSeek-вызов).** Засеял 2 завершённые силовые одного упр. (Жим лёжа: workout A 7д назад 60×5, workout B сегодня 60×6) → enqueue ai_job(on_demand, B) → trigger `/api/cron/process-ai-jobs` → job `succeeded`. `ai_analyses.result_json->exerciseComparisons` = `[{name:"Жим лёжа со штангой", prevTopSet:"60×5", curTopSet:"60×6", deltaReps:1, deltaWeightKg:0, status:"improved"}]` — модель корректно прочитала прошлую сессию из контекста и посчитала дельту. Runtime-render (`/workouts/B/trainer`, authed throwaway): `TrainerResultCard` рисует ЦВЕТНУЮ `ComparisonRow` «Жим лёжа со штангой 60×5 → 60×6 ↑», row `bg-success/10`, span `text-success` (computed green) — **цветная карточка, НЕ plain markdown** (page рендерит `resultJson` через `TrainerResultCard`, не `content`). Это доказывает и F4 (дельты), и G3 (красивый цветной разбор). Тест-юзер + сид-скрипт удалены (FK cascade: analyses=0), прод git-tree чист, без правок кода/деплоя.

### F5 — Форматы [L]
- [~] schema: format enum + schedule — **schedule-часть DONE** (`workout_schedules`, master `ace93ac`); format enum (EMOM/Tabata как template-формат) — опц./не нужен (живут как cardio-пресеты)
- [x] **run-1: unified пикер `/create` + explainers** (master `6b501a2`) — аддитивная серверная страница, 3 карточки (Силовая→/templates/new, Круговая→/circuits/new, Кардио/HIIT incl. Tabata/EMOM/Norwegian→/cardio/new) с короткими пояснениями; роутит в существующие билдеры; дашборд CTA→/create. Tokens, тапы ≥56px. Без схемы. Прод-verified (h1 + 3 href + dashboard link).
- [x] EMOM/Tabata билдеры — **уже существуют** как cardio-пресеты (`PRESET_META`, `/cardio/new` `startCardioAction`); пикер их сёрфит. Отдельные template-билдеры — опц. (run-2).
- [x] **run-3a: расписание по дням/часу — схема+repo+UI (БЕЗ пуша)** (master `ace93ac`) — `/schedule`: `workout_schedules` (label+daysOfWeek ISO+hour+enabled), repo R-7, server actions create/toggle/delete, форма (day-toggles ≥56px, hour-Select), ссылка из /settings. Ручная идемпотентная миграция через psql (снапшот-дрифт из circuits — см. RESUME). Прод-verified: create→persist, toggle→ВЫКЛЮЧЕНО, delete→empty.
- [x] **run-3b: cron-доставка push-напоминаний** (master `4b0a139`) — `push_kind`+=`workout_reminder` (идемпотентная миграция `0008` через psql, pg_dump бэкап) + repo `listEnabledSchedulesForUser` (R-7) + хелпер `localIsoDay(tz)` (1=Пн..7=Вс, DST-safe) + ветка в `push-reminders` cron (ISO-день+локальный час match, дедуп kind+день через notifications_log) → `sendPushToUser` title «Пора тренироваться: <label>» url `/create`. **Прод-verify:** seeded fake-sub+расписание на текущий MSK час/день → cron-ответ `workoutSent:1`, строка `workout_reminder` в notifications_log; тест-данные удалены (cascade).
- [x] **полиш: несколько напоминаний/день** (master `1100e40`) — дедуп `workout_reminder` по kind+ЧАС вместо kind+день; расписания на разные часы шлют отдельные напоминания в один день; все расписания текущего часа объединяются в один пуш (label через запятую). `alreadySentToday`→делегирует `alreadySentSince(since)`; `startOfHour`; `.find`→`.filter`. Аддитивно, без миграции, один файл. **Прод-verify:** seeded delivered workout_reminder ранее сегодня + 2 расписания на текущий час → cron `workoutSent:1` (старый day-window заблокировал бы), новая строка title=«…Ноги·тест, Кардио·тест» (combine). Тест-данные удалены (cascade). **F5 ПОЛНОСТЬЮ ЗАКРЫТ** (прямой запуск шаблона из напоминания — отложен, требует миграцию templateId→schedule, YAGNI).
- [x] **run-2: единый список с подписями формата** (master `d9fb345`) — `/workouts` теперь сливает завершённые силовые + круговые + кардио в один хронологический список по неделям. `buildHistory()` (discriminated union `HistoryItem`) переиспользует существующие репо (`listCircuits`, `listRecentCardio`, `listRecentWorkouts`) — без схемы/новых роутов. Каждая карточка: неяркий формат-pill (Силовая / Круговая / Tabata·EMOM·4×4·Кардио, токены R-36) + формат-специфичные KPI + свой detail-роут (`/workouts|/circuits|/cardio/[id]`). AI-бейдж сохранён для силовых. EmptyState CTA → `/create`. `groupByWeek` обобщён на `HistoryItem`. **Прод-verify (master `d9fb345`):** seeded 1 силовая+1 круговая+1 кардио(tabata) → `/workouts` показал ровно 3 карточки в одном списке (сортировка по времени), pill'ы «Силовая»/«Круговая»/«Tabata», KPI подходов·кругов·мин/bpm соответственно, href'ы в 3 разных роута. Тест-данные удалены (--cleanup, FK cascade).

### F6 — Друзья [L]
- [x] **run-1: schema friendships + repo (R-7), без UI** (master `5e6ac65`) — enum `friendship_status` (pending|accepted); таблица `friendships(requester→users, addressee→users, status, ts)` + `UNIQUE(requester,addressee)` + индексы обеих сторон + FK cascade; repo `sendFriendRequest`/`acceptFriendRequest`/`listFriends`/`listIncoming|OutgoingRequests`. Ручная идемпотентная миграция `0009` через psql (pg_dump бэкап). **Прод-verify:** enum_range+`\d` точны; txn-roundtrip (ROLLBACK) — idempotent insert (дубль 0 строк), accept→accepted, user-delete→friendship cascade. Privacy-настройки — отдельным run при просмотре тренировок друга.
- [x] **run-2: add/accept flow + /friends UI** (master `220fbfd`) — repo `findUserByEmail`+`list*Detailed` (inArray, без N+1, R-7); Zod `friendRequestSchema`; actions `sendFriendRequestAction`(find-by-email→send, self/not-found как error-state)/`acceptFriendRequestAction`; страница `/friends` (R-37 входящие/add-form/исходящие/друзья, тапы ≥56px R-41, токены R-36); ссылка из /settings. Аддитивно, без миграции. **Прод-verify:** полный A→B→accept→оба видят друг друга в «Ваши друзья» (pending→accepted на обеих сторонах); тест-юзеры удалены (friendships=0, FK cascade). Код был задеплоен предыдущим раном (prod на 220fbfd), этот ран довёл verify+план.
- [x] **run-3: просмотр тренировок друга (read-only, R-7)** (master `b10c1b1`) — repo `areFriends`+`getFriendProfile` (гейт по accepted-дружбе, иначе null); страница `/friends/[friendId]` (requireUser→getFriendProfile→notFound, потом `listRecentWorkouts(friendId)` completed; read-only карточки без detail-ссылок/действий, R-37 empty, R-36 токены); карточки друзей→Link min-h-14. Аддитивно, без миграции. **Прод-verify:** A видит read-only тренировку друга B (2×80×5 → 2 подхода/800 kg·reps/60 мин); stranger-id и self-id → 404 (R-7-гейт). Privacy opt-out отложен (YAGNI v1 — accepted-друзья видят по умолчанию).
- [x] **run-4a: schema `ai_analyses.share_token` + sharing repo (R-7), без UI** (master `f605e67`) — capability-токен (nullable text + `uniqueIndex` NULLS DISTINCT → много нерасшаренных NULL-строк сосуществуют); repo `enableAnalysisSharing` (R-7, идемпотентно — не перевыпускает токен, старая ссылка живёт), `disableAnalysisSharing` (R-7), `getSharedAnalysis(token)` — СОЗНАТЕЛЬНОЕ R-7-исключение (токен = право доступа, без userId-фильтра), отдаёт только non-PII (content/resultJson/modelVersion/createdAt, без userId/email). Идемпотентная миграция `0010` (snapshot-дрифт circuit — meta-снапшот пересоздан, лечит дрифт). Gate: typecheck 0/lint 0 err/24 теста/build. **Прод-verify (master `f605e67`, pg_dump бэкап 13.6MB сделан):** psql применил ADD COLUMN+UNIQUE INDEX; `\d ai_analyses` показал `share_token` + `ai_analyses_share_token_unq UNIQUE`; CREATE UNIQUE INDEX успешно над таблицей с существующими NULL-строками = доказывает coexist NULLs; ROLLBACK-роундтрип (set token на реальную строку → SELECT by token вернул content/model_version/created_at БЕЗ userId → ROLLBACK → 0 строк с тест-токеном) = enable→getSharedAnalysis работает end-to-end, прод-данные не тронуты. Прод здоров (login 200, оба pm2 online).
- [x] **run-4b: actions + публичный роут `/a/[token]` + кнопка «Поделиться»** (master `a7f9c8c`) — server actions enable/disable share (R-7); публичный `app/a/[token]/page.tsx` (Next 16 async params, БЕЗ авторизации, `getSharedAnalysis`→notFound, `trainerSchema.safeParse`→TrainerResultCard / fallback markdown, robots noindex); client `ShareAnalysisButton` (enable→ссылка+copy, disable→приватный, тапы ≥56px, origin через useSyncExternalStore); `trainerSchema` экспортнут, `getLatestTrainerResult`+=shareToken. Аддитивно, без миграции. **Прод-verify:** enable→ссылка→`/a/{token}` read-only виден (unauth fetch=200, robots noindex,nofollow); persist через reload; disable→UI revert + `/a/{token}`=404. Тест-данные удалены (cascade).
- [x] verify (privacy/шеринг) — **F6 ЗАКРЫТ** (prod-runtime, см. run-4b выше)

### F9 — Единый поток тренировок [запрос владельца]
«по тренировкам один поток, а не три разные вкладки на каждую» (силовая/круговая/кардио).
- [x] **run-1: единый «Недавние» на дашборде** (master `c80070b`) — вынес `buildHistory`/`HistoryItem`/`HistoryCard` в общий `components/workouts/workout-history.tsx` (DRY с `/workouts`); дашборд «Недавние» теперь сливает силовые+круговые+кардио (`listCircuits` добавлен), а не только силовые. Аддитивно, без миграции. **Прод-verify:** seeded силовая+Tabata → дашборд показал обе карточки разных форматов с правильными detail-роутами; `/workouts` рендерит идентично. Тест-данные удалены (cascade).
- [x] **run-2: единый вход «Начать тренировку» + общий ResumeBanner** (master `d13eb23`) — три формат-тайла дашборда (StartCard силовая / локальный CardioTile / CircuitTile) схлопнуты: один CTA «Начать тренировку»→`/create` вместо трёх стартов; активные сессии любого формата → общий `ResumeBanner` (R-4 DRY, формат = только подпись); `getActiveCircuitId` поднят в Promise.all; orphaned `CircuitTile.tsx` удалён. Аддитивно, без миграции. **Прод-verify:** без активных = StartCard→/create + 0 старых /cardio·/circuits CTA; seeded active силовая+кардио = 2 ResumeBanner с верными href/label, circuit-баннер отсутствует (conditional), порядок детерминир.; тест-юзер удалён (cascade). Подтверждение направления = повторный запрос владельца в этом ране.
- [x] **run-3: ретайр дублирующих формат-вкладок истории `/circuits`·`/cardio`** (master `a3d3dcc`) — обе bare-list-страницы дублировали единый `/workouts` (та же история по формату) + свой active-resume + свою кнопку «Новая». Теперь они `redirect('/workouts')` (Next 16 `redirect` из `next/navigation`). `/workouts` стал ПОЛНЫМ единым экраном истории: surface active-сессию ЛЮБОГО формата через общий `ResumeBanner` (DRY с дашбордом — раньше показывал resume только силовой). Back-ссылки билдеров/деталей (`/circuits/new`·`/circuits/[id]`·`/cardio/new`·`/cardio/[id]`) репойнтнуты на `/workouts` (без редирект-хопа). Detail-роуты `/circuits/[id]`·`/cardio/[id]` и билдеры `/new` нетронуты; создание — через `/create`. Аддитивно, без схемы/миграции, обратимо. `/templates` (переиспользуемые силовые шаблоны — иной концепт) сохранена. Gate green (typecheck 0/lint 0 err/24 теста/build). **Прод-verify (master `a3d3dcc`, throwaway-юзер + seeded active circuit `c1a78da4`):** authed nav `/circuits`→приземлился на `/workouts`, `/cardio`→`/workouts` (редиректы LIVE); `/workouts` показал `ResumeBanner` «Активная круговая Продолжить»→`/circuits/c1a78da4` (доказывает новый all-format resume — старый strength-only код НЕ показал бы круговую); detail `/circuits/c1a78da4`=200 (не проглочен редиректом), back-ссылка=«К истории»→`/workouts`. Тест-юзер удалён (--cleanup, FK cascade → orphan circuit count=0), temp SQL удалён с VPS, оба pm2 online, login=200. **Единый поток тренировок: теперь ровно ОДИН экран истории (`/workouts`) + один вход (`/create`) + один resume-баннер на формат. Запрос владельца «один поток, не три вкладки» выполнен на всех уровнях (nav · feed · start · resume · history).**
- [x] **run-4: тайл «Последняя» на дашборде = последняя сессия ЛЮБОГО формата** (master `81fece5`) — `LastWorkoutMini` (strength-only `listRecentWorkouts`) → `LastSessionMini({item})` в общем `workout-history.tsx` (DRY: FORMAT_HREF/CARDIO_FORMAT_LABEL/formatShortDate/FormatPill; одна метрика на формат; AI-бейдж для силовых). Дашборд: `latestSession = buildHistory(...)[0]`. `last` (силовая) сохранён для TrainerTrigger. Аддитивно, без миграции. **Прод-verify:** seeded силовая(−2дня)+кардио(−1ч) → тайл «Последняя» = кардио `/cardio/...` «Tabata · 4 мин» (старый код показал бы силовую); тест-данные удалены (cascade 0|0|0). Последний strength-only осколок единого потока закрыт.

### F7 — Графики прогресса [M]
- [x] **run-1: `trendStatus()` хелпер** (master `0100b52`) — `lib/domain/progression/trend.ts`: `TrendStatus` тип + `trendStatus(prev,cur,{epsilon,higherIsBetter})` + `TREND_LABEL`; 12 unit-тестов; DRY канонический `TrendStatus` (trainer-structured + TrainerResultCard импортят). Цвет-токены `TREND_TONE` отложены до run-2 (YAGNI — 2-й потребитель = графики). Аддитивно, deployed, прод здоров. Runtime-verify на run-2 (хелпер ещё не в UI).
- [x] run-2 /stats: e1RM-линии (Recharts) с цветом тренда + бейдж Рост/Стагнация/Регресс через `trendStatus`; вынесен общий `TREND_TONE` в `lib/ui/trend-tone.ts` (DRY с F4 TrainerResultCard) (master `2d318b1`, prod-verified: бейдж «Рост» + «+11.5 кг» + `stroke=var(--success)`)
- [x] **run-3: объём по неделям (бар) — тренд-цвет + бейдж** (master `7dddf8a`) — `VolumeBarChart` красит бары по тренду периода (`trendStatus` higherIsBetter, относит. epsilon 5%) + бейдж Рост/Регресс/Стагнация + дельта тоннажа; DRY с `TREND_TONE`/`TREND_LABEL` (та же семантика что e1RM run-2 и карточка F4). Один файл, аддитивно. Вес тела (линия) — уже был (`BodyTrendChart`). **Прод-verify:** seeded 300→480 (+60%, 2 ISO-недели) → 2 зелёных бара `fill=var(--success)`, бейдж «↑Рост», дельта «+180»; тест-данные+seed-скрипт удалены.
- [x] verify (прод-runtime, см. run-3 выше) — **F7 ЗАКРЫТ**

### F8 — Стриминг разбора [M] — NEXT, дизайн готов
**Текущий флоу (poll):** `app/(app)/workouts/[id]/trainer/page.tsx` → `requestTrainerOnDemand` (`server/actions/trainer.ts`) → aiJob pending → worker `app/api/cron/process-ai-jobs/route.ts::processJob` (`generateTrainerResponse` = `generateText` + `extractJson` + Zod, т.к. deepseek-thinking не даёт чистый structured) → сохраняет `ai_analyses` (content=md из `renderMarkdown`, resultJson) → `components/trainer/TrainerJobPoller.tsx` поллит `/api/ai/jobs/[id]` 2с. Боль: ожидание/перезаход.

**Развилка (решить + реализовать):**
- **A. live-markdown** (низкий риск): новый `POST /api/ai/trainer/stream` — `buildTrainerContext`+RAG + `streamText({model: aiClient(COACH_MODEL), system: <markdown-prompt>, prompt})` (как `app/api/ai/coach/route.ts`) → `toTextStreamResponse()`; `onFinish` → сохранить `ai_analyses`. Клиент стримит токены live. **Минус:** теряются цветные дельты F4-инкр.1 (markdown вместо structured card).
- **B. live-structured** (`streamObject`): сложно — thinking-модель эмитит reasoning перед JSON, partial-object стрим рвётся. Нужен эксперимент (вырезать thinking, или non-thinking модель для трейнера).
- **✅ ВЫБОР ВЛАДЕЛЬЦА: B** — цветные карточки СОХРАНЯЕМ, генерация inline+стрим (одна генерация, без poll/перезахода).
- **B-план:** новый `POST /api/ai/trainer/stream` (requireUser → `buildTrainerContext`+RAG → `streamText(aiClient(COACH_MODEL), system=TRAINER_SYSTEM_PROMPT+JSON_SHAPE, prompt)`; `onFinish`: `extractJson`+Zod → save `ai_analyses` md+resultJson). Экспортнуть `JSON_SHAPE_INSTRUCTION`/`extractJson` из `lib/ai/trainer-structured.ts`. Клиент `/workouts/[id]/trainer`: на on_demand вместо `TrainerJobPoller` — стрим-консьюмер: live «тренер анализирует…» (анимация/прогресс по стриму), по завершении → `TrainerResultCard` (цветные дельты F4). Poll-путь оставить fallback (push/повторный заход).
- [x] **run-1: endpoint `/api/ai/trainer/stream`** (additive, deployed+verified на проде). `POST` → requireUser → `buildTrainerContext`(on_demand)+RAG → `streamText(aiClient(COACH_MODEL), system=TRAINER_SYSTEM_PROMPT+JSON_SHAPE_INSTRUCTION)` → `toTextStreamResponse`; `onFinish({text,usage})` → `parseTrainerJson`+`renderTrainerMarkdown` → insert `ai_analyses`. Экспортнуты из `trainer-structured.ts`: `JSON_SHAPE_INSTRUCTION`, `extractJson`, `parseTrainerJson`, `renderTrainerMarkdown` (вынесена из cron, cron теперь импортит — поведение идентично; DRY). **Прод-verify (master 2da7220):** unauth POST=307 (guard ✓); authed POST на seeded completed workout → HTTP 200, валидный structured JSON стримом (2402 B / 12s); `onFinish` сохранил ровно 1 `ai_analyses` (overallScore=45, exerciseComparisons=1 → цветные дельты сохранены, md 1339 chars, токены записаны). Тест-данные удалены (--cleanup, FK cascade).
- [x] **run-3: скелетон карточки при стриме** (master `6cb50b5`, prod-verified) — `LoadingState` (bare-спиннер) → `TrainerSkeleton` в форме `TrainerResultCard` (~16 `animate-pulse` `bg-muted`-блоков, helper `Bar`); реальный статус-текст сверху меняется по байтам стрима (анализирует→пишет→собираю); `role=status`+`aria-busy`+`aria-live`, скелетон `aria-hidden`, `motion-reduce:animate-none`; токены R-36. Честный индикатор (стрим без Content-Length → каркас, не проценты). Один client-файл, аддитивно, без миграции. **Прод-verify:** заход во время стрима → `[role=status]` + 16 баров + «Тренер пишет разбор…» (screenshot `f8-run3-skeleton.png`); через ~50с → карточка, скелетон ушёл (`statusGone, skeletonBars:0, cardPresent`). Тест-данные удалены (cascade). **F8 ПОЛНОСТЬЮ ЗАКРЫТ.**
- [x] **run-2: клиент-консьюмер** (LIVE+verified на проде, master `ab8fcd6`). Реализовано: `page.tsx` 3-ходовая развилка — (1) сохранённый `resultJson` есть → `TrainerResultCard` сразу; (2) pending/running/succeeded aiJob есть → `TrainerJobPoller` (fallback, cron-путь); (3) свежий on_demand → новый client `TrainerStreamConsumer`. Консьюмер: `POST /api/ai/trainer/stream`, дренирует ReadableStream РАДИ ПРОГРЕССА (сырой JSON НЕ рендерит — индикатор «Тренер анализирует…» по факту байт), по завершении ретраит `GET /api/ai/trainer/latest?workoutId=` (6×1с) → `TrainerResultCard` (цветные дельты F4). Решение из развилки: выбран вариант **(a)** — новый endpoint `GET /api/ai/trainer/latest` + repo `getLatestTrainerResult(userId, workoutId)` (R-7), серверная Zod-валидация при сохранении. Poll-путь (dashboard `TrainerTrigger` + cron) НЕ тронут. Аддитивно, без миграции. **Прод-verify:** seeded completed workout → on_demand-заход показал «Тренер анализирует…» → через ~30с появился `TrainerResultCard` (overallScore 40/100, exerciseComparisons «Жим лёжа 60×7 new», рекомендации) БЕЗ перезахода; reload → карточка сразу из сохранённого (no re-stream); `ai_analyses count=1` (без дублей). Тест-данные удалены (--cleanup, FK cascade).
