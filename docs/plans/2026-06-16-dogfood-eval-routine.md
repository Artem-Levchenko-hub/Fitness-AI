# Dogfood-Eval Routine — протокол (источник правды)

**Создан:** 2026-06-16. **Запуск:** scheduled-task `dogfood-eval-loop`, cron `*/10 * * * *` (каждые 10 мин), последовательно, уступает живым сессиям. Токены не ограничены.

## Миссия

Автономно догфудить ПРОДУКТ (constructor.lead-generator.ru) как живой пользователь: генерить приложения / скрипты / ботов, оценивать **дизайн И функциональность** по скриншотам + логам, **находить тупняки и слепые зоны** генератора, и **чинить их по доказательствам (логи + скриншоты), НЕ вслепую**. Каждый прогон — один цикл, evidence → diagnosis → (fix или proposal) → ledger.

## Главный известный тупняк (гипотеза H1 — проверить ПЕРВОЙ)

Владелец: «написал "создай сайт" → получил статику; кинул 5 промптов "переделай в полноценное веб-приложение" — генератор по сути ничего не делал, чуть перегенривал статику, не отвечал по запросу».

**Гипотеза причины** (из root-cause анализа пайплайна):
- Первый промпт → `discovery` выбирает stack; дефолт `static` (`discovery._DEFAULT_STACK`), а полноценный апп — только если LLM-рекомендер активно выберет контейнер.
- **Follow-up промпты идут через triage → surgical-edit / edit-путь, который правит СУЩЕСТВУЮЩИЙ static HTML и НИКОГДА не пере-запускает discovery / stack_routing.** То есть «переделай в приложение» трактуется как правка статики, а не как смена стека static→nextjs_entities/spa. Эскалации стека на follow-up НЕТ.
- Проверить: `apps/api/src/omnia_api/routers/messages.py` (triage/intent → surgical/edit ветка), `services/intent_triage.py`, `services/discovery.py` (запускается ли на follow-up), `services/stack_routing.py` (`switch_to_stack` вызывается ли вне первого билда).
- **Фикс-направление** (когда подтверждено логами): detect intent «сделай настоящее приложение / добавь вход / БД / кабинет» на follow-up → re-trigger discovery/stack-escalation (static→container), а не surgical-edit статики. С тестом-репро.

## Протокол одного прогона (строго по шагам)

### 0. ГАРД (если не прошёл — выйти, записать строку в LEDGER «skipped: <причина>»)
- **Sequential-lock:** файл `_routine/dogfood.lock`. Если есть и моложе 15 мин → другой прогон идёт → ВЫЙТИ. Иначе записать в него ISO-время старта.
- **Уступи живой сессии:** glob `C:\Users\79133\.claude\coordination\omnia-mvp-*\active-sessions.json`. Если есть сессия с heartbeat за последние 5 мин и это НЕ этот прогон → ВЫЙТИ («yielded to active session»). Нет файла/дир → считать idle, продолжать.
- **Resource-guard:** `ssh i48ptgvnis@170.168.72.200 'free -m'`. Если available < 3000 МБ → НЕ генерировать: `docker rm -f` висяки `omnia-dev-{routine,test,dogfood}-*`, перепроверить; всё ещё мало → ВЫЙТИ.
- Снять lock в КОНЦЕ прогона всегда (даже при ошибке).

### 1. ВЫБОР СЦЕНАРИЯ (ротация по номеру прогона = число строк RUNS в LEDGER mod N)
0. **H1-репро (приоритет, пока H1 открыт):** «создай сайт для кофейни» → дождаться → затем «переделай это в полноценное веб-приложение: вход, личный кабинет, база записей» → ещё 1-2 таких. Замерить: эскалировал ли stack static→container или остался статикой.
1. **Web-app с нуля:** «CRM для записи клиентов: вход, список клиентов, добавление, заметки».
2. **Скрипт/инструмент (spa):** «калькулятор ипотеки с графиком».
3. **Телеграм-бот:** «бот для записи в барбершоп» (проверить — доступен ли вообще tgbot-стек: ранее orphaned).
4. **Свободный:** что-то из реальных запросов в логах прода (грепни недавние user-промпты).

### 2. ГЕНЕРАЦИЯ (через продукт)
- Драйв через браузер (авторизован) — gstack/playwright/Chrome MCP на `constructor.lead-generator.ru`: создать проект, отправить промпт(ы), дождаться сборки, открыть live-превью. ЛИБО через API минт-токеном (надёжнее headless): owner проекта-песочницы; токен — `docker exec omnia-prod-api /app/.venv/bin/python -c "from omnia_api.core.security import create_access_token; from uuid import UUID; print(create_access_token(UUID('<owner>')))"`, api на localhost:8200 (с VPS). Публичное превью: `<slug>-dev.preview.lead-generator.ru`.
- Снять **скриншоты** результата (desktop+mobile, light+dark если есть): сохранить в `_routine/runs/<ISO>/`.
- Скачать сгенерённый артефакт (HTML/.tsx/файлы) для анализа.

### 3. ОЦЕНКА (две оси)
- **Функциональность:** совпал ли результат с запросом? static vs реальный апп (есть `/app`-роуты, auth, БД, интерактив)? Для H1-репро — ЭСКАЛИРОВАЛ ли стек на follow-up? Кнопки/ссылки живые или тупики? Бот/скрипт реально делает заявленное?
- **Дизайн:** скриншот → оценка (rubric: иерархия, глубина, палитра-под-нишу, типографика, «не серый дефолт»). Можно прогнать через vision-судью продукта или оценить самому по скриншоту. Балл 0-10 + 2-3 конкретные проблемы.

### 4. EVIDENCE с сервера (корреляция «что увидел» ↔ «что решил сервер»)
- SSH прод, по project_id/slug грепнуть логи `omnia-prod-api` / `omnia-prod-worker`:
  - какой stack выбрал discovery, какой `gen_mode` (freeform/static/catalog), какая модель, был ли re-discovery на follow-up;
  - `[VSEGPT] stream usage … cache_hit=…` (кэш B), ошибки/traceback, пустые брифы, таймауты.
- Зафиксировать ИМЕННО серверное решение, которое привело к видимому провалу.

### 5. ДИАГНОЗ + СЛЕПЫЕ ЗОНЫ
- Назвать тупняк одним предложением + file:line, где решение принимается неверно.
- **Слепые зоны:** чего генератор НЕ проверяет/НЕ ищет (напр. не перечитывает интент на follow-up; не эскалирует стек; не зовёт судью; не видит, что страница — мёртвая статика с нерабочими кнопками).

### 6. УЛУЧШЕНИЕ (по доказательствам, не вслепую)
- Если фикс ЯСНЫЙ, безопасный, доказан логом+скриншотом → реализовать МАЛЫМ диффом + тест-репро → доставить (commit+push main+деплой нужных сервисов) → health-check. Цитировать строку лога/скриншот, что доказывает.
- Если фикс крупный/рискованный → НЕ шипить вслепую: записать PROPOSAL в LEDGER (что, почему, какой evidence, какой план).
- Один прогон — максимум один фикс (последовательность, не ломать прод).

### 7. ЗАПИСЬ
- Append строку в RUNS (ниже): время MSK, сценарий, stack/gen_mode/модель, design-балл, тупняк, действие (fix `<commit>` / proposal / none), PASS/FAIL.
- Сырьё (скриншоты, дампы, полный лог прогона) — в `_routine/runs/<ISO>/` (gitignored).
- Снять lock.

## Доставка / прод (CLAUDE.md)
- atomic commit + трейлер `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`; push main (через origin/main, `git fetch` сперва — main двигают 4 агента; rebase при race).
- Деплой: api/worker → `cd /opt/omnia/apps/llm-gateway/deploy/full && docker compose up -d --build api worker`; gateway → `… up -d --build gateway`; **фикс ШАБЛОНА `apps/orchestrator/templates/*` → ПЕРЕСБОРКА base-образа на VPS** (`cd /opt/omnia/apps/orchestrator && docker build -t omnia-template-nextjs-postgres-drizzle:dev -f templates/nextjs-postgres-drizzle/Dockerfile.dev templates/nextjs-postgres-drizzle/`), git pull НЕ достаточно.
- Прод-compose = `apps/llm-gateway/deploy/full` (контейнеры `omnia-prod-*`), НЕ `infra/`. Прод git: `git fetch && git merge --ff-only origin/main` (не pull). Health-check: curl прод 200 ИЗВНЕ (с VPS NAT-hairpin даёт ложный 000).

## ЗАПРЕТЫ
- Стоимость/токены/бюджет — НЕ упоминать НИ В КАКОЙ форме.
- НЕ трогать клиентские аппы `kofeinia`/`legomagazin` и чужие проекты `signal-telekom`/`crm-sistema`; не ломать прод. Запрещённые действия (оплата, удаление пользовательских данных, права доступа) — не выполнять.
- Песочница: использовать ТОЛЬКО свои тест-проекты (слаг с префиксом `dogfood-`), удалять тест-контейнеры после прогона.
- Модели — всё через vsegpt; proxyapi не использовать.

---

## BLIND-SPOTS LEDGER (накопительный — слепые зоны генератора)
<!-- одна строка на подтверждённую слепую зону: ID | дата | описание | file:line | статус(open/fixed commit) -->
- H1 | 2026-06-16 | follow-up «переделай в приложение» не эскалирует stack (surgical-edit правит статику, discovery/stack_routing не пере-зовутся) | messages.py:626,675 | **CONFIRMED** (run #1, code-path proof + live prod repro) → PROPOSAL ниже, фикс рискованный (деструктивный re-scaffold) → НЕ шипить вслепую
- BS-2 | 2026-06-16 | `decide_intent` не имеет «app-ification» интента: «вход / личный кабинет / база записей» не матчат ни _REBUILD, ни _STRUCTURAL keywords → CHEAP surgical-edit (auth/login-стемы намеренно исключены, чтобы не фолс-фолсить на «кнопка входа») | intent_triage.py:62,103 | CONFIRMED (run #1) — часть H1; xfail-репро в test_intent_triage.py
- BS-3 | 2026-06-16 | ЗЕРКАЛО H1: discovery ПЕРЕ-эскалирует инструмент с явным «без регистрации/входа/аккаунтов» в auth-стек nextjs_entities → калькулятор оказывается за /signin + ненужная БД; safety-net `_infer_stack_from_text` однонаправленный (только static→entities, никогда spa, не вето́ит LLM-pick) | discovery.py:838 | **FIXED 04bb7cf** (run #2: live-репро dogfood-mortgage-calc-68f79c + screenshot CTA→/signin; негативный safety-net `_explicit_no_backend`→spa, 3 теста-репро, задеплоено api/worker, VETO_FIRES verified live)
- BS-6 | 2026-06-16 | **E-commerce/storefront запрос схлопывается в authed-dashboard CRM** — нет публичного каталога (он за /signin), нет КОРЗИНЫ, нет ОФОРМЛЕНИЯ ЗАКАЗА, хотя бриф их явно называет. Entity writer SYSTEM_PROMPT.md жёстко зашивает «app home is /dashboard (binding)» + «functional app screens (dashboard/CRM/SaaS), not a landing» + «route per entity + a dashboard» → витрина с покупательской воронкой структурно недостижима. Bouquet=access:public, но публичного каталога-UI не генерится; CTA лендинга «В каталог»→/signin (тупик). Order=access:owner single-bouquetId (админ-CRUD), не customer-checkout | apps/orchestrator/templates/nextjs-entities/SYSTEM_PROMPT.md:82,96,118 | **CONFIRMED** (run #5: live-репро dogfood-flowershop-flora-6b00df — entities+routes+CTA+screenshot; BS-4 эскалация сработала end-to-end, но воронка отсутствует) → PROPOSAL P-EC, фикс = новый storefront-архетип (крупный) → НЕ шипить вслепую
- BS-6b (design) | 2026-06-16 | На entity-аппах writer красит страницу INLINE-хексом (`bg-[#450A0A] text-[#FAFAFA]` в page.tsx), но НЕ переопределяет `:root`-токены (globals.css `:root` остаётся дефолтным LIGHT: `--background oklch(0.99 0 0)`=white) → themed shadcn-компоненты рассинхронятся: `Button variant="outline"`=`bg-background`(white) + унаследованный near-white текст = НЕВИДИМАЯ вторичная CTA. Entity palette-guard HTML-only (07/08_palette_guard chars=0 на entity) → нет enforcement токенов на entity-поверхностях | globals.css:17 + nextjs-entities/SYSTEM_PROMPT.md:89 | CONFIRMED (run #5 screenshot: «Войти в кабинет» = пустой белый бокс) — часть design-фейла, фикс generator-prompt-level → не вслепую
- BS-4 | 2026-06-16 | ПЕРВЫЙ билд с пропущенным discovery (quiz/«просто сгенерируй» шлёт skip_clarify=true, либо select-mode) НЕ эскалирует stack: `switch_to_stack` живёт ТОЛЬКО в discovery-BUILD ветке, а discovery гейтится `interview_eligible = is_first_build and not skip_clarify` → явный app-запрос («CRM, вход, кабинет, база записей») собирается freeform-статикой с мёртвыми кнопками входа. Это ПЕРВО-билдовый близнец H1 (H1 = follow-up, остаётся PROPOSAL P-H1) | messages.py:706 | **FIXED 5878059** (run #3: live-репро dogfood-crm-zapis-d640f6 → template=blank/gen_mode=freeform; фикс — на first-build без discovery переиспользовать `_infer_stack_from_text`, is_first_build-гейт = недеструктивно в отличие от P-H1; verify dogfood-crm-verify-90b839 → template=nextjs_entities, реальный entity-CRM brief (clients/bookings/notes + auth-кабинет), задеплоено api/worker; 88 passed/1 xfail)
- BS-8 | 2026-06-16 | Сгенерённые entity-аппы гейтят защищённую `(app)`-группу ТОЛЬКО КЛИЕНТСКИ: writer пишет `(app)/layout.tsx` как `"use client"` с `auth.me()` в `useEffect`→`router.push("/signin")`. Shell рендерится+гидрируется ДО редиректа; без JS (curl/бот) гейт не срабатывает → анонимный GET /dashboard и /dashboard/clients = **HTTP 200 + полный app-shell**. Данные НЕ текут (API 401 + owner-scope), но «приватный кабинет» визуально сломан + `safeCollection` глотает 401 в `[]` → истёкшая сессия видит пустой апп («Пока пусто»), а не релогин. Корень — промпт: SYSTEM_PROMPT.md L78 делает клиентский `auth.me()`-гейт ОСНОВНЫМ, серверный `requireUser()` (session.ts:54, делает server-redirect) — лишь опциональным «you may» (L79); middleware.ts нет | nextjs-entities/SYSTEM_PROMPT.md:78,118 + (gen)(app)/layout.tsx + sdk/index.ts:112 | **CONFIRMED** (run #6, live-репро dogfood-crm-live-b4c0c7: anon 200+shell, anon API 401, full транскрипт) → PROPOSAL P-AUTHGATE, фикс security-sensitive+кросс-зонно (edge-safe middleware ИЛИ mandatory server-gate в промпте + base-rebuild + regen-verify) → НЕ шипить вслепую; acceptance-lock test_entity_auth_gate.py (2 green evidence + 1 xfail)
- BS-7 | 2026-06-16 | КОНСЬЮМЕР-БУКИНГ → /signin-стена: лендинг с «запись на X / бронирование Y» (без «без регистрации») форс-эскалируется в auth-gated nextjs_entities; ВСЕ конверсионные CTA («Записаться онлайн», «Записаться») ведут на /signin → клиент обязан ЗАВЕСТИ АККАУНТ, чтобы записаться на замену масла. `_BACKEND_SIGNALS` трактует lead-capture-слова («запись на», «бронирован») как proof нужды в аккаунтах/CRUD (discovery.py:90); `_infer_stack_from_text` всегда возвращает nextjs_entities (discovery.py:130), а негативный safety-net `_explicit_no_backend` спасает только дословное «без регистрации». Явный выбор «Лендинг» в quiz НЕ имеет веса в stack-решении. Новый триггер класса BS-3, который BS-3-downgrade не ловит | discovery.py:90,130,867-885 | **CONFIRMED** (run #5, live prod repro dogfood-autoservice-turbofix-342143: «Лендинг»+автосервис+«запись на ремонт онлайн» → nextjs_entities, page.tsx 5× href="/signin", screenshot в _routine/runs/; live `_infer_stack_from_text` «запись на ремонт»/«запись на стрижку»/«бронирование столика» → все nextjs_entities, «лендинг кофейни» → None) → PROPOSAL P-BS7 ниже; фикс двунаправленно-рискованный (сузить «запись на»/«бронирован» → недо-эскалация настоящих букинг-аппов) + архитектурный пробел (нет пути «static-лендинг + lead-form без клиентского auth») → НЕ шипить вслепую; acceptance-lock в test_stack_routing.py (3 xfail + 3 evidence)
- BS-9 | 2026-06-16 | **МУЛЬТИ-РОЛЕВОЙ / RBAC ПРОБЕЛ** — entity-рантайм НЕ имеет модели ролей: `AccessPolicy = owner\|public\|admin` (registry.ts:46), `normalize()` молча коэрсит любой иной `access` → `owner` (registry.ts:107-108), `roleForNewUser()` штампует только `admin\|user` (auth.ts:149), `authorize()` сравнивает `user.role` лишь с `"admin"` (engine.ts:55-67). AD-бриф для школы спроектировал 3 роли с compound-доступом (Student: teacher write/parent read; JournalEntry: teacher write/student+parent **read-own**), но writer ВЫНУЖДЕН схлопнуть всё в 3 ведра → Student/Teacher/Parent/Lesson/Homework/Announcement=`public`, JournalEntry=`owner`. Два live-фейла: (1) **УТЕЧКА ПЕРСДАННЫХ** — anon `GET /api/entities/Parent` → 200 + телефоны родителей в открытый интернет («+7 (993) 584-93-56», engine.ts:60-61 public-read открыт всем); (2) **ЦЕНТРАЛЬНАЯ ФИЧА МЕРТВА** — JournalEntry owner-scoped → родитель/ученик (роль "user") читает только own-created → ПУСТОЙ журнал, оценки ребёнка не видны никогда. Класс шире школы: clinic doctor/patient, marketplace buyer/seller — любой мульти-актор с разными вью структурно невозможен | registry.ts:46,107 + auth.ts:149 + engine.ts:55-67 + nextjs-entities/SYSTEM_PROMPT.md | **CONFIRMED** (run #7, live-репро dogfood-school-zhurnal-da6a5c: anon Parent-leak 200+phones + JournalEntry 401/owner + writer-access-flattening + screenshot; BS-4 эскалация снова сработала ✓) → PROPOSAL P-RBAC ниже, фикс = реальные per-app роли + role-assignment UX + relationship-scoped read-own (engine+registry+auth+apps/web+writer-промпт, security-sensitive) → НЕ шипить вслепую; acceptance-lock test_entity_rbac_gap.py (4 green + 1 xfail)
- BS-10 (low-sev) | 2026-06-16 | НЕТ детерминированного static→spa пути: `spa` выбирает ТОЛЬКО LLM-discovery. Детерминированный `_infer_stack_from_text` (discovery.py:120) возвращает лишь `nextjs_entities`|None, дефолт `_DEFAULT_STACK="static"` (discovery.py:55). Следствия: (а) на gateway-ReadTimeout запрос-инструмент падает в static (probe: 1/6 «калькулятор ипотеки» → ReadTimeout → stack=static); (б) BS-4-путь (skip_clarify/quiz «просто сгенерируй») переиспользует `_infer_stack_from_text` → инструмент без backend-сигналов собирается freeform-СТАТИКОЙ, в spa никогда не эскалирует (live: dogfood-mortgage-calc-tool-db81b7 skip_clarify=true → template=blank/gen_mode=freeform). НИЗКАЯ severity: для self-contained калькулятора static+inline-JS даёт ПОЛНОСТЬЮ рабочий инструмент (run #8 verified: live-слайдеры пересчитывают платёж 83 311₽→347 430₽, SVG-график 7 эл., lead-форма). Дыра проявится на тяжёлых tool-аппах (нужен React-state/npm-либы: редактор слоёв, карта с кластеризацией) — freeform-static недотянет | discovery.py:55,120 + messages.py:706 | CONFIRMED low-sev (run #8) — НЕ фиксить: LLM на discovery-пути уже корректно берёт spa (probe 5/6); форс static→spa эвристикой = риск фолс-фолса + лишний build-cost, выигрыша нет для текущих запросов
- BS-11 | 2026-06-16 | **REMOVAL-EDIT ОРФАНИТ IN-PAGE ЯКОРЯ** — surgical-правка «убери секцию X» удаляет `id="X"`, но nav-ссылка `href="#X"` остаётся → мёртвый якорь. Dead-link-пасс СОЗНАТЕЛЬНО пропущен на surgical-правках (messages.py:2586 `not surgical` — чтобы не трогать ПРЕД-существующие мёртвые ссылки, не упомянутые юзером), поэтому якорь, осиротевший САМОЙ запрошенной правкой, отгружается дохлым. Вторично: `repair_dead_links_inline` чинит лишь плейсхолдеры (`#`/`javascript:void(0)`), НЕ anchor-to-missing-id (link_validator.py:112), а LLM-repair гейтнут policy `auto_regenerate_enabled=False`. Также наблюдение: removal почти всегда промахивается `<edit>` SEARCH/REPLACE на cheap deepseek-chat → дорогой whole-file rewrite-fallback (медленно, но guarded ratio≥0.6 сохраняет контент — НЕ silent no-op). ПОЗИТИВ прогона: additive+modify правки (новая секция «Цены» + телефон) приземлились идеально, все 6 секций сохранены — owner-жалоба «генератор ничего не делал» НЕ воспроизвелась на легитимных контент-правках (изолирована к app-ification = H1) | link_validator.py:112 + messages.py:2586 | **FIXED 585e65c** (run #9, live-репро dogfood-bakery-edit-18ca73: «убери секцию отзывов» → `id="reviews"` удалён, nav `href="#reviews"` повис ×3; фикс `repair_orphaned_anchors_inline(old,new)` delta-scoped — чинит ТОЛЬКО id, удалённые ЭТОЙ правкой, детерминированно/без LLM/без regen, редирект на выживший CTA-таргет; 3 теста-репро + 5 зелёных, задеплоено api/worker, health 200 извне; live-verify на DEPLOYED-бинаре: 3 dead→0, `#reviews`→`#contacts`; live E2E «удали секцию Три дня…» → лог `orphaned_anchors repaired`, snapshot 1a66807f: `#process` починен, пред-существующий `#reviews` корректно НЕ тронут = delta-scope доказан)
- BS-12 | 2026-06-16 | **ENTITY-ФАЙЛ ИМЯ≠NAME → ВЕСЬ АПП DOA** — рантайм-реестр резолвит сущность СТРОГО по `entities/<Name>.json` (registry.ts `entityPath`→`${name}.json`, `listEntities` ключует по имени файла, registry.ts:57-103), и ВСЕ потребители ссылаются по `name` (SDK `entities.Client`, reference-поля, `<CrudResource entity="Client">`). Контракт — «filename === Name» (SYSTEM_PROMPT.md + стартовый `Task.json`). Но AD-бриф порой перечисляет файлы в lowercase/plural (`clients.json`) и writer копирует имя ДОСЛОВНО (art_director_writer.py:565), → `entities/clients.json` декларирует `{"name":"Client"}`, а рантайм стат'ит `entities/Client.json` → ENOENT → `loadEntity`=null → **404 «unknown entity 'Client'» на КАЖДЫЙ read/write** → апп полностью мёртв (пустые списки везде, запись не создаётся), хотя бриф/экраны/референсы внутренне консистентны | apps/api routers/messages.py + nextjs-entities/src/lib/entities/registry.ts:57-103 | **FIXED 989a7a8** (run #10, live-репро dogfood-crm-edit-563092: console `ApiError: unknown entity 'Client'/'Booking'`, все коллекции пусты, create молча падал; фикс `_normalize_entity_filenames(files)` в container-guard-цепочке рядом с `_preserve_auth_schema` ПЕРЕД commit+hot_reload — детерм/идемпотентно/fail-soft: переименовывает `entities/<X>.json` под внутренний `name` только если JSON парсится, name валидный идентификатор≠stem и нет уже корректного сиблинга; 5 тестов-репро + 43 passed/6 xfail без регресса, задеплоено api/worker; live-verify DEPLOYED: guard на broken-input → `clients->Client, notes->Note`; новый билд dogfood-crm-verify-2a9337 → `Client.json/Note.json/Booking.json`, `GET /api/entities/Client` 404→**401** (entity резолвится, апп жив), dashboard рендерит KPI+nav Заметки)
- BS-13 (architecture) | 2026-06-16 | **`CrudResource` НЕ ИМЕЕТ СЛОТА ДЛЯ СВЯЗАННЫХ СУЩНОСТЕЙ** — detail-панель managed-компонента (`detailFor`→`RecordDetail`, crud-resource.tsx:312-329) рендерит ТОЛЬКО собственные колонки/поля записи; в `CrudResourceProps` (crud-resource.tsx:46-108) нет `renderDetail`/`detailExtra`/`children`. AD-бриф для CRM детально спроектировал master-detail карточку клиента с разделом «Заметки по клиенту» (лента Note, фильтр по clientId, add+delete) + «История визитов» (Booking по clientId), но writer, опираясь на `CrudResource` («fast path for any list screen»), не может выразить per-record child-секции → либо ДРОПАЕТ их (билд #1 dogfood-crm-edit: Note-сущность вообще без UI/роута/nav — осиротевшая), либо деградирует до отдельного ГЛОБАЛЬНОГО списка (билд #2 dogfood-crm-verify: standalone роут «Заметки», но не «заметки ЭТОГО клиента в его карточке»). Класс шире CRM: любой master-detail с дочерними записями (пациент+визиты, заказ+позиции, проект+задачи) | nextjs-entities/src/components/omnia/crud-resource.tsx:46-108,312-329 + SYSTEM_PROMPT.md | **CONFIRMED** (run #10, source-proof + 2 живых билда) → PROPOSAL P-RELATED (additive `detailExtra?:(row)=>ReactNode` слот в CrudResource + writer-директива хэндбилдить detail-страницу с child-CrudResource scoped по ref-полю; kit+prompt+base-rebuild+regen-verify → не вслепую)
- BS-14 | 2026-06-16 | **МУЛЬТИ-СТАТЕЙНЫЙ БЛОГ → НЕКЛИКАБЕЛЬНЫЕ КАРТОЧКИ-ТУПИКИ** — запрос «блог … отдельная страница для каждой статьи (полный текст, фото, автор, дата)» собирается freeform-СТАТИКОЙ (template=blank/gen_mode=freeform; discovery не эскалирует и не существует blog/content-collection-архетипа — `STATIC_TEMPLATES={blank,landing,portfolio,blog}` это лишь стартовые index.html, не мультистраничник, prompt_builder.py:2297). Writer написал ПОЛНЫЙ текст ровно для ОДНОЙ featured-статьи (in-page секция `#article-kyoto`, достижима из hero-CTA), а остальные 4 статьи (свои title/анонс/автор/время чтения) выдал как `<article class="article-card hover-lift">` БЕЗ `<a>`/href/onclick → 4 из 5 запрошенных статей нечитаемы (Playwright: все 4 `hasLink=False, cursor=auto`). Категорийный фильтр (JS) и форма подписки РАБОТАЮТ — провал именно в достижимости контента. Два слоя: (1) writer НАРУШИЛ собственный промпт — freeform-правила уже ЗАПРЕЩАЮТ «кнопка-обманка без обработчика»/«пункт меню на несуществующий id» (prompt_builder.py:499-500) и велят делать мультистраничные пункты in-page-якорями (prompt_builder.py:496-497); (2) детерминированный safety-net СЛЕП — `find_dead_links` смотрит только `<a href>` на placeholder/missing-anchor (link_validator.py:24-46), а интерактивно-СТИЛИЗОВАННАЯ карточка вообще без `<a>` ему невидима → acceptance-vision поймал 2 футерных `href="#"`, но не 4 actionless-карточки. Класс шире блога: любой feed-of-items, где карточки должны открывать детали (каталог→товар, портфолио→кейс, новости→новость) | prompt_builder.py:496-500,2297 + link_validator.py:24-46 | **CONFIRMED** (run #11, live-репро dogfood-travel-blog-029341: 4 карточки hasLink=False + source + acceptance struct=False; картинки 200/490KB у реального юзера — белая полоса в моём рендере = NAT-hairpin контейнера к public MinIO, НЕ баг продукта) → PROPOSAL P-BLOG ниже, фикс = blog/content-collection-архетип ИЛИ writer-директива «каждая статья достижима» + base-rebuild + regen-verify (детерм. авто-репэйр FP-опасен: 4 карточки некуда вести при 1 статье) → НЕ шипить вслепую; acceptance-lock test_blog_feed_reachability.py (1 green evidence + 1 xfail)
- BS-15 | 2026-06-16 | **LEAD-ФОРМА = КЛИЕНТСКИЙ ТЕАТР, ЗАЯВКА В НИКУДА** — freeform-static лендинги генерят контакт/lead/подписка-формы, которые СОБИРАЮТ PII (имя/телефон/email), показывают success («Спасибо, перезвоним за 15 минут»), и МОЛЧА ВЫБРАСЫВАЮТ данные: нет `action`, нет `mailto:`, нет `fetch` → лид не доходит НИКОМУ. Хуже мёртвой кнопки — посетитель получает ложное подтверждение, владелец получает НОЛЬ заявок, пайплайн этого не замечает. Два слоя: (1) freeform-промпт (prompt_builder.py:491-493,606-607) велит форме `e.preventDefault()`+валидация+ВИДИМЫЙ success, `action`=«mailto: либо formspree как ПЛЕЙСХОЛДЕР» — даже при дословном следовании formspree-плейсхолдер не сконфигурен → лид теряется; на практике writer вообще дропает action. (2) НЕТ lead-приёмника: `routers/public.py` GET-only (get_index/get_file/remix), ни одного POST-эндпойнта, куда статик-лендинг мог бы слать заявку; escape-hatch промпта (prompt_builder.py:607-610) — «лучший static + ОДНОЙ строкой предложи full-stack», но сайт-визитка ЭТО и есть канонический кейс → его #1 конверсия молча мертва by design. Детерминированный нет-сейф слеп: `find_dead_links` смотрит только `<a href>` (link_validator.py:24-46), форму без `<a>` не видит (как actionless-карточки BS-14). Класс шире фотостудии: любой сайт-визитка/лендинг с «оставить заявку/обратная связь/подписка» | prompt_builder.py:491-493,606-610 + routers/public.py (GET-only) + link_validator.py:24-46 | **CONFIRMED** (run #12: live-gen заблокирован WALLET-EMPTY → пивот на код+2 РЕАЛЬНЫХ артефакта из repo_svc.read_files: dogfood-mortgage-calc-tool-db81b7 lead-форма name+phone → handler читает name/phone и hide-form+show-success, action=0/mailto=0/fetch=0 = ВЫБРОС; dogfood-travel-blog-029341 `onsubmit="preventDefault(); this.innerHTML='Спасибо!'"` = чистый театр) → PROPOSAL P-LEAD ниже, фикс архитектурный+кросс-зонный (public lead-receiver + storage + owner-inbox apps/web + writer-POST-директива) → НЕ шипить вслепую; acceptance-lock test_lead_capture_sink.py (2 green evidence + 1 xfail)
- BS-16 | 2026-06-16 | **REFERENCE-ПОЛЕ НЕВИДИМО В СПИСКЕ — "чья это запись?" не ответить** — `<CrudResource>` авто-РАСШИРЯЕТ reference-поля в list-params (`expand = fields.filter(f=>f.kind==="reference").map(f=>f.name)`, crud-resource.tsx:171-184 → `row._expanded.<ref>` грузится на каждый list), но НИКОГДА авто-не-РЕНДЕРИТ для них колонку: колонки берутся из props дословно (только `sortable`-дефолт, crud-resource.tsx:168), а SYSTEM_PROMPT не требует показывать relation колонкой. Когда writer забывает ref-колонку — расширенные данные грузятся и ВЫБРАСЫВАЮТСЯ, relation невидим в главном (list) вью. Чистая writer-вариативность под ОДНИМ генератором: dent-crm appointments рендерит `r._expanded?.patientId?.name`+doctorId ✓, а crm-verify bookings (cols=дата/время/услуга/цена/статус) И notes («Текстовые примечания к клиентам», cols=текст/дата) ОБА дропнули clientId-колонку → в списке записей/заметок не видно КЛИЕНТА. ЗДОРОВО (опровергнутая гипотеза): сам picker формы корректен — writer мапит schema-reference→`kind:"reference"`+`refEntity`, EntityForm грузит `entities[refEntity].list({limit:200})`→реальный `<Select>` (не UUID-текстбокс) в обоих аппах. Класс шире CRM: любой feed записей с родителем (заказ→клиент, тикет→проект, визит→пациент, заметка→клиент) | crud-resource.tsx:168,171-184 + nextjs-entities/SYSTEM_PROMPT.md (нет директивы) | **CONFIRMED** (run #13, wallet-empty→артефакт-анализ repo_svc.read_files 2 реальных песочниц crm-verify-2a9337 + dent-crm-bdc3f3 + template-source) → PROPOSAL P-REFCOL ниже, фикс = детерм. авто-колонка из reference-полей в CrudResource (template→base-rebuild+regen-verify, сейчас wallet-blocked) ИЛИ prompt-директива → НЕ шипить вслепую; acceptance-lock test_reference_column_surfacing.py (4 green + 1 xfail)
- BS-17 | 2026-06-16 | **РЕДАКТИРОВАНИЕ НЕ МОЖЕТ ОЧИСТИТЬ ОПЦИОНАЛЬНОЕ ПОЛЕ — молчаливый no-op с ложным success** — в entity-аппе можно ИЗМЕНИТЬ поле, но НЕЛЬЗЯ ОЧИСТИТЬ опциональное: стираешь телефон/заметку/срок, жмёшь «Сохранить» → тост «Изменения сохранены» → значение возвращается. Три поверхности: (1) `EntityForm.validate()` пустое non-required поле → `continue` ДО `out[f.name]=…` → стёртое поле ВООБЩЕ не уходит в payload (entity-form.tsx:158-164); (2) `updateSchema` каждое поле `zodForField(f).optional()` — НЕ `.nullable()` → даже если послать `null` чтобы очистить, zod отвергнет и 400-ит весь апдейт → очистка НЕВЫРАЗИМА в контракте (registry.ts:176-182); (3) `updateRecord` shallow-merge `{...existing, ...parsed.data}` → отсутствующее в payload поле сохраняет старое значение (engine.ts:286). omit-on-empty + merge-keeps-absent = очистка исчезает; наивный form-only фикс (послать null) отвергнется схемой. Класс шире барбершопа: любой entity-апп, любое опциональное поле, операция un-set (убрать телефон, обнулить заметку, снять срок/статус). Семья «ложного подтверждения» (BS-11 осиротевшие якоря, BS-15 lead-форма-театр) | entity-form.tsx:158-164 + registry.ts:176-182 + engine.ts:286 | **CONFIRMED** (run #14, LIVE RUNTIME на DEPLOYED-бинаре omnia-dev-dogfood-barber-crm-25cc9e starter Task: CREATE notes+due → PUT {title,priority} (точный payload формы при очистке, omit пустых) → 200 + updated_at сдвинулся (апдейт ВЫПОЛНИЛСЯ) → GET: notes/due ВЫЖИЛИ = CLEAR_BUG; CONTROL смена на новые non-empty значения применяется ✓ — ломается ТОЛЬКО un-set; live-gen WALLET-EMPTY обойдён через прямой CRUD к живому контейнеру, генерация не нужна — баг в базовом engine, одинаков во всех аппах) → PROPOSAL P-CLEAR ниже, фикс мульти-поверхностный (nullable updateSchema + form-emit-clear в edit + number/date/reference null-edge + base-rebuild + regen-verify), меняет write-validation семантику всех аппов → НЕ шипить вслепую; acceptance-lock test_entity_field_clear_gap.py (3 green + 1 xfail)
- BS-18 | 2026-06-16 | **СПИСОК ENTITY-АППА МОЛЧА ОБРЕЗАН НА 50 — нет server-pagination, записи 51+ недостижимы через UI** — managed `<CrudResource>` грузит коллекцию ОДНИМ вызовом `useEntity`→`entities[name].list(params)` БЕЗ `limit`/`page`, и движок отдаёт `Number(limit) || DEFAULT_LIMIT`=50 (engine.ts:38-39,187-189). DataTable пагинирует ТОЛЬКО эти ≤50 строк В ПАМЯТИ через `pageSize` (crud-resource.tsx:549,555) → выглядит полным («5 страниц по 10»), хотя ~10% данных не отдано. `useEntity` грузит один раз и не имеет page-state/fetch-next (use-entity.ts: «client-side paging happen in <DataTable>, not here»). Движок ПАГИНИРОВАТЬ умеет (`page`/`offset`, engine.ts:191-192) — UI просто не просит. SYSTEM_PROMPT перечисляет `list({…limit,page})` (L71), но НЕ велит wire-ить пагинацию, а CrudResource — документированный «fast path for any list screen» → 50-кап = дефолт почти всех генеримых списков. При дефолтном sort created_at DESC исчезают САМЫЕ СТАРЫЕ записи. Класс шире CRM: любой растущий список — клиенты, заказы, товары, брони, лиды, ученики; семья BS-14 (недостижимые карточки) + BS-16 (невидимая ref-колонка) | engine.ts:38-39,187-189 + use-entity.ts + crud-resource.tsx:177-184,549,555 + nextjs-entities/SYSTEM_PROMPT.md:71 | **CONFIRMED** (run #15, LIVE-RUNTIME на DEPLOYED base-образе: throwaway omnia-dev-dogfood-listcap-probe-cd7c7c, starter Task, auth Auth.js; CREATE 55 Task #1..#55 → GET без limit = 50 строк #6..#55 (старейшие #1-#5 пропали), GET `?limit=200`=55, GET `?page=2`=5 → движок умеет, UI не просит; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14) → PROPOSAL P-PAGINATE ниже, фикс TEMPLATE-уровня (base-rebuild): band-aid `limit:200` лишь двигает обрыв + грузит 200×ref-expand на каждый экран; правильный = реальная server-pagination (engine total + useEntity page-state + DataTable refetch) мульти-поверхностно → НЕ шипить вслепую; acceptance-lock test_list_pagination_cap.py (4 green + 1 xfail)
- BS-19 | 2026-06-16 | **ENTITY-ДВИЖОК БЕЗ РЕФЕРЕНЦИАЛЬНОЙ ЦЕЛОСТНОСТИ — удаление родителя молча сиротит детей** — три слоя нулевой integrity: (1) `createRecord` (engine.ts:217-226) только zod-парсит body + `insert` — НЕ грузит target-сущность чтобы проверить, что referenced-строка существует (reference = просто `z.string()`) → запись с несуществующим ref принимается; (2) `deleteRecord` (engine.ts:295-315) удаляет по id и возвращает `{deleted:true}` — НИКОГДА не сканит другие сущности на детей, ссылающихся сюда, нет restrict/cascade/warn/affected-count; (3) `expandRecords` (engine.ts:143-148) `_expanded[field]=map.get(refId) ?? null` — мёртвый родитель НЕОТЛИЧИМ от «связь не задана» → осиротевший ребёнок рендерит relation-колонку как «—» без сигнала, что родителя снесли. Семья BS-13 (нет слота для child-записей) + BS-16 (ref-колонка невидима). Класс шире CRM: заказ→клиент, бронь→пациент, задача→проект, комментарий→пост, счёт→клиент — удаление родителя тихо корраптит каждого ребёнка | engine.ts:143-148,217-226,295-315 | **CONFIRMED** (run #16, LIVE-RUNTIME на DEPLOYED base-образе: throwaway omnia-dev-dogfood-refdel-probe-41de56, template nextjs_entities, 2 сущности через docker cp — Client{name,phone}+Note{text,clientId:ref→Client}, auth Auth.js; CREATE Note с bogus clientId «00000000-…»→**201** (write не валидирует ref), DELETE Client b966bf5a→**200** (нет restrict), GET Note?expand=clientId после удаления → обе ноты `_expanded.clientId=null` при живом `clientId` на мёртвый id = ОРФАН; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14/#15) → PROPOSAL P-REFINTEGRITY ниже, фикс = семантическое решение политики (RESTRICT 409 / CASCADE / WARN-affected-count), мульти-поверхностный engine+apps/web, base-rebuild+regen-verify, data-integrity-чувствительный → НЕ шипить вслепую; acceptance-lock test_entity_referential_integrity.py (3 green + 1 xfail)
- BS-20 | 2026-06-16 | **СГЕНЕРЁННЫЙ ENTITY-АПП НЕ МОЖЕТ НАЙТИ ЗАПИСЬ ПРИ МАСШТАБЕ — поиск ложно сообщает «Ничего не найдено»** — два слоя компаундятся: (1) движок НЕ имеет server-side текст-поиска: `listRecords` фильтр = `data->>field = value` ЧИСТО точное равенство (engine.ts:176), нет ILIKE/substring/`%`/case-insensitive нигде → запрос поисковика («Zebra») невозможно протолкнуть на сервер, матчит только ПОЛНОЕ значение в ТОЧНОМ регистре; (2) managed-поиск ЧИСТО клиентский: `<DataTable>` фильтрует `String(v).toLowerCase().includes(q)` по `rows`-пропу (data-table.tsx:299), а rows = ≤50 загруженных одним `useEntity`-вызовом без limit (корень BS-18, use-entity.ts «search/sort/paging happen in <DataTable>, not here») → поиск видит только новейшие 50, старше — невидимо И нет серверного эндпойнта чтобы дотянуться. Отлично от BS-18 (P-PAGINATE): даже починка 50-капа НЕ чинит поиск — некуда проталкивать text-query. Хуже тихого обрыва BS-18: видимый поисковик АКТИВНО утверждает, что записи нет. Семья BS-14/BS-16/BS-18 — видимый контрол подразумевает достижимость всего набора. Класс шире CRM: любой растущий список (клиенты/заказы/товары/брони/лиды/ученики) | engine.ts:176 + data-table.tsx:299 + use-entity.ts | **CONFIRMED** (run #17, LIVE-RUNTIME на DEPLOYED base-образе: throwaway omnia-dev-dogfood-search-probe-972063, starter Task, 55 записей, owner-auth Auth.js; OLDEST «Zebra-UNIQUE-OMEGA-001» с заметкой «найди-меня-секрет»: GET без params=50 строк MARK ОТСУТСТВУЕТ, ?title=Zebra-UNIQUE-OMEGA-001(точное)=1, ?title=Zebra(префикс)=0, ?title=UNIQUE(инфикс)=0, ?title=zebra-…(неверный регистр)=0, ?notes=найди-меня-секрет(точное)=1, ?notes=секрет(substring)=0, CONTROL ?priority=high(точное)=1 / ?priority=hi(префикс)=0, ?limit=200=55 MARK ПРИСУТСТВУЕТ → запись в БД есть, но UI-дефолт её не грузит, клиент-поиск не видит, серверного text-search нет; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14/#15/#16) → PROPOSAL P-SEARCH ниже, фикс = новый server-side text-search примитив (engine ILIKE/`q`-параметр по text-полям) + проталкивание поиск-запроса из useEntity/CrudResource на сервер; TEMPLATE+engine, base-rebuild+regen-verify, меняет list-data-flow контракт всех аппов → не вслепую; acceptance-lock test_entity_search_findability.py (4 green + 1 xfail)
- BS-21 | 2026-06-16 | **СОРТИРОВКА ВЫРОСШЕГО СПИСКА ВОЗВРАЩАЕТ ЭКСТРЕМУМ ЗАГРУЖЕННЫХ ~50, А НЕ ВСЕГО НАБОРА — "сортируй по цене/сроку" даёт уверенно НЕВЕРНЫЙ топ** — движок ИМЕЕТ корректную типизированную server-side сортировку: `listRecords` читает `sort`/`order` (engine.ts:180-181), `fieldExpr` кастит jsonb-значение `::numeric` для number и `::timestamptz` для date (engine.ts:74-75) → весь набор сортируется правильно (числа численно, даты хронологически). НО managed-UI её НЕ ИСПОЛЬЗУЕТ: CrudResource делает каждую колонку сортируемой по умолчанию (crud-resource.tsx:168), клик по заголовку → `toggleSort` ставит КЛИЕНТСКИЙ стейт (data-table.tsx:363), таблица пересортировывает загруженный `rows`-проп В ПАМЯТИ (data-table.tsx:305-307); rows = ≤50 от одного `useEntity.list()` без `sort`/`order`, хук явно делегирует сортировку таблице (use-entity.ts:21 "search/sort/paging happen in <DataTable>, not here"). Отлично от BS-18 (записи 51+ недостижимы) и BS-20 (НЕТ server text-search примитива): здесь примитив СУЩЕСТВУЕТ и работает даже при 50-капе — дыра лишь в непроброшенном read-path. Хуже тихого обрыва BS-18 и ложного «не найдено» BS-20: сортировка возвращает НЕВЕРНУЮ строку, которая ВЫГЛЯДИТ авторитетной, без сигнала. Класс шире CRM: любой растущий список с упорядоченной колонкой (заказы по сумме, счета по сроку, товары по цене, лиды по дате, ученики по оценке) | engine.ts:74-75,180-181 + data-table.tsx:305-307,363 + use-entity.ts:21 + crud-resource.tsx:168 | **CONFIRMED** (run #18, LIVE-RUNTIME на DEPLOYED base-образе: throwaway omnia-dev-dogfood-sortcap-probe-1bb521 port 3302, starter Task + built-in `due` date-поле, owner-auth Auth.js; 55 Task, OLDEST «AAA-EARLIEST-DUE-MARK» due 2018-01-01 (глобальный минимум), 54 филлера due 2026-*: GET без params=50 строк MARK ОТСУТСТВУЕТ, min-due в окне=2026-01-15 (лучшее, что покажет клиент-сорт), ?sort=due&order=asc&limit=200 → TOP=MARK 2018-01-01, ?sort=due&order=asc (default 50) → TOP=MARK 2018-01-01 MARK ЕСТЬ (сервер сортирует ВЕСЬ набор ПОТОМ берёт топ-50 → проброс на сервер чинит даже при капе), ?limit=200=55 MARK есть; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14/#15/#16/#17) → PROPOSAL P-SORT ниже (пробросить sort-стейт DataTable в server-параметры useEntity, переиспользуя готовую engine-сортировку; малый, но TEMPLATE→base-rebuild+regen-verify, меняет list-data-flow всех аппов, тесно связан с BS-18/BS-20 — логично делать с P-PAGINATE; wallet-blocked → не вслепую) + acceptance-lock test_entity_sort_windowing.py (4 green + 1 xfail)
- BS-22 | 2026-06-16 | **ОДНА JS-ВАЛИДНАЯ-НО-PG-НЕВАЛИДНАЯ ДАТА 500-ит ВЕСЬ СПИСОК ПРИ СОРТИРОВКЕ — тотальный denial-of-view от обычного небрежного ввода даты** — расхождение write↔sort: (1) `date`-поле валидируется ТОЛЬКО JS `Date.parse` (registry.ts:139 `z.string().refine(s=>!Number.isNaN(Date.parse(s)))`) — лениво, пропускает «2025», «2025-06», «June 2025», «Jun 2025», «2025/06», «2025-02-30»; сырая строка кладётся в JSONB без нормализации; (2) сортировка кастит сохранённый текст `(data->>field)::timestamptz` (engine.ts:75) — парсер Postgres СТРОЖЕ и ОТВЕРГАЕТ каждое из этих значений; (3) сырая Postgres-ошибка не `EngineError` → `run()` отдаёт generic 500 «internal error» (http.ts:13-19); (4) CrudResource делает каждую колонку сортируемой (BS-21, crud-resource.tsx:168) → дата-колонка в одном клике от 500. Фильтр (`data->>key=value`, без каста) БЕЗОПАСЕН — запись фильтруется, падает только sort. Хуже BS-21 (неверный-но-присутствующий топ) и BS-18 (тихо скрытые строки): ВЕСЬ список крашится opaque-ошибкой для ВСЕХ юзеров от одной набираемой-руками даты (год / год-месяц / «June 2025»). Класс шире CRM: любой растущий список с дата-колонкой (счета по сроку, заказы по дате, брони, лиды) | registry.ts:139 + engine.ts:75 + http.ts:13-19 | **FIXED ad0a71b** (run #19, LIVE-RUNTIME на DEPLOYED base-образе: throwaway omnia-dev-dogfood-datecast-probe-2fe2f6 port 3399, starter Task built-in `due`, owner-auth Auth.js; pre-fix: POST due="June 2025"→201, ?sort=due&order=asc/desc→**500** «internal error», ?due=June 2025 фильтр→200 (находит), CONTROL delete-poison→?sort=due→200 (доказывает именно это значение), лог `invalid input syntax for type timestamp with time zone: "June 2025" at listRecords engine.ts:194`; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14-18. ФИКС crash-proof: `safe_to_timestamptz(text)` STABLE plpgsql→NULL на ошибке каста (init-db.mjs, только exception-catch truly crash-proof — regex не ловит range-ошибки «2025-02-30») + engine.ts fieldExpr дата-сорт ЧЕРЕЗ обёртку вместо сырого `::timestamptz`; детерм/fail-soft/non-regressive: `safe_to_timestamptz(valid)===valid::timestamptz`, ZERO read/write-контракт-change (в отличие от P-PAGINATE/SEARCH/SORT); deploy = base-rebuild на проде; VERIFY на ПЕРЕСОБРАННОМ base (fresh omnia-dev-dogfood-datecast-verify-95e13b): POST «June 2025»→201, ?sort=due&order=asc→**200** (Beta 2026-01-15, Alpha 2026-03-01, poison→NULL sorts last), desc→200, фильтр→200, лог 0×«unexpected error»; 5 green + 1 xfail test_entity_date_cast_crash.py). СЛЕДУЮЩИЙ ШАГ (НЕ шипнут, P-DATECAST): write-валидатор остаётся ленивым (ужесточить = сменить write-семантику всех аппов, ср. BS-17 P-CLEAR)
- BS-23 (low-sev, robustness; integration HEALTHY) | 2026-06-16 | **МЕДИА/UPLOAD-ИНТЕГРАЦИЯ ЗДОРОВА, но конфиг хрупок** — провижер `_integration_env()` читает ТОЛЬКО `OMNIA_MINIO_*`-префиксные env БЕЗ фолбэка на плоские `MINIO_*`, которыми пользуется остальной стек (api/worker). Работает СЕЙЧАС только потому, что в `/opt/omnia/apps/orchestrator/.env` руками заданы `OMNIA_MINIO_SECRET_KEY` + `OMNIA_MINIO_PUBLIC_URL`; остальное падает в КОРРЕКТНЫЕ дефолты (endpoint `omnia-prod-minio:9000` резолвится из `omnia-runtime_default`; access `omnia` = реальный). VERIFY (опровергнута severe-гипотеза «upload мёртв у всех аппов»): live-e2e faithful server.ts-репликой (node+minio pkg на omnia-runtime_default) — STEP1 дефолты-без-секрета→`UPLOAD_FAIL AccessDenied`, STEP2 реально-инжектируемый env→`UPLOAD_OK` + внешний curl public URL → **HTTP 200** точные байты; `_integration_env()` спред в env контейнера (provisioner.py:193); kit рендерит картинки плоским `<img>` везде (gallery-grid/storefront/testimonial/record-detail/master-detail + entity-form, comment «no next/image remotePatterns needed») — без remotePatterns-гочи; sendEmail = stub `{sent:false,stubbed:true}` ВСЕГДА (server.ts:98-110), но writer-промпт честно это документирует (prompt_builder.py:1248-1249). ЛАТЕНТНЫЙ РИСК: если тот единственный `.env` дрейфнёт к плоской `MINIO_*`-конвенции стека / перегенерится / оркестратор мигрирует → upload МОЛЧА умрёт стек-широко (AccessDenied→500→«Не удалось загрузить файл») без сигнала, генератор мёртвый storage не детектит | provisioner.py:52-77,193 + integrations/server.ts:76-78,98-110 + nextjs-entities `<img>`-кит | **VERIFY HEALTHY + low-sev latent** (run #20) — НЕ фиксить: текущего видимого провала нет; дефенсив-фолбэк (`OMNIA_MINIO_*`→`MINIO_*` для access/secret/public_url, endpoint НЕ фолбэчить — `minio:9000` алиас стека не резолвится на omnia-runtime_default) мал и доказан-безопасен (== STEP2 values), но шип без видимого провала = спекулятивно (ср. BS-10 low-sev не фикшен)
- BS-24 | 2026-06-16 | **CSV-ЭКСПОРТ МОЛЧА ОБРЕЗАН ДО ЗАГРУЖЕННЫХ ≤50 — выгрузка «всех данных» теряет записи 51+ в доверенном файле** — managed `<CrudResource>` включает кнопку «Экспорт» ПО УМОЛЧАНИЮ (`exportable = true`, crud-resource.tsx:150) на КАЖДОМ list-экране, а `handleExport` сериализует `sorted`←`rows`-проп = уже загруженные в память ≤50 строк (data-table.tsx:373-388), НИКОГДА не перезапрашивая сервер; `rows` = `data.rows` от `useEntity` (один `list()` без `limit` → дефолт движка 50, корень BS-18). Владелец жмёт «Экспорт» для бэкапа/отчёта → молча получает первые 50 из N без сигнала. Хуже in-app капа BS-18: артефакт ВЫХОДИТ из приложения как доверенный авторитетный файл — недостача невидима именно когда юзер верит в полноту; коммент хэндлера «not just the visible page» усиливает ложную уверенность (спанит client-пейджер, но не серверный набор). Семья «выглядит полным, но нет» (BS-14 карточки / BS-16 ref-колонка / BS-18 кап / BS-20 поиск / BS-21 сортировка) — поверхность data-portability: обрезка УЕЗЖАЕТ из приложения в файле | crud-resource.tsx:150,556 + data-table.tsx:373-388 + use-entity.ts | **CONFIRMED** (run #21, LIVE-RUNTIME на DEPLOYED base-образе: throwaway omnia-dev-dogfood-export-probe (schema proj_dogfood_export_916), starter Task, owner-auth Auth.js; 55 записей → GET без params (то, что сериализует экспорт) = 50, ?limit=200 = 55, default-окно first=EXPORT-ROW-01 last=EXPORT-ROW-50 → строки 51-55 в БД есть, но в выгрузку не попадают; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14-20) → PROPOSAL P-EXPORT ниже, фикс = дотянуть полный набор перед сериализацией на уровне CrudResource ИЛИ server-export route (TEMPLATE, base-rebuild+regen-verify, связан с P-PAGINATE) → НЕ шипить вслепую; acceptance-lock test_entity_export_truncation.py (3 green + 1 xfail)
- BS-25 | 2026-06-16 | **ENTITY-ДВИЖОК БЕЗ ОПТИМИСТИЧЕСКОЙ БЛОКИРОВКИ — два редактора ОДНОЙ записи молча затирают друг друга (last-write-wins, без 409, без сигнала)** — `updateRecord` читает существующую строку, делает `merged = {...existing, ...parsed.data}` и пишет целиком; WHERE = только id+entity+createdBy, БЕЗ version/updated_at-предусловия (engine.ts:285-297) → ничто не замечает, что `existing` изменился с момента загрузки редактором. Нет version/etag/If-Match/expectedVersion НИГДЕ в entity-рантайме (единственный 409 в шаблоне — auth-register «email занят»). Усугубляет форма: `<EntityForm>` в edit-режиме сидит КАЖДОЕ поле из загруженной записи (entity-form.tsx:82-88) и `validate()` эмитит КАЖДОЕ непустое (entity-form.tsx:149-167) → каждое сохранение = ПОЛНЫЙ устаревший снапшот, а не патч поля → устаревшее значение поля РЕАЛЬНО шлётся и затирает чужую правку через wholesale-merge. Сценарий живого юзера: solo-владелец на телефоне+ноуте, две вкладки, или double-submit медленного сохранения. Семья «тихих data-integrity»: BS-17 (clear = no-op), BS-19 (delete сиротит), BS-22 (одна дата 500-ит sort). Класс шире CRM: любой растущий entity-апп с правкой из >1 места | engine.ts:285-297 + entity-form.tsx:82-88,149-167 + api/entities/[entity]/[id]/route.ts (PUT) | **CONFIRMED** (run #22, LIVE-RUNTIME на DEPLOYED base omnia-template-nextjs-entities:dev: throwaway omnia-dev-dogfood-lostupdate-probe, schema proj_dogfood_lostup_3880545, starter Task, owner-auth Auth.js; CREATE {priority:low,notes:orig}→200 done:false(дефолт применён); Editor A PUT priority→high→200, CONTROL GET=high (апдейт РАБОТАЕТ); Editor B PUT stale priority=low + notes=edited-by-B→**200 НЕ 409**; FINAL GET priority=**low** (A's high МОЛЧА ПОТЕРЯН) + notes=edited-by-B; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14-21; контейнер+schema удалены) → PROPOSAL P-LOSTUPDATE ниже, фикс = version/updated_at-токен + If-Match/expectedVersion-предусловие в updateRecord (409 на mismatch) + форма/SDK шлёт загруженную version + conflict-resolution UX в apps/web/CrudResource; write-контракт всех аппов, мульти-поверхностно, base-rebuild+regen-verify → НЕ шипить вслепую; acceptance-lock test_entity_concurrent_update.py (3 green + 1 xfail)
- BS-26 | 2026-06-17 | **ДАШБОРД-KPI СЧИТАЮТСЯ ПО ЗАГРУЖЕННЫМ ≤50 — ГЛАВНАЯ ЦИФРА НА ПЕРВОМ ЭКРАНЕ МОЛЧА ЗАМОРОЖЕНА НА 50** — сгенерённый дашборд считает каждый headline-KPI («Активных сделок: N», «Клиентов: N», «Средний чек») КЛИЕНТСКИ как `.length`/`.reduce()` над массивами, которые `useEntity`/`entities[X].list()` грузит БЕЗ `limit` → движок отдаёт DEFAULT_LIMIT=50. Entity-рантайм НЕ имеет count/aggregate-примитива НИГДЕ: `listRecords` возвращает голую ≤50 страницу без `total` (engine.ts:205-210), нет `countRecords`/`count(*)` (engine.ts), SDK `list`→`Promise<Row[]>` без count (sdk/index.ts:85,126), `useEntity` отдаёт только `rows` без total (use-entity.ts), нет /count|/stats-роута (api/entities/[entity]/route.ts — единственный). Кит САМ учит этому паттерну (dashboard-hero.tsx:71-72 `value={clients.length}`/`value: open.length`, count-up.tsx:13) → структурно, не writer-шум. Хуже BS-18 (тихий обрыв списка) и BS-24 (обрезка экспорта): BS-18 прячет старые строки в таблице (юзер чует пейджер), BS-26 показывает АВТОРИТЕТНЫЙ total, который выглядит точным, замирает на 50 по мере роста бизнеса, и это ПЕРВОЕ, что видно при логине; avgCheck/conversion считаются над НОВЕЙШИМИ 50 = статистически скошены, не просто обрезаны. Семья «выглядит полным, но нет» BS-14/16/18/20/21/24, поверхность aggregate/headline-metric. Класс шире CRM: любой дашборд-headline (клиенты/заказы/товары/брони/лиды/ученики, суммы выручки, средние, конверсии) | engine.ts:38-39,205-210 + sdk/index.ts:85,126 + use-entity.ts + dashboard-hero.tsx:71-72 + count-up.tsx:13 + SYSTEM_PROMPT.md:99 | **CONFIRMED** (run #23, LIVE-RUNTIME на DEPLOYED base omnia-template-nextjs-entities:dev: throwaway omnia-dev-dogfood-kpicount-probe, schema proj_dogfood_kpi_1781632650, starter Task, owner-auth Auth.js; 60 записей → GET без params (что считает rows.length-KPI) = 50, тело = голый `{"data":[...]}` БЕЗ total-поля и БЕЗ X-Total-Count хедера, ?limit=200=60, /count|/stats|/aggregate→500 (нет count-роута), ?count=true игнор; + WRITER-PATTERN на РЕАЛЬНОМ сгенерённом CRM demo-crm-dlia-prosmotra dashboard/page.tsx: list() без limit ×3 + `activeDeals.length`/`newClientsThisMonth.length`/`overdueTasks.length` + `avgCheck=deals.reduce/deals.length`; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14-22; контейнер+schema удалены). ПОПУТНО опровергнута гипотеза numeric-cast-crash (BS-22 sibling): number-валидатор строгий `z.number()` (registry.ts:130) → JSONB всегда настоящее число → `::numeric` (engine.ts:74) не крашит через write-path | PROPOSAL P-KPICOUNT ниже, фикс = новый server count/aggregate-примитив (engine countRecords + /count-роут ИЛИ {rows,total}-envelope + SDK count() + useEntity total + kit-гайд), причём count ОДИН не чинит avgCheck/conversion (нужен server SUM/AVG); TEMPLATE+engine, base-rebuild+regen-verify, тесно связан с P-PAGINATE/SEARCH/SORT/EXPORT → НЕ шипить вслепую; acceptance-lock test_entity_kpi_count_windowing.py (5 green + 1 xfail). **Real-world repro #34** (inventory `dogfood-sklad-uchet`): сгенерённая categories-page считает count/Σ-стоимость/Σ-кол-во по категориям из `Product.list()` без limit → агрегаты по ≤50 товарам.
- BS-27 | 2026-06-17 | **ENTITY-ДВИЖОК БЕЗ УНИКАЛЬНОСТИ — одна и та же запись добавляется дважды молча, без 409** — сгенерённый апп НЕ может объявить поле уникальным (email клиента, SKU товара, слот брони, username), и write-путь НЕ дедупит → добавление ИДЕНТИЧНОЙ записи создаёт дубль с новым id, без 409, без предупреждения. Резкий контраст: платформа УМЕЕТ уникальность — делает её для `users.email` РЕАЛЬНЫМ Postgres unique-индексом (db/schema.ts:50 `email…notNull().unique()`) + чистый 409 в auth-register (api/auth/register/route.ts:48) — но эта машинерия проведена ТОЛЬКО к фиксированной auth-таблице; entity-записи не получают ничего. Два слоя нулевой уникальности: (1) СХЕМА не может ОБЪЯВИТЬ — `FieldDef` (registry.ts:34-43) несёт type/required/default/options/entity, нет `unique`-флага → бриф «без дублей клиентов / уникальный SKU» невыразим; (2) write НЕ ПРОВЕРЯЕТ — `createRecord` (engine.ts:213-234) только zod-парсит body + insert, не SELECT-ит существующую строку с тем же значением, нет unique-индекса на `records.data->>field` нигде. Семья тихих data-integrity BS-17 (clear=no-op)/BS-19 (delete сиротит)/BS-25 (concurrent клобберит) — провал невидим в момент создания («создалось успешно»). Класс шире CRM: любой естественный ключ — email/телефон клиента, SKU/slug товара, username, бронь (комната,слот), номер счёта | registry.ts:34-43 + engine.ts:213-234 + db/schema.ts:50 (контраст) + api/auth/register/route.ts:48 (контраст) | **CONFIRMED** (run #24, LIVE-RUNTIME на DEPLOYED base omnia-template-nextjs-entities:dev: throwaway omnia-dev-dogfood-uniq-probe, schema proj_dogfood_uniq633526, starter Task, owner-auth Auth.js; CRM-оператор добавляет одного walk-in клиента 3× (title=name+phone): CREATE ×3 идентичных {title:"Иван Петров | +7 999 123-45-67",priority:high,notes:VIP}→3× **201**, LIST_TOTAL 3 / IDENTICAL_TITLE_ROWS 3 / DISTINCT_IDS 3 / ALL_SAME_DATA true, ?title=точное→FILTER_EXACT_RETURNS 3 (неразличимы), UNIQUENESS_ENFORCED **NO** / DUPLICATE_BUG **CONFIRMED**; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14-26; контейнер+schema+role удалены) → PROPOSAL P-UNIQUE ниже, фикс = `unique`-флаг в registry+SYSTEM_PROMPT + enforcement (partial unique-индекс на `(entity, data->>field)` ИЛИ pre-insert проверка, со скоупом global-vs-per-owner) + 409 через SDK+EntityForm (как уже делает register-route для email); base-rebuild+regen-verify, меняет write-семантику всех аппов, data-integrity-чувствительно → НЕ шипить вслепую; acceptance-lock test_entity_uniqueness_gap.py (3 green + 1 xfail)
- BS-28 | 2026-06-17 | **ПРОВАЛ СБОРКИ НА HANDLED LLM-ОШИБКЕ ОСТАВЛЯЕТ ПУСТУЮ, ВЕЧНО-«СТРИМЯЩУЮСЯ» СТРОКУ ЧАТА — юзер видит blank-бабл со стак-спиннером и НИ СЛОВА почему** — когда brief/writer-пасс падает на HANDLED upstream-ошибке (budget / gateway-timeout / rate-limit / model_unavailable — все нормальные транзиенты, ср. BS-10 gateway-ReadTimeout), ветка `stream_error` в `_process_prompt` финализировала assistant-сообщение пустым `accumulated`-строкой + falsy `usage_data` → персистится `content=''` И `tokens_out=NULL`; ошибка доходила до юзера ТОЛЬКО через эфемерный `llm.error` WS-event. Юзер, перезагрузивший страницу или потерявший сокет за время (многоминутной) сборки, видит пустой бабл, который по `tokens_out=NULL` выглядит «ещё стримится» (стак-спиннер), без объяснения и без retry. КОНТРАСТ-асимметрия: crash-путь `_emergency_error` (messages.py:158) УЖЕ персистит `[Ошибка: …]` + zero-tokens ровно для этого, а handled stream_error-путь — нет. Сам код это знает: коммент messages.py:3751 «otherwise tokens_out stays NULL and ChatPanel [spins]». Класс: семья «ложного/отсутствующего сигнала» (BS-11 осиротевшие якоря, BS-15 lead-форма-театр) — но тут провал ЖЁСТКИЙ, а сигнала НОЛЬ | messages.py:2113-2123 (stream_error) + _finalize_message:3751 + _emergency_error:158 (контраст) | **FIXED c4c9d9a** (run #25, LIVE-репро на ПРОД ДО фикса: dogfood-crm-klienty-e28e8d, art-director brief budget-error → writer-fail → assistant-строка `content='' / tokens_in=NULL / tokens_out=NULL / snapshot_id=NULL` + лог `[PP] stream_error err='writer pass failed: Недостаточно средств'`; фикс = pure-helper `_failed_build_body(accumulated, err)` персистит читаемое тело когда ничего не стримилось, сохраняя любой partial + `usage_data or {tokens_in:0,tokens_out:0}` зануляет токены — зеркало _emergency_error; деплой api+worker, helper загружен на проде (import OK → `[Ошибка генерации: …]`); 4 теста test_failed_build_message_persist.py: 2 pure-logic GREEN локально + 2 DB-lock (Postgres-fixture, как прочие DB-тесты — verify на деплое); ZERO контракт-change, не трогает success-путь). ПОБОЧНО (run #25): live-gen ВОССТАНОВЛЕН после ~13 прогонов WALLET-EMPTY — 2 реальные генерации прошли (CRM «InkBook» cost 5.17 + storefront cost 4.88), BS-4 escalation blank→nextjs_entities подтверждён LIVE, дизайн ~8/10
- BS-29 | 2026-06-17 | **БРЕНД-РАССИНХРОН — публичный лендинг брендирован ОДНИМ именем, а кабинет/таб-тайтл/OG-карточка ДРУГИМ** — арт-директор-бриф авторитетно объявляет `БРЕНД-НАЗВАНИЕ` (live: `"Aura CRM"`), writer честно печатает его по всему публичному лендингу («AURA ДЛЯ САЛОНОВ КРАСОТЫ» в hero/features/CTA, page.tsx) — НО `share_meta.build_share_card` зовётся с `project.name` (messages.py:3159 `_proj_name`), а `_derive_title` берёт человеческое имя проекта ДОСЛОВНО (share_meta.py:90-91) → `omnia-share.ts` title = «dogfood CRM Аврора». Этот share.title — единственный источник для трёх поверхностей: `<title>` таба (layout.tsx generateMetadata), OG-карточки виральной расшарки (opengraph-image.tsx, ПИЛЛАР 4) и **wordmark'а auth-shell'а** (auth-shell.tsx читает `share.title`, omnia-share.ts). Бриф (`state["brief"]`, messages.py:3176) доступен в той же области, но НЕ парсится на бренд. Итог: посетитель видит «AURA» на маркетинг-странице, затем «dogfood CRM Аврора» в ту же секунду как жмёт «Войти в кабинет»→/signin — а таб-тайтл на самом лендинге уже не совпадает с hero. Класс шире салона: ЛЮБОЙ entity/fullstack-апп, где имя проекта (утилитарный бэк-офис-лейбл, тем более с dogfood-/Untitled-мусором) ≠ бренд, который AD изобрёл и writer показал; семья «ложного/рассинхронного сигнала» с фокусом на бренд-идентичность | share_meta.py:90-91,106-115 + routers/messages.py:3159 (зовёт с project.name, не с brief-брендом) + components/omnia/auth-shell.tsx (рендерит share.title) + art_director брифовый `БРЕНД-НАЗВАНИЕ` | **CONFIRMED** (run #26, РЕАЛЬНАЯ live-gen — live-gen восстановлен 2-й прогон подряд: dogfood-crm-avrora-6450ab, blank→nextjs_entities эскалация ✓, healthy entity-апп Client/Master/Booking+dashboard-роуты; брифовый `БРЕНД-НАЗВАНИЕ: "Aura CRM"` из 02_brief.md/01b_writer_input.md, page.tsx hero «Aura для салонов красоты»/«Aura CRM», omnia-share.ts title «dogfood CRM Аврора»; **live Playwright на DEPLOYED dev-контейнере**: landing tab-title=«dogfood CRM Аврора» + hero=«AURA ДЛЯ САЛОНОВ КРАСОТЫ», signin wordmark=«dogfood CRM Аврора» + screenshot landing.png/signin.png; подтверждает run #25 passing-ноту «InkBook vs slug») → PROPOSAL P-BRAND ниже, фикс затрагивает виральную share-поверхность (owner-намеренный «project-name-first» title-preference, share_meta docstring + test_real_name_becomes_the_title) = продуктовое решение → НЕ шипить вслепую; acceptance-lock test_share_meta.py (1 green evidence + 1 xfail)
- BS-30 (low-sev, freeform trust/links) | 2026-06-17 | **FREEFORM ШЬЁТ КОНКРЕТНЫЕ ФАЛЬШ-СИГНАЛЫ ДОВЕРИЯ НА ГОЛЫЙ ИНСТРУМЕНТ + ФУТЕР-ССЫЛКИ СХЛОПЫВАЮТСЯ В #hero** — чистый запрос-ИНСТРУМЕНТ («калькулятор ипотеки с графиком») роутится в gen_mode=freeform (НЕ spa: `_infer_stack_from_text` без backend-сигнала и без дословного «без регистрации» → None → дефолт-freeform; spa достижим ТОЛЬКО через BS-3-негатив-safety-net или LLM-discovery-pick), и арт-директор/writer оборачивает калькулятор в МАРКЕТИНГ-ЛЕНДИНГ с ВЫДУМАННЫМИ конкретными бизнес-claim'ами, под которыми нет реального бизнеса: фейк-телефон `tel:+78005553535` («8 800 555-35-35», 2× в hero/footer), «15 банков-партнёров», рейтинг «4.9 ★ на Банки.ру» (названный сторонний сайт!), «одобрение за 12 минут». Нет гарда, отличающего «голый инструмент без бизнеса за ним» от «бизнес-лендинг» → генератор изобретает телефон, число партнёров и стороннюю звёздную оценку даже для калькулятора. ВТОРОЙ слой: футер рендерит 10 различно-подписанных ссылок (О проекте, Банки-партнёры, Контакты, **Политика конфиденциальности**, Условия использования, Раскрытие информации, Условия) — ВСЕ с `href="#hero"` → семантические тупики: клик по «Политика конфиденциальности» скроллит в hero, а не показывает политику. Dead-link-чекер валидирует, что якорь РЕЗОЛВИТСЯ (#hero существует), но НЕ что различно-подписанные ссылки ведут на РАЗНЫЕ/осмысленные таргеты. Класс: любой freeform-инструмент/одностраничник (конвертер, генератор, чек-лист), где writer добавляет бизнес-хром (контакты, соцпруф, легал-футер), которого у инструмента нет | discovery.py (`_infer_stack_from_text`→None для tool-без-backend) + art_director/freeform writer-промпт (фабрикует phone/rating/partner-count) + freeform dead-link-чекер (label↔target не проверяется) | **CONFIRMED** (run #27, РЕАЛЬНАЯ live-gen — live-gen восстановлен 3-й прогон подряд: dogfood-ipoteka-kalkuliator-a0f845, gen_mode=freeform/template=blank/preset=realestate-premium/model=deepseek-v4-pro-thinking; артефакт index_raw.html 41KB; **ФУНКЦ PASS** — калькулятор РЕАЛЬНО работает: корректная аннуитет-формула + полный график амортизации + реактивные слайдеры (live-eval: 6M→12M удвоило платёж 66 324→132 648 ₽, точно ×2) + canvas-чарт «График погашения» painted=true; публичный share `/p/<slug>` = **200** + полный 68KB-HTML (НЕ -dev.preview subdomain — та 502 для freeform, она для entity dev-контейнеров); дизайн ~8.5/10 emerald/gold real-estate; **слепые зоны:** grep index_raw.html — `tel:+78005553535` ×2, «4.9 ★ на Банки.ру», «15 банков-партнёров», «одобрение … за 12 минут»; 10× `href="#hero"` с различными легал/нав-подписями; «OOMNIA.ESTATE» = ЛОЖНАЯ тревога (badge-«O»+wordmark «OMNIA.ESTATE» склеились в textContent, в исходнике 7× корректный single-O)) → НЕ фиксить вслепую: оба под-фикса РАЗМЫТЫ (лендинги легитимно нуждаются в контактах/соцпруфе — нельзя просто забанить телефон; «#hero для легал-ссылки» не явно «неверно» на одностраничнике) + max один фикс/прогон + freeform tool-путь в остальном ЗДОРОВ (правильно НЕ эскалировал в auth/entities — зона BS-3 цела) → blind-spot записан, фикс отложен как fuzzy/low-value
- BS-5 | 2026-06-16 | tgbot/api стеки ОРФАНЫ от discovery: шаблоны `telegram-bot-aiogram` + `fastapi-postgres` полностью собраны и провижатся (stack_registry.py:63, prompt_builder `_TGBOT_STACK`/`_BACKEND_TEMPLATES`, schemas/project.py:46 tgbot→telegram-bot-aiogram), НО ни один NL-запрос их не достигает. discovery `_STACKS` = {static, fullstack, nextjs_entities, spa} и меню `_SYSTEM` предлагают LLM только эти 4; out-of-vocab pick → форс в static (discovery.py:843). Двойной разрыв: `_DISCOVERY_STACK_TO_TEMPLATE` (stack_routing.py:47) тоже без tgbot. Юзер просит бота → молча получает веб-апп | discovery.py:53,418-450 + stack_routing.py:47 | **CONFIRMED** (run #4, live prod repro: «сделай телеграм-бота для записи в барбершоп»→`nextjs_entities`; «telegram бот … без сайта»→`fullstack`) → PROPOSAL P-BS5 ниже; фикс крупный (вокаб+меню+routing-map+backend-only provisioning без web-preview+TELEGRAM_BOT_TOKEN secret-UX) → НЕ шипить вслепую; acceptance-lock в test_stack_routing.py (2 xfail + evidence)
- BS-31 | 2026-06-17 | **WRITER ШЛЁТ НЕВАЛИДНЫЙ JSON В `entities/<X>.json` → СУЩНОСТЬ МОЛЧА DOA (404 «unknown entity»), И НИ ОДИН ГАРД ЭТО НЕ ЛОВИТ** — на реальном CRM-запросе из логов прода («CRM для отдела продаж: сделки со стадиями … канбан-доска …») writer (deepseek) выдал СЛОМАННЫЙ JSON ровно для одной сущности: `entities/Client.json` = `{ "name": "Client", "access":",\n "fields": {…} }` — НЕЗАКРЫТАЯ строка у `access` проглотила перевод строки и весь `fields`-объект (`JSON.parse` → «Bad control character in string literal»). Рантайм `loadEntity` ГЛОТАЕТ ошибку парса и возвращает `null` (registry.ts:87-89) → на ЖИВОМ контейнере `GET /api/entities/Client` → **404 `{"error":"unknown entity 'Client'"}`**, тогда как валидный `GET /api/entities/Deal` → 401 (резолвится). Client — ЦЕНТРАЛЬНАЯ сущность (Deal.clientId→Client, страница /clients, дашборд-KPI «Новые клиенты») → целый срез CRM молча мёртв, БЕЗ сигнала где-либо. Слепая зона: НИ ОДИН гард не проверяет, что writer-JSON сущностей вообще парсится — `_normalize_entity_filenames` ЯВНО пропускает непарсящиеся файлы (messages.py:1227-1228 `except Exception: continue # malformed JSON — leave it for the registry/normalize() path`), а registry потом тихо нуллит. Отлично от BS-12 (filename≠Name, JSON ВАЛИДЕН — гард чинит): здесь сам JSON битый, BS-12-гард (срабатывает только если JSON парсится) структурно не ловит. ПОЗИТИВ прогона: BS-4 эскалация сработала (template=nextjs_entities, реальный entity-апп), и writer КОРРЕКТНО применил канбан — `<CrudResource entity="Deal" view="board">` ×2 (дашборд-виджет + /deals), а кит board-view с drag-to-save-status РЕАЛЬНО поддерживает (crud-resource.tsx:92-102,302) → запрошенная headline-фича доставлена структурно, провалила её именно битая Client-сущность. Семья «ложного успеха» (BS-12 DOA / BS-28 пустой стрим): «создалось», но молча сломано | registry.ts:87-89 + messages.py:1227-1228 (+ writer/deepseek JSON-fumble в 03_writer_raw) | **CONFIRMED** (run #28, live-gen ВОССТАНОВЛЕН 4-й прогон подряд: dogfood-crm-kanban-sdelki-882231, mint-token owner 2aa9b1a2, skip_clarify=true → blank→nextjs_entities, freeform+art-director/deepseek-v4-pro-thinking/preset=local-services/cost_rub 5.50; live-proof `node JSON.parse`→PARSE_FAIL + `GET /api/entities/Client` 404 vs Deal 401 + writer-raw `"access":",` + screenshots landing/deals в `_routine/runs/2026-06-16T1931Z/`; контейнер+проект удалены) → PROPOSAL P-ENTITYJSON ниже, фикс = валидировать каждый `entities/*.json` в guard-цепочке и на parse-fail РЕГЕНЕРИТЬ только этот файл (или валить билд видимой ошибкой), НЕ молча шипить DOA; generic-JSON-repair тут НЕ спасает (незакрытая строка съела `fields` → даже json-repair смержит мусор), regen policy-смежен (auto_regenerate off) + кросс-поверхностно → НЕ шипить вслепую; acceptance-lock test_entity_json_validity.py (2 green evidence + 1 xfail strict). **RE-CONFIRMED run #30 (2/2 CONSECUTIVE РЕАЛЬНЫХ CRM-ГЕНА — НЕ ФЛУК, ДОМИНИРУЮЩИЙ FAILURE-MODE):** scenario-1 CRM-с-нуля dogfood-crm-klienty-87e683 (mint-token owner 2aa9b1a2, skip_clarify=true → BS-4 эскалация СНОВА сработала: blank→nextjs_entities, deepseek-v4-pro-thinking, preset=local-services, cost_rub 4.33), сущности Client+Note(clientId→Client)+Task(clientId→Client) + (app)-роуты /dashboard /clients /tasks. И СНОВА сломалась ровно ЦЕНТРАЛЬНАЯ `entities/Client.json`, но ДРУГИМ glitch-классом, чем #28: `"name": { "type": "string",": true }` — deepseek ПРОГЛОТИЛ имя ключа `"required"`, оставив `,": true` → `JSON.parse` «Invalid control character at line 6 col 42»; Note.json+Task.json парсятся ОК. Live-proof на работающем dev-контейнере omnia-dev-dogfood-crm-klienty-87e683: anon `GET /api/entities/Client`→**404 unknown entity** vs Note→401 / Task→401 (резолвятся) → весь client-management-срез CRM молча мёртв, билд отрапортовал SUCCESS. Два РАЗНЫХ corruption-shape (#28 `"access":","` value, #30 dropped key) подтверждают вывод P-ENTITYJSON: детерм. repair НЕВОЗМОЖЕН (corruption съедает контент — нельзя знать, что было `"required"`), нужен либо targeted-regen-одного-файла, либо visible-build-fail, либо drop-entity = ПРОДУКТОВОЕ решение + policy-смежно (auto_regenerate off) → НЕ шипить вслепую. СЕВЕРНОСТЬ ПОВЫШЕНА: 2 из 2 реальных CRM-генов DOA на Client → реальные юзеры CRM-сценария получают мёртвый апп ~половину времени → P-ENTITYJSON приоритет ВЫШЕ. Evidence: `_routine/runs/2026-06-16T20-31Z/` (00_PROOF.md, 01_landing.png, 02_broken_Client.json). ПОБОЧНО: design лендинга ~5/10 (чистая иерархия+типографика, но полностью МОНОХРОМ — forced --primary green не используется на лендинге вообще, нет ниши/глубины/картинок, фейк-цифры 142/68%/3); BS-29 повторился (hero «Omnia.CRM» vs tab-title «dogfood crm klienty»). **DETECTION-HALF SHIPPED run #31 (e3c97ad):** безопасная часть P-ENTITYJSON step 1 — `_warn_unparseable_entity_json(files)` логирует `[PP] entity_json_INVALID: <name>.json does not parse (...)` для каждого непарсящегося `entities/*.json` сразу после `_normalize_entity_filenames` (messages.py, gated CONTAINER_NEXT); чисто аддитивно (не мутирует files, не кидает, конвенция `[PP]`), деплой api+worker (НЕ base-rebuild). Убрана «тишина в build-логе» — доминирующий failure-mode теперь диагностируем В МОМЕНТ ВОЗНИКНОВЕНИЯ. Repair-ACTION (micro-regen / visible-fail / drop-entity) ОСТАЁТСЯ deferred (policy-смежно, rule 15) → strict-xfail `test_no_unparseable_entity_json_ships` (build-time ГАРАНТИЯ) ОСТАЁТСЯ красным; BS-31 ОСТАЁТСЯ CONFIRMED (UX-DOA не починен, только наблюдаемость). Test: test_invalid_entity_json_is_flagged_in_build_log (3 green + 1 strict-xfail в файле); deployed-grep worker=2/api=1, clean boot, api /health 200
- BS-32 | 2026-06-17 | **ENTITY-ДВИЖОК БЕЗ TIME-OF-DAY — у любого расписания можно сохранить ДЕНЬ записи, но НЕ ВРЕМЯ (10:00 vs 16:30)** — система типов полей сущностей не имеет `datetime`/`time`: `FieldType = string\|text\|number\|boolean\|date\|enum\|reference` (registry.ts:22-32), т.е. «когда» можно объявить только как календарную `date`. Три поверхности: (1) нет типа времени в модели; (2) виджет `kind:"date"` рендерит `<Input type="date">` (entity-form.tsx:258-261) — HTML-датапикер без часов/минут, юзер физически не может ввести время; (3) `toDateInput` форматит значение `.toISOString().slice(0,10)` (entity-form.tsx:67) → ОБРЕЗАЕТ до YYYY-MM-DD, теряя время даже если оно было. Две записи в один день неразличимы по времени и не упорядочиваются внутри дня. Writer САМ хотел время — назвал поле Booking-сущности «Дата и время визита» и дал booking-листу колонку «Время», но смог выбрать только `"type":"date"`. Класс шире барбершопа: запись к врачу, бронь столика, групповое занятие, слот консультации — любой scheduling-апп. Отлично от семьи дат BS-19/BS-22 (P-DATECAST = краш сортировки на JS-валидной-но-PG-невалидной дате, robustness-баг каста); тут — ОТСУТСТВИЕ типа времени, feature/семантический пробел. Семья «форма обещает то, что не может дать» (BS-15 lead-театр, BS-17 clear-noop): лейбл говорит «время», виджет его не ловит | registry.ts:22-32 + entity-form.tsx:67,258-261 | **CONFIRMED** (run #29, РЕАЛЬНАЯ live-gen dogfood-barber-crm-5d864d: «CRM для барбершопа … записи на стрижку (дата, услуга, мастер)», skip_clarify=true → BS-4 эскалация сработала → реальный nextjs_entities апп Client+Booking; live Playwright booking-форма `FORM_DATE_INPUT_TYPES==['date']`, `entities/Booking.json` date-поле `"type":"date"` при лейбле «Дата и время визита» + колонка «Время»; скриншоты `_routine/runs/2026-06-16T20-02Z/05_booking_form.png` mm/dd/yyyy + 03_dashboard.png) → PROPOSAL P-DATETIME ниже, фикс мульти-поверхностный TEMPLATE-уровня (new `datetime` FieldType + `<input type="datetime-local">` + снять day-truncation + CalendarView по времени + writer-директива emit datetime для слот-полей без over-emit для простых дат + base-rebuild + regen-verify) → НЕ шипить вслепую; acceptance-lock test_entity_datetime_gap.py (3 green evidence + 1 xfail). ПОЗИТИВ прогона: эскалация+фильтр статуса (filterField/filterTabs значения 1:1 с enum)+ref-колонка clientId+view=calendar/split (кит поддерживает все)+auth — всё рабочее; design лендинга ~7/10 (чисто, но монохром, без барбер-характера), BS-29 brand-desync повторился (<title> «dogfood barber crm» vs бренд «Chop & Crown»)
- BS-33 | 2026-06-17 | **ENTITY-ДВИЖОК БЕЗ ЧИСЛОВЫХ ОГРАНИЧЕНИЙ — `number`-поле принимает ЛЮБОЙ double (ОТРИЦАТЕЛЬНЫЙ, дробный, экстремальный), и writer НЕ МОЖЕТ объявить «цена ≥ 0» / «количество — неотриц. целое»** — на реальном складском CRM-запросе («товары: цена в рублях, количество на складе … цена и количество числовые») writer корректно пометил price/quantity как required `number`, НО система типов не имеет vocabulary ограничений. Три открытых слоя: (1) `FieldDef = {type,required?,default?,options?,entity?}` (registry.ts:34-43) — НЕТ `min`/`max`/`integer`/`nonnegative`, модель не может объявить границу; (2) `zodForField(number)` = голый `z.number()` (registry.ts:127-150) без `.min(0)`/`.int()` — серверный backstop (createSchema→safeParse в engine.createRecord:223-226) пропускает каждый double; (3) виджет `kind:"number"` = `<Input type="number">` (entity-form.tsx:246-256) без `min`/`step`, клиент тоже не гардит. Downstream: дашборд (dashboard/page.tsx:113) `totalValue += price*quantity` → (-50000)×(-30) = +1 500 000 ₽ ФАНТОМНЫЙ актив (одна небрежная отрицательная запись РАЗДУВАЕТ headline-KPI «Стоимость склада»); criticalCount (:116) считает -30 как low-stock; список (products/page.tsx:234-238) красит -30 бейджем «Мало», не ошибкой. Семья value-range: отлично от BS-22/P-DATECAST (краш каста даты-строки) и BS-27/P-UNIQUE (нет уникальности — дубль) — тут нет ограничения ДИАПАЗОНА (out-of-domain число садится как валидное). Семья «форма обещает то, что не enforce'ит» (BS-15/BS-17): поле типизировано `number`, но любая чушь-число залипает | registry.ts:34-43,127-150 + entity-form.tsx:246-256 | **CONFIRMED** (run #32, РЕАЛЬНАЯ live-gen dogfood-sklad-uchet-a086b4: «Складской учёт …», skip_clarify=true → BS-4 эскалация → реальный nextjs_entities апп Product; authed Auth.js v5 POST /api/entities/Product `{price:-50000,quantity:-30}`→201, `{price:1999.999,quantity:2.5}`→201; GET ?sort=quantity&order=asc отдаёт -30 ПЕРВЫМ как валидную; entity JSON чистый, БЕЗ entity_json_INVALID; evidence `_routine/runs/2026-06-16T21-00Z/EVIDENCE.md`+03_writer_raw.html) → PROPOSAL P-NUMRANGE ниже, фикс мульти-поверхностный TEMPLATE-уровня (FieldDef +min/max/integer + zodForField threading + Input min/step + writer-директива emit min/integer для price/qty/age БЕЗ over-emit для balance/delta/temp/coord/rating + base-rebuild + regen-verify) → НЕ шипить вслепую; acceptance-lock test_entity_number_constraints_gap.py (3 green evidence + 1 xfail)

- BS-34 | 2026-06-17 | **ФИЛЬТР/СЕГМЕНТ СПИСКА РАБОТАЕТ ТОЛЬКО ПО ЗАГРУЖЕННОМУ ≤50-ОКНУ (CLIENT-SIDE) — статус-фильтр / колонка канбана молча НЕДОСЧИТЫВАЕТ или ложно «пусто» при масштабе** — НОВАЯ ось windowing-семьи (search BS-20, sort BS-21, export BS-24, KPI BS-26, pagination BS-18 уже покрыты; ФИЛЬТР/СЕГМЕНТ — нет). Путь ПРЕДПИСАН, не edge-case: SYSTEM_PROMPT.md:104-109 велит writer'у для сущности со стадиями (заявка/тикет/заказ/сделка/задача) ставить `view="board"`+`filterField`+`filterTabs` — real run #28 так и сгенерил («сделки со стадиями … канбан-доска»). Цепочка (4 поверхности шаблона): `useEntity` зовёт `list(params)` ОДИН раз без пагинации (use-entity.ts:33-48, док «client-side search/sort/paging happen in <DataTable>») → движок отдаёт `DEFAULT_LIMIT=50` (engine.ts:39,193-206) → `data.rows`=окно; быстрый фильтр `segmented = all.filter(row => rawValue(row,filterField)===want)` по `rows` CLIENT-SIDE (data-table.tsx:281-291, хедер «client-side search, sort and pagination»); канбан `grouped` бакетит `for (const card of filtered)` по колонкам в памяти (board-view.tsx:201-211); `mergedParams` добавляет ТОЛЬКО `expand`, активный таб/колонка НИКОГДА не уходит в `list()` (crud-resource.tsx:177-184, boardCards из data.rows). Фикс ДОСТИЖИМ server-side: `listRecords` уже применяет `data->>field = value` (engine.ts:178-183) → достаточно пробросить активный сегмент в list-params. **Следствие:** «покажи только VIP» / колонка «Выиграна» на >50 записях → видны лишь матчи среди первых 50; VIP на строках 51+ → ложное «Ничего не найдено», счётчики колонок неверны, drag по частичной доске. | data-table.tsx:281-291, board-view.tsx:201-211, crud-resource.tsx:177-184, use-entity.ts:37 | **PROPOSAL P-FILTERWINDOW** (фикс архитектурно-связный как у сиблингов P-SORT/P-SEARCH/P-EXPORT/P-KPICOUNT — лифт filter-state из DataTable + server re-query + согласование board-drag с пагинацией; ships на КАЖДОМ сгенерённом аппе → НЕ шипить вслепую) + acceptance-lock `apps/orchestrator/tests/test_filter_windowing_gap.py` (5 green + 1 xfail). **Real-world repro #34** (inventory `dogfood-sklad-uchet`): products-page `filterTabs` по category — client-side по загруженному окну.
- BS-35 | 2026-06-17 | **ENTITY-ДВИЖОК МИСХЭНДЛИТ ДВА КОНСТРЕЙНТ-ПРИМИТИВА, КОТОРЫЕ У НЕГО ЕСТЬ (`required`, `default`)** — facet 1 (ПОЧИНЕНО): поле с ОБОИМИ `"required":true`+`"default":<x>` было НЕВЫПОЛНИМО при пропуске. `createRecord` зовёт `createSchema(def).safeParse(body)` ПЕРВЫМ и `applyDefaults(def, parsed.data)` ВТОРЫМ (engine.ts:223-227), а `createSchema` делал КАЖДОЕ required-поле non-optional (registry.ts:170 `f.required ? base : base.optional()`) → пропуск required+default падал zod'ом «Required» и 400-ил ДО того, как дефолт успевал примениться. То, ради чего дефолт существует (покрыть пропуск), — ровно то, что 400-ило. Натуральная writer-схема `"status":{"type":"enum","options":["new","paid","shipped"],"required":true,"default":"new"}` («у каждого заказа есть статус; если форма опустила — ставь new») → quick-create, полагавшийся на дефолт, отвергался. Facet 2 (PROPOSAL, не шипить): `required` строка = presence-only, НЕ non-empty — `zodForField(string|text)` = голый `z.string()` → POST name:"" / name:"   " (пробелы) проходит, садится пустая-на-вид строка; ужесточение `.trim().min(1)` меняет write-семантику ВСЕХ аппов (ср. BS-33 P-NUMRANGE write-half) → не вслепую. Отлично от семьи: BS-33 нет числового ДИАПАЗОНА, BS-27 нет уникальности, BS-22 краш date-каста, BS-19 нет ref-integrity — тут движок ломает примитивы, которые У НЕГО ЕСТЬ. | registry.ts:166-175 (createSchema) + engine.ts:223-227 (parse→default order) | **FIXED a92f73d** (run #35, faithful-proof через `node --experimental-strip-types` на DEPLOYED-образе omnia-template-nextjs-entities:dev импортируя РЕАЛЬНЫЙ registry.ts createSchema/applyDefaults: ДО — A required+default OMITTED→**400 «status: Required»**, B same field required:false→201 status:"new" (control: дефолт работает только когда НЕ required), C required string ""→201, D "   "→201, E provided→201; ПОСЛЕ-фикса на ПЕРЕСОБРАННОМ base — A→**201 status:"new"**, F required+default ='bogus'→**400 «Invalid enum value»** (value-валидация цела), G required-no-default OMIT→**400 «Required»** (default-less остаётся hard-required); фикс `requiredInPayload = f.required && f.default === undefined` ПУСТО-ПЕРМИССИВЕН (только 400→201-with-default, не может зарегрессить рабочий апп), base пересобран на проде + verify на rebuilt image без bind-mount, evidence `_routine/runs/2026-06-17T00-53Z/`) — facet 2 ОСТАЁТСЯ как proposal (strict-xfail `test_required_string_should_reject_empty_and_whitespace`); acceptance-lock test_entity_required_default_semantics.py (5 green + 1 xfail).
- BS-36 | 2026-06-17 | **ENTITY-ДВИЖОК ПРИМЕНЯЕТ `default` БЕЗ ВАЛИДАЦИИ — невалидный writer-дефолт молча садится в строку на КАЖДОМ create-с-пропуском, в обход проверки, которую проходит любое ПРЕДОСТАВЛЕННОЕ значение** — сиблинг BS-35 в семье «движок ломает примитив, который У НЕГО ЕСТЬ». `createRecord` зовёт `createSchema(def).safeParse(body)` ПЕРВЫМ, `applyDefaults(def, parsed.data)` ВТОРЫМ (engine.ts:223-227). createSchema делает поле-с-дефолтом optional в payload (BS-35 facet-1 fix) → пропущенное поле валидация НЕ ВИДИТ, и `applyDefaults` инжектил `f.default` БЕЗ ПРОВЕРКИ (registry.ts:191-200, старый `if (out[key]===undefined && f.default!==undefined) out[key]=f.default;`). Невалидный по СОБСТВЕННОМУ типу/enum дефолт писался молча: (a) enum-дефолт НЕ из `options` (`"status":{"type":"enum","options":["new","paid","shipped"],"default":"pending"}`) → на пропуске садится `status:"pending"` — РОВНО то значение, что движок 400-ит когда оно ПРЕДОСТАВЛЕНО (createSchema z.enum его reject-ит); (b) НЕ-числовой дефолт в number-поле (`qty:"none"`) → единственный вектор посадить нечисло в number-колонку, а numeric-сорт кастит `(data->>field)::numeric` БЕЗ safe-cast (engine.ts:74, в отличие от date BS-22 `safe_to_timestamptz`) → одна такая строка 500-ит весь список при сортировке; (c) string-дефолт в boolean-поле (`inStock:"yes"`). | registry.ts:191-200 (applyDefaults) + engine.ts:223-227 (parse→default order) + engine.ts:74 (unguarded ::numeric) | **FIXED 8745c31** (run #36, faithful-proof `node --experimental-strip-types` на DEPLOYED-образе omnia-template-nextjs-entities:dev импортируя РЕАЛЬНЫЙ registry.ts createSchema/applyDefaults + реплей createRecord parse→default: ДО — A enum'pending'-not-in-options OMIT→**201 status:"pending"**, C number"none" OMIT→**201 qty:"none"**, E bool"yes" OMIT→**201 inStock:"yes"** (poison сел); B enum'new'-in-options OMIT→201 status:"new", D number 0 OMIT→201 qty:0 (controls); F provided'pending'→**400 Invalid enum** (движок 400-ит значение, что сам молча пишет). ПОСЛЕ на ПЕРЕСОБРАННОМ base без bind-mount — A/C/E→**201 БЕЗ битого поля** (дропнут), B/D→201 с валидным дефолтом (не зарегрессило), F→**400** (validation цела). Фикс `if (zodForField(f).safeParse(f.default).success) out[key]=f.default;` ПУСТО-ПЕРМИССИВЕН: валидный дефолт инжектится как был, битый дропается (поле остаётся пропущенным) вместо отравления строки — только invalid-stored→not-stored, никогда наоборот; base пересобран на проде. evidence `_routine/runs/2026-06-16T22-13Z/`); acceptance-lock test_entity_default_validation.py (6 green).
- BS-37 | 2026-06-17 | **ЯВНЫЙ ЗАПРОС «КАРТА» → ФАЛЬШ-ПЛЕЙСХОЛДЕР «Интерактивная карта» (НЕТ реального эмбеда), хотя бесплатный keyless map-iframe доступен** — freeform-лендинг с явным «контакты с картой» (или «карта проезда / как добраться») рендерит в контакт-секции `<div>` с ЛИТЕРАЛЬНЫМ текстом «Интерактивная карта» поверх градиента — БЕЗ `<iframe>`, без maps.google/yandex-эмбеда, без статик-картинки карты НИГДЕ в HTML. Корень: 98KB freeform-system-prompt НЕ имеет НИ ОДНОЙ директивы про карту/iframe (grep «карт|map|iframe|maps.google|yandex» → только «карточки»/card-упоминания), а АРТ-ДИРЕКТОР-бриф ЯВНО прописал карту (02_brief.md:182 «справа — контактная информация и **интерактивная карта**») → writer сматчил СЛОВА брифа инертным боксом-обманкой. Реальная карта в static БЕСПЛАТНА и без API-ключа (`<iframe src="https://maps.google.com/maps?q=<addr>&output=embed">` или Yandex map-widget) — возможность есть, директивы нет. Хуже простого пропуска: бокс АКТИВНО подписан «Интерактивная карта», посетитель ждёт, что карта прогрузится. Вторичная фабрикация: адрес/телефон карты выдуманы (нет реального бизнеса) → даже добавив эмбед, он показал бы карту НЕсуществующего адреса. Класс шире студии: ЛЮБОЙ локальный бизнес с «сайт с картой» (ресторан, клиника, магазин, салон, автосервис). Семья «форма обещает то, чего нет» (BS-15 lead-театр, BS-30 фальш-доверие/телефон) | freeform system_prompt (нет map/iframe-директивы) + art_director brief (прописывает карту, brief:182) + writer | **CONFIRMED** (run #37, РЕАЛЬНАЯ live-gen dogfood-arch-studio-kontur-b29604, freeform/preset=studio-showreel/deepseek-v4-pro-thinking/cost 9.04₽/passes 2, ui_audit 8/10, acceptance struct+resp+vision True, image_resolver 10/10; source-proof 03_writer_raw.html:368-371 placeholder «Интерактивная карта» 0×iframe + system-prompt grep 0 map-директив + brief:182 prescribes map; evidence `_routine/runs/2026-06-16T22-26Z/00_PROOF.md` + 01_landing.png) → PROPOSAL P-MAP ниже, фикс = writer-prompt-директива «на запрос карты вставляй РЕАЛЬНЫЙ keyless map-iframe, не фальш-плейсхолдер» (prompt-quality, fabricated-address-sensitive, CSP/sandbox-проверка, нужен regen-verify) → НЕ шипить вслепую (ср. BS-30 fuzzy-fabrication отложен, max 1 фикс/прогон)
- BS-38 | 2026-06-17 | **FOLLOW-UP, РАСШИРЯЮЩИЙ МОДЕЛЬ ДАННЫХ ENTITY-АППА, РОУТИТСЯ В CHEAP SURGICAL-EDIT → ШИПИТ БИТЫЙ .tsx → ПОЛНОСТЬЮ КЛАДЁТ РАБОТАВШИЙ АПП (build рапортует SUCCESS)** — НОВАЯ never-probed ось, ПРЯМО по жалобе владельца «кинул 5 промптов — генератор ничего не делал / ломал». H1 = static→app НЕ эскалирует; здесь зеркало: на УЖЕ рабочем entity-аппе follow-up «добавь раздел/сущность» (расширение модели данных) `decide_intent`→**CHEAP** (intent_triage.py:62-69 «добавь раздел» не матчит ни _REBUILD ни _STRUCTURAL → дешёвая правка by design), и дешёвая модель (deepseek-chat) получает КОНТЕЙНЕРНЫЙ edit-промпт `[_EDIT_IDENTITY_NEXT,_EDIT_FAITHFUL,_EDIT_RESPONSE_NEXT]` (prompt_builder.py:4005-4016), который учит править/создавать page.tsx, но НИКОГДА — entity-engine-конвенциям, и (главное) **НЕТ НИКАКОЙ syntax/compile-валидации .tsx перед commit+hot_reload**: entity/fullstack-аппы ПРОПУСКАЮТ acceptance-гейт (messages.py:3227), dead-link-пасс пропущен на surgical (messages.py:2724), а образ api/worker = NO_NODE (парсера в процессе нет). Итог: битый .tsx шипится молча, build пишет `[PP] hot_reload OK written=N`. Класс шире CRM: любой entity/container-апп, любой follow-up «добавь раздел/таблицу/сущность/поле». Семья «ложного успеха» BS-12/BS-28/BS-31 (build OK, апп молча мёртв), но тут провал ЖЁСТКИЙ (весь апп 500) и от ОДНОЙ дешёвой правки | intent_triage.py:62-69 + prompt_builder.py:4005-4016,3890-3893 + messages.py:763,2724,3227 (нет .tsx-гейта) | **CONFIRMED** (run #38, РЕАЛЬНАЯ live-gen dogfood-crm-followup-37c973: 1-й билд BS-4 escalation blank→nextjs_entities → рабочий CRM clients/appointments/notes/dashboard, cost 4.65, 11 файлов, все routes 200/✓Compiled; FOLLOW-UP «Добавь раздел Услуги: сущность название/цена/длительность, своя страница, пункт в меню» → mode=edit/surgical=True/deepseek-chat/cost 0.21/**hot_reload OK written=3**: entities/services.json ВАЛИДЕН (shape скопирован из контекста), но services/page.tsx БИТЫЙ (`columns={[` не закрыт, `fields={[` вложен внутрь → «Expression expected») И (app)/layout.tsx БИТЫЙ (surgical SEARCH/REPLACE съел `="/`: `href="/dashboard/services"`→`hrefdashboard/services"` → «Expected ',', got string literal»); layout.tsx оборачивает ВСЮ (app)-группу → live на DEPLOYED dev-контейнере /dashboard /clients /appointments /notes /services /  = **ВСЕ 6 routes 500** (были 200). evidence `_routine/runs/2026-06-16T22-53-09Z/` 00_PROOF.md + broken_followup_files.txt + 01_bricked_dashboard_500.png=blank; контейнер+проект удалены) → PROPOSAL P-TSXEDIT-VALIDATE ниже, фикс = реальный parse/compile-гейт контейнерных .tsx ПЕРЕД commit+hot_reload + revert-to-last-good (битая правка НЕ должна класть живой апп) + regen-verify; Python-эвристика баланса скобок FP-опасна на валидном TSX (скобки в строках/regex/template-literal) И не ловит класс attribute-corruption (layout) → НЕ шипить флаки-полугейт; acceptance-lock test_container_edit_tsx_validity.py (2 green: routing CHEAP + corruption-evidence + 1 strict-xfail: build-time validation gate)
- BS-39 | 2026-06-17 | **РЕКУРРЕНТНОЕ/НЕДЕЛЬНОЕ РАСПИСАНИЕ РЕНДЕРИТ ПУСТОЙ КАЛЕНДАРЬ — все уроки молча выпадают, и EmptyState тоже не показывается** — на реальном prod-промпте «веб-приложение для школы №7: расписание уроков … журнал …» (это ПРОМПТ run #7 → BS-9/P-RBAC уже покрыт, НЕ долбил) writer выписал `schedule/page.tsx` с `view="calendar" dateField="dayOfWeek"`, но `Schedule.dayOfWeek` — это ENUM `[monday..saturday]` (день недели, НЕ дата). `parseLocalDate("monday")`→`new Date("monday")`=Invalid→null (calendar-view.tsx:63-79), каждый такой event `continue`-скипается из `placed[]` (calendar-view.tsx:233-241), а `noRecords=rawCount===0` остаётся false при N строках → EmptyState тоже не рисуется (calendar-view.tsx:313); table-фолбэк срабатывал ТОЛЬКО при ОТСУТСТВУЮЩЕМ dateField (crud-resource.tsx:305). Итог: сетка недели с 0 уроков и без объяснения (репро node: 12 уроков → 0 placed). Класс шире школы: любое weekday/recurring-расписание (спортзал, часы работы клиники/ресторана) → пустой grid. Данные и `columns` есть — usable-таблица в одном фолбэке | calendar-view.tsx:63-79,233-241,313 + crud-resource.tsx:305 | **FIXED 58aef74** (CrudResource считает `calendarPlaceable`: view=calendar с загруженными строками, но 0 date-placeable → деградирует в DataTable, которую writer уже сконфигурил; чистый рендер-фолбэк, срабатывает только когда календарь и так пуст, generalises на любой misprescribed-календарь; parseLocalDate экспортирован для пречека; tsc clean; base-образ omnia-template-nextjs-entities:dev пересобран+проверен grep'ом; acceptance-lock test_calendar_unplaceable_fallback.py 3 green)
- BS-40 | 2026-06-17 | **PREVIEW-РЕНДЕР НЕ ДОСТАЁТ ПУБЛИЧНЫЙ MinIO → СНИМОК/СУДЬЯ ВИДЯТ БЕЗ-КАРТИНОЧНУЮ СТРАНИЦУ, ХОТЯ ЖИВОЙ САЙТ В ПОРЯДКЕ** — на freeform-портфолио фотографа (run #40) `image_resolver resolved=8/8` (все фото реальны, 200 ИЗВНЕ), НО снимок-thumbnail продукта показал ПУСТОЙ чёрный герой. Корень: resolver запекает фото абсолютным ПУБЛИЧНЫМ URL `https://constructor.lead-generator.ru/minio/...` (image_resolver.py:357), а preview-воркер скриншотит страницу ИЗНУТРИ контейнера через `file://index.html` (preview.py `capture()` + `_render_async` static-ветка) — и chromium тянет те публичные `<img src>`. Контейнер НЕ достаёт публичный хост (→ собственный публичный IP хоста, нет hairpin-NAT): `docker exec omnia-prod-worker curl <public img>`=**http=000** (и корень главного домена=000), а `curl http://minio:9000/...`=**200 за 9ms**. `_await_paint` `_IMAGE_WAIT_MS=3000` (preview.py:70) — пластырь, который НЕ лечит недостижимый URL (его коммент даже винит «медленные картинки» — мисдиагноз). Радиус: КАЖДАЯ freeform-страница с реальными `<img data-omnia-gen>` фото (а не CSS-shader-фоном) → thumbnail И экран дизайн-судьи без картинок; юзер в воркспейсе видит ложно-пустой превью, судья может «чинить» пустого героя, который вживую в порядке | preview.py:73-97(capture wait),148-152(capture loop),236-240(_render_async loop) + image_resolver.py:357 | **FIXED c8d4160** (`_rewrite_minio_to_internal`: репойнт `minio_public_url`→внутренний `minio:9000` ТОЛЬКО в in-memory копии для chromium в обеих ветках; закоммиченные/отдаваемые файлы хранят публичный URL → юзеры не затронуты; no-op для без-картиночных страниц и для local-dev где public==internal; 3 теста test_preview.py 14/14 green, ruff clean; deploy api+worker; ДОКАЗАНО: re-render snapshot на НОВОМ воркере → thumbnail 118KB→712KB, герой-портрет рендерится). Каверзат: внешне-CDN сток (Openverse fallback) тоже не достанется из воркера — отдельный egress, не чинено
- BS-41 | 2026-06-17 | **INLINE DEAD-LINK REPAIR = ПОЛНЫЙ NO-OP НА КОНТЕНТ/РЕДАКЦИОННЫХ САЙТАХ (блог/портфолио/журнал) — словарь fallback-целей зашит под витрины** — `repair_dead_links_inline` переписывает мёртвый `href="#"` на реальную секцию ТОЛЬКО если у страницы есть `id` из ХАРДКОД-КОММЕРЧЕСКОГО списка `_REPAIR_PREFERRED_TARGETS = contacts/contact/cta/order/booking/pricing/services/catalog/about/top` (link_validator.py:55). Арт-директор именует секции по нише, поэтому у блога о путешествиях секции были `hero/archive/manifesto/subscribe` — ни одна не коммерческая → `_pick_repair_target`→None → файл возвращается БЕЗ ИЗМЕНЕНИЙ (старый код link_validator.py:106-108) → ВСЕ плейсхолдеры шипятся. Дальше acceptance их пропускает (`acceptance_score_only=True`, score 9≥7, хотя `struct=False`). Проявление шире блога: ЛЮБОЙ не-витринный архетип, где AD даёт семантические id (портфолио, журнал, статья-детейл, лендинг-движения). Семья BS-11 (dead-link на surgical), но тут провал на ПЕРВОМ билде и тотальный (0 из N) | link_validator.py:55,61-70,106-108 (старый `_pick_repair_target` Optional, no-op при None) | **FIXED 4ab0ff9** (run #41, РЕАЛЬНАЯ live-gen dogfood-travelblog-41-57bde6, freeform/preset=hospitality-escape/deepseek-v4-pro-thinking/cost 9.51₽/passes 2; writer выдал РЕАЛЬНЫЙ мультистраничный блог index.html + 3 статьи iceland/japan/morocco + assets, навигация на статьи рабочая — но `[PP] dead_links initial=28 after_inline=28` (0 починено): index.html 4× `href="#"`, article-iceland.html 8× (теги ИСЛАНДИЯ/ВУЛКАНЫ + футер Политика/TELEGRAM). Фикс: новый `_pick_dead_link_target(ids, content)` ТОЛЬКО для полностраничного пасса (консервативный edit-orphan фиксер сохраняет строгое CTA-или-ничего) — всегда возвращает цель: коммерческая CTA → ПЕРВАЯ секция, на которую страница САМА навигирует (`id` присутствует И есть in-page `href="#id"`) → `#top` (валиден на любом документе по HTML-спеке). ДОКАЗАНО на РЕАЛЬНЫХ файлах run #41: 28→0 (index плейсхолдеры→`#hero`, статья теги/футер→`#top`); end-to-end на DEPLOYED prod-бинаре api: content-fixture 2→0; deploy api+worker, health 200 ИЗВНЕ (public /api/projects 200 с Mac); 12/12 test_link_validator green вкл. 4 новых репро/регресс-теста; 0 нового lint-долга (B023 3→3 pre-existing). evidence `_routine/runs/2026-06-17T00-41-42Z/`; проект удалён, контейнеров нет (freeform))
- BS-42 | 2026-06-17 | **ROLLBACK («ВЕРНУТЬСЯ НАЗАД» / «ОТКАТ ЗА 1 SEC», ФЛАГМАНСКАЯ ФИЧА ВЛАДЕЛЬЦА) — NO-OP НА ЖИВОМ ПРЕВЬЮ ДЛЯ КОНТЕЙНЕРНЫХ АППОВ** — `POST /api/projects/{id}/rollback` (rollback.py) ревертил git (`repo_svc.checkout`) и слал новый скриншот (`enqueue_preview`), но — в отличие от build (messages.py:3683), edit и style_patch (style_patch.py:209) — НИКОГДА не пушил реверт-файлы в живой dev-контейнер через `orchestrator_client.hot_reload`. Контейнерные аппы (nextjs_entities/fullstack/spa) отдают превью из работающего `omnia-dev-<slug>` (ingress.py:43), и в контейнере НЕТ git (hot_reload пишет файлы напрямую) → после отката git держит СТАРУЮ версию, а живой апп продолжает отдавать пост-edit код. Статика (blank/landing/portfolio/blog) ре-рендерит превью из файлов репо → откатывается корректно; дыра контейнер-онли. Класс: вся ось «откат» на флагман-аппах мёртв — юзер жмёт «вернуться назад», таймлайн обновляется, а реальный апп не меняется | apps/api/src/omnia_api/routers/rollback.py:50,66 | **FIXED e39931a** (run #42, live-репро dogfood-rollback-crm-2af92e: blank→nextjs_entities BS-4 OK; state A hero «Управляйте ремзоной без хаоса» → edit hot_reload state B «ДОГФУД-Б-МАРКЕР» в живой контейнер → POST /rollback к снапу A = 200 OK, git-снап реверта держит A, но контейнерный src/app/page.tsx ВСЁ ЕЩЁ отдавал B = git и апп разошлись; фикс — на `_CONTAINER_NEXT` читать реверт-дерево `read_files(new_sha)` + `hot_reload(slug,files)` best-effort, паритет style_patch; задеплоено api/worker, health /api/projects 401 извне; live-verify на DEPLOYED-бинаре: контейнер B→A на откате; тест test_rollback_container_hot_reload.py 8 passed +1 strict-xfail (deletion-aware sync — реверт-дерево, ДРОПНУВШЕЕ файл, оставляет орфан: hot_reload пишет, но не удаляет → BS-42 follow-up))
- BS-43 | 2026-06-17 | **FREEFORM-ПИСАТЕЛЬ ГАЛЛЮЦИНИРУЕТ CDN-URL ВНЕШНЕЙ БИБЛИОТЕКИ (404) → ИНТЕРАКТИВНЫЙ ИНСТРУМЕНТ DOA (ВЫГЛЯДИТ ГОТОВЫМ, НЕ ДЕЛАЕТ НИЧЕГО), И НИ ОДИН ГАРД НЕ ПРОВЕРЯЕТ, ЧТО `<script src>` РЕЗОЛВИТСЯ** — на чистом запросе-инструменте «генератор QR-кодов» (run #43, live-gen dogfood-qr-generator-43-826f8c, freeform/preset=local-services/deepseek-v4-pro-thinking/cost 7.42₽/passes 2) писатель выдал ПОЛНОСТЬЮ корректный по структуре инструмент: реальный `<canvas id="qr-canvas">`, live-`input`-листенер, селектор размера, скачивание PNG через `toDataURL`, инициализирующий `generateQR()` на DOMContentLoaded — НО подключил библиотеку по `<script src="https://cdn.jsdelivr.net/npm/qrcode@1.5.3/build/qrcode.min.js">`, который **404** (этого пути не существует: npm-пакет `qrcode`/node-qrcode шипит `lib/browser.js` — CommonJS-модуль, а НЕ browser-global UMD-бандл; `build/qrcode.min.js` нет ни в 1.5.3, ни в 1.5.4, ни на unpkg). Итог: `typeof QRCode === "undefined"` → `QRCode.toCanvas` бросает `QRCode is not defined` (pageerror) → canvas остаётся пустым (chromium: 250000 dark / 0 light px) → **QR не генерируется НИ В ОДНОМ браузере, на любом устройстве**. Скриншот (`_routine/runs/2026-06-17T01-39Z/01_qr_desktop.png`): отполированный тёмный лендинг «QRAURA — Мгновенные QR-коды в высоком разрешении» с полем/селектором/кнопкой «Скачать PNG» и ПУСТЫМ боксом превью — классический static-theater, но хуже: импортирована реально-звучащая библиотека по несуществующему пути. Tailwind-CDN (тот же jsdelivr-класс) ЗАГРУЗИЛСЯ (его warning в консоли) → https-egress есть, дело именно в 404-пути, не в сети. Корень: system-prompt (00_system_prompt.md) даёт СПИСОК approved-ассетов (Tailwind/Lenis/anime.min.js/omnia-kit.js — строки 740,964,987-988), но НЕТ правила для библиотек ВНЕ этого списка → для любого инструмента, чью библиотеку промпт не предусмотрел (QR, штрихкод, signature-pad, PDF, …), писатель импровизирует CDN-путь и пайплайн его не валидирует. Lenis на строке 740 («подключи Lenis по CDN» без пинованного URL) — тот же риск. Приёмка СЛЕПА к этому: `vision_attempt0` vision_ran=False (визуальный судья даже не запускался — упал в structural), structural-гейт видит только мёртвые якоря (`href="#api"`) + типографику, а пустой `<canvas>` от 404-скрипта DOM-проверкой не ловится → шипнут DOA (acceptance_score_only). Класс: ЛЮБОЙ freeform-инструмент/виджет, требующий библиотеки вне approved-набора. ДИЗАЙН при этом здоров ≈8/10 (тёмная luxe-палитра, иерархия, проф; ui_audit 6/10 — typography_count/4 button-heights/icon-stroke) | apps/api system-prompt библиотек-правило (00_system_prompt.md:740,964) + НЕТ build-time валидации внешних `<script src>` (link_validator.py делает только in-page якоря) + accept_gauntlet vision_ran=False | **PROPOSAL P-CDNDEAD** (фикс мульти-поверхностный + node-qrcode API не имеет global-UMD на CDN → naive URL-swap НЕ сработает (qrcodejs/qrcode-generator живут на CDN, но API другой: `new QRCode(el,text)` ≠ `QRCode.toCanvas`); build-time HEAD-валидация всех внешних script-src — горячий путь + сетевая флакость → не шипить вслепую) — evidence `_routine/runs/2026-06-17T01-39Z/` (final.html, 01_qr_desktop.png, console-dump). **CONFIRMED НА РЕАЛЬНОМ ПРОДЕ (run #44, 2026-06-17):** настоящий (НЕ-sandbox) юзер-проект `8dd41e42-b46c-4a5c-a8c8-f0ce24da252e` на тот же запрос «генератор QR-кодов» сгенерил ИДЕНТИЧНЫЙ мёртвый URL `qrcode@1.5.3/build/qrcode.min.js` (re-probe: **404**) + call-site `QRCode.toCanvas` (prod-дамп `08_post_palette_guard.html:19,336`) → это уже шипится живым юзерам, не догфуд-артефакт. Re-verify «naive URL-swap не сработает»: GH-build node-qrcode (`gh/soldair/node-qrcode@master|@1.5.3/build/qrcode.min.js`) тоже 404 (build/ gitignored из npm И из репо); jsdelivr flat-листинг `qrcode@1.5.3` = только `/lib/`+`/helper/`, никакого `/build/`. Acceptance-lock добавлен: `apps/api/tests/test_freeform_qr_library_resolves.py` — 3 green (dead-URL-сигнатура `build/`-путь; call-site требует node-qrcode API ≠ резолвящиеся qrcodejs/qrious/qrcode-generator; KIT_FILES без QR сегодня) + 1 strict-xfail `test_working_qr_library_is_bundled_as_kit_asset` (XPASS когда план A добавит `assets/qrcode.min.js` в KIT_FILES). evidence `_routine/runs/2026-06-17T02-15Z/00_PROOF.md`
- BS-44 (low-sev, cosmetic; interactivity HEALTHY) | 2026-06-17 | **FAQ-АККОРДЕОН СВОРАЧИВАЕТ ТЕКСТ, НО НЕ ПАДДИНГ — под КАЖДОЙ закрытой строкой остаётся ~20px цветной полоски** — на freeform-лендинге фитнес-студии (run #45, dogfood-fit-studio-1df6f7, freeform/blank/deepseek-v4-pro-thinking) ВСЯ интерактивность здорова end-to-end (доказано функционально): бургер `#menu-toggle`↔`#mobile-menu` тогглится (hidden True→False, visible), FAQ `.faq-question` тоггл `.faq-item.is-open` (max-height 0→600px, offsetHeight 20→72), kit-ассеты резолвятся на `/p/<slug>/assets/` (omnia-kit.js/anime.min.js/omnia-kit.css все 200, `<base href>` инжектится). ЕДИНСТВЕННЫЙ дефект: kit `omnia-kit.css:115` сворачивает `.faq-answer{overflow:hidden;max-height:0}` — клипает КОНТЕНТ, но НЕ паддинг, а writer-сниппет (`prompt_builder.py:964`) вешает `pb-5 px-6` прямо на `.faq-answer` → закрытый ответ оставляет ~20px нижнего паддинга цвета `var(--card)` под каждым вопросом (offsetHeight 20 при computed max-height 0). Видно как лёгкий лишний gap в FAQ-секции, аккордеон при этом полностью функционален. Шаренный ассет (blank/landing/portfolio/blog kits) + восстановление паддинга в open-state хрупко к выбору writer'ом pb-N → НЕ чистый blind-ship | omnia-kit.css:115 (collapse не зануляет padding) + prompt_builder.py:964 (writer вешает pb-5 на .faq-answer) | **PROPOSAL P-FAQPAD** — evidence `_routine/runs/2026-06-17T023030Z/` (NOTES.md, desktop/mobile/fullpage.png, final.html). **REPRO #2 (run #46, 2026-06-17, dogfood-saas-widgets-e4ac66, ДРУГАЯ ниша — SaaS-аналитика):** идентичный sliver на независимой генерации — headless Playwright `.faq-item .faq-answer` offsetHeight **20 (closed) → 72 (open) → 20 (closed again)**, `.faq-item.is-open` тоггл false→true→false (аккордеон полностью рабочий), kit-ассеты `omnia-kit.css/js`+`anime.min.js` все 200. Кросс-нишевое воспроизведение усиливает P-FAQPAD (детерминированный kit-баг, не ниша-специфичный артефакт)
- BS-45 | 2026-06-17 | **ADMIN-WRITES / PUBLIC-READS КОЛЛИЗИЯ ДОСТУПА — ОДНА КОНФИГ/БАННЕР-СУЩНОСТЬ, КОТОРОЙ УПРАВЛЯЕТ АДМИН, НО ЗНАЧЕНИЕ КОТОРОЙ ПОКАЗЫВАЕТСЯ НА ПУБЛИЧНОЙ ГЛАВНОЙ, ПОЛУЧАЕТ `access:"admin"` → ПУБЛИЧНЫЙ ЧИТАТЕЛЬ 401 → ФИЧА МОЛЧА ИСЧЕЗАЕТ, А ТЕКСТ СТРАНИЦЫ ЕЁ РЕКЛАМИРУЕТ** — НОВАЯ never-probed ось (live countdown timer на промо-лендинге, run #47, dogfood-promo-countdown-ed2649, promo «Чёрная пятница» магазина электроники). Discovery эскалировал промо-ЛЕНДИНГ → `nextjs_entities` (/signin=200). Писатель создал `entities/Campaign.json` `access:"admin"` (поля name/countdownEnd/isActive/promoCode) + `PublicLanding`, который делает `entities.Campaign.list({limit:1})` чтобы покормить `<CountdownChip end={campaign.countdownEnd}>`. Т.к. Campaign `admin` → анонимный публичный читатель **401** (container log: `GET /api/entities/Campaign?limit=1 401 in 441ms` = именно вызов страницы), `campaign` остаётся null → `campaign?.countdownEnd ? <CountdownChip/> : null` рендерит ПУСТОТУ → ТАЙМЕРА НЕТ (Playwright hydrated: timerDigits=false, ровно ОДНА console-ошибка `401 Unauthorized`). При этом КОПИРАЙТ страницы УТВЕРЖДАЕТ обратное: hero «таймер уже тикает», FAQ «Таймер на сайте показывает точное время до конца». (Товары — здоровы: grid 6 карточек + 6 зачёркнутых старых цен после гидрации, Product=public; «Товары скоро появятся» — лишь pre-hydration SSR-фолбэк.) Корень — КОЛЛИЗИЯ ДВУХ writer-правил доступа: prompt_builder.py:1438 («сущность, читаемую на анонимной /, объявляй access:public») framed ТОЛЬКО под ЖИВОЙ КАТАЛОГ (магазин/витрина) и предупреждает только про `owner`-сущности; prompt_builder.py:1538 («операционные данные оператора → access:admin, движок 403-ит не-админа»). Campaign — ТРЕТИЙ непокрытый паттерн (admin ПИШЕТ, public ЧИТАЕТ); ни одно правило его не разрешает, ни один гейт не ловит публичный read admin/owner-сущности | apps/api/src/omnia_api/services/prompt_builder.py:1438 (правило public-read framed под каталог) + :1538 (admin-managed → admin) | **CONFIRMED** (run #47, live prod repro: Campaign access:admin в writer-output + 401 в браузере + timerDigits=false + копирайт врёт; evidence `_routine/runs/2026-06-17T0318Z/`) → **PROPOSAL P-PUBCFG** — фикс behavioral (prompt), проверяем только re-gen'ом (вероятностно), широкий blast-radius, и НАИВНЫЙ «просто сделай Campaign public» УТЕЧЁТ секретный `promoCode` → НЕ clear+safe → не шипить вслепую

## PROPOSAL P-PUBCFG — admin-managed config с публично-отображаемыми полями (2026-06-17, run #47)
**Проблема (доказана):** писатель распознаёт только два паттерна доступа — (a) ЖИВОЙ ПУБЛИЧНЫЙ КАТАЛОГ → `public` (prompt_builder.py:1438) и (b) операционные данные оператора → `admin`/`owner` (prompt_builder.py:1538). Сущность-КОНФИГ, которой управляет админ, НО чьё значение выводится на публичной «/» (таймер кампании, промо-баннер, часы работы, объявление, контакты) — не покрыта; писатель кладёт её в `admin`, и публичный read 401-ит → фича исчезает молча. **Evidence:** run #47, `entities/Campaign.json access:"admin"`, `GET /api/entities/Campaign?limit=1 401`, Playwright timerDigits=false, hero/FAQ-копирайт обещает таймер. **Почему PROPOSAL, а не ship:** (1) фикс — поведенческое правило в prompt_builder.py, его эффект НЕДЕТЕРМИНИРОВАН (проверяется только полной ре-генерацией, и один сэмпл не доказывает робастность); (2) ШИРОКИЙ blast-radius — затрагивает КАЖДУЮ entity-генерацию; (3) ОПАСНОСТЬ УТЕЧКИ: наивное «помечай такие сущности public» сделает публично-читаемыми СЕКРЕТНЫЕ поля той же сущности (`promoCode` промокода, который должен открыться только на старте; `isActive`-флаг; внутренние заметки) — это РЕГРЕСС безопасности, а не фикс. **План (когда возьмут, аккуратно):** в prompt_builder.py добавить ТРЕТИЙ паттерн рядом с 1438/1538 — «КОНФИГ/БАННЕР, КОТОРЫМ УПРАВЛЯЕТ АДМИН, НО ЧЬИ ПОЛЯ ВИДНЫ НА ПУБЛИЧНОЙ /»: РАЗДЕЛИ — публично-отображаемые поля (дедлайн/заголовок баннера/часы) вынеси в ОТДЕЛЬНУЮ `public`-сущность (или public read-only эндпойнт), а секреты (promoCode/internal флаги) оставь в `admin`-сущности; ИЛИ, если у сущности НЕТ секретных полей — целиком `public`. ⛔ НИКОГДА не делай `public` сущность, несущую секрет. +тест-репро (lock): публичный (без cookie) GET сущности, которую читает `src/app/page.tsx`, не должен возвращать 401/403. Рассмотреть ДЕТЕРМИНИРОВАННЫЙ post-gen гейт: распарсить `page.tsx` на `entities.<X>.list/get` и сверить `entities/<X>.json` access ∈ {public} — иначе fail/auto-repair (надёжнее prompt-нуджа, не зависит от модели).

## PROPOSAL P-FAQPAD — FAQ-аккордеон должен сворачивать паддинг вместе с контентом (2026-06-17, run #45)
**Что:** закрытый `.faq-answer` оставляет ~20px нижнего паддинга (`pb-5`) под каждым вопросом, т.к. kit-CSS клипает `max-height:0`, но writer вешает паддинг прямо на сворачиваемый элемент. **Evidence:** run #45, `omnia-kit.css:115` + `prompt_builder.py:964`; функц-проба — offsetHeight закрытого ответа 20px при computed max-height 0px; открытого — 72px. **Почему PROPOSAL, а не ship:** (1) low-sev косметика, аккордеон полностью работает; (2) `omnia-kit.css` — ШАРЕННЫЙ ассет 4 шаблонов (blank/landing/portfolio/blog), регресс затронет ВСЕ freeform-лендинги; (3) чистый фикс требует либо `padding:0!important` на closed + восстановление в `.is-open` (хрупко: writer может выбрать pb-4/pb-6, хардкод значения сломает их), либо смены контракта (паддинг на внутренний wrapper, не на `.faq-answer`) — обе ноги нужно проверить на нескольких pb-вариантах. **План (когда возьмут):** в writer-сниппете (prompt_builder.py:957-964) перенести `pb-5 px-6` с `.faq-answer` на ВНУТРЕННИЙ `<div>`, оставив `.faq-answer` без собственного паддинга → `max-height:0` занулит всё; +тест-репро, что закрытый ответ имеет offsetHeight 0. **НЕ менять `omnia-kit.css` с `!important`** — фрагильно к writer-padding.

## PROPOSAL P-H1 — эскалация стека на follow-up (2026-06-16, run #1)

> ✅ **SHIPPED + LIVE-VERIFIED 2026-06-21** (commits `b3aa985` fix, `154124d` flag-on). Реализовано НЕ через `switch_to_stack` (он деструктивен — `init_repo` сносит историю), а через неразрушающий `stack_routing.pivot_static_to_app` — клон владельцева `pivot_code_to_web`: флип `project.template` static→nextjs_entities + commit, БЕЗ `init_repo`. История статики цела (старый снапшот rollback-абелен), скаффолд контейнера даёт оркестратор (у nextjs_entities нет api-side template-дира — git-репо держит только юзер-файлы). Детект — `discovery.detect_appification` (negation-aware backend-сигнал `_signal_fires` + явное «обрамление» appification; «личный кабинет» — только СИГНАЛ, не обрамление, чтобы «сделай личный кабинет на тёмном фоне» осталось дешёвой правкой). Гейт в `messages.post_prompt`: follow-up на static-class проекте + `detect_appification` → `pivot_static_to_app` + force-ORCHESTRATE (`is_first_prompt |= escalated_to_app`), за флагом `use_followup_appification` (default OFF; ON на стенде через прод-compose). **Live-proof на проде:** собранный static (`blank`) проект + «переделай это в полноценное веб-приложение: вход, личный кабинет, база записей» → `template blank→nextjs_entities`, `mode=build` (не surgical), старый static-снапшот цел рядом с новым app-снапшотом. **Отложено (Phase 4, отдельной задачей):** rollback эскалированного проекта к статике пока не восстанавливает `project.template` обратно (тот же принятый компромисс, что у `pivot_code_to_web`). Исходный анализ ниже сохранён для истории.

**Проблема (доказана):** на follow-up `is_first_build=False` → discovery и `stack_routing.switch_to_stack` ВООБЩЕ не вызываются (gate `interview_eligible = is_first_build and …`, messages.py:626; switch только внутри first-build-ветки, messages.py:675). Параллельно `decide_intent` роутит «сделай настоящее приложение» в CHEAP (BS-2). Итог: статический проект НИКОГДА не станет контейнерным аппом, что бы юзер ни писал. Live-репро (prod-контейнер): все 4 формулировки app-ification → `CHEAP`.

**Почему не фикшу вслепую:** `switch_to_stack` → `repo_svc.init_repo` создаёт СВЕЖИЙ репо с parentless-коммитом и `_upload` ПЕРЕЗАПИСЫВАЕТ MinIO-ключ репо (repo.py:79-102). На first-build это ок (пустой starter), но на follow-up это СНОСИТ существующий сайт юзера + ломает rollback в таймлайне (старые snapshot-SHA исчезают из репо). Плюс нужна consent-UX (спросить юзера перед сменой стека) — это cross-zone (apps/web).

**План безопасного фикса (для отдельного прогона/согласования):**
1. `decide_intent` (или новый `detect_appification`) — распознать «сделать настоящее приложение / вход+кабинет+БД» на static-проекте. Тайтовый набор сигналов, тест-репро против фолс-фолса (см. xfail в test_intent_triage.py).
2. На таком follow-up + `project.template == 'static'` — НЕ surgical-edit, а **non-destructive** эскалация: `switch_to_stack`-вариант, который НЕ сносит историю (новый снапшот как child текущего; старый static остаётся rollback-абельным), ИЛИ consent-turn («это статика; пересоздать как приложение? старую версию сохраню в таймлайне») через существующий `_spawn_text_turn`.
3. Health-check на песочнице `dogfood-*`: static → app, кнопки живые, rollback к старому static работает.

**Acceptance-lock (ОБНОВЛЁН — xfail снят):** `test_intent_triage.py::test_appification_followup_escalates_when_flag_on` (был xfail → теперь passing), `::test_appification_followup_stays_cheap_when_flag_off` (kill-switch), `::test_appification_false_positives_stay_cheap` (FP-корпус: «сделай личный кабинет на тёмном фоне», «добавь форму входа», «лендинг без регистрации» и др. → CHEAP), `test_stack_routing.py::test_pivot_static_to_app_*` (неразрушающий флип / идемпотентность / пропуск scaffolded-стека). Все зелёные (48 passed).

## PROPOSAL P-BS5 — вернуть tgbot/api в NL-пайплайн (2026-06-16, run #4)

**Проблема (доказана):** `telegram-bot-aiogram` и `fastapi-postgres` — полноценные, провижабельные стеки (stack_registry.py:63, `is_fullstack` true, prompt_builder `_TGBOT_STACK` + `_BACKEND_TEMPLATES`, schemas/project.py:46), но НЕДОСТИЖИМЫ из любого пользовательского запроса. discovery — единственный, кто выбирает стек по NL — имеет `_STACKS = {static, fullstack, nextjs_entities, spa}` (discovery.py:53) и `_SYSTEM`-меню, которое предлагает LLM ровно эти 4 (формат build-JSON хардкодит `"stack":"static|spa|nextjs_entities|fullstack"`, discovery.py:450). Любой out-of-vocab pick форсится в `static` (discovery.py:843). Параллельно `_DISCOVERY_STACK_TO_TEMPLATE` (stack_routing.py:47) тоже без tgbot/api — даже если discovery вернёт "tgbot", routing вернёт None → стек не переключится. Live-репро (prod-контейнер `run_discovery(force_build=True)`): «сделай телеграм-бота для записи в барбершоп» → `nextjs_entities`; «telegram бот … без сайта» → `fullstack`. Единственное упоминание tgbot в NL-слое — инструкция nextjs-промпта «скажи юзеру: пересоздай как tgbot через омнию и ОСТАНОВИСЬ» (prompt_builder.py:1124-1128) — тупик: нет ни кнопки, ни discovery-пути это сделать.

**Почему не фикшу вслепую:**
1. Минимум 3 кодовых поверхности: discovery `_STACKS` + `_SYSTEM`-меню (+ детект-сигнал «бот/telegram»), `_DISCOVERY_STACK_TO_TEMPLATE`, и first-build provisioning, который должен принять backend-only шаблон.
2. **Нет web-preview:** tgbot/api — backend-only, нет `dev_url`-страницы. Таймлайн/preview-UX ожидает живой URL → бот покажет «сломанный» preview. Нужна отдельная trim-UX для бэкенд-стеков (логи/статус вместо iframe).
3. **TELEGRAM_BOT_TOKEN:** бот без токена — мёртв при старте (main.py:38 «missing TELEGRAM_BOT_TOKEN — set it in the Omnia secrets panel»). Авто-роутинг бота БЕЗ сбора токена = DOA, хуже чем веб-апп. Нужен secrets-UX (cross-zone apps/web) ДО того как роутить.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Детект «телеграм-бот / aiogram / бот для X» (тайтовый, против фолс-фолса на «кнопка телеграма на сайте») → discovery предлагает tgbot как явный выбор/вопрос consent.
2. Добавить tgbot (и api) в `_STACKS` + `_SYSTEM`-меню + `_DISCOVERY_STACK_TO_TEMPLATE` ОДНОВРЕМЕННО (иначе двойной разрыв).
3. Backend-only preview-mode + secrets-turn (запросить TELEGRAM_BOT_TOKEN до запуска) через `_spawn_text_turn`/secrets-panel.
4. Health-check на песочнице `dogfood-tgbot-*`: запрос бота → tgbot-контейнер, preview не сломан, бот стартует с токеном.

**Acceptance-lock (уже в репо):** `apps/api/tests/test_stack_routing.py::test_tgbot_should_be_a_discovery_stack` + `::test_tgbot_should_route_to_template` (xfail strict=False — XPASS, когда фикс приземлится); `::test_tgbot_is_currently_unreachable_evidence` (зелёный сегодня).

## PROPOSAL P-EC — storefront/commerce-архетип (2026-06-16, run #5)

**Проблема (доказана, BS-6):** реальный prod-промпт «Магазин цветов «Флора»: каталог букетов, КОРЗИНА, оформление заказа с доставкой, отзывы, личный кабинет с историей» (skip_clarify=true) → BS-4 эскалация корректно подняла stack blank→nextjs_entities, entity-аппа собралась (Bouquet/Client/Order/Review + /signin + (app)/dashboard/{orders,profile,reviews}). НО покупательская воронка отсутствует: каталог за /signin, нет корзины, нет checkout. Причина — entity writer SYSTEM_PROMPT.md зашивает authed-dashboard как единственный архетип (`/dashboard` binding; «app screens, not a landing»; route-per-entity+dashboard). Bouquet=access:public, но публичный каталог-UI не генерится; CTA «В каталог»→/signin.

**Почему не фикшу вслепую:** добавить storefront-воронку — это НЕ prompt-твик. Нужны: (1) распознавание commerce/storefront-ниши (магазин/каталог/корзина/доставка/заказ) vs internal-tool; (2) публичные роуты `src/app/(shop)/{catalog,product/[id],cart,checkout}` помимо authed (app)/dashboard; (3) клиентская корзина (state) + checkout, пишущий Order для гостя/клиента; (4) kit-компоненты витрины (ProductGrid/ProductCard/CartDrawer/CheckoutForm) — часть уже есть (storefront-hero, gallery-grid), но cart/checkout/public-catalog нет. Это новый template-архетип + writer-инструкции + verify по нишам (магазин/доставка/услуги) → крупно и кросс-зонно (orchestrator templates + api system-prompt). Шип вслепую сломает текущие dashboard-аппы.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Сигнал «storefront»: niche/brief содержит каталог+корзина+заказ/доставка → пометить gen как commerce.
2. Entity template: добавить опциональную public-shop ветку (catalog/product/cart/checkout) рядом с (app)/dashboard; CTA лендинга «В каталог» → публичный `/catalog`, НЕ /signin.
3. Order: гостевой/клиентский checkout-путь (не только owner-CRUD).
4. Verify на песочнице dogfood-shop-*: гость видит каталог без логина, добавляет в корзину, оформляет заказ; админ-dashboard остаётся для владельца.
**Дополнительно (BS-6b, отдельно):** entity-палитра — заставить writer переопределять `:root`-токены под brief-палитру (а не inline-хекс), ИЛИ детерминированный entity-palette-guard, инжектящий `:root{--primary…}` из brief → чинит невидимые/рассинхроненные shadcn-компоненты.
## PROPOSAL P-BS7 — консьюмер-букинг не должен запирать запись за клиентским auth (2026-06-16, run #5)

**Проблема (доказана live + кодом):** запрос «лендинг автосервиса … запись на ремонт онлайн» при ЯВНОМ выборе «Лендинг» в quiz → discovery собирает `nextjs_entities` (контейнерный entity-app со staff-кабинетом), и сгенерённый page.tsx делает ВСЕ конверсионные CTA `href="/signin"` (hero «Записаться онлайн», 3× прайс-карты «Записаться», CTA-band) — клиент обязан зарегистрировать аккаунт, чтобы записаться на услугу. Корень: `_BACKEND_SIGNALS` (discovery.py:90) содержит lead-capture-стемы «запись на», «бронирован», которые трактуются как нужда в аккаунтах/CRUD; `_infer_stack_from_text` (discovery.py:130, upgrade-net 867-870) форсит nextjs_entities; негативный net `_explicit_no_backend` (879-885) рескьюит ТОЛЬКО дословное «без регистрации». Live: «запись на ремонт» / «запись на стрижку» / «бронирование столика» → все `nextjs_entities`; «лендинг кофейни» → None. Явный quiz-pick «Лендинг» в stack-решении не участвует.

**Почему не фикшу вслепую:**
1. **Двунаправленный риск.** Просто вырезать/сузить «запись на»/«бронирован» из `_BACKEND_SIGNALS` → недо-эскалация настоящих букинг-/CRM-аппов («система записи с кабинетом мастера», «бронирование с личным кабинетом»), которые backend реально нужен. Сигнал амбивалентен: «запись на стрижку» (consumer lead) vs «управление записями» (staff CRUD) различаются контекстом, не стемом.
2. **Архитектурный пробел.** Правильный результат для консьюмер-лендинга с записью — public lead-capture форма (имя+телефон+услуга+время → staff), БЕЗ клиентского аккаунта. У Omnia нет стека/паттерна между `static` (нет backend для лида) и `nextjs_entities` (полный auth-gated entity-app). Лид-форме нужен куда-то слать заявку.
3. **Даже при правильном стеке** customer-facing «Записаться» не должен вести на /signin — это template/prompt-builder-уровень (writer сам выбрал href:"/signin"). Фикс рассыпан по поверхностям.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Различить consumer-lead-booking vs staff-CRUD-booking: «запись на <услуга>» / «бронирование <ресурс>» на ЛЕНДИНГЕ без слов про кабинет/аккаунт/роли → lead-capture, не auth-stack. Тайтовый детект против фолс-фолса; учитывать явный quiz-pick «Лендинг» как сигнал-кап.
2. Дать lead-capture-путь: либо static-лендинг с формой → существующий backend для заявок (email/CRM-стаб), либо новый «landing+leadform»-вариант nextjs_entities, где CTA ведёт на public `/booking` форму, а НЕ на /signin (auth — только для staff-кабинета, отдельным входом).
3. Health-check на песочнице `dogfood-*`: запрос букинг-лендинга → primary CTA = живая форма записи (имя/телефон/услуга), НЕ /signin; заявка долетает.

**Acceptance-lock (уже в репо):** `apps/api/tests/test_stack_routing.py::test_consumer_booking_landing_should_not_force_customer_auth` (3 xfail strict=False — XPASS, когда фикс приземлится) + `::test_consumer_booking_landing_is_currently_force_escalated_evidence` (3 зелёных сегодня).

## PROPOSAL P-AUTHGATE — серверный гейт защищённой `(app)`-группы (2026-06-16, run #6)

**Проблема (доказана, BS-8):** entity-аппы защищают `(app)`-группу только клиентски. Live-репро (dogfood-crm-live-b4c0c7, anon без cookie извне): `GET /dashboard` и `/dashboard/clients` → **HTTP 200 + полный app-shell** (sidebar «Дашборд/Клиенты/Заметки»), редиректа на /signin нет. `GET /api/entities/Client` (anon) → 401 — данные owner-scoped, НЕ текут. Корень — writer-генерённый `(app)/layout.tsx` = `"use client"` с `auth.me()` в `useEffect`→`router.push("/signin")`: shell рендерится+гидрируется до редиректа, без JS гейт не срабатывает. Это верно по SYSTEM_PROMPT.md (L78 клиентский `auth.me()` = ОСНОВНОЙ; L79 серверный `requireUser()` = опциональный «you may»; L118 определяет route-group layout без упоминания auth). `requireUser()` (session.ts:54) уже делает server-`redirect('/signin')` — кирпич есть, writer не направлен его применить к layout. Вторичный баг: `safeCollection` (sdk:112-119) глотает 401 в `[]` → истёкшая сессия видит пустой апп.

**Почему не фикшу вслепую:** это security-чувствительная auth-правка, кросс-зонная (orchestrator templates), требует пересборки base-образа + regen-verify:
1. Option A (middleware.ts): наивный `export { auth as middleware }` тащит node-postgres/drizzle auth-конфиг в EDGE-runtime → ломает билд КАЖДОГО нового аппа. Нужен split edge-safe (jwt-only) auth-конфиг + matcher, исключающий `/`, `/signin`, `/signup`, `/api/auth`. Неверный matcher = лочит юзеров / 500-ит публичный лендинг на каждом аппе.
2. Option B (промпт): сделать `(app)/layout.tsx` СЕРВЕРНЫМ компонентом с `await requireUser()` до рендера (клиентский `auth.me()` — только для in-page UI). Меньше, но LLM-вероятностно + base-rebuild + regen-verify.
Оба = medium-risk → PROPOSAL, не блайнд-шип, один фикс/прогон.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Решить A vs B (A детерминированнее: гейт не зависит от LLM; но сложнее edge-safe-конфиг).
2. Для B — добавить в SYSTEM_PROMPT MANDATORY-директиву: route-group `(app)/layout.tsx` — серверный, `await requireUser()` до рендера; обновить acceptance-lock на XPASS.
3. Заодно: `safeCollection` на 401 не глотать молча — пробросить/триггерить релогин (отдельно, чтобы истёкшая сессия не выглядела пустой базой).
4. Health-check на dogfood-*: anon GET /dashboard → 307→/signin (а не 200+shell); залогиненный — апп работает; публичный лендинг + /signin/signup живы.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_auth_gate.py` — `test_server_gate_building_block_is_already_shipped` + `test_entity_template_ships_no_route_middleware_today` (green evidence) + `test_protected_route_group_layout_must_be_server_gated` (xfail strict=False — XPASS, когда серверный гейт станет обязательным/появится edge-safe middleware).

## PROPOSAL P-RBAC — мульти-ролевой доступ для entity-аппов (2026-06-16, run #7)

**Проблема (доказана live + кодом, BS-9):** реальный prod-промпт «веб-приложение для школы №7 … личные кабинеты ученик/родитель/учитель (учитель ставит оценки, ученик и родитель только смотрят)» (skip_clarify=true) → stack корректно эскалировал blank→nextjs_entities, AD-бриф спроектировал 3 роли с per-role access (Student: teacher write/parent+student read; JournalEntry: teacher write/student+parent read-own; Parent: parent read). НО рантайм поддерживает ровно 3 режима — `AccessPolicy = "owner"|"public"|"admin"` (registry.ts:46). Writer ВЫНУЖДЕННО схлопнул: 6 сущностей → `public`, JournalEntry → `owner`. Live-репро (dogfood-school-zhurnal-da6a5c, external curl): anon `GET /api/entities/Parent` → **HTTP 200 + телефоны родителей** (`"+7 (993) 584-93-56","Мария Соколова"`) — персданные в открытый интернет (public-read открыт всем, engine.ts:60-61); anon `GET /api/entities/JournalEntry` → 401 owner-scope → залогиненный родитель/ученик (роль "user", auth.ts:149 даёт только admin|user) читает только own-created строки → **пустой журнал, оценки ребёнка не видны**. Role-conditional UI из брифа («кнопка "Выставить оценку" только для роли "Учитель"») не может сработать — роли "teacher" не существует.

**Почему не фикшу вслепую:** реальный RBAC = крупная кросс-зонная security-правка:
1. **engine + registry:** расширить `AccessPolicy` за owner|public|admin → app-specific роли + relationship-scoped read-own (родитель видит строки СВОЕГО ребёнка через `parentId`-связь, а не `created_by`). Это новая модель скоупинга в `authorize()`/`listRecords` (engine.ts:53-67,166-169).
2. **auth:** `roleForNewUser` + назначение ролей — кто-то должен мочь сделать юзера «учителем»/«родителем»; нужна role-assignment UX (cross-zone apps/web) + JWT-роль за пределами admin|user (auth.ts:98-117,149).
3. **writer-промпт:** SYSTEM_PROMPT должен уметь декларировать роли и маппить compound-access брифа на них, а не флэтить в public/owner.
4. **Регрессия:** наивно поменять дефолт `public`→что-то закрытое сломает текущие публичные витрины/каталоги; поменять флэттинг writer'а вероятностно. Security-sensitive (сейчас УТЕЧКА персданных) → требует пересборки base-образа + regen-verify по нишам (школа/клиника/маркетплейс).

**План безопасного фикса (для отдельного прогона/согласования):**
1. Решить модель ролей: фиксированный набор (owner/admin/member) + relationship-scope, ИЛИ декларируемые per-app роли в entity-JSON. Relationship-scoped read-own (`scope: {via: "parentId"}`) закрывает «родитель видит своего ребёнка».
2. До полноценного RBAC — СУЗИТЬ утечку: сущности с явно персональными полями (phone/email родителя/ученика) НЕ должны флэтиться в `public` (anon-read). Минимальный кирпич: writer-директива «персданные → owner/admin, не public» + детект persональных полей.
3. Health-check на песочнице dogfood-*: учитель ставит оценку → родитель ребёнка видит ИМЕННО её, посторонний/anon — нет; персданные не читаются анонимно.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_rbac_gap.py` — `test_access_policy_has_only_three_modes_today` + `test_unknown_access_is_silently_coerced_to_owner_today` + `test_new_users_can_only_be_admin_or_user_today` + `test_engine_only_distinguishes_admin_today` (4 green evidence) + `test_entity_runtime_should_support_app_specific_roles` (xfail strict=False — XPASS, когда per-app роли + relationship-scope приземлятся).

## PROPOSAL P-BLOG — мультистатейный блог должен делать каждую статью достижимой (2026-06-16, run #11)

**Проблема (доказана live + кодом, BS-14):** prod-промпт «блог о путешествиях «Дальние страны»: лента статей, ОТДЕЛЬНАЯ СТРАНИЦА для каждой статьи (полный текст, фото, автор, дата), рубрики с фильтром, «Об авторе», подписка» (skip_clarify=true) → freeform-static (template=blank/gen_mode=freeform). Writer дал полный текст 1 featured-статье (in-page `#article-kyoto`) + 4 article-card-тизера БЕЗ ссылки/обработчика → 4 из 5 статей нечитаемы. Категорийный фильтр и подписка работают; «Об авторе» — in-page секция (не страница, что для static приемлемо). Картинки у реального юзера грузятся (200/490KB — verified с Mac); «отдельная страница для каждой статьи» структурно невыполнима в одностраничной статике.

**Почему не фикшу вслепую:**
1. **Нет blog/content-collection-архетипа.** `STATIC_TEMPLATES={blank,landing,portfolio,blog}` (prompt_builder.py:2297) — это лишь разные стартовые index.html + ассеты, НЕ мультистраничник со статья-страницами. discovery не выбирает `blog` по NL (проект создаётся `blank`, discovery роутит лишь static/spa/nextjs_entities/fullstack). Дать «страницу на статью» = либо мультистраничная static-генерация (writer создаёт article-1.html… в том же ответе — промпт это допускает на L496, но writer не делает для feed), либо новый content-collection-стек. Крупно.
2. **Детерминированный авто-репэйр FP-опасен.** При 4 actionless-карточках и 1 in-page-статье ссылать все 4 на ту же статью = вводить в заблуждение; «обернуть в `<a href="#article-X">`» требует, чтобы для каждой карточки СУЩЕСТВОВАЛА своя секция — её нет. Детект «интерактивно-стилизованная карточка без `<a>`/onclick» сам по себе FP-склонен (декоративные карточки легитимны) → нельзя как блокирующий гейт («flip gates only after generator is good»).
3. **Слой промпта вероятностен.** Правило уже есть (prompt_builder.py:496-500), writer его нарушил; усиление = LLM-вероятностно + base-rebuild + regen-verify по нишам (блог/каталог/портфолио/новости).

**План безопасного фикса (для отдельного прогона/согласования):**
1. Сигнал «content-collection» (блог/журнал/новости/портфолио-кейсы/каталог-как-чтение): N однотипных item-ов, каждый со своим контентом → пометить gen как collection.
2. Дать достижимость: либо writer-директива MANDATORY «каждый item фида = либо своя `<article-slug>.html` (создаётся в этом же ответе), либо in-page секция + card→anchor; запрещено оставлять item без входа» (усилить L496-500 + few-shot), либо content-collection-генерация со списком+детал-страницами.
3. Health-check на песочнице dogfood-blog-*: каждая карточка статьи кликабельна и ведёт к полному тексту; фильтр/подписка живы.

**Acceptance-lock (уже в репо):** `apps/api/tests/test_blog_feed_reachability.py` — `test_actionless_article_cards_are_invisible_to_dead_link_finder` (green evidence: `find_dead_links` не видит карточки без `<a>`) + `test_blog_feed_article_cards_should_be_reachable` (xfail strict=False — XPASS, когда каждая статья станет достижимой или валидатор начнёт флагать actionless-карточки).

## PROPOSAL P-UNIQUE — объявляемая+enforced уникальность поля для entity-аппов (2026-06-17, run #24)

**Проблема (доказана live + кодом, BS-27):** entity-движок не имеет уникальности нигде. Сгенерённый апп не может объявить поле уникальным, а `createRecord` не дедупит → добавление идентичной записи создаёт дубль с новым id, без 409, без сигнала. Резкий контраст: платформа УМЕЕТ уникальность для `users.email` (реальный PG unique-индекс db/schema.ts:50 + 409 в api/auth/register/route.ts:48) — но эта машинерия проведена ТОЛЬКО к фиксированной auth-таблице. Live-репро (throwaway omnia-dev-dogfood-uniq-probe из DEPLOYED base, starter Task, owner-auth): CRM-оператор добавляет одного клиента 3× → 3× 201, 3 distinct id, ALL_SAME_DATA, exact-filter возвращает все 3 (неразличимы), UNIQUENESS_ENFORCED=NO. Два слоя: (1) `FieldDef` (registry.ts:34-43) без `unique`-флага → бриф «без дублей клиентов / уникальный SKU» невыразим; (2) `createRecord` (engine.ts:213-234) только zod+insert, нет SELECT-проверки, нет unique-индекса на `records.data->>field`.

**Почему не фикшу вслепую:** реальная уникальность мульти-поверхностна и требует семантического решения:
1. **registry + SYSTEM_PROMPT:** новый `unique?: boolean` в `FieldDef` + `normalize()` его проносит; writer-директива — какие поля помечать unique (естественный ключ: email/phone/SKU/slug/username).
2. **enforcement:** либо partial unique-индекс на `(entity, (data->>field))` per-declared-field (создаётся в init-db.mjs по схеме сущности — но схема живёт в JSON, а индекс в Postgres → нужен путь миграции/idempotent-ensure при hot-reload), либо pre-insert SELECT-проверка в `createRecord` (race-небезопасна без индекса). Правильно — индекс (атомарно, как users.email).
3. **СКОУП:** global vs per-owner. Два разных владельца, добавляющих один SKU — возможно ок (каждый ведёт свой каталог); один владелец дважды — нет. Скоуп `(created_by, entity, field)` для owner-сущностей vs `(entity, field)` для public/admin → решение на политику.
4. **409 UX:** unique-violation → дружелюбный 409 через engine→route→SDK (`ApiError`)→`EntityForm` inline-ошибка «уже существует», ровно как register-route для email. Без этого violation = generic 500.
5. **Регрессия:** наивно повесить unique на существующие данные с дублями → миграция упадёт; нужен ensure-index fail-soft. base-rebuild + regen-verify по нишам (CRM-клиенты, каталог-SKU, букинг-слоты).

**План безопасного фикса (для отдельного прогона/согласования):**
1. `unique`-флаг в registry + writer-директива пометки естественных ключей.
2. Ensure partial unique-индекс per unique-поле в init-db (idempotent, fail-soft на существующих дублях) со скоупом owner-vs-global.
3. 409-путь engine→SDK→EntityForm (переиспользовать register-409-паттерн).
4. Health-check на песочнице dogfood-*: добавить клиента/SKU дважды → второй 409 «уже существует», не молчаливый дубль; разные владельцы с одним значением (для per-owner-скоупа) — ок.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_uniqueness_gap.py` — `test_field_def_has_no_unique_flag_today` + `test_create_record_never_dedupes_today` + `test_records_table_has_no_uniqueness_constraint_today` (3 green evidence) + `test_entity_engine_should_support_a_unique_field` (xfail strict=False — XPASS, когда `unique`-флаг + enforcement + 409 приземлятся).

## PROPOSAL P-LEAD — у сгенерённой lead-формы должен быть настоящий приёмник (2026-06-16, run #12)

**Проблема (доказана код + 2 реальных артефакта, BS-15):** freeform-static лендинги генерят контакт/lead/подписка-формы, которые собирают имя/телефон/email и МОЛЧА их выбрасывают, показав фейковый success. Live-доказательство (repo_svc.read_files прямо из прод-хранилища):
- `dogfood-mortgage-calc-tool-db81b7` hero lead-форма (имя+телефон): submit-handler `e.preventDefault()` → читает `name`/`phone` в локальные переменные → `ctaForm.style.display='none'; ctaSuccess.classList.remove('hidden')`. `action=`0, `mailto:`0, `formspree`0, `fetch(`0 → PII прочитан и ВЫБРОШЕН.
- `dogfood-travel-blog-029341` подписка: `<form onsubmit="event.preventDefault(); this.innerHTML='Спасибо! Проверьте почту.'">` — email ниоткуда не читается, никуда не шлётся.
Корень: (1) freeform-промпт (prompt_builder.py:491-493, 606-607) санкционирует «action = mailto: либо formspree как ПЛЕЙСХОЛДЕР» — placeholder-formspree не сконфигурен, лид теряется; (2) `routers/public.py` GET-only — нет POST lead-эндпойнта, куда форма могла бы слать заявку. `find_dead_links` слеп к форме без `<a>` (link_validator.py:24-46).

**Почему не фикшу вслепую:** правильный фикс — настоящий lead-pipeline, кросс-зонно и архитектурно:
1. **apps/api:** публичный POST lead-receiver (напр. `POST /api/public/{slug}/leads`) с rate-limit/anti-spam + хранение заявок (новая таблица `leads`), привязка к project/owner.
2. **apps/web:** owner-видимый inbox заявок (где владелец читает лиды) — иначе «доходит», но невидимо.
3. **writer-промпт:** директива — форма POST'ит на этот receiver (`fetch('/api/public/<slug>/leads', …)`), а не mailto-плейсхолдер; success-state только ПОСЛЕ 2xx. base-rebuild не нужен (static writer = prompt-only), но regen-verify по нишам.
4. Наивный prompt-only нудж на `mailto:` — клуджи/часто-мёртв на мобиле и НЕ owner-видим (не закрывает дыру). Поэтому = PROPOSAL, один фикс/прогон.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Решить sink: встроенный lead-receiver+inbox (полноценно, но cross-zone) ИЛИ минимальный «leads» эндпойнт+таблица + временный owner-вид в дашборде.
2. Writer-директива: контакт/lead/подписка-форма ОБЯЗАНА слать данные на receiver и показывать success только при успехе; запрет на «success без отправки».
3. Health-check на dogfood-*: отправка формы → запись в leads видна владельцу; success-state не показывается при сетевой ошибке.

**Acceptance-lock (уже в репо):** `apps/api/tests/test_lead_capture_sink.py` — `test_generated_lead_forms_capture_pii_but_have_no_sink` + `test_dead_link_finder_is_blind_to_a_lead_form_that_drops_data` (green evidence) + `test_generated_lead_forms_should_reach_a_real_destination` (xfail strict=False — XPASS, когда лид станет доходить до настоящего owner-видимого приёмника).

## PROPOSAL P-REFCOL — reference-поле должно быть видно в списке по умолчанию (2026-06-16, run #13)

**Проблема (доказана код + 2 реальных артефакта, BS-16):** `<CrudResource>` авто-расширяет reference-поля (crud-resource.tsx:171-184: `expand = fields.filter(f=>f.kind==="reference").map(f=>f.name)`, мёржит в list-params → `row._expanded.<ref>` есть на каждый list), НО колонки берутся из props дословно (crud-resource.tsx:168, только `sortable`-дефолт) — никакой колонки из reference-полей не выводится, и SYSTEM_PROMPT этого не требует. Итог: если writer не добавил ref-колонку вручную, расширенные данные грузятся и выбрасываются, а relation невидим в списке. Live-вариативность (один генератор, repo_svc.read_files): dent-crm appointments/page.tsx рендерит `{key:"patientId",render:(r)=>(r._expanded?.patientId)?.name ?? "—"}`+doctorId ✓; crm-verify bookings/page.tsx (cols=дата/время/услуга/цена/статус) и notes/page.tsx («Текстовые примечания к клиентам», cols=текст/дата) — clientId есть в `fields` (форма), но НЕ колонка → в списке не видно клиента. Picker формы при этом здоров в обоих (kind:"reference"+refEntity → EntityForm `entities[refEntity].list({limit:200})` → `<Select>`).

**Почему не фикшу вслепую:**
1. **Template-change → base-rebuild.** Правка `crud-resource.tsx` требует пересборки base-образа `omnia-template-nextjs-entities:dev` на проде (git pull недостаточно).
2. **Нужен regen-verify по нишам, а live-gen сейчас заблокирован WALLET-EMPTY** (vsegpt out of budget) — не могу прогнать свежую генерацию, чтобы доказать фикс end-to-end на dogfood-*. Без этого — не шипить (правило: фикс доказан логом+скриншотом + health-check).
3. **Риск авто-инъекции.** Слепо добавлять колонку на КАЖДОЕ reference-поле может (а) дублировать колонку, которую writer уже добавил под другим `key`, (б) показать вторичный/служебный ref, который writer намеренно скрыл. Нужен дедуп по уже присутствующим колонкам + аккуратный label-резолв (`_expanded[name]?.name ?? title ?? label ?? "—"`).

**План безопасного фикса (для отдельного прогона/согласования):**
1. В CrudResource: после нормализации колонок — для каждого reference-поля, чьего `name` НЕТ среди `columns[].key`, авто-добавить колонку `{key:name, header: field.label, render:(r)=>(r._expanded?.[name])?.name ?? "—"}` (детерм., дедуп по key, в конец после первой title-колонки или рядом). Поведение опционально-отключаемо пропом на случай намеренного скрытия.
2. Альтернатива/дополнение: MANDATORY-директива в SYSTEM_PROMPT — «каждое reference-поле обязано присутствовать колонкой списка, рендерящей имя связанной записи (`_expanded`)».
3. Health-check на песочнице dogfood-* (когда live-gen разблокируется): список записей с родителем (booking→client, note→client, appointment→patient) показывает имя родителя в колонке, не пусто и не UUID.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_reference_column_surfacing.py` — `test_reference_picker_is_healthy_today` + `test_crud_resource_expands_reference_data_today` + `test_crud_resource_never_auto_renders_a_reference_column_today` + `test_system_prompt_does_not_require_reference_columns_today` (4 green evidence) + `test_reference_fields_should_be_surfaced_in_the_list` (xfail strict=False — XPASS, когда CrudResource начнёт авто-выводить ref-колонку или промпт её потребует).

## PROPOSAL P-CLEAR — редактирование должно уметь очищать опциональное поле (2026-06-16, run #14)

**Проблема (доказана LIVE RUNTIME + кодом, BS-17):** в сгенерённом entity-аппе можно ИЗМЕНИТЬ значение поля, но НЕЛЬЗЯ ОЧИСТИТЬ опциональное — очистка молча проваливается, а UI рапортует «Изменения сохранены». Live-репро на DEPLOYED-бинаре (omnia-dev-dogfood-barber-crm-25cc9e, starter Task: notes/due/priority опциональны; auth через public HTTPS, AuthJS ставит secure-cookie только по TLS):
- CREATE Task `notes="перезвонить клиенту в 18:00"`, `due="2026-07-01"` → 200.
- PUT `{title, priority}` — ТОЧНЫЙ payload, который шлёт форма при очистке notes+due (форма omit-ит пустые опциональные) → 200, `updated_at` сдвинулся 15:05:51→15:05:55 (апдейт ВЫПОЛНИЛСЯ).
- GET после очистки: `notes` и `due` ВЫЖИЛИ → CLEAR_BUG.
- CONTROL: PUT с новыми non-empty значениями (`title→"Стрижка Пётр"`, `notes→"новая заметка"`) — применяется. Правка значения работает; ломается ТОЛЬКО un-set.

Корень (три поверхности): (1) `EntityForm.validate()` — пустое non-required поле → `continue` ДО `out[f.name]=…`, стёртое поле не попадает в payload (entity-form.tsx:158-164); (2) `updateSchema` — каждое поле `zodForField(f).optional()`, НЕ `.nullable()` → послать `null` для очистки нельзя (zod 400-ит весь апдейт), очистка невыразима в контракте (registry.ts:176-182); (3) `updateRecord` — shallow-merge `{...existing, ...parsed.data}`, отсутствующее поле сохраняет старое (engine.ts:286). omit-on-empty (1) + merge-keeps-absent (3) = очистка исчезает; даже наивный form-only фикс (слать null) разобьётся о (2).

**Почему не фикшу вслепую:** правильный фикс мульти-поверхностный и меняет write-validation СЕМАНТИКУ каждого аппа:
1. `updateSchema` (registry.ts:179): опциональные поля → `.nullable().optional()`, чтобы `null` принимался как «очистить». Риск: null теперь валиден для ЛЮБОГО опц. поля во всех аппах — нужно проверить, что engine-merge запишет null (запишет: спред перезатирает) и что потребители (`_expanded`, фильтры, рендер колонок) корректно читают null.
2. `EntityForm.validate()` (entity-form.tsx:158-164): в EDIT-режиме поле, которое БЫЛО в `initial` (имело значение) и теперь пусто → слать `null`, а не omit-ить; в CREATE — omit как сейчас. Нужен доступ к `initial` в `validate()` и diff.
3. Edge-cases по типам: number очищенный → null (не `Number("")=NaN`); date → null; reference (`z.string().min(1)`) → требует nullable; boolean не очищается. Каждый тип — своя проверка.
4. TEMPLATE-change → пересборка base-образа `omnia-template-nextjs-entities:dev` на проде + regen-verify по нишам. Verify можно сделать БЕЗ генерации (throwaway-контейнер из пересобранного base со starter-Task + повтор runtime-репро), но сама правка схемы рискованна → PROPOSAL, один фикс/прогон.

**План безопасного фикса (для отдельного прогона/согласования):**
1. `updateSchema` → `.nullable().optional()` для опц. полей; убедиться, что engine-merge пишет null и читатели его переживают.
2. `EntityForm` — в edit-режиме (есть `initial`) emptied-поле с непустым `initial` слать как `null`; типовые edge-cases (number/date/reference).
3. Health-check БЕЗ генерации: пересобрать base, поднять throwaway omnia-dev-* со starter Task, повторить runtime-репро — очистка теперь стирает (`notes`/`due` → null/пусто), CONTROL-смена и CREATE не задеты.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_field_clear_gap.py` — `test_form_omits_empty_optional_fields_today` + `test_update_schema_makes_a_clear_inexpressible_today` + `test_engine_update_is_a_shallow_merge_that_keeps_absent_fields_today` (3 green evidence) + `test_clearing_an_optional_field_should_persist_the_clear` (xfail strict=False — XPASS, когда updateSchema примет null И форма начнёт слать явную очистку в edit-режиме).

## PROPOSAL P-PAGINATE — managed-список должен достигать записи за дефолтным капом (2026-06-16, run #15)

**Проблема (доказана LIVE-RUNTIME + кодом, BS-18):** генеримый managed-список молча обрезается на 50 записях. Live-репро на DEPLOYED base-образе `omnia-template-nextjs-entities:dev` (throwaway omnia-dev-dogfood-listcap-probe-cd7c7c, starter `Task`, owner-auth через Auth.js credentials — без LLM, без генерации):
- CREATE 55 Task (#1..#55).
- GET `/api/entities/Task` (БЕЗ limit — ровно то, что шлёт `useEntity`/`CrudResource`) → **50 строк, #6..#55** (старейшие #1-#5 отсутствуют, дефолтный sort created_at DESC).
- GET `/api/entities/Task?limit=200` → 55 строк #1..#55 (все на сервере).
- GET `/api/entities/Task?page=2` → 5 строк #1..#5 (движок ПАГИНИРОВАТЬ умеет).
Корень: (1) `engine.ts` DEFAULT_LIMIT=50/MAX_LIMIT=200, listRecords клампит `Number(limit) || DEFAULT_LIMIT`→50 (engine.ts:38-39,187-189); (2) `use-entity.ts` грузит коллекцию ОДНИМ `entities[name].list(params)` (params дословно), без `limit`/`page`/`offset` и без fetch-next — комментарий «client-side paging happen in <DataTable>, not here»; (3) `crud-resource.tsx` mergedParams инжектит только `expand`, без дефолтного limit; DataTable получает `rows={data.rows}` (уже загруженные ≤50) и пагинирует их В ПАМЯТИ через `pageSize` (crud-resource.tsx:177-184,549,555) — никогда не запрашивает серверную страницу; (4) SYSTEM_PROMPT.md перечисляет `list({…limit,page})` (L71), но не велит wire-ить пагинацию, а CrudResource — «fast path for any list screen» → 50-кап = дефолт почти всех генеримых списков. Хуже мёртвой кнопки: DataTable рисует «5 страниц по 10» → список ВЫГЛЯДИТ полным, ~10% данных недостижимо без ошибки/индикации.

**Почему не фикшу вслепую:**
1. **TEMPLATE-change → base-rebuild.** Правка `use-entity.ts`/`crud-resource.tsx`/`engine.ts` требует пересборки base-образа `omnia-template-nextjs-entities:dev` на проде (git pull недостаточно) + regen-verify по нишам (verify можно без генерации — throwaway-контейнер из пересобранного base + повтор runtime-репро, как сам этот прогон).
2. **Band-aid лишь двигает обрыв.** Дефолтить managed-fetch в `limit: 200` (MAX_LIMIT) детерминированно и просто, НО (а) >200 записей всё равно исчезают; (б) каждый managed-экран начнёт грузить 200 строк + их `_expanded` ref-расширения на каждый рендер — реальная нагрузка, не замерена; меняет дефолт для ВСЕХ будущих аппов.
3. **Правильный фикс мульти-поверхностный.** Реальная server-pagination: движок возвращает total/has-more (сейчас не возвращает — `listRecords` отдаёт голый массив), `useEntity` держит page-state и рефетчит, DataTable-пейджер драйвит её вместо in-memory нарезки. Меняет контракт data-flow списка. Крупно/рискованно.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Решить: (A) минимальный — managed-fetch дефолтит `limit: 200`, закрывает подавляющее большинство малых CRM (<200 записей) детерминированно; ИЛИ (B) полноценная server-pagination (engine total + useEntity page-state + DataTable refetch).
2. Для (B): `listRecords` вернуть `{rows,total}` (или заголовок), `useEntity` экспонировать `page`/`setPage`/`hasMore`, DataTable-пейджер триггерит reload.
3. Health-check БЕЗ генерации: пересобрать base, поднять throwaway omnia-dev-* со starter Task, засеять 55+ строк → managed-список достаёт ВСЕ (limit-bump: 55/55 ≤200; pagination: страница 2 догружает #1-#5), DataTable-пейджер ходит по полному набору.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_list_pagination_cap.py` — `test_engine_caps_list_at_fifty_by_default_today` + `test_use_entity_loads_once_with_no_limit_or_page_today` + `test_crud_resource_does_not_request_a_limit_or_paginate_server_side_today` + `test_system_prompt_does_not_require_wiring_pagination_today` (4 green evidence) + `test_managed_list_should_reach_records_beyond_the_default_cap` (xfail strict=False — XPASS, когда managed-fetch дефолтит MAX_LIMIT ИЛИ появится реальная server-pagination).

## PROPOSAL P-REFINTEGRITY — entity-движок должен иметь референциальную целостность (2026-06-16, run #16)

**Проблема (доказана LIVE RUNTIME + кодом, BS-19):** entity-движок не имеет НИКАКОЙ референциальной целостности — родителя можно удалить, оставив каждого ребёнка с мёртвой ссылкой, и UI молча резолвит её в null без ошибки/предупреждения. Live-репро на DEPLOYED base-образе `omnia-template-nextjs-entities:dev` (throwaway omnia-dev-dogfood-refdel-probe-41de56, 2 сущности через docker cp: Client{name,phone}, Note{text, clientId:reference→Client}, owner-auth через Auth.js — без LLM, без генерации):
- CREATE Client «Ирина Петрова» → 200 (id b966bf5a…).
- CREATE Note {clientId: b966bf5a…} → 200 (резолвится до удаления).
- CREATE Note {clientId: "00000000-0000-…-000000000000"} → **201** — bogus-ref ПРИНЯТ (нет валидации на write).
- DELETE Client b966bf5a… → **200** — нет restrict / нет warn.
- GET Client list → [] (родитель снесён).
- GET Note?expand=clientId ПОСЛЕ удаления → обе ноты: `clientId` хранит мёртвый id, `_expanded.clientId=null` = ОРФАН (мёртвая ссылка молча обнулена).

Корень (три поверхности, код-доказан): (1) `createRecord` (engine.ts:217-226) только zod-парсит body + `insert`, не грузит target-сущность для проверки существования referenced-строки; (2) `deleteRecord` (engine.ts:295-315) удаляет по id, возвращает `{deleted:true}`, не сканит детей, нет restrict/cascade/warn; (3) `expandRecords` (engine.ts:143-148) `_expanded[field]=map.get(refId) ?? null` — мёртвый родитель неотличим от «связь не задана» → silent «—». Класс шире CRM: заказ→клиент, бронь→пациент, задача→проект, комментарий→пост.

**Почему не фикшу вслепую:** референциальная целостность — это СЕМАНТИЧЕСКОЕ решение политики, меняющее write/delete-поведение КАЖДОГО генеримого аппа, и правильная политика реально неоднозначна:
1. **RESTRICT** — блокировать delete, пока существуют дети, → 409. Безопасно для данных, но ломает «просто удалить» UX и требует сканить ВСЕ сущности на reference-поля, целящие в эту (граф связей), на каждый delete.
2. **CASCADE** — удалять детей вместе. Деструктивно, легко снести больше, чем юзер ожидал.
3. **SOFT/WARN** — вернуть affected-children count и требовать confirm. Кросс-зонно (apps/web + CrudResource confirm-UX).
Все варианты мульти-поверхностные (engine + apps/web), требуют base-rebuild + regen-verify, и data-integrity-чувствительны (сейчас тихая порча данных). Плюс отдельный пробел: нет валидации ref на write (bogus-ref принимается). Max один фикс/прогон, не блайнд-шип.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Выбрать политику delete (RESTRICT по умолчанию безопаснее всего для CRM; CASCADE опционально per-relation; WARN как UX-надстройка). Для RESTRICT — построить обратный индекс «кто ссылается на эту сущность» из всех `entities/*.json` reference-полей, на delete запросить детей → при наличии 409 с affected-count.
2. Валидация ref на write: createRecord/updateRecord — для reference-поля проверить, что target-строка существует и доступна (scope-aware), иначе 400.
3. `expandRecords` — отличать «ref задан, но мёртв» (флаг dangling) от «ref не задан», чтобы UI мог показать «удалён» вместо «—» (опционально).
4. Health-check БЕЗ генерации: пересобрать base, throwaway omnia-dev-* с Client+Note, DELETE Client с живой Note → restrict 409 (или cascade удаляет Note); write с bogus-ref → 400.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_referential_integrity.py` — `test_create_does_not_validate_reference_targets_today` + `test_delete_is_unconditional_with_no_child_scan_today` + `test_expand_silently_nulls_a_dead_reference_today` (3 green evidence) + `test_deleting_a_referenced_record_should_not_silently_orphan_children` (xfail strict=False — XPASS, когда движок станет референциально-осведомлённым на delete).

## PROPOSAL P-SEARCH — entity-апп должен уметь НАЙТИ запись при масштабе (2026-06-16, run #17)

**Проблема (доказана LIVE RUNTIME + кодом, BS-20):** в сгенерённом entity-аппе нельзя НАЙТИ запись, как только коллекция перерастает загруженную страницу — поисковик ложно сообщает «Ничего не найдено». Live-репро на DEPLOYED base-образе `omnia-template-nextjs-entities:dev` (throwaway omnia-dev-dogfood-search-probe-972063, starter `Task`, 55 записей, owner-auth Auth.js — без LLM, без генерации):
- Сид: 55 Task, OLDEST = «Zebra-UNIQUE-OMEGA-001» (заметка «найди-меня-секрет»).
- GET без params (ровно то, что шлёт useEntity) → 50 строк, MARK ОТСУТСТВУЕТ (дефолтный sort created_at DESC роняет старейшую за 50-кап).
- GET `?title=Zebra-UNIQUE-OMEGA-001` (точное полное) → 1 (запись СУЩЕСТВУЕТ, движок точное равенство умеет).
- GET `?title=Zebra` (префикс) → 0; `?title=UNIQUE` (инфикс) → 0; `?title=zebra-…` (неверный регистр) → 0 (нет ILIKE); `?notes=секрет` (substring) → 0.
- CONTROL: `?priority=high` (точное enum) → 1; `?priority=hi` (префикс) → 0 — механизм фильтра РАБОТАЕТ, ломается ровно substring/case.
- GET `?limit=200` → 55, MARK ПРИСУТСТВУЕТ (запись реально в БД).

Корень (два слоя, код-доказан): (1) `listRecords` фильтр = `data->>field = value` чистое точное равенство (engine.ts:176); нет ILIKE/substring/`%`/case-insensitive нигде в движке → нет server-side text-search примитива, к которому поисковик мог бы обратиться. (2) `<DataTable>` поиск = `String(v).toLowerCase().includes(q)` по `rows`-пропу в памяти (data-table.tsx:299), а rows = ≤50 загруженных одним `useEntity.list()` без limit (корень BS-18; use-entity.ts: «search/sort/paging happen in <DataTable>, not here»). Поиск видит только новейшие 50; что старше — невидимо, и серверного эндпойнта дотянуться нет.

**Почему не фикшу вслепую:**
1. **TEMPLATE + engine change → base-rebuild.** Правка `engine.ts` (новый search-примитив) + `use-entity.ts`/`crud-resource.tsx` (проталкивание query) требует пересборки base-образа `omnia-template-nextjs-entities:dev` на проде (git pull недостаточно) + regen-verify по нишам (verify можно без генерации — throwaway-контейнер из пересобранного base + повтор runtime-репро, как сам этот прогон).
2. **Меняет list-data-flow контракт всех аппов.** Сейчас список «загрузи раз → фильтруй в памяти». Серверный поиск = useEntity держит search-state и рефетчит (debounced), DataTable шлёт query на сервер вместо in-memory `.includes()`. Многоповерхностно, как и P-PAGINATE — и логично делать ВМЕСТЕ с пагинацией (один рефактор list-data-flow).
3. **Дизайн примитива неоднозначен.** `q`-параметр ILIKE по ВСЕМ text/string-полям (просто, но может матчить лишнее/медленно без индекса) vs per-field `field~=value` (точнее, но writer должен указать searchable-поля). Требует решения + проверки на отсутствие SQL-инъекции (drizzle-параметризация) и регресса фильтр-вкладок (exact-equality сейчас используется quick-filter'ами — нельзя сломать).

**План безопасного фикса (для отдельного прогона/согласования):**
1. Движок: добавить text-search примитив — `q`-параметр, делающий `data->>field ILIKE '%'||$q||'%'` по text/string-полям сущности (через drizzle-параметры, не конкатенацию строк); не трогать существующий exact-equality фильтр (его используют quick-filter-вкладки).
2. useEntity/CrudResource: проталкивать запрос поискового бокса на сервер (debounced) — `list({ q })` — вместо/в дополнение к in-memory `.includes()`; согласовать с server-pagination (P-PAGINATE) как единый list-data-flow рефактор.
3. Health-check БЕЗ генерации: пересобрать base, throwaway omnia-dev-* со starter Task, засеять 55+ строк где старейшая имеет уникальный title → поиск по substring находит её (server-side), не «Ничего не найдено».

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_search_findability.py` — `test_engine_list_filter_is_exact_equality_only_today` + `test_engine_has_no_substring_or_case_insensitive_search_today` + `test_managed_search_box_is_client_side_over_loaded_rows_today` + `test_use_entity_punts_search_to_the_client_table_today` (4 green evidence) + `test_entity_app_should_be_able_to_find_a_record_by_search` (xfail strict=False — XPASS, когда движок получит server-side text-search И managed read-path начнёт проталкивать query на сервер).

## PROPOSAL P-SORT — сортировка колонки должна упорядочивать весь набор, не загруженное окно (2026-06-16, run #18)

**Проблема (доказана LIVE RUNTIME + кодом, BS-21):** клик по заголовку колонки («сортируй по цене / сроку / сумме») упорядочивает только загруженные ~50 строк в памяти, поэтому глобальный экстремум вне этого окна никогда не попадает наверх — пользователь видит уверенно НЕВЕРНЫЙ топ. Live-репро на DEPLOYED base-образе `omnia-template-nextjs-entities:dev` (throwaway omnia-dev-dogfood-sortcap-probe-1bb521 port 3302, starter `Task` + встроенное `due` date-поле, owner-auth Auth.js — без LLM, без генерации):
- Сид: 55 Task, OLDEST-created = «AAA-EARLIEST-DUE-MARK» due **2018-01-01** (глобальный минимум), 54 филлера due 2026-*.
- GET без params (ровно то, что шлёт useEntity) → 50 строк, MARK ОТСУТСТВУЕТ; минимальный `due` в окне = **2026-01-15** → лучшее, что покажет клиентская «сортировка по due ↑» как ТОП.
- GET `?sort=due&order=asc&limit=200` (server-sort, весь набор) → ТОП = MARK, due **2018-01-01**.
- GET `?sort=due&order=asc` (server-sort, дефолтный 50-кап) → ТОП = MARK, due **2018-01-01**, MARK ПРИСУТСТВУЕТ — сервер сортирует ВЕСЬ набор ПОТОМ берёт топ-50, т.е. проброс sort на сервер чинит даже при капе.
- CONTROL `?limit=200` → 55, MARK есть (запись реально в БД).

Корень (два слоя, код-доказан): (1) движок ИМЕЕТ корректную типизированную server-side сортировку — `listRecords` читает `sort`/`order` (engine.ts:180-181), `fieldExpr` кастит jsonb `::numeric`/`::timestamptz` (engine.ts:74-75) → весь набор упорядочивается правильно. (2) Managed-UI её НЕ зовёт: CrudResource делает каждую колонку сортируемой по умолчанию (crud-resource.tsx:168), клик → `toggleSort` ставит клиентский стейт (data-table.tsx:363), таблица пересортировывает `rows`-проп в памяти (data-table.tsx:305-307); rows = ≤50 от одного `useEntity.list()` без `sort`/`order` (use-entity.ts:21 «search/sort/paging happen in <DataTable>, not here»).

**Почему не фикшу вслепую:**
1. **TEMPLATE change → base-rebuild + regen-verify.** Правка `crud-resource.tsx`/`use-entity.ts`/`data-table.tsx` (проброс sort-стейта в server-params) требует пересборки base-образа на проде. Live-gen сейчас WALLET-blocked → regen-verify этим прогоном недостижим (как BS-16/#13).
2. **Меняет list-data-flow контракт всех аппов.** Клик по заголовку теперь = серверный фетч (loading-стейт, дебаунс) вместо in-memory сорта. Тесно связан с BS-18 (P-PAGINATE) и BS-20 (P-SEARCH) — то же клиентское ≤50-окно; чинить сортировку отдельно, пока поиск/пагинация остаются клиентскими, непоследовательно. Логично landed вместе с P-PAGINATE как единый read-path рефактор.
3. **Малый, но не блайнд.** В отличие от P-SEARCH (нужен НОВЫЙ engine-примитив), здесь примитив уже есть и работает даже при капе — фикс «всего лишь» провод. Но мультиповерхностный TEMPLATE+regen → один фикс/прогон, не вслепую.

**План безопасного фикса (для отдельного прогона/согласования):**
1. CrudResource/DataTable: поднять выбранный sort-ключ/направление наверх (callback `onSortChange` или lifted state), CrudResource добавляет `{ sort, order }` в server-params useEntity.
2. useEntity: держать sort-state и рефетчить при изменении (`list({ sort, order })`), переиспользуя готовую engine-сортировку; согласовать с server-pagination (P-PAGINATE) как единый рефактор.
3. Health-check БЕЗ генерации: пересобрать base, throwaway omnia-dev-* со starter Task, засеять 55+ строк где старейшая имеет глобальный экстремум по типизированному полю → клик «сортируй по due ↑» поднимает её наверх (server-side), а не экстремум загруженного окна.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_sort_windowing.py` — `test_engine_has_typed_server_side_sort_today` + `test_managed_sort_is_client_side_over_loaded_rows_today` + `test_crud_resource_makes_every_column_sortable_but_never_pushes_sort_today` + `test_use_entity_defers_sort_to_the_client_table_today` (4 green evidence) + `test_managed_sort_should_order_the_full_dataset` (xfail strict=False — XPASS, когда managed read-path начнёт проталкивать `sort`/`order` на сервер).

## PROPOSAL P-DATECAST (follow-up — write-side ужесточение даты, 2026-06-16, run #19)

**Контекст:** BS-22 crash-proof READ-half ЗАШИПЛЕН (ad0a71b, `safe_to_timestamptz` — список больше не 500-ит на одной кривой дате). Остаётся WRITE-half: валидатор всё ещё принимает любую `Date.parse`-валидную строку («2025», «June 2025») и кладёт её сырой. Последствие после фикса: значение отображается странно («June 2025») и сортируется в NULL-конец, но НЕ крашит. Это косметика/корректность, не denial-of-view.

**Почему НЕ шипнуто вслепую:** ужесточение `zodForField` date (registry.ts:139) с ленивого `Date.parse` на строгий ISO-8601 (`z.iso.date()`/`.datetime()` или normalize-on-write) меняет **write-валидационную семантику КАЖДОГО date-поля во ВСЕХ генеримых аппах** — тот же класс риска, что BS-17 P-CLEAR. Конкретные риски: (1) over-rejection — если LLM-writer или demo-seeder когда-либо эмитят не-строго-ISO даты, апп начнёт 400-ить легитимные записи; (2) normalize-on-write («June 2025»→«2026-06-01T00:00:00Z») ломает exact-equality фильтр по дате (`data->>field=value` сравнивает с сырой строкой формы «2026-03-01», а сохранена будет полная ISO). Нужна проверка форматов writer/seeder + согласование с фильтр-контрактом → не вслепую.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Решить семантику: REJECT не-ISO на write (fail-loud 400) ИЛИ normalize-to-ISO (и тогда чинить фильтр под полную ISO). REJECT проще и не трогает фильтр.
2. Заменить ленивый `Date.parse`-refine на строгий ISO-чек, принимающий И `YYYY-MM-DD` (что даёт `<input type=date>`) И полный ISO-datetime.
3. Проверить, что demo-seeder и типичный writer-вывод проходят (не over-reject); health-check на throwaway base со sloppy-датой → 400 на write, а не тихий приём.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_date_cast_crash.py::test_date_write_validator_should_reject_non_iso_dates` (xfail strict=False — XPASS, когда write-валидатор станет строго-ISO).

## PROPOSAL P-EXPORT — CSV-экспорт должен покрывать весь набор, а не загруженное окно (2026-06-16, run #21)

**Проблема (доказана, BS-24):** managed `<CrudResource>` включает кнопку CSV-экспорта «Экспорт» ПО УМОЛЧАНИЮ (`exportable = true`, crud-resource.tsx:150, коммент «On by default for managed screens») на КАЖДОМ list-экране. `handleExport` строит CSV из `sorted`←`rows`-проп = уже загруженные в память ≤50 строк (data-table.tsx:373-388, `exportRows = selectedRows.length ? selectedRows : sorted`), НИКОГДА не перезапрашивая сервер. `rows` = `data.rows` от `useEntity`, который грузит один `entities[name].list()` без `limit` → дефолт движка 50 (корень BS-18). Итог: владелец жмёт «Экспорт» чтобы сделать бэкап/отчёт/выгрузку → молча получает первые 50 из N записей БЕЗ предупреждения. Хуже in-app капа BS-18: артефакт ВЫХОДИТ из приложения как доверенный авторитетный файл — недостающие строки невидимы именно в момент, когда юзер больше всего верит в полноту. Коммент хэндлера «not just the visible page» усиливает ложную уверенность (он спанит страницы клиент-пейджера, но не серверный набор). Live-репро (run #21, throwaway из DEPLOYED base, schema proj_dogfood_export_916, starter Task, owner-auth Auth.js): 55 записей → GET без params (то, что сериализует экспорт) = 50, ?limit=200 = 55, default-окно first=EXPORT-ROW-01 last=EXPORT-ROW-50 (строки 51-55 в БД есть, но в выгрузку не попадают).

**Почему НЕ фикшу вслепую:** `DataTable` презентационна (получает `rows`, не знает имя сущности), поэтому корректный экспорт — дотянуть ВЕСЬ набор (или стримить серверно) ПЕРЕД сериализацией — должен быть зашит на уровне `CrudResource` (он знает entity) ИЛИ через отдельный server-export-эндпойнт. Любой вариант трогает list-data-flow / лимиты, тесно связан с P-PAGINATE, и это TEMPLATE-изменение → пересборка base-образа на проде + regen-verify. Band-aid «экспорт грузит limit=200» лишь двигает обрыв и тянет 200 строк + ref-expand on-demand; правильный фикс — настоящий «export all» с пейджингом всего набора. Крупно/рискованно, ≤1 фикс/прогон.

**План безопасного фикса (для отдельного прогона/согласования):**
1. На уровне `CrudResource` дать `DataTable` колбэк `onExportAll` (или проп `exportFetch`), который при экспорте без выделения дотягивает полный набор (`entities[name].list` пейджингом до конца / с server-export), и кормит ИМ `rowsToCsv` вместо in-memory `rows`.
2. ИЛИ server-export route, который стримит все строки сущности (owner-scope) в CSV.
3. Поправить коммент хэндлера, чтобы не утверждал полноту.
4. Health-check на песочнице: 55 записей → CSV содержит 55 строк, а не 50.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_export_truncation.py::test_export_should_cover_the_full_dataset_not_the_loaded_window` (xfail strict=False — XPASS, когда экспорт начнёт покрывать весь набор).

## PROPOSAL P-LOSTUPDATE — оптимистическая блокировка для entity-аппов (2026-06-16, run #22)

**Проблема (доказана live + кодом, BS-25):** entity-движок не имеет оптимистической блокировки. `updateRecord` (engine.ts:285-297) читает существующую строку, делает `merged = {...existing, ...parsed.data}` и пишет целиком; WHERE — только id+entity+createdBy, без version/updated_at-предусловия → не замечает, что строка изменилась с момента загрузки редактором. Нет version/etag/If-Match НИГДЕ в entity-рантайме. Форма усугубляет: edit-режим `<EntityForm>` сидит каждое поле из загруженной записи (entity-form.tsx:82-88) + `validate()` эмитит каждое непустое (entity-form.tsx:149-167) → каждое сохранение = полный устаревший снапшот, а не патч → устаревшее поле реально шлётся и затирает чужую правку. Live-репро (DEPLOYED base, omnia-dev-dogfood-lostupdate-probe, starter Task): CREATE {priority:low}; Editor A PUT priority→high→200 (CONTROL GET=high, апдейт работает); Editor B PUT stale priority=low→**200 НЕ 409**; FINAL GET priority=**low** (A's high молча потерян), без сигнала конфликта. Сценарий: solo-владелец на телефоне+ноуте, две вкладки, double-submit.

**Почему не фикшу вслепую:** оптимистическая блокировка — это смена write-контракта КАЖДОГО генеримого аппа, мульти-поверхностно:
1. **schema/init-db:** version-колонка в `records` (или использовать `updated_at` как токен).
2. **engine:** `updateRecord` принимает `If-Match`/`expectedVersion`, добавляет предусловие в WHERE (или сравнивает version перед записью) → 409 на mismatch вместо тихого затирания.
3. **form/SDK:** грузит и шлёт текущую version при сохранении.
4. **apps/web/CrudResource:** conflict-resolution UX («запись изменилась с момента открытия — перезагрузить / перезаписать»), иначе 409 = просто новая мёртвая стена.
5. **Регрессия:** наивное предусловие сломает легитимные последовательные правки одного редактора (version должна корректно инкрементиться/возвращаться). base-rebuild + regen-verify по нишам. Data-integrity-чувствительно. Один фикс/прогон.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Выбрать токен: отдельная version-колонка (чище) ИЛИ `updated_at` как If-Match (без миграции, но грубее).
2. engine: предусловие + 409 `EngineError(409, "stale")`; вернуть актуальную строку в теле конфликта, чтобы UI показал «их» версию.
3. SDK/form: прокинуть version load→save; CrudResource — диалог конфликта (reload/overwrite).
4. Health-check на песочнице dogfood-*: A сохранил high; B (stale) сохраняет → 409 + предложение перезагрузить; после reload B видит high и его правка notes ложится поверх, А's high НЕ теряется.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_concurrent_update.py` — `test_update_is_unconditional_last_write_wins_today` + `test_no_concurrency_token_anywhere_in_entity_runtime_today` + `test_edit_form_submits_a_full_stale_snapshot_today` (3 green evidence) + `test_concurrent_edit_should_not_silently_clobber` (xfail strict=False — XPASS, когда updateRecord получит оптимистическое предусловие с 409).

## PROPOSAL P-KPICOUNT — server count/aggregate-примитив для дашборд-KPI (2026-06-17, run #23)

**Проблема (доказана live + кодом + реальным сгенерённым аппом, BS-26):** дашборд entity-аппа считает каждую headline-цифру КЛИЕНТСКИ как `.length`/`.reduce()` над массивами `useEntity`/`entities[X].list()`, которые грузятся БЕЗ `limit` → движок отдаёт DEFAULT_LIMIT=50. Live-репро (throwaway omnia-dev-dogfood-kpicount-probe из DEPLOYED base, 60 Task): GET без params = 50 строк, тело = голый `{"data":[...]}` БЕЗ `total`-поля и БЕЗ `X-Total-Count`-хедера; `?limit=200`=60; `/count`|`/stats`|`/aggregate`→500 (нет count-роута). Реальный сгенерённый CRM (demo-crm-dlia-prosmotra `dashboard/page.tsx`): `entities.Deal.list()`/`Client.list()`/`Task.list()` все без limit + `value={activeDeals.length}`, `<CountUp value={newClientsThisMonth.length}/>`, `<CountUp value={overdueTasks.length}/>`, `avgCheck=deals.reduce(...)/deals.length`. Кит САМ учит этому (dashboard-hero.tsx:71-72, count-up.tsx:13) → структурно. Итог: «Активных сделок: 50» навсегда, средний чек/конверсия считаются над новейшими 50 = скошены. Это ПЕРВЫЙ экран при логине, цифра выглядит авторитетной — хуже тихого обрыва BS-18 и обрезки экспорта BS-24.

**Почему не фикшу вслепую:**
1. **Нет примитива.** Движок не умеет считать кардинальность без выборки всех строк (кап MAX_LIMIT=200). Нужен НОВЫЙ server count (`SELECT count(*)` с теми же scope/filter-условиями, что `listRecords`) — это новая поверхность в engine + API-роут или `{rows,total}`-envelope + SDK `count()` + `useEntity` `total`. Меняет list-data-flow контракт всех аппов.
2. **Count ОДИН не чинит avg/sum/ratio.** avgCheck/conversion/выручка-фаннел — это server SUM/AVG/GROUP BY, а не count. Даже с верным total средний-над-загруженными-50 останется скошенным. Правильный фикс = маленькая aggregate-поверхность (count + sum/avg по числовому полю + group-by-enum для фаннела), а не band-aid `limit:200` (двигает обрыв + грузит 200×ref на каждый KPI).
3. **Writer-слой вероятностен + кит учит `.length`.** Надо ОДНОВРЕМЕННО дать примитив И переписать kit-гайд (dashboard-hero/count-up JSDoc + SYSTEM_PROMPT) на «KPI = server count/aggregate, не `rows.length`», иначе writer продолжит `.length`. TEMPLATE+engine → base-rebuild + regen-verify. Тесно связан с P-PAGINATE/P-SEARCH/P-SORT/P-EXPORT (вся windowing-ремедиация) — логично делать вместе.

**План безопасного фикса (для отдельного прогона/согласования):**
1. engine: `countRecords(def,user,params)` (count(*) с scope+filter) и/или `aggregateRecords` (sum/avg/group-by по whitelisted-полю); вернуть `{rows,total}`-envelope из list ИЛИ отдельный `/api/entities/[entity]/count` роут.
2. SDK: `entities[X].count(params)` + `aggregate`; `useEntity` отдаёт `total` (и опц. агрегаты) для KPI-чтения.
3. Kit + промпт: dashboard-hero/count-up JSDoc + SYSTEM_PROMPT → MANDATORY «headline-KPI = server count/aggregate, НЕ `loadedRows.length`»; обновить few-shot дашборда.
4. Health-check на песочнице dogfood-*: 60+ записей → KPI показывает 60 (не 50); средний/сумма считаются по всему набору.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_kpi_count_windowing.py` — `test_engine_list_returns_a_bare_capped_page_with_no_total_today` + `test_sdk_list_returns_bare_array_with_no_count_method_today` + `test_use_entity_exposes_only_a_capped_rows_array_today` + `test_kit_jsdoc_teaches_length_over_loaded_arrays_for_kpis_today` + `test_no_count_or_stats_route_exists_today` (5 green evidence) + `test_runtime_should_expose_a_server_count_primitive_for_kpis` (xfail strict=False — XPASS, когда появится server count/aggregate-примитив).

## PROPOSAL P-BRAND — единый бренд через все поверхности (2026-06-17, run #26)

**Проблема (доказана live-gen + кодом + screenshot, BS-29):** реальная генерация dogfood-crm-avrora-6450ab (live-gen восстановлен 2-й прогон подряд; BS-4 эскалация blank→nextjs_entities ✓, healthy entity-апп). Арт-директор-бриф объявил `БРЕНД-НАЗВАНИЕ: "Aura CRM"` (02_brief.md / 01b_writer_input.md), writer честно нанёс «AURA» по всему лендингу (page.tsx: hero «Aura для салонов красоты», features «три кита Aura CRM», CTA «Попробуйте Aura CRM»). НО `share_meta.build_share_card(_proj_name, prompt, accent)` зовётся с `project.name` (messages.py:3159), `_derive_title` берёт имя проекта дословно (share_meta.py:90-91) → `omnia-share.ts` `title="dogfood CRM Аврора"`. Этот один `share.title` питает ТРИ поверхности: `<title>` таба, OG-карточку виральной расшарки (opengraph-image.tsx, пиллар 4) и wordmark auth-shell'а (auth-shell.tsx читает `share.title`). Live Playwright на DEPLOYED dev-контейнере: landing tab-title=«dogfood CRM Аврора», landing hero=«AURA», signin wordmark=«dogfood CRM Аврора» (screenshots landing.png/signin.png). Посетитель видит «AURA» на маркетинг-странице → «dogfood CRM Аврора» при входе; даже таб-тайтл лендинга не совпадает с его hero. Бриф (`state["brief"]`, messages.py:3176) лежит в той же области, но на бренд не парсится.

**Почему не фикшу вслепую:**
1. **Затрагивает виральную share-поверхность (пиллар 4), намеренно настроенную владельцем.** `share_meta` docstring явно фиксирует приоритет: «человеческое имя проекта → ниша → промпт». Тест `test_real_name_becomes_the_title` КОДИРУЕТ это («Кофейня Лофт» → title «Кофейня Лофт»). Сделать брифовый бренд приоритетным над именем проекта = смена этого продуктового решения для `<title>`/OG/wordmark ВСЕХ AD-билдов. Это владельческая территория (что считать публичной идентичностью аппа — имя проекта или AD-бренд), а не чистый баг-фикс.
2. **Продуктовая неоднозначность направления.** Когда юзер назвал проект реальным брендом («Кофейня Лофт»), а AD изобрёл другой — кто прав? Консистентность говорит «то, что видит посетитель на лендинге» (брифовый бренд), но это спорно, если юзер хотел именно своё имя. Нужна владельческая отмашка на политику разрешения.

**План безопасного фикса (для отдельного прогона/согласования):**
1. `_derive_brand_from_brief(brief: str|None) -> str|None` в share_meta.py — детерм. парс `БРЕНД-НАЗВАНИЕ:\s*"?(.+?)"?$` из брифа, fail-soft→None (пустой/мусорный → None, та же длина-обрезка + `_UNTITLED`-гард).
2. `build_share_card(..., brand: str|None=None)` — `title = _clean(brand) or _derive_title(name, prompt)` (брифовый бренд приоритетнее имени проекта, когда есть и валиден; иначе текущее поведение → `test_real_name_becomes_the_title` остаётся зелёным).
3. messages.py:3159 — передать `brand=_derive_brand_from_brief(state.get("brief"))` (бриф уже в области, см. :3176). Только entity/fullstack-путь, не surgical — там же, где сейчас.
4. (опц., альтернатива/дополнение) writer-директива: если бренд НЕ должен переопределять имя проекта по политике владельца — наоборот, скормить writer'у `share.title` как канонический бренд лендинга. НО это «уродует» лендинг именем «dogfood CRM Аврора» — хуже UX, чем вариант брифового бренда.
5. Health-check на песочнице dogfood-*: лендинг-hero, таб-тайтл, signin-wordmark и OG-карточка показывают ОДИН бренд.

**Acceptance-lock (уже в репо):** `apps/api/tests/test_share_meta.py::test_share_card_ignores_brief_brand_today` (green — фиксирует, что share-card сегодня берёт имя проекта, не брифовый бренд) + `::test_brief_brand_should_win_when_present` (xfail strict=False — `build_share_card(..., brand="Aura CRM")` сейчас падает TypeError; XPASS, когда `brand=`-параметр приземлится и брифовый бренд начнёт выигрывать).

## PROPOSAL P-ENTITYJSON — невалидный JSON сущности не должен молча шипиться DOA (2026-06-17, run #28)

**Проблема (доказана live-gen + live-API + writer-raw, BS-31):** writer (deepseek) на реальном CRM-запросе выдал битый `entities/Client.json` (`"access":",` — незакрытая строка, проглотившая `fields`). Файл прошёл ВСЕ гарды без изменений и зашился в контейнер; `loadEntity` молча вернул `null` (registry.ts:87-89) → `GET /api/entities/Client` = 404 «unknown entity 'Client'» (live), а Deal/User (валидный JSON) работают. Центральная сущность мертва, целый срез CRM (список клиентов, picker клиента в форме сделки, KPI «Новые клиенты») молча сломан, БЕЗ сигнала. `_normalize_entity_filenames` ЯВНО пропускает непарсящийся файл (messages.py:1227-1228 `except Exception: continue`), а другого валидатора entity-JSON нет.

**Почему не фикшу вслепую:**
1. **Generic JSON-repair НЕ спасает этот класс.** Незакрытая строка съела `fields`-объект → лениентный парсер (json5 / json-repair) либо тоже падает, либо слепит `access`-строку из остатка и ПОТЕРЯЕТ `fields` → сущность всё равно битая, просто иначе. Корректное восстановление = регенерация именно этого файла.
2. **Регенерация policy-смежна.** Owner-policy `auto_regenerate_enabled=False` запрещает авто-перегенерацию всей страницы; «дорегенерить один структурно-битый entity-файл» — это НЕ whole-page re-roll, но граница тонкая, нужно согласование, чтобы не нарушить дух правила 15.
3. **Кросс-поверхностно.** Точка детекта — guard-цепочка messages.py (рядом с `_normalize_entity_filenames`/`_preserve_auth_schema`), но действие (regen-one-file ИЛИ видимая build-fail с сообщением юзеру в чат) затрагивает pipeline + UX-сигнал.

**План безопасного фикса (для отдельного прогона/согласования):**
1. Новый гард `_validate_entity_json(files)` в цепочке ПЕРЕД commit+hot_reload: `json.loads` каждого `entities/*.json`; собрать список непарсящихся.
2. На непарсящийся файл — целевой micro-regen ТОЛЬКО этого entity-JSON по брифу (узкий промпт «верни валидный JSON для сущности X», детерм. валидация результата перед записью), с фолбэком: если regen недоступен/снова битый → НЕ шипить тихо, а финализировать ассистент-сообщение видимой ошибкой («Сущность Client не сгенерировалась корректно, нажмите „переделать"») — устранить именно МОЛЧАЛИВОСТЬ.
3. (защитный дубль на рантайме) registry.ts:87-89 — вместо тихого `null` логировать parse-fail сущности (наблюдаемость; не чинит UX, но снимает «нет сигнала нигде»).
4. Health-check на песочнице dogfood-*: сгенерить апп с заведомо-битым entity → подтвердить, что либо файл починен (parse OK + сущность резолвится 401, не 404), либо юзер получил явный сигнал.

**Acceptance-lock (уже в репо):** `apps/api/tests/test_entity_json_validity.py::test_current_guard_chain_ships_malformed_entity_json_verbatim` (green — фиксирует, что сегодня битый Client.json проходит гард без изменений) + `::test_live_payload_is_genuinely_unparseable` (green — сам live-payload не парсится) + `::test_no_unparseable_entity_json_ships` (xfail strict=True — XPASS, когда гард начнёт гарантировать, что ни один `entities/*.json` на выходе не битый).

## PROPOSAL P-DATETIME — entity-апп должен уметь хранить время записи, не только день (2026-06-17, run #29)

**Проблема (доказана live-gen + live Playwright + source, BS-32):** система типов полей сущностей не имеет `datetime`/`time` — только `date`. Любое расписание (барбершоп, клиника, бронь столика, групповое занятие, слот) хранит ДЕНЬ записи, но не ВРЕМЯ. На реальном барбершоп-CRM writer сам назвал поле «Дата и время визита» и дал колонку «Время», но мог выбрать только `"type":"date"` → форма рендерит `mm/dd/yyyy`-пикер без часов (`FORM_DATE_INPUT_TYPES==['date']`), а `toDateInput` обрезает любое значение до YYYY-MM-DD. Время визита неуловимо в UI.

**Почему не фикшу вслепую:**
1. **Мульти-поверхностный TEMPLATE-фикс → base-rebuild на проде.** Меняются registry.ts (FieldType + zodForField), entity-form.tsx (виджет + снятие truncation), CalendarView (раскладка по времени) — фикс шаблона требует пересборки base-образа `omnia-template-nextjs-entities:dev`, не git pull.
2. **Writer-промпт — самая тонкая часть.** Нужно научить writer выбирать `datetime` для слот/appointment-полей, НЕ over-emit'я его для простых дат (день рождения, дедлайн, дата регистрации) — иначе day-пикеры всюду станут datetime-пикерами. Это quality-sensitive prompt-правка → regen-verify по нишам.
3. **Round-trip / отображение.** `toDateInput` truncate + форматтеры (`formatDate` в utils) показывают только дату; для datetime нужен формат с часами и сортировка с учётом времени (смежно BS-22 date-cast).

**План безопасного фикса (для отдельного прогона/согласования):**
1. registry.ts: добавить `"datetime"` в `FieldType`; `zodForField` для datetime = `z.string().refine(Date.parse)` (как date, но семантика «с временем»); тип маппинга для сортировки оставить date-совместимым.
2. entity-form.tsx: ветка `kind === "datetime"` → `<Input type="datetime-local">`; `toDateTimeInput` без `.slice(0,10)` (формат `YYYY-MM-DDTHH:mm`); не ломать существующий `date`-путь.
3. CalendarView/booking-листы: при datetime раскладывать событие по времени дня + показывать `HH:mm`.
4. Writer (art_director_writer / SYSTEM_PROMPT.md): директива «для полей-СЛОТОВ (запись/визит/бронь/приём/начало-конец события) используй `datetime`; для календарных дат без времени (дедлайн, дата рождения, дата регистрации) — `date`».
5. Health-check на песочнице dogfood-*: сгенерить scheduling-апп → подтвердить, что booking-форма даёт ввод времени и время переживает round-trip.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_datetime_gap.py` — 3 green (FieldType без datetime/time; виджет только `type="date"`; truncation `slice(0,10)`) + 1 xfail (`test_field_system_should_support_a_time_of_day` — XPASS когда появится datetime-тип + time-capable input).

## PROPOSAL P-NUMRANGE — number-поле должно уметь объявлять диапазон (min/integer), и сервер должен его enforce'ить (2026-06-17, run #32)

**Проблема (доказана live-gen + live authed-API + source, BS-33):** entity-движок не имеет vocabulary числовых ограничений. На реальном складском CRM (`dogfood-sklad-uchet-a086b4`) writer пометил price/quantity как required `number`, но authed POST `{price:-50000,quantity:-30}` и `{price:1999.999,quantity:2.5}` оба вернули **201 Created** и сохранились; `?sort=quantity&order=asc` отдаёт -30 первым как валидную. Headline-KPI «Стоимость склада» = Σ price×quantity раздувается на +1,5 млн ₽ от одной отрицательной записи (минус×минус).

**Почему не фикшу вслепую:**
1. **Мульти-поверхностный TEMPLATE-фикс → base-rebuild на проде.** Меняются `registry.ts` (FieldDef + zodForField) и `entity-form.tsx` (min/step на `<Input type="number">`); фикс шаблона требует пересборки base-образа `omnia-template-nextjs-entities:dev`, не git pull.
2. **Writer-промпт — самая тонкая часть.** Нужно научить writer ставить `min:0`/`integer` ТАМ, где это уместно (цена, остаток, количество, возраст, рейтинг ≥ 0), и НЕ over-emit'ить, где отрицательное/дробное легитимно (баланс счёта, дельта, температура, координата, изменение). Иначе поля, которым нужен полный диапазон, станут ложно-ограниченными. Quality-sensitive → regen-verify по нишам.
3. **Совместимость данных.** Уже сохранённые записи могут нарушать новый `min` — валидатор create/update не должен ретроактивно ломать чтение/список существующих out-of-range строк (enforce только на write новой/правимой записи).

**План безопасного фикса (для отдельного прогона/согласования):**
1. `registry.ts`: расширить `FieldDef` опциональными `min?: number; max?: number; integer?: boolean`; `zodForField(number)` собирает `let n = z.number(); if(integer) n = n.int(); if(min!=null) n = n.min(min); if(max!=null) n = n.max(max)`.
2. `entity-form.tsx`: на `kind:"number"` пробросить `min={f.min} max={f.max} step={f.integer?1:"any"}` в `<Input>`; клиентский `validate()` — отразить границу в сообщении.
3. Writer (`SYSTEM_PROMPT.md`/art_director_writer): директива «для количеств/цен/остатков/возраста — `"min":0` (и `"integer":true` для штучных счётчиков); НЕ ставь для balance/delta/temperature/coordinate/rating-со-знаком».
4. Health-check на песочнице dogfood-*: сгенерить inventory-апп → подтвердить, что POST отрицательной цены/количества теперь 400 (а не 201), а легитимный отрицательный баланс в финансовом аппе по-прежнему проходит.

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_entity_number_constraints_gap.py` — 3 green (FieldDef без min/max/integer; zodForField number = голый z.number(); number-Input без min/step) + 1 xfail (`test_number_fields_should_be_constrainable` — XPASS когда FieldDef понесёт числовую границу И zodForField её применит).

## PROPOSAL P-FILTERWINDOW — фильтр/сегмент списка должен достигать всего набора, а не загруженного ≤50-окна (2026-06-17, run #33)

**Проблема (доказана source code-path + writer-prescription, BS-34):** сгенерённый entity-апп фильтрует/сегментирует список ТОЛЬКО по загруженному ≤50-окну, client-side, поэтому статус-фильтр или колонка канбана молча недосчитывает (или ложно «пусто») как только коллекция переросла первую страницу. Это windowing-семья (search BS-20, sort BS-21, export BS-24, KPI BS-26, pagination BS-18) на НЕ-ПРОБОВАННОЙ оси ФИЛЬТРА/СЕГМЕНТА, и она наиболее болезненна для канбан-`board` — который writer ОБЯЗАН использовать для каждой pipeline-сущности (SYSTEM_PROMPT.md:104-109; real run #28 = «сделки со стадиями … канбан-доска»). Цепочка: `useEntity` → `list(params)` один раз без пагинации (use-entity.ts) → `DEFAULT_LIMIT=50` (engine.ts) → `data.rows`=окно → `segmented`/`grouped` фильтруют это окно client-side (data-table.tsx:281-291, board-view.tsx:201-211) → `mergedParams` шлёт только `expand`, активный фильтр НЕ уходит на сервер (crud-resource.tsx:177-184). Фикс ДОСТИЖИМ: `listRecords` уже умеет `data->>field = value` (engine.ts:178-183).

**Почему не фикшу вслепую (идентично сиблингам P-SORT/P-SEARCH/P-EXPORT/P-KPICOUNT — все PROPOSAL):**
1. **Связный TEMPLATE-фикс на КАЖДОМ сгенерённом аппе.** Нужно вынести активный сегмент (tabIdx/board-column) из приватного state `DataTable`/`BoardView` наверх и пробросить в `useEntity`-params для server re-query — затрагивает managed-компонент, который рендерится в каждом entity-аппе; ошибка ломает фильтр/доску везде. Шаблонный фикс → пересборка base-образа на проде.
2. **Согласование board-drag с server-пагинацией.** Канбан грузит окно, бакетит client-side и сохраняет статус на drag; при server-side сегментации нужно решить, как считать per-column totals и как вести себя при >окна карт в колонке (count-бейдж vs «load more»), не сломав DnD.
3. **Search/sort на той же поверхности уже client-side (BS-20/BS-21).** Единый правильный фикс — server-driven list (filter+search+sort+paginate) через `listParams`, а не лоскутный перевод одного фильтра; делать заодно, согласованно.

**План безопасного фикса (для отдельного прогона/согласования):**
1. `crud-resource.tsx`: поднять активный фильтр-сегмент в состояние managed-компонента; когда выбран не-null таб/колонка — добавить `{ [filterField]: value }` в `mergedParams` → `useEntity` ре-запрашивает сервер. `DataTable`/`BoardView` получают `onFilterChange` вместо приватного tabIdx-state.
2. `engine.ts`: уже поддерживает equality-фильтр; убедиться, что count/total для бейджей колонок считается server-side (см. P-KPICOUNT — общий count-примитив).
3. `data-table.tsx`/`board-view.tsx`: рендерить сегменты из server-результата; убрать client-side `segmented`/`grouped`-фильтрацию по `filterField` (search/sort окна оставить как есть до общего server-driven фикса).
4. Health-check на песочнице dogfood-*: сгенерить pipeline-CRM, засеять >50 записей со статусами, подтвердить, что фильтр «VIP»/колонка «Выиграна» теперь показывают записи 51+ (а не ложное «пусто»/недосчёт).

**Acceptance-lock (уже в репо):** `apps/orchestrator/tests/test_filter_windowing_gap.py` — 5 green (single-window load; engine 50-cap + server-filter существует; table-filter client-side; board groups client-side; активный фильтр НЕ уходит на сервер) + 1 xfail (`test_active_filter_should_reach_the_full_dataset` — XPASS когда активный сегмент пробрасывается в server-query). Проверено: `5 passed, 1 xfailed`.

## PROPOSAL P-MAP — запрос «карта» должен давать реальный keyless-эмбед, не фальш-плейсхолдер (2026-06-17, run #37)
**Что:** freeform-лендинг на явный «контакты с картой / карта проезда» рендерит инертный `<div>«Интерактивная карта»` вместо настоящей карты (BS-37). Фикс — добавить в freeform-writer-промпт директиву: на map-запрос вставлять реальный keyless map-iframe (`https://maps.google.com/maps?q=<URL-encoded address>&output=embed` или Yandex `https://yandex.ru/map-widget/v1/?text=<addr>`), НИКОГДА не подписанный-но-пустой бокс.
**Почему НЕ вслепую:** (1) prompt-quality change — нужен regen-verify, writer compliance вариативна (ср. BS-14: writer нарушил собственный промпт); (2) адрес фабрикуется writer'ом → эмбед покажет карту НЕсуществующего адреса (нужно гейтить на реальный адрес от юзера, или geocode-to-city); (3) CSP/sandbox preview может блокировать сторонний map-iframe — проверить рендер; (4) max 1 фикс/прогон. Сродни BS-30 (fuzzy-fabrication отложен).
**Evidence:** run #37 `_routine/runs/2026-06-16T22-26Z/00_PROOF.md` (source 03_writer_raw.html:368-371 + system-prompt grep 0 map-директив + brief:182 prescribes map).
**Плана-минимум:** writer-prompt map-directive + acceptance/detection-half (флаг когда brief зовёт карту, а HTML без iframe) + regen-verify на rebuilt prompt; гейт на наличие реального адреса.

## PROPOSAL P-TSXEDIT-VALIDATE — контейнерный .tsx-edit должен валидироваться перед commit+hot_reload, битая правка не должна класть живой апп (2026-06-17, run #38)
**Что (доказано, BS-38):** follow-up, расширяющий модель данных рабочего entity-аппа («добавь раздел/сущность … своя страница … пункт в меню»), роутится в CHEAP surgical-edit (intent_triage.py:62-69 — by design), дешёвая модель пишет контейнерный .tsx, и НИ ОДИН гейт не проверяет, что этот .tsx вообще парсится: entity/fullstack пропускают acceptance-гейт (messages.py:3227), dead-link-пасс пропущен на surgical (messages.py:2724), образ api/worker = NO_NODE (нет JS-парсера в процессе). Live run #38: одна правка выдала `services/page.tsx` с незакрытым `columns={[` + `(app)/layout.tsx` с побитым атрибутом `hrefdashboard/...` → ВСЕ 6 routes 200→500, build отрапортовал `hot_reload OK written=3`.
**Почему НЕ вслепую:** (1) нужен НАСТОЯЩИЙ parser/compiler контейнерных .tsx, которого в api/worker-образе нет (NO_NODE) — либо node-сайдкар/`tsc --noEmit`/swc в build-шаге оркестратора, либо валидация внутри dev-контейнера ПЕРЕД пометкой build=success; (2) нужен revert-to-last-good: при невалидной правке откатить файлы к последнему рабочему снапшоту, а не оставлять 500 (политически-смежно с rollback/snapshot-моделью); (3) Python-эвристика баланса скобок FP-опасна на ВАЛИДНОМ TSX (скобки/кавычки в строках, regex, template-literal, JSX-выражениях) И структурно не ловит класс attribute-corruption (`hrefdashboard` — баланс цел) → флаки-полугейт хуже отсутствия; (4) regen-verify на реальном follow-up; max 1 фикс/прогон.
**Evidence:** run #38 `_routine/runs/2026-06-16T22-53-09Z/00_PROOF.md` (mode=edit/surgical/deepseek-chat, 3 файла, 2 corruption-сигнатуры, live 6×500 на DEPLOYED dev-контейнере) + `broken_followup_files.txt` + `01_bricked_dashboard_500.png`.
**Acceptance-lock (в репо):** `apps/api/tests/test_container_edit_tsx_validity.py` — 2 green (routing→CHEAP + 2 corruption-сигнатуры) + 1 strict-xfail `test_container_surgical_edit_rejects_unparseable_tsx` (XPASS, когда появится build-time .tsx-гейт `validate_container_tsx`).
**План-минимум:** контейнерный build/hot_reload-путь прогоняет .tsx через реальный парсер (tsc/swc) в dev-контейнере или оркестраторе → на parse-fail НЕ помечать success, откатить к last-good, вернуть видимую ошибку юзеру; regen-verify на dogfood-песочнице (рабочий апп → follow-up «добавь раздел» → апп остаётся 200).

## PROPOSAL P-CDNDEAD — внешняя библиотека по 404-CDN-URL не должна молча класть инструмент DOA; пайплайн должен либо вести писателя к рабочей библиотеке, либо валидировать `<script src>` (2026-06-17, run #43)
**Что (доказано, BS-43):** freeform-писатель на запросе-инструменте «генератор QR-кодов» выдал структурно-корректный инструмент (canvas, live-input, селектор размера, PNG-download, init на DOMContentLoaded), но подключил библиотеку по `https://cdn.jsdelivr.net/npm/qrcode@1.5.3/build/qrcode.min.js` — **HTTP 404** (этого пути не существует). `typeof QRCode==="undefined"` → `QRCode.toCanvas` бросает → canvas пуст → QR не генерируется НИ В ОДНОМ браузере. Класс шире QR: любой инструмент/виджет, чью библиотеку 98KB-system-prompt не предусмотрел в approved-наборе (Tailwind/Lenis/anime/omnia-kit), → писатель импровизирует CDN-путь, пайплайн его не проверяет, приёмка слепа (vision_ran=False; structural-гейт пустой canvas не видит).
**Почему НЕ вслепую (мульти-поверхностный, FP-/флак-риск):** (1) node-qrcode (API `QRCode.toCanvas`, который писатель использовал) НЕ имеет browser-global UMD ни на jsdelivr, ни на unpkg (проверено: `build/qrcode.min.js`/`build/qrcode.js` 404 для 1.5.3/1.5.4) → **простой URL-swap не работает**: рабочие CDN-библиотеки QR (`qrcodejs@1.0.0`, `qrcode-generator@1.4.4` — обе 200) имеют ДРУГОЙ API (`new QRCode(el,text)`), переписать пришлось бы и src, И call-site (фрагильно, класс BS-38); (2) build-time HEAD-валидация всех внешних `<script src>` — это сетевые запросы на ГОРЯЧЕМ пути генерации (латентность + флак: транзиентный CDN-сбой ложно завалит нормальный билд) + неясная политика на 404 (блокировать билд? стрипнуть скрипт? всё равно инструмент мёртв без замены); (3) чисто prompt-quality-директива («используй только проверенные библиотеки / approved-список») LLM-compliance-зависима и не верифицируема за один прогон (прецедент P-MAP/BS-37).
**Evidence:** run #43 `_routine/runs/2026-06-17T01-39Z/` — `final.html` (08_post_palette_guard, src на 404-URL + `QRCode.toCanvas` call-site), `01_qr_desktop.png` (полированный лендинг QRAURA + ПУСТОЙ QR-бокс), console-dump (`typeof QRCode = undefined`, `PAGEERROR: QRCode is not defined`, Tailwind-CDN warning = https-egress жив), CDN-проба (`qrcode@1.5.3/build/qrcode.min.js → 404`; `qrcodejs@1.0.0 → 200` но иной API). Server: gen_mode=freeform, preset=local-services, cost 7.42₽, vision_attempt0 vision_ran=False struct=False (поймал только `href="#api"` + типографику, не пустой canvas).
**План (на согласование, безопасные ветки):** (A) РЕКОМЕНДУЕТСЯ — захостить рабочий node-qrcode UMD как kit-ассет (`/api/kit/qrcode.min.js`, рядом с omnia-kit.js, копируется в freeform-вывод как `assets/`), + директива в system-prompt: «для QR — уже-подключённый `assets/qrcode.min.js`, `QRCode.toCanvas` глобально» (детерминированно-рабочая библиотека под контролем, без сетевой валидации); (B) опц. — детерминированный post-process в `link_validator.py`: HEAD-чек внешних script-src с кэшем + fail-open на сетевую ошибку, на жёсткий 404 известного CDN-хоста — пометить снапшот/лог (детект, не блок); (C) расширить approved-список prompt'а на частые tool-библиотеки (QR, Chart.js с пинованным рабочим URL, signature-pad) + правило «вне списка — не выдумывай CDN-путь». Regen-verify на dogfood-песочнице: «генератор QR» → реальный сканируемый QR в canvas. Вторичный под-issue (НЕ блокер): даже при рабочей библиотеке писатель задал инвертированную полярность QR (`dark:'#F8FAFC'` светлые модули на `light:'#0F172A'` тёмном фоне, под тёмную палитру) — light-on-dark QR спека не обязывает сканеры читать → ненадёжен на части камер; правило «функциональные коды всегда тёмные-на-светлом» — в ту же prompt-директиву.

## RUNS LOG (append-only — одна строка на прогон)
<!-- время MSK | сценарий | stack/gen_mode/модель | design-балл | тупняк | действие | PASS/FAIL -->
- 2026-06-17 04:57 MSK | #43 ротация 43 mod 5 = 3 (tgbot, заблокирован P-BS5) → взят НИКОГДА-не-пробованный SPA-архетип: **инструмент НЕ-калькулятор, требующий ВНЕШНЕЙ JS-библиотеки** (все прежние SPA-прогоны #8/#27 — калькуляторы с inline-логикой; QR-генератору нужна реальная lib-кодировка — острый тест «static-theater для интерактивных инструментов»). Live-gen dogfood-qr-generator-43-826f8c (mint-token owner b2aec0fc), skip_clarify=true «генератор QR-кодов: ссылка/текст→QR→скачать PNG, выбор размера» | gen_mode=**freeform** (✓ корректно — инструмент без auth/БД, НЕ переэскалировал в entities) / preset=local-services / deepseek-v4-pro-thinking · cost_rub 7.42 · passes 2 · ui_audit 6/10 [typography_count, 4 button-heights, icon-stroke] · vision_attempt0 **vision_ran=False** struct=False (поймал только `href="#api"` + типографику) | дизайн **≈8/10** (тёмная luxe-палитра QRAURA, чистая иерархия hero+поле+селектор+CTA, проф; минус ui_audit-нитки) · ФУНКЦ **СЛОМАН НАСМЕРТЬ**: QR-инструмент структурно полон (canvas/live-input/размер/PNG-download/init), но `<script src=qrcode@1.5.3/build/qrcode.min.js>` **404** → `QRCode undefined` → canvas пуст → QR не генерится НИ В ОДНОМ браузере; инструмент выглядит готовым, не делает ничего | **NEW BS-43**: писатель галлюцинировал несуществующий CDN-путь библиотеки (node-qrcode шипит CommonJS `lib/browser.js`, НЕ browser-UMD `build/qrcode.min.js`), и ни build-time-валидации `<script src>`, ни приёмки (vision_ran=False, structural не видит пустой canvas) → DOA шипнут. Корень: system-prompt approved-набор (Tailwind/Lenis/anime/omnia-kit, 00_system_prompt.md:740,964,987) без правила для библиотек ВНЕ списка. Класс: любой freeform-инструмент с непредусмотренной lib. Дизайн ОПРОВЕРГ «static-theater для design» (лендинг отличный) — провал именно функциональный/integration | **PROPOSAL P-CDNDEAD** (фикс мульти-поверхностный: node-qrcode API не имеет global-UMD на CDN → URL-swap не сработает; build-time HEAD-валидация = горячий путь+флак; prompt-quality LLM-зависим → не вслепую; план A=захостить QR-UMD как kit-ассет + prompt-директива, B=детект-валидатор fail-open, C=расширить approved-список) + вторичный под-issue инвертированная QR-полярность (light-on-dark, ненадёжно для сканеров) | PASS (новая never-ledgered ось «галлюцинированный CDN-URL библиотеки → DOA-инструмент»: func FAIL доказан РЕАЛЬНОЙ live-gen + 404-проба CDN + `typeof QRCode=undefined`/pageerror в chromium + canvas 250000dark/0light + screenshot пустого QR-бокса + vision_ran=False; локализован file:line; фикс мульти-поверхностный/FP-рисковый → PROPOSAL, не вслепую; freeform = без контейнера; sandbox-проект удалён, tmp очищен)
- 2026-06-17 22:05 MSK | #26 ротация 27 mod 5 = 2 (SPA-калькулятор), но сценарий-2 закрыт ЗДОРОВЫМ run #8 + H1/tgbot заблокированы PROPOSAL → по духу дока взял НОВЫЙ never-ledgered угол, замеченный мельком в run #25 passing-ноте: **БРЕНД-РАССИНХРОН лендинг↔кабинет**. **LIVE-GEN ВОССТАНОВЛЕН 2-й прогон подряд** — сделал РЕАЛЬНУЮ end-to-end генерацию (не CRUD-пробу runs #14-24): dogfood-crm-avrora-6450ab, mint-token owner 2aa9b1a2, skip_clarify=true | blank→**nextjs_entities** (BS-4 эскалация LIVE ✓, `switch_to_stack` флипнул template, `_route.md` template=nextjs_entities) / freeform+art-director / deepseek-v4-pro-thinking · preset=beauty-elegant · cost_rub 4.47 | дизайн ~7/10 (чистый premium-лендинг hero+dual-CTA+6 feature-карточек+CTA-band, иерархия ок; МИНУС: моно-grey без нишевого акцент-цвета — BS-6b опять; пустой N-аватар) · ФУНКЦ **HEALTHY** (entities Client{name,phone,notes,totalSpent,lastVisit}/Master/Booking{clientId→Client,masterId→Master,service,date,price,status} + dashboard-роуты clients/masters/bookings + auth-shell — точно под запрос; гипотеза «broken гибрид freeform-в-entity-скаффолде» ОПРОВЕРГНУТА: gen_mode=freeform при freeform_traffic_pct=100 — это НОРМАЛЬНЫЙ entity-путь, реальный entity-апп) · попутно ОПРОВЕРГНУТА гипотеза «эскалация не сработала» (template таки nextjs_entities в БД+route.md) | **NEW BS-29**: бренд-рассинхрон — арт-директор-бриф объявил `БРЕНД-НАЗВАНИЕ: "Aura CRM"`, writer нанёс «AURA» по всему лендингу, но `share_meta.build_share_card` зовётся с `project.name` (messages.py:3159), `_derive_title` берёт имя дословно (share_meta.py:90-91) → `omnia-share.ts` title=«dogfood CRM Аврора»; этот один share.title питает `<title>` таба + OG-карточку (пиллар 4) + wordmark auth-shell'а (auth-shell.tsx); бриф `state["brief"]` доступен (messages.py:3176) но не парсится на бренд. **Live Playwright на DEPLOYED**: landing tab-title=«dogfood CRM Аврора» + hero=«AURA», signin wordmark=«dogfood CRM Аврора» (screenshots в _routine/runs/). Класс шире салона: любой апп где имя проекта ≠ AD-бренд (всегда, когда юзер назвал проект утилитарно). Подтверждает run #25 passing-ноту «InkBook vs slug» | PROPOSAL P-BRAND (`_derive_brand_from_brief` + `build_share_card(brand=)` приоритет брифового бренда + проброс `state["brief"]`-бренда на call-site; затрагивает виральную share-поверхность owner-настроенную «project-name-first» = продуктовое решение → не вслепую) + acceptance-lock test_share_meta.py (1 green evidence + 1 xfail, проверено локально 8 passed/1 xfailed) | PASS (новая never-ledgered слепая зона «бренд-консистентность через поверхности»: func-десинк доказан РЕАЛЬНОЙ live-gen + брифом + page.tsx + omnia-share.ts + live Playwright-render landing↔signin + 2 screenshot, локализован file:line, заперт тестом; ПЛЮС verify: генератор ЗДОРОВ end-to-end — эскалация+entity-апп+дизайн, опровергнуты 2 побочные гипотезы (broken-гибрид + эскалация-не-сработала); live-gen восстановлен; throwaway omnia-dev-dogfood-crm-avrora-6450ab удалён, 9517MB free)
- 2026-06-17 21:45 MSK | #25 ротация 26 mod 5 = 1 (сценарий-1 CRM-с-нуля «вход, список клиентов, добавление, заметки, история записей», skip_clarify=true). **ВПЕРВЫЕ за ~13 прогонов LIVE-GEN РАЗБЛОКИРОВАН** — vsegpt budget восстановился (tiny-probe deepseek-chat→200, и 2 РЕАЛЬНЫЕ end-to-end генерации прошли: cost_rub 5.17 + 4.88, passes=2). Сделал НАСТОЯЩУЮ генерацию (а не CRUD-пробу runs #14-24) | blank→**nextjs_entities** / freeform+art-director / «Оркестратор Sonnet+DeepSeek» · preset=beauty-elegant | дизайн **~8/10** (лендинг «InkBook» чистый, hero+dual-CTA+preview-card+stats 3сек/100%/0₽, 3 feature-карточки+how-it-works+отзывы+CTA-band, иерархия+глубина, нишевый studio-masters, НЕ серый-дефолт; signin split-screen dark/white; минусы: моно-палитра без нишевого цвета-акцента, пустые testimonial-аватары, **БРЕНД-РАССИНХРОН** лендинг=«InkBook» vs auth-shell=слаг «dogfood-crm-failverify» — writer выдумывает бренд только для лендинга, auth берёт project.name) · ФУНКЦ **HEALTHY** (entities Client{name,phone,email,tags}/Booking/Note/Task + routes (app)/dashboard/{clients,bookings,notes}+signin/signup+landing — точно под запрос; access=owner = внутренний CRM, корректно; live screenshots в _routine/runs/; BS-4 escalation LIVE-подтверждён blank→nextjs_entities) · ПОБОЧНО поймана НОВАЯ слепая зона на ПЕРВОЙ генерации (до восстановления budget) | **NEW BS-28**: handled LLM-stream-fail (`stream_error` messages.py:2113) финализирует assistant-строку пустой `content=''` + `tokens_out=NULL` → blank вечно-«стримящийся» бабл без сигнала; crash-путь `_emergency_error`:158 уже защищён (`[Ошибка:…]`+zero-tokens), handled-путь нет (асимметрия hardening); сам код это знает (коммент :3751 «tokens_out stays NULL → ChatPanel spins»); live-repro dogfood-crm-klienty-e28e8d `content=''/tokens_out=NULL/snapshot_id=NULL` + лог `[PP] stream_error err='writer pass failed: Недостаточно средств'` | **FIX c4c9d9a** (pure-helper `_failed_build_body` персистит читаемую ошибку когда ничего не стримилось + сохраняет partial + `usage_data or {0,0}` зануляет токены = зеркало _emergency_error; деплой api+worker; helper загружен на проде import-OK; 4 теста test_failed_build_message_persist.py 2 pure-green локально + 2 DB-lock; ZERO контракт-change) | PASS (новая зона «build-failure-persistence» — ОРТОГОНАЛЬНА 27 entity/discovery-находкам, новая поверхность=orchestration-pipeline; ФИКС доставлен+доказан before/after на ПРОДЕ (broken empty/NULL → deployed helper персистит error+zero-tokens) + 2 pure-теста green; ПЛЮС verify: генератор ЗДОРОВ end-to-end — реальный CRM, дизайн ~8/10, escalation работает; live-gen восстановлен; throwaway-контейнеры crm-{klienty,failverify,postfix} удалены, 9431MB free)
- 2026-06-17 21:16 MSK | #24 ротация 25 mod 5 = 0 (H1), но H1 заблокирован PROPOSAL P-H1 → по духу дока (run #13-23 искали НОВЫЕ never-probed углы) взял НОВЫЙ never-probed угол: **УНИКАЛЬНОСТЬ / ПРЕДОТВРАЩЕНИЕ ДУБЛЕЙ**. Семья тихих data-integrity (BS-17 clear / BS-19 delete-orphan / BS-25 concurrent) мерила single/concurrent write, но НИКОГДА — что если добавить ИДЕНТИЧНУЮ запись дважды. Попутно найден резкий контраст: платформа УМЕЕТ уникальность (users.email = реальный PG unique-индекс + 409), но проведена ТОЛЬКО к auth-таблице, не к entity-записям. **Live-gen всё ещё ЗАБЛОКИРОВАН WALLET-EMPTY** → LIVE-RUNTIME на DEPLOYED base omnia-template-nextjs-entities:dev (метод runs #14-26) | nextjs_entities (engine+registry базовый) · live-runtime CRUD на starter Task, рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ FAIL (одна и та же запись добавляется дважды молча, без 409) | **NEW BS-27**: entity-движок без уникальности — `FieldDef` без `unique`-флага (registry.ts:34-43) → объявить нельзя; `createRecord` zod+insert без дедупа (engine.ts:213-234), нет unique-индекса на `records.data->>field` → проверить нечем; контраст users.email (db/schema.ts:50 unique + register 409). Live на DEPLOYED (omnia-dev-dogfood-uniq-probe, schema proj_dogfood_uniq633526): CRM-оператор добавляет одного клиента 3× {title:"Иван Петров | +7 999 123-45-67",priority:high,notes:VIP} → 3× **201**, LIST_TOTAL 3 / IDENTICAL_TITLE_ROWS 3 / DISTINCT_IDS 3 / ALL_SAME_DATA true, ?title=точное→3 (неразличимы), UNIQUENESS_ENFORCED **NO** / DUPLICATE_BUG **CONFIRMED**. Семья BS-17/19/25, класс шире CRM (email/phone/SKU/slug/username/слот/номер счёта) | PROPOSAL P-UNIQUE (`unique`-флаг в registry+SYSTEM_PROMPT + partial unique-индекс на `(entity, data->>field)` со скоупом owner-vs-global + 409 через SDK+EntityForm как register-route; base-rebuild+regen-verify, меняет write-семантику всех аппов → не вслепую) + acceptance-lock test_entity_uniqueness_gap.py (3 green + 1 xfail) | PASS (новая слепая зона в never-probed «uniqueness/dedup»-оси: func FAIL доказан LIVE-RUNTIME на deployed-бинаре (3× идентичных CREATE → 3× 201, 3 distinct id, ALL_SAME_DATA, exact-filter не различает) + код-root 2 поверхности + контраст users.email + локализован file:line + заперт тестом; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14-26; throwaway-контейнер+schema+role удалены; entity-сабсет 103 passed/13 xfailed без регресса)
- 2026-06-17 21:03 MSK | #23 ротация 24 mod 5 = 4 (free), но H1/tgbot заблокированы PROPOSAL + SPA-calc закрыт ЗДОРОВЫМ run #8 → по духу дока (run #13-22 искали НОВЫЕ функц-углы) взял НОВЫЙ never-probed угол: **ТОЧНОСТЬ ДАШБОРД-KPI / AGGREGATE**. Раньше мерили list-строки (cap/search/sort/export/date-cast #15-22) и writes (create/clear/concurrent/ref-integrity #6,14,16,22) и media #20, но НИКОГДА — **aggregate headline-цифру**, которую владелец видит ПЕРВОЙ при логине. Попутно опровергнута гипотеза numeric-cast-crash (BS-22 sibling): number-валидатор строгий `z.number()` (registry.ts:130) → JSONB всегда число → `::numeric` (engine.ts:74) не крашит через write-path (чистый негатив). **Live-gen всё ещё ЗАБЛОКИРОВАН WALLET-EMPTY** (vsegpt balance 5.446205 < 9 для writer deepseek-v4-pro-thinking; tiny deepseek-chat работает) → LIVE-RUNTIME на DEPLOYED base omnia-template-nextjs-entities:dev (метод runs #14-22) | nextjs_entities (engine+kit+SDK базовый) · live-runtime CRUD на starter Task (60 записей) + артефакт-чтение реального CRM dashboard/page.tsx, рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ FAIL (дашборд-KPI замирают на 50, avg/conversion скошены к новейшим 50) | **NEW BS-26**: дашборд-KPI считаются `.length`/`.reduce()` над загруженными ≤50 — entity-рантайм без count/aggregate-примитива: listRecords голая ≤50 страница без total (engine.ts:205-210), нет countRecords/count(*), SDK list→Row[] без count (sdk/index.ts:85,126), useEntity только rows (use-entity.ts), нет /count-роута; кит САМ учит `value={clients.length}` (dashboard-hero.tsx:71-72, count-up.tsx:13). Live на DEPLOYED (omnia-dev-dogfood-kpicount-probe, schema proj_dogfood_kpi_1781632650): 60 Task → GET без params=50, тело голый `{"data":[...]}` БЕЗ total + БЕЗ X-Total-Count, ?limit=200=60, /count|/stats|/aggregate→500, ?count=true игнор; WRITER-PATTERN на реальном CRM demo-crm-dlia-prosmotra: list() без limit ×3 + activeDeals.length/newClientsThisMonth.length/overdueTasks.length + avgCheck=deals.reduce/deals.length. Хуже BS-18/BS-24 — авторитетный total выглядит точным, замирает на 50, первый экран; семья «выглядит полным, но нет» BS-14/16/18/20/21/24 | PROPOSAL P-KPICOUNT (server count + sum/avg/group-by примитив + SDK count() + useEntity total + kit-гайд «KPI=server count, не rows.length»; count один не чинит avg/conversion → нужен SUM/AVG; TEMPLATE+engine, base-rebuild+regen-verify, связан с P-PAGINATE/SEARCH/SORT/EXPORT → не вслепую) + acceptance-lock test_entity_kpi_count_windowing.py (5 green + 1 xfail) | PASS (новая слепая зона в never-probed «aggregate/KPI»-оси: func FAIL доказан LIVE-RUNTIME на deployed-бинаре (60 создано → KPI-источник=50, нет total в теле/хедере, нет count-роута) + WRITER-PATTERN на реальном сгенерённом CRM + код-root 5 поверхностей + локализован file:line + заперт тестом; попутно опроверг numeric-cast-crash; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14-22; throwaway-контейнер+schema+tmp удалены; windowing-семья регресс-чек 20 passed/5 xfailed)
- 2026-06-16 20:21 MSK | #22 ротация 23 mod 5 = 3 (tgbot), но tgbot заблокирован PROPOSAL P-BS5 + H1 заблокирован + сценарий-2 закрыт ЗДОРОВЫМ run #8 → по духу дока (run #13-21 искали НОВЫЕ функц-углы) взял НОВЫЙ never-probed угол: КОНКУРЕНТНАЯ ПРАВКА / ОПТИМИСТИЧЕСКАЯ БЛОКИРОВКА. Раньше мерили single-actor write-операции (create #6, relation #13, clear #14, pagination #15, ref-integrity-на-DELETE #16, search #17, sort #18, date-cast #19, media #20, export #21), но НИКОГДА — что происходит, когда ДВА редактора пишут ОДНУ запись. Орто семье windowing и отлично от BS-19 (ref-integrity между сущностями) — здесь конфликт ВНУТРИ одной строки. Попутно verify-подтвердил: дефолты применяются на CREATE (`applyDefaults` engine.ts:227, done:false материализовался) + number-поле form-коэрсит `Number(raw)` (entity-form.tsx:163) = create-валидация здорова (опровергнуты 2 побочные гипотезы). **Live-gen всё ещё ЗАБЛОКИРОВАН WALLET-EMPTY** → LIVE-RUNTIME на DEPLOYED base omnia-template-nextjs-entities:dev (метод runs #14-21) | nextjs_entities (engine+form базовый) · live-runtime CRUD на starter Task, рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ FAIL (две правки одной записи молча затирают друг друга) | **NEW BS-25**: entity-движок без оптимистической блокировки — `updateRecord` read-merge-write без version-предусловия (engine.ts:285-297), нет version/etag/If-Match нигде, форма шлёт полный stale-снапшот (entity-form.tsx:82-88,149-167) → устаревшее поле затирает чужую правку. Live на DEPLOYED (omnia-dev-dogfood-lostupdate-probe, schema proj_dogfood_lostup_3880545): CREATE priority=low; A PUT→high→200 (CONTROL GET=high, апдейт работает); B PUT stale priority=low→**200 НЕ 409**; FINAL=low (A's high молча потерян). Семья тихих data-integrity BS-17/19/22; класс шире CRM | PROPOSAL P-LOSTUPDATE (version/updated_at-токен + If-Match-предусловие + 409 + conflict-UX; write-контракт всех аппов, мульти-поверхностно, base-rebuild+regen-verify → не вслепую) + acceptance-lock test_entity_concurrent_update.py (3 green + 1 xfail) | PASS (новая слепая зона в never-probed «concurrent-edit»-оси: func FAIL доказан LIVE-RUNTIME на deployed-бинаре (A→high, CONTROL=high, B-stale→200, FINAL=low → A потерян, NOT 409) + код-root 3 поверхности + локализован file:line + заперт тестом; CONTROL опровергает «апдейт сломан» — ломается ровно конкурентный stale-write; попутно verify подтвердил здоровье defaults+number-create; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14-21; throwaway-контейнер+schema удалены)
- 2026-06-16 20:08 MSK | #21 сценарий-2 по ротации (22 mod 5 = 2 → SPA-калькулятор), но сценарий-2 ЗАКРЫТ ЗДОРОВЫМ run #8 + H1/tgbot заблокированы PROPOSAL → по духу дока (run #13-20 искали НОВЫЕ функц-углы) взял НОВЫЙ never-probed угол: ДОСТИЖИМОСТЬ ДАННЫХ ПРИ ВЫГРУЗКЕ (CSV-экспорт). Раньше мерили in-app list-data-flow (create/relation/clear/pagination/ref-integrity/search/sort/date-cast #6,13-19) + media/upload #20, но НИКОГДА — что попадает в ЭКСПОРТИРУЕМЫЙ файл (data-portability, артефакт уезжает из приложения). Попутно verify-опроверг 2 гипотезы: (a) форм-валидация-фидбек ЗДОРОВ (inline client-errors + toast серверного message через сохранённый ApiError, sdk/index.ts:75-77 + entity-form.tsx:183-184); (b) faceted/quick-filter UI СУЩЕСТВУЕТ (filterTabs), но тоже клиент-сайд над ≤50 — сиблинг BS-18, не новый. **Live-gen всё ещё ЗАБЛОКИРОВАН WALLET-EMPTY** → LIVE-RUNTIME на DEPLOYED base-образе omnia-template-nextjs-entities:dev (метод runs #14-20) | nextjs_entities (CrudResource+DataTable+engine базовый) · live-runtime CRUD на starter Task (55 записей), рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ FAIL (выгрузка «всех данных» молча теряет записи 51+) | **NEW BS-24**: CSV-экспорт молча обрезан до загруженных ≤50 — `exportable=true` по умолчанию на каждом managed-экране (crud-resource.tsx:150), handleExport сериализует `sorted`←`rows`-проп = in-memory ≤50 (data-table.tsx:373-388), `rows`=useEntity один `list()` без limit→дефолт 50 (корень BS-18). Хуже BS-18: артефакт уезжает из приложения как доверенный файл, недостача невидима; коммент «not just the visible page» усиливает ложную полноту. Live на DEPLOYED (omnia-dev-dogfood-export-probe, schema proj_dogfood_export_916): 55 записей → GET без params (что сериализует экспорт)=50, ?limit=200=55, default-окно first=ROW-01 last=ROW-50 → 51-55 в выгрузку не попадают. Семья «выглядит полным, но нет» BS-14/16/18/20/21, поверхность data-portability | PROPOSAL P-EXPORT (дотянуть полный набор перед сериализацией на уровне CrudResource ИЛИ server-export route; TEMPLATE→base-rebuild+regen-verify, связан с P-PAGINATE → не вслепую) + acceptance-lock test_entity_export_truncation.py (3 green + 1 xfail) | PASS (новая слепая зона в never-probed «export/data-portability»-оси: func FAIL доказан LIVE-RUNTIME на deployed-бинаре (55 создано → экспорт-источник=50, overflow в БД но не в выгрузке) + код-root 3 поверхности + локализован file:line + заперт тестом; попутно verify опроверг 2 гипотезы (форм-валидация + quick-filter); live-gen WALLET-EMPTY обойдён прямым CRUD как run #14-20; throwaway-контейнер+schema удалены)
- 2026-06-16 19:52 MSK | #20 сценарий-1 (entity-app, НОВЫЙ класс, НИКОГДА не пробованный: МЕДИА/UPLOAD-ИНТЕГРАЦИЯ — раньше мерили только list-data-flow семью create/relation/clear/pagination/ref-integrity/search/sort/date-cast (#6,13,14,15,16,17,18,19), но НИКОГДА — uploadFile/sendEmail/рендер картинок; ортогонально семье windowing; rotation 21 mod 5 = 1; H1 заблокирован PROPOSAL P-H1 → ротация). **Live-gen всё ещё ЗАБЛОКИРОВАН WALLET-EMPTY** (vsegpt balance 5.447765 < 9 — пробил writer-модель deepseek-v4-pro-thinking через gw, 400 «only 5.447765 on account») → LIVE-RUNTIME на DEPLOYED base-образе omnia-template-nextjs-entities:dev (метод runs #14-19) | nextjs_entities (integrations базовый) · live faithful server.ts uploadFile-реплика (node+minio pkg) на omnia-runtime_default, рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ: **VERIFY HEALTHY** (опровергнута severe-гипотеза) | **VERIFY + NEW BS-23 (low-sev, latent)**: ГИПОТЕЗА была severe — провижер читает `OMNIA_MINIO_*`, прод-стек ставит плоские `MINIO_*` → у всех аппов пустой public_url + нет secret → upload мёртв. ОПРОВЕРГНУТА: провижер крутится в ХОСТ-systemd-процессе оркестратора (не worker, который я сперва ошибочно проверил), его `/opt/omnia/apps/orchestrator/.env` задаёт `OMNIA_MINIO_SECRET_KEY`+`OMNIA_MINIO_PUBLIC_URL`, остальное → корректные дефолты (endpoint omnia-prod-minio:9000 резолвится из omnia-runtime_default, access omnia = реальный). Live-e2e: STEP1 дефолты-без-секрета→`UPLOAD_FAIL AccessDenied`, STEP2 реально-инжектируемый env→`UPLOAD_OK` + внешний curl public URL→**HTTP 200** точные байты; `_integration_env()` спред в env контейнера (provisioner.py:193); kit рендерит плоским `<img>` (без next/image remotePatterns-гочи); sendEmail-stub честно документирован writer-промпту (prompt_builder.py:1248-1249). BS-23 latent: единственный orchestrator `.env` без фолбэка на плоские `MINIO_*` → если дрейфнёт, upload молча умрёт стек-широко без сигнала | NONE (фикса нет — интеграция ЗДОРОВА для текущих провижнов; дефенсив-фолбэк мал+доказан-безопасен но без видимого провала = спекулятивно, ср. BS-10) | PASS (verify: проверил never-probed media/upload класс — оказался здоровым; severe-гипотеза опровергнута по live-e2e доказательству (broken-env AccessDenied vs real-env OK+внешний 200); попутно локализован low-sev latent BS-23 file:line; live-gen WALLET-EMPTY обойдён прямой server.ts-репликой как run #14-19; тест-объект из MinIO удалён, контейнеры --rm)
- 2026-06-16 19:34 MSK | #19 сценарий-1 (entity-app, НОВЫЙ функц-угол: CRASH-НА-КАСТЕ при сортировке — раньше мерили create-persist #6, relation #13, clear #14, pagination #15, ref-integrity #16, search #17, sort-windowing #18, но НИКОГДА — что если значение поля валидно на write, но КРАШИТ read-каст; rotation 20 mod 5 = 0 → H1 заблокирован PROPOSAL P-H1 → ротация; взял неисследованную ось write↔read type-divergence, отличную от семейства windowing #18/#20/#21). **Live-gen всё ещё ЗАБЛОКИРОВАН WALLET-EMPTY** (vsegpt balance 5.447765 < 9 для writer — пробил writer-модель deepseek-v4-pro-thinking напрямую через gw, тот же 400 «Potentially out of budget… only 5.447765») → LIVE-RUNTIME против throwaway-контейнера из DEPLOYED base-образа (баг в базовом engine, одинаков во всех аппах; method runs #14-18) | nextjs_entities (engine+schema базовый) · live-runtime CRUD на starter Task (built-in `due` date-поле), рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ FAIL→FIXED (sort по дата-колонке с кривым-но-JS-валидным значением 500-ит ВЕСЬ список) | **NEW BS-22 (severe, denial-of-view)**: одна JS-валидная-но-PG-невалидная дата 500-ит весь список при сортировке. Расхождение: write-валидатор date = ленивый `Date.parse` (registry.ts:139) принимает «2025»/«June 2025»/«2025-02-30», sort кастит `::timestamptz` (engine.ts:75) — Postgres строже, отвергает → generic 500 (http.ts:13-19); CrudResource каждая колонка сортируема (BS-21). Доказано: Half-A node Date.parse (11 значений PASS), Half-B PG ::timestamptz (8 тех же FAIL), Half-C live на DEPLOYED (omnia-dev-dogfood-datecast-probe-2fe2f6): POST due="June 2025"→201, ?sort=due asc/desc→**500**, ?due=June 2025 фильтр→200 (находит), CONTROL delete→sort→200, лог `invalid input syntax… "June 2025" at listRecords engine.ts:194`. Фильтр (без каста) безопасен — падает только sort. Хуже BS-21/BS-18 — крашит ВЕСЬ вью opaque-ошибкой | **FIX ad0a71b**: crash-proof `safe_to_timestamptz(text)` STABLE plpgsql→NULL на ошибке (init-db.mjs) + engine.ts date-сорт через обёртку вместо сырого `::timestamptz`; детерм/fail-soft/non-regressive (`safe(valid)===valid::timestamptz`), ZERO контракт-change; deploy=base-rebuild на проде; VERIFY на ПЕРЕСОБРАННОМ base (fresh omnia-dev-dogfood-datecast-verify-95e13b): POST «June 2025»→201, ?sort=due asc→**200** (Beta 2026-01-15, Alpha 2026-03-01, poison→NULL last), desc→200, фильтр→200, лог 0×unexpected-error; test_entity_date_cast_crash.py 5 green + 1 xfail (88 passed/8 xfail в entity-сабсете без регресса; обновил stale-ассерт BS-21 test_entity_sort_windowing.py под новый каст). Write-half оставлен как PROPOSAL P-DATECAST (ужесточение валидатора = смена write-семантики всех аппов, ср. BS-17) | PASS (новая слепая зона в never-tested «crash-on-cast»-оси: func FAIL доказан LIVE на deployed-бинаре (500 + delta-control + лог) + код-root 3 поверхности + локализован file:line; ФИКС доставлен и доказан на ПЕРЕСОБРАННОМ DEPLOYED base end-to-end 500→200 + НЕ-регрессия healthy-сортировки; live-gen WALLET-EMPTY обойдён прямым CRUD как run #14-18)
- 2026-06-16 19:13 MSK | #18 сценарий-4 (free — НОВЫЙ функц-угол: СОРТИРОВКА выросшего списка — раньше мерили create-persist #6, relation-visibility #13, clear #14, list-scale/pagination #15, ref-integrity #16, search/findability #17, но НИКОГДА — упорядочивает ли «сортировка по колонке» весь набор или только загруженное окно; rotation 19 mod 5 = 4 (free), H1+tgbot заблокированы PROPOSAL → по духу дока (run #13-17 искали НОВЫЕ функц-углы) взял неисследованную ось sorting). **Live-gen всё ещё ЗАБЛОКИРОВАН WALLET-EMPTY** (vsegpt balance 5.449685 < 9 для writer — подтверждено live в omnia-prod-gw логах `[VSEGPT] HTTP 400 ... Potentially out of budget ... expected price 9, but you have only 5.449685`) → LIVE-RUNTIME против throwaway-контейнера из DEPLOYED base-образа (баг в базовом engine+kit, одинаков во всех аппах; method runs #14/#15/#16/#17) | nextjs_entities (engine+kit базовый) · live-runtime CRUD на starter Task (55 записей, built-in `due` date-поле), рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ FAIL (сортировка возвращает экстремум загруженных 50, не всего набора → неверный «топ») | **NEW BS-21**: сортировка выросшего списка возвращает экстремум загруженных ~50, а не всего набора — два слоя: (1) движок ИМЕЕТ корректную типизированную server-side сортировку (`sort`/`order` engine.ts:180-181 + касты `::numeric`/`::timestamptz` engine.ts:74-75); (2) managed-UI её НЕ зовёт — CrudResource каждая колонка сортируема (crud-resource.tsx:168), клик→`toggleSort` клиентский (data-table.tsx:363), сорт `rows`-пропа в памяти (data-table.tsx:305-307), useEntity не пробрасывает sort (use-entity.ts:21). Live на DEPLOYED (omnia-dev-dogfood-sortcap-probe-1bb521 port 3302, 55 Task, OLDEST due 2018-01-01): GET без params=50 MARK отсутствует, min-due в окне=2026-01-15 (лучший клиент-сорт топ), ?sort=due&order=asc&limit=200→TOP MARK 2018-01-01, ?sort=due&order=asc (50)→TOP MARK 2018-01-01 MARK ЕСТЬ (сервер сортирует ВЕСЬ набор потом топ-50 → проброс чинит даже при капе), ?limit=200=55 MARK есть. Отлично от BS-18 (записи скрыты) и BS-20 (нет server-примитива — здесь ЕСТЬ и работает при капе). Хуже тихого обрыва — сортировка возвращает НЕВЕРНУЮ строку, выглядящую авторитетно. Класс шире CRM | PROPOSAL P-SORT (пробросить sort-стейт DataTable в server-параметры useEntity, переиспользуя готовую engine-сортировку; TEMPLATE→base-rebuild+regen-verify, меняет list-data-flow всех аппов, логично с P-PAGINATE; wallet-blocked → не вслепую) + acceptance-lock test_entity_sort_windowing.py (4 green + 1 xfail) | PASS (новая слепая зона в never-tested «sorting»-оси: func FAIL доказан LIVE-RUNTIME на deployed-бинаре (клиент-окно топ=2026-01-15 vs server-sort топ=2018-01-01; server-sort работает даже при 50-капе) + код-root 2 поверхности + локализован file:line + заперт тестом; live-gen WALLET-EMPTY обойдён прямым CRUD к живому контейнеру как run #14/#15/#16/#17)
- 2026-06-16 18:53 MSK | #17 сценарий-1 (CRM/entity-app, НОВЫЙ функц-угол: НАХОДИМОСТЬ / ПОИСК при масштабе — раньше мерили create-persist #6, relation-visibility #13, clear #14, list-scale/pagination #15, ref-integrity #16, но НИКОГДА — может ли юзер НАЙТИ запись через поисковик, когда список вырос; rotation 18 mod 5 = 3 (tgbot), но tgbot заблокирован PROPOSAL P-BS5 + H1 заблокирован → по духу дока (run #13-16 искали НОВЫЕ функц-углы) взял неисследованную ось findability). **Live-gen всё ещё ЗАБЛОКИРОВАН WALLET-EMPTY** (vsegpt баланс 5.447765 < ~9 для writer; balance-call disabled на ключе — пробил writer-модель напрямую через gateway, тот же 400 out-of-budget) → LIVE-RUNTIME против throwaway-контейнера из DEPLOYED base-образа (баг в базовом engine+kit, одинаков во всех аппах; method runs #14/#15/#16) | nextjs_entities (engine+kit базовый) · live-runtime CRUD на starter Task (55 записей), рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ FAIL (запись за загруженной страницей ненаходима, поиск ложно «Ничего не найдено») | **NEW BS-20**: entity-апп не может НАЙТИ запись при масштабе — два слоя: (1) движок без server-side text-search, фильтр = точное равенство `data->>field = value` (engine.ts:176), нет ILIKE/substring/case; (2) DataTable-поиск чисто клиентский `.includes(q)` по ≤50 загруженным (data-table.tsx:299 + useEntity без limit, корень BS-18). Live на DEPLOYED (omnia-dev-dogfood-search-probe-972063, 55 Task): GET без params=50 MARK отсутствует, ?title=точное-полное=1, ?title=Zebra/UNIQUE(substring)=0, ?title=неверный-регистр=0, CONTROL ?priority=high(точное)=1 / ?priority=hi(префикс)=0, ?limit=200=55 MARK есть → запись в БД, но ненаходима через UI. Отлично от BS-18: починка капа НЕ чинит поиск (некуда проталкивать query). Хуже тихого обрыва — поисковик активно утверждает «нет записи». Класс шире CRM | PROPOSAL P-SEARCH (server-side text-search примитив engine ILIKE/`q` + проталкивание query из useEntity/CrudResource; TEMPLATE+engine, base-rebuild+regen-verify, меняет list-data-flow всех аппов, логично делать с P-PAGINATE → не вслепую) + acceptance-lock test_entity_search_findability.py (4 green + 1 xfail) | PASS (новая слепая зона в never-tested «findability/search»-оси: func FAIL доказан LIVE-RUNTIME на deployed-бинаре (substring=0 / exact=1 / CONTROL filter работает) + код-root 2 поверхности + локализован file:line + заперт тестом; live-gen WALLET-EMPTY обойдён прямым CRUD к живому контейнеру как run #14/#15/#16)
- 2026-06-16 18:37 MSK | #16 сценарий-1 (CRM/entity-app, НОВЫЙ функц-угол: РЕФЕРЕНЦИАЛЬНАЯ ЦЕЛОСТНОСТЬ при DELETE — раньше мерили create-persist run #6, update/clear run #14, list-scale run #15, relation-visibility/picker run #13, но НИКОГДА удаление родителя, на которого ссылаются дети; rotation 17 mod 5 = 2 (SPA), но сценарий-2 закрыт ЗДОРОВЫМ run #8 + H1 заблокирован PROPOSAL → по духу дока (run #13-15 искали НОВЫЕ функц-углы, а не ре-раны) взял неисследованную ось relational-DELETE). Впервые измерено, что происходит с РЕБЁНКОМ, когда удаляешь РОДИТЕЛЯ. **Live-gen всё ещё ЗАБЛОКИРОВАН WALLET-EMPTY** → LIVE-RUNTIME против throwaway-контейнера из DEPLOYED base-образа (баг в базовом engine, одинаков во всех аппах; method runs #14/#15) | nextjs_entities (engine базовый) · live-runtime CRUD (Client+Note через docker cp), рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ FAIL (родитель удаляется, дети сиротеют молча) | **NEW BS-19**: entity-движок без референциальной целостности — три слоя: write принимает bogus-ref (Note clientId «00000000-…»→201), DELETE без restrict/cascade/warn (DELETE Client→200, Note выжила), expandRecords молча обнуляет мёртвую ссылку (`_expanded.clientId=null` при живом мёртвом `clientId`). Live на DEPLOYED (omnia-dev-dogfood-refdel-probe-41de56): обе ноты осиротели «—», «чья запись?» неотвечаемо. Корень: createRecord engine.ts:217-226 (нет ref-валидации) + deleteRecord engine.ts:295-315 (нет child-scan) + expandRecords engine.ts:143-148 (silent null). Семья BS-13+BS-16; класс шире CRM | PROPOSAL P-REFINTEGRITY (семантическое решение RESTRICT/CASCADE/WARN, мульти-поверхностно engine+apps/web, base-rebuild+regen-verify, data-integrity-чувствительно → не вслепую) + acceptance-lock test_entity_referential_integrity.py (3 green + 1 xfail) | PASS (новая слепая зона в never-tested «relational-DELETE»-оси: func FAIL доказан LIVE-RUNTIME на deployed-бинаре (orphan + bogus-ref-201 + delete-200) + код-root 3 поверхности + локализован file:line + заперт тестом; live-gen WALLET-EMPTY обойдён прямым CRUD к живому контейнеру как run #14/#15)
- 2026-06-16 18:26 MSK | #15 сценарий-1 (CRM/entity-app, НОВЫЙ функц-угол: МАСШТАБ СПИСКА / server-pagination — раньше мерили create-persist run #6, update/clear run #14, relation-visibility run #13, но НЕ что происходит, когда список перерастает дефолтный кап; rotation 16 mod 5 = 1; H1 заблокирован PROPOSAL → ротация). Впервые измерено ПОВЕДЕНИЕ РАСТУЩЕГО СПИСКА (>50 записей), а не stack-эскалация/auth/runtime/anchors/lead-sink/relation/clear. **Live-gen всё ещё ЗАБЛОКИРОВАН WALLET-EMPTY** (balance-call disabled на ключе; баланс ~5.45 < ~9 нужных для writer как run #12-14) → пивот на LIVE-RUNTIME против throwaway-контейнера из DEPLOYED base-образа (генерация не нужна: кап в базовом engine+kit, одинаков во всех аппах) | nextjs_entities (engine+kit базовый) · live-runtime CRUD на starter Task, рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ FAIL (overflow недостижим) | **NEW BS-18**: managed-список молча обрезан на 50 — нет server-pagination, записи 51+ недостижимы через UI. Live на DEPLOYED (omnia-dev-dogfood-listcap-probe-cd7c7c): 55 Task → GET без limit = 50 (#6..#55, старейшие #1-#5 пропали), `?limit=200`=55, `?page=2`=5 (движок умеет, UI не просит). Корень: useEntity грузит 1 раз без limit/page (use-entity.ts) + engine DEFAULT_LIMIT=50 (engine.ts:38,187) + DataTable пагинирует in-memory ≤50 (crud-resource.tsx:549,555) → «5 страниц по 10» выглядит полным. Класс шире CRM: любой растущий список; семья BS-14+BS-16 | PROPOSAL P-PAGINATE (TEMPLATE→base-rebuild; band-aid limit:200 двигает обрыв+грузит 200×ref, правильный = server-pagination мульти-поверхностно → не вслепую) + acceptance-lock test_list_pagination_cap.py (4 green + 1 xfail) | PASS (новая слепая зона в never-tested «list scale»-оси: func FAIL доказан LIVE-RUNTIME на deployed-бинаре #1-#5 dropped + код-root 4 поверхности + локализован file:line + заперт тестом; live-gen WALLET-EMPTY обойдён прямым CRUD к живому контейнеру как run #14)
- 2026-06-16 18:09 MSK | #14 сценарий-1 (CRM/entity-app, НОВЫЙ функц-угол: ЦИКЛ РЕДАКТИРОВАНИЯ/UPDATE — раньше проверяли лишь create-persist run #6; rotation 15 mod 5 = 0 → H1 заблокирован PROPOSAL P-H1 → ротация → 1). Впервые измерена операция UPDATE и в частности ОЧИСТКА поля (un-set), а не stack-эскалация/auth/runtime/anchors/lead-sink/relation-visibility. **WALLET ВЕРНУЛСЯ частично, но live-gen всё равно ЗАБЛОКИРОВАН**: tiny deepseek-chat max_tokens=5 probe прошёл (почти бесплатно), но реальная AD/writer-генерация (deepseek-v4-pro-thinking) 400-ит — `Potentially out of budget: …expected price 9, but you have only 5.447765 on account` (баланс даже ниже run #12/#13 5.4497). 2 retry → brief chars=0 → writer rate-limit/out-of-budget → пивот на TEMPLATE-source + LIVE-RUNTIME против уже-живого контейнера (генерация не нужна: баг в базовом engine, одинаков во всех аппах) | nextjs_entities (engine базовый) · live-runtime CRUD + template-source, рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ: change PASS / clear FAIL | **NEW BS-17**: редактирование НЕ может очистить опц. поле — molчаливый no-op с ложным success. Live на DEPLOYED: CREATE notes+due → PUT {title,priority} (точный payload формы при очистке) → 200 + updated_at сдвинулся → GET: notes/due ВЫЖИЛИ (CLEAR_BUG=True); CONTROL смена на новые значения применяется (CHANGE_WORKS=True). Корень: form omit-ит пустые (entity-form.tsx:158-164) + updateSchema `.optional()` не `.nullable()` → null невыразим (registry.ts:176-182) + engine shallow-merge keeps-absent (engine.ts:286) | PROPOSAL P-CLEAR (мульти-поверхностный: nullable updateSchema + form-emit-clear в edit + type-edge + base-rebuild; меняет write-validation семантику всех аппов → не вслепую) + acceptance-lock test_entity_field_clear_gap.py (3 green + 1 xfail) | PASS (новая слепая зона в never-tested UPDATE-цикле: func FAIL доказан LIVE-RUNTIME на deployed-бинаре + 3-поверхностный код-root + локализован file:line + заперт тестом; CONTROL опровергает «edit полностью сломан» — ломается ровно un-set; live-gen WALLET-EMPTY обойдён прямым CRUD к живому контейнеру)
- 2026-06-16 17:51 MSK | #13 сценарий-1 (CRM/entity-app, НОВЫЙ функц-угол: РЕЛЯЦИОННАЯ ЮЗАБИЛИТИ — резолвятся ли связи в UI; rotation 14 mod 5 = 4 → но H1 заблокирован PROPOSAL и сценарий-4 закрыт run #11/#12 → проверил неисследованную ось relational-CRM на entity-пути). Впервые измерена ДОСТИЖИМОСТЬ/ВИДИМОСТЬ СВЯЗАННЫХ записей в entity-аппе (а не stack-эскалация/auth/runtime/anchors/lead-sink). Live-gen ЗАБЛОКИРОВАН WALLET-EMPTY (vsegpt HTTP 400 «only 5.449685 on account») → пивот на артефакт-анализ repo_svc.read_files 2 РЕАЛЬНЫХ песочниц (crm-verify-2a9337, dent-crm-bdc3f3) + template-source | nextjs_entities (оба) · артефакт-анализ, рендер не запускался | дизайн n/a (не рендерил) · ФУНКЦ: picker PASS / list-видимость FAIL | **VERIFY+NEW**: (1) опроверг гипотезу «relation = UUID-текстбокс» — writer в ОБОИХ аппах корректно мапит schema-reference→`kind:"reference"`+`refEntity`, EntityForm грузит `entities[refEntity].list({limit:200})`→реальный `<Select>` picker (ЗДОРОВО). (2) **NEW BS-16**: reference-поле невидимо в СПИСКЕ — CrudResource авто-расширяет ref-данные (crud-resource.tsx:171-184, `row._expanded` грузится) но НЕ авто-рендерит колонку (:168 колонки дословно), SYSTEM_PROMPT не требует → writer-вариативность: dent-crm рендерит `_expanded.patientId.name`✓, а crm-verify bookings И notes ОБА дропнули clientId-колонку → «чья запись?» не ответить в списке; расширенные данные выброшены | PROPOSAL P-REFCOL (детерм. авто-колонка в CrudResource ИЛИ prompt-директива; template→base-rebuild+regen-verify, сейчас wallet-blocked → не вслепую) + acceptance-lock test_reference_column_surfacing.py (4 green + 1 xfail) | PASS (новая слепая зона: func FAIL доказан кодом+2 живыми артефактами+template-source, локализован file:line, заперт тестом; попутный verify: relation-picker ЗДОРОВ — опровергнуто по доказательству; live-gen WALLET-EMPTY обойдён через repo-storage evidence как run #12)
- 2026-06-16 17:36 MSK | #12 сценарий-4 (free — НОВЫЙ функц-угол: РАБОТАЕТ ЛИ lead-форма; rotation 14 mod 5 = 4). Впервые измерено ПОВЕДЕНИЕ ПРИ ОТПРАВКЕ ФОРМЫ (а не stack-эскалация/auth/runtime/anchors/card-reachability) — прямой замер owner-жалобы «кнопки ничего не делают» на канон-кейсе сайт-визитки. Live-gen dogfood-photostudio-kadr-773864 (mint-token owner 2aa9b1a2) — фотостудия-лендинг с формой обратной связи, skip_clarify=true | blank / freeform / deepseek-v4-pro-thinking · **live-gen ЗАБЛОКИРОВАН WALLET-EMPTY** (vsegpt HTTP 400 «out of budget … only 5.449685 on account» → brief chars=0 → writer 429; snapshot не создан) → пивот на код+2 РЕАЛЬНЫХ артефакта (repo_svc.read_files из прод-хранилища) | дизайн n/a (нет рендера) · ФУНКЦ FAIL | **NEW BS-15**: freeform lead/контакт/подписка-формы = клиентский ТЕАТР — собирают PII (имя/тел/email), показывают «Спасибо, перезвоним», и МОЛЧА выбрасывают (action=0/mailto=0/fetch=0). Доказано: mortgage-форма handler читает name+phone→hide+success (дроп); travel-blog `onsubmit="preventDefault();innerHTML='Спасибо!'"` чистый театр. Корень: промпт санкционирует «formspree-ПЛЕЙСХОЛДЕР» (prompt_builder.py:491-493,606-607) + нет lead-приёмника (public.py GET-only) + find_dead_links слеп к форме без `<a>`. Хуже мёртвой кнопки — ложное подтверждение, владелец = НОЛЬ заявок | PROPOSAL P-LEAD (public lead-receiver+storage+owner-inbox apps/web+writer-POST-директива → кросс-зонно, не вслепую) + acceptance-lock test_lead_capture_sink.py (2 green + 1 xfail) | PASS (новый класс слепой зоны: func FAIL доказан кодом+2 живыми артефактами+архитектурным root (нет POST-эндпойнта), локализован file:line, заперт тестом; live-gen WALLET-EMPTY обойдён через repo-storage evidence)
- 2026-06-16 17:28 MSK | #11 сценарий-4 (free — НОВЫЙ угол: МУЛЬТИ-СТРАНИЧНЫЙ КОНТЕНТ, до сих пор не тестился; rotation 12 mod 5 = 2 → сценарий-2 закрыт ЗДОРОВЫМ run #8 → ротация → 4). Впервые проверена ДОСТИЖИМОСТЬ контента в feed-of-items (а не stack-эскалация/auth/runtime). Live E2E: dogfood-travel-blog-029341 (mint-token owner 2aa9b1a2) — «блог о путешествиях: лента, ОТДЕЛЬНАЯ страница на статью, рубрики+фильтр, об авторе, подписка», skip_clarify=true | blank / freeform / deepseek-v4-pro-thinking · preset=hospitality-escape (discovery не эскалировал — корректно для блога, но архетипа content-collection нет) | дизайн ~7/10 (editorial-luxury по брифу: Instrument Serif+DM Sans, мятный акцент дозой, чистая иерархия; минусы по ui_audit: typography>Malewicz-бюджет, 10 цветов>8, button-heights [36,40,52]; + acceptance struct=False: overflow@360/768/1440 — shipped advisory) · ФУНКЦ FAIL | **NEW BS-14**: мультистатейный блог → 4 из 5 article-card НЕКЛИКАБЕЛЬНЫ (нет `<a>`/href/onclick; Playwright hasLink=False ×4), полный текст лишь у 1 featured (#article-kyoto); «отдельная страница на статью» структурно невыполнима в одностраничной статике; writer НАРУШИЛ свой промпт-запрет «кнопка-обманка без обработчика» (prompt_builder.py:499-500), а `find_dead_links` слеп к карточкам без `<a>` (link_validator.py:24-46). ПОЗИТИВ: категорийный фильтр (JS) + форма подписки работают; картинки 200/490KB у реального юзера (белая полоса = render-env NAT-hairpin, НЕ баг) | PROPOSAL P-BLOG (blog/content-collection-архетип ИЛИ writer-директива «каждая статья достижима» + base-rebuild+regen-verify; детерм. авто-репэйр FP-опасен → не вслепую) + acceptance-lock test_blog_feed_reachability.py (1 green + 1 xfail) | PASS (новый класс слепой зоны: func FAIL доказан live Playwright+source+acceptance, локализован file:line, заперт тестом; ложная гипотеза «broken images» отброшена по доказательству)
- 2026-06-16 16:22 MSK | #10 сценарий-1 (CRM с нуля, skip_clarify=true; rotation 11 mod 5 = 1; H1/BS-5/P-EC/P-AUTHGATE/P-RBAC заблокированы как PROPOSAL → ротация). НОВЫЙ угол: впервые проверена ФУНКЦ-ЦЕЛОСТНОСТЬ entity-аппа НА УРОВНЕ РАНТАЙМА (резолвятся ли вообще сущности), а не только stack-эскалация/auth. Live E2E: dogfood-crm-edit-563092 (mint-token owner 2aa9b1a2) | blank→nextjs_entities / freeform / deepseek-v4-pro-thinking (writer) · preset=local-services (BS-4 эскалация снова ✓) | дизайн ~6.5/10 (чистая иерархия, KPI-карты, хорошие empty-states; но МОНОХРОМ-grey вместо brief-палитры blue #2563EB = BS-6b опять) · ФУНКЦ FAIL→FIXED | **NEW BS-12 (severe, app-killer)**: AD-бриф назвал entity-файлы lowercase-plural (`clients.json`), writer скопировал ДОСЛОВНО, рантайм-реестр резолвит по `entities/<Name>.json` → 404 «unknown entity 'Client'» на КАЖДЫЙ read/write → весь CRM DOA (console `ApiError: unknown entity`, всё пусто, create падал). Вторично NEW BS-13: `CrudResource` detail-панель без слота для child-сущностей → per-client «Заметки/История визитов» из брифа недостижимы (Note-сущность осиротела в билде #1) | **FIX 989a7a8**: `_normalize_entity_filenames` realign filename↔name в container-guard перед commit+hot_reload (детерм/идемпотент/fail-soft); 5 тестов + 43 passed/6 xfail без регресса; deploy api/worker; live-verify DEPLOYED: guard `clients->Client,notes->Note`, новый билд → `Client.json`, `GET /api/entities/Client` 404→401 (резолвится), dashboard рендерит. BS-13 → PROPOSAL P-RELATED (не вслепую) | PASS (severe app-killer найден live по console+source+registry-коду, фикс доставлен и доказан на DEPLOYED-бинаре end-to-end 404→401; 2-я слепая зона локализована file:line)
- 2026-06-16 15:48 MSK | #9 сценарий-4 (free — НОВАЯ ОСЬ: качество ITERATION/surgical-edit на static-лендинге, а не first-build stack-selection; rotation 10 mod 5 = 0 → H1 заблокирован PROPOSAL → ротация → 4). Прямой замер owner-жалобы «генератор ничего не делал» на пути, который ДОЛЖЕН работать. Live E2E: dogfood-bakery-edit-18ca73 (mint-token owner 2aa9b1a2) — first-build «лендинг пекарни» skip_clarify=true, затем 3 follow-up-правки | blank/freeform/deepseek-v4-pro-thinking (build) + surgical/plain/deepseek-chat (edits) · preset restaurant-warm | дизайн ~7.5/10 (тёплая bakery-палитра #B45309, ui_audit 8/10, чистая иерархия) · ФУНКЦ: EDIT-путь PASS | НАХОДКА BS-11: (1) additive+modify правка (новая секция «Цены» 90/130/1600₽ + телефон +7 999 248-13-77) — ПРИЗЕМЛИЛАСЬ идеально, все 6 секций + tel: сохранены → owner-жалоба НЕ воспроизвелась на легит-правках (изолирована к app-ification=H1). (2) removal-правка «убери отзывы» осиротила nav-якорь `href="#reviews"` (id удалён, ссылка осталась ×3) — surgical-путь пропускает dead-link-пасс (messages.py:2586), inline-fixer не чинит anchor-to-missing-id (link_validator.py:112) | FIX 585e65c: `repair_orphaned_anchors_inline` delta-scoped (только id, удалённые этой правкой; детерм/без LLM/без regen), wired в surgical-путь; 3 теста+5 зелёных; deploy api/worker, health 200; live-verify DEPLOYED 3 dead→0 + live E2E «удали секцию» → `orphaned_anchors repaired`, `#process` починен, пред-существ `#reviews` НЕ тронут | PASS (новая слепая зона найдена+локализована+live-репро, фикс доставлен и доказан на DEPLOYED-бинаре; попутно опроверг owner-страх для контент-правок)
- 2026-06-16 15:24 MSK | #7 сценарий-4 (free, реальный prod-промпт «веб-приложение для школы №7: журнал/расписание/ДЗ/новости, кабинеты ученик/родитель/учитель», skip_clarify=true; rotation 8 mod 5 = 3 → tgbot заблокирован P-BS5 → ротация → 4). НОВЫЙ угол: впервые мульти-РОЛЕВОЙ апп (3 актора с разными вью), а не single-owner CRUD | blank→nextjs_entities / freeform / deepseek-v4-pro-thinking · preset education-bright (BS-4 эскалация снова ✓) | дизайн ~5.5/10 (структура чистая, 3-role «Три роли — один интерфейс»-карты; но ПУРЕ-монохром — нишевая палитра брифа #7C2D12/#FEFCE8/#0369A1+Archivo НЕ применена к :root = BS-6b опять; сломанный стат «0P») · ФУНКЦ FAIL | NEW BS-9: мульти-ролевой/RBAC пробел — рантайм знает только owner\|public\|admin (registry.ts:46) + admin\|user (auth.ts:149); AD спроектировал 3 роли, writer схлопнул → 6×public + JournalEntry=owner. Live: anon GET Parent → 200+телефоны родителей (УТЕЧКА персданных); JournalEntry 401/owner → родитель видит ПУСТОЙ журнал (оценки ребёнка недостижимы) | PROPOSAL P-RBAC (per-app роли + relationship-scoped read-own — engine+registry+auth+apps/web+writer, security-sensitive → не вслепую) + acceptance-lock test_entity_rbac_gap.py (4 green + 1 xfail) | PASS (новый класс слепой зоны: func FAIL доказан live+код+screenshot, локализован file:line, заперт тестом)
- 2026-06-16 15:07 MSK | #6 сценарий-1 (CRM с нуля, skip_clarify=true; rotation 6 mod 5 = 1; H1/BS-5/P-EC заблокированы как PROPOSAL → ротация). НОВЫЙ угол: впервые проверена ФУНКЦ-ЛИВНОСТЬ escalated entity-аппа live (auth+CRUD+refs), а не только «эскалировал ли stack» | blank→nextjs_entities / freeform / deepseek-v4-pro (writer) | дизайн ~6/10 (структура чистая; но лендинг — серый дефолт, нет нишевой палитры — BS-6b) | FUNC PASS: auth (register 200→signin 302→session), CRUD персистит (Client 201+list, Note 201+`_expanded.clientId`), CrudResource авто-expand рефов — escalated entity-апп ГЕНУИННО живой (закрывает H1-страх для entity-пути). NEW BS-8: защищённая `(app)`-группа гейтится ТОЛЬКО клиентски → anon GET /dashboard = 200+полный shell (данные НЕ текут, API 401+owner-scope; но «приватный кабинет» сломан + 401→`[]`→пустой апп при истёкшей сессии) | PROPOSAL P-AUTHGATE (edge-safe middleware ИЛИ mandatory server-gate — security+кросс-зонно → не вслепую) + acceptance-lock test_entity_auth_gate.py (2 green + 1 xfail) | PASS (FUNC PASS доказан live; NEW тупняк локализован file:line + live-репро + заперт тестом)
- 2026-06-16 15:27 MSK | #8 сценарий-2 (SPA-tool «калькулятор ипотеки с графиком платежей»; rotation 7 mod 5 = 2 на момент старта; параллельные run #6/#7 приземлились по ходу — relabel #6→#8; H1 уже CONFIRMED+locked → ротация). РЕАЛЬНАЯ E2E-генерация через продукт: mint-token owner 2aa9b1a2 → POST /projects → POST /prompt skip_clarify=true → dogfood-mortgage-calc-tool-db81b7 | freeform-static / template=blank / deepseek-v4-pro-thinking · preset=realestate-premium | дизайн ~7/10 (тёмно-зелёный монохром, рабочие слайдеры + результат-карты + donut-SVG + таблица графика + lead-форма; минусы: donut мелкий, ui_audit=5/10 typography/button-rules/a11y) · ФУНКЦ PASS | BS-10 (low-sev): нет детерминированного static→spa, spa только от LLM-discovery; skip_clarify-путь собирает инструмент freeform-СТАТИКОЙ. НО для калькулятора static+inline-JS = ПОЛНОСТЬЮ рабочий (Playwright: слайдеры пересчитывают 83 311₽→347 430₽, SVG-график, lead-форма; CHANGED=True). Probe: «калькулятор/конвертер» × норм → spa 5/6, 1× ReadTimeout→static | NONE (фикса нет — сценарий ЗДОРОВ: discovery берёт spa корректно, static-инструмент работает; BS-10 записан low-sev, форс static→spa = риск+cost без выигрыша). Verified acceptance: vision-score ADVISORY с V1.6 (acceptance.py:217), score=5 не гейтит — by design, не баг | PASS (verify: проверил подозреваемую слабую зону — оказалась здоровой; evidence: live-gen + Playwright-интеракция + screenshot + discovery-probe + код)
- 2026-06-16 14:44 MSK | #5 сценарий-4 (free, реальный prod-промпт «Магазин цветов «Флора»: каталог+корзина+оформление+отзывы+кабинет», skip_clarify=true; rotation 4 mod 5) | nextjs_entities / freeform-render / deepseek-v4-pro (writer) | дизайн ~4/10 (broken hero img → gold-плейсхолдер; НЕВИДИМАЯ вторичная CTA white-on-white; dark-on-dark low contrast) | BS-6: e-commerce запрос схлопывается в authed-dashboard CRM — каталог за /signin, НЕТ корзины/checkout, хотя бриф их явно назвал (SYSTEM_PROMPT.md:82,96,118 зашивает «/dashboard binding, app not landing»). BS-4-эскалация сама сработала end-to-end ✓ | PROPOSAL P-EC (новый storefront-архетип, крупно+кросс-зонно → не шипить вслепую) + design-нота BS-6b | PASS (func FAIL пойман: entities+routes+CTA+screenshot+system-prompt; тупняк локализован file:line, доказан)
- 2026-06-16 13:02 MSK | #0 H1-репро | n/a (анализ кода + live decide_intent в prod-контейнере, генерация не запускалась) | n/a | H1 CONFIRMED: follow-up app-ification → CHEAP surgical-edit статики, stack-эскалация структурно недостижима (messages.py:626,675); 4/4 формулировки → CHEAP в prod | repro-тест (xfail) + PROPOSAL P-H1 (фикс деструктивный → не шипить вслепую) | PASS (тупняк локализован, доказан)
- 2026-06-16 14:22 MSK | #3 сценарий-1 (CRM с нуля, skip_clarify=true = quiz-путь) | PRE: blank/freeform/deepseek-v4-pro-thinking → POST: nextjs_entities/entity-app | ~8/10 (бриф: emerald+amber, Libre Franklin+Lora, нишевая утилитарность) | BS-4: first-build с пропущенным discovery не эскалирует stack → app-запрос = freeform-статика с мёртвыми кнопками входа (messages.py:706) | FIX 5878059 (на first-build без discovery переиспользовать `_infer_stack_from_text`; deploy api+worker; repro в test_stack_routing.py) — verify: тот же промпт → template flips blank→nextjs_entities, реальный CRM-бриф (clients/bookings/notes + auth-кабинет) | PASS. Сайд-нота: /api/models 500 (model config provider='google' ∉ ModelInfo literal) — отдельный пре-существующий баг, не фиксил (1 фикс/прогон)
- 2026-06-16 13:24 MSK | skipped: guard-3 sequential-lock fresh (_routine/dogfood.lock = 10:20:08Z, age 4 мин < 15) — параллельный прогон #1 в полёте, lock не снимал | n/a | n/a | yielded | SKIP
- 2026-06-16 14:07 MSK | #2 SPA-tool «калькулятор ипотеки с графиком» (rotation 2 mod 5; H1 уже CONFIRMED+locked → ротация) | nextjs_entities / freeform / deepseek-v4-pro-thinking | 7/10 (лендинг чистый монохром; но это лендинг, не калькулятор) | BS-3: «без регистрации» ×3 → LLM-discovery выбрала nextjs_entities → калькулятор за /signin + БД-история; safety-net однонаправленный (discovery.py:838) | fix 04bb7cf (негативный safety-net `_explicit_no_backend`→spa, 3 теста, деплой api/worker, health 200, VETO live-verified) | PASS (func FAIL пойман по скриншоту+логам+DB, фикс доставлен и доказан)
- 2026-06-16 14:46 MSK | #5 сценарий-1 вариант (лендинг автосервиса «ТурбоФикс» — реальная E2E-генерация через normal discovery path, skip_clarify=false, quiz «Лендинг»+«Строгое») | nextjs_entities / freeform / deepseek-v4-pro-thinking · preset=local-services | дизайн 7.5/10 (тёмный+crimson, нишевый automotive, иерархия ок; минусы: crimson-радиал чуть AI-generic, hero тесноват) · ФУНКЦ FAIL | BS-7: консьюмер-букинг «запись на ремонт» форс-эскалирован в auth-gated nextjs_entities, ВСЕ 5 CTA «Записаться» → /signin (клиент обязан зарегаться, чтобы записаться); discovery.py:90,130 — `_BACKEND_SIGNALS` «запись на»/«бронирован» = proof аккаунтов, negative-net ловит только «без регистрации», quiz-pick «Лендинг» без веса | acceptance-lock test_stack_routing.py (3 xfail + 3 evidence, 27 passed/5 xfailed) + PROPOSAL P-BS7 (фикс двунаправленно-рискованный + архитектурный пробел lead-form без клиентского auth → не шипить вслепую) | PASS (новая слепая зона: func FAIL пойман по rendered page.tsx + screenshot + live discovery, локализован, заперт тестом)
- 2026-06-16 14:22 MSK | #4 Telegram-бот «бот для записи в барбершоп» (scenario 3 — выбран при старте, когда RUNS=3; параллельный run #3 приземлился по ходу, сместив rotation; tgbot помечен «ранее orphaned» — проверено) | n/a (live `run_discovery(force_build=True)` в prod-контейнере; генерация не запускалась — стек недостижим by design) | n/a | BS-5: tgbot/api ОРФАНЫ — discovery `_STACKS`/`_SYSTEM`-меню (discovery.py:53,418-450) + `_DISCOVERY_STACK_TO_TEMPLATE` (stack_routing.py:47) без tgbot; запрос бота → `nextjs_entities`/`fullstack` (веб-апп), готовый telegram-bot-aiogram unreachable | acceptance-lock test_stack_routing.py (2 xfail + evidence) + PROPOSAL P-BS5 (фикс крупный: вокаб+routing+backend-preview+token-UX → не шипить вслепую) | PASS (тупняк локализован, live-доказан, заперт тестом)
- 2026-06-17 22:25 MSK | #27 сценарий-2 SPA-tool «калькулятор ипотеки с графиком» (rotation 27 mod 5 = 2; H1 заблокирован PROPOSAL P-H1 → ротация; РЕАЛЬНАЯ live-gen, 3-й прогон подряд после wallet-recovery; повтор сценария #2 — тогда нашёл BS-3 «калькулятор форс-эскалировался в auth/entities», FIX 04bb7cf; этот прогон проверяет, что фикс держится) | gen_mode=freeform · template=blank · preset=realestate-premium · model=deepseek-v4-pro-thinking | дизайн ~8.5/10 (emerald/gold real-estate, serif-hero, рабочая карточка калькулятора, canvas-чарт амортизации; mobile responsive чисто) | **ФУНКЦ PASS** — калькулятор РЕАЛЬНО работает (корректная аннуитет-формула 66 324 ₽ @ 6M/10.5%/15л, полный график амортизации, реактивные слайдеры 6M→12M ⇒ ×2 платёж, canvas painted), публичный share `/p/<slug>`=200+68KB, BS-3-зона ЦЕЛА (tool НЕ эскалирован в auth/entities) | **NEW BS-30 (low-sev)**: freeform шьёт ВЫДУМАННЫЕ конкретные trust-сигналы на голый инструмент (фейк-`tel:+78005553535`, «15 банков-партнёров», «4.9★ на Банки.ру», «одобрение за 12 мин») + футер 10× различно-подписанных ссылок (легал/нав) ВСЕ→`#hero` (семантические тупики; dead-link-чекер валидирует резолв якоря, не label↔target) | none (оба под-фикса fuzzy/low-value, max 1 фикс/прогон, tool-путь в остальном здоров) | **PASS** (тупняк #2 фикс держится, найдены 2 новые слепые зоны, заперты в BS-30)
- 2026-06-17 22:54 MSK | #28 сценарий-4 (free, реальный промпт из логов прода): **«CRM для отдела продаж: сделки со стадиями (новая/в работе/переговоры/выиграна/проиграна) в виде КАНБАН-доски с перетаскиванием»** — повторяющийся реальный запрос (3 отдельных проекта: chistyi-crm-demo, demo-crm, pokaz-crm), никогда не догфуженный канбан-угол. **LIVE-GEN ВОССТАНОВЛЕН 4-й прогон подряд**: dogfood-crm-kanban-sdelki-882231, mint-token owner 2aa9b1a2, skip_clarify=true | blank→**nextjs_entities** (BS-4 эскалация LIVE ✓) / freeform+art-director / deepseek-v4-pro-thinking · preset=local-services · cost_rub 5.50 | дизайн ~6.5/10 (чистый SaaS-лендинг Aura CRM: hero+stat-strip+features+pricing+testimonials+FAQ+CTA-band, секция-кит работает, иерархия ок; МИНУС: моно-grey-на-grey, акцент #0EA5E9 почти не используется кроме одного ПУСТОГО hero-градиент-бокса — BS-6b снова; generic feature-карточки) · ФУНКЦ **ЧАСТИЧНО** — позитив: канбан ДОСТАВЛЕН структурно (writer применил `<CrudResource entity="Deal" view="board">` ×2, кит board-view с drag-to-save РЕАЛЬНО есть crud-resource.tsx:92-102,302), entities Client/Deal/User + dashboard-роуты + auth-shell под запрос; **НО НОВЫЙ БАГ**: `entities/Client.json` пришёл с НЕВАЛИДНЫМ JSON (`"access":",`) → `loadEntity`→null → live `GET /api/entities/Client` **404 «unknown entity»** (Deal=401 валиден) → центральная сущность DOA, список клиентов/picker/KPI молча мёртвы; BS-8 переподтверждён (anon→полный app-shell без редиректа) | **NEW BS-31** (writer шлёт битый entity-JSON, ни один гард не ловит — `_normalize_entity_filenames` пропускает непарсящееся messages.py:1227-1228, registry тихо нуллит registry.ts:87-89; отлично от BS-12 где JSON валиден) + PROPOSAL P-ENTITYJSON (фикс = валидация entity-JSON в guard-цепочке + micro-regen битого файла ИЛИ видимая build-fail; generic-repair не спасает, regen policy-смежен → не вслепую) + acceptance-lock test_entity_json_validity.py (2 green + 1 xfail) | **PASS** (live-gen, реальный recurring-запрос, канбан-фича доставлена, 1 новая слепая зона заперта; max 1 фикс/прогон → proposal, не blind-ship)
- 2026-06-16 23:25 MSK | #29 сценарий-4 (free, реальный prod-промпт «CRM для барбершопа … список клиентов (статус: новый/постоянный/VIP) … записи на стрижку (дата, услуга, мастер) … заметки», skip_clarify=true; ротация 29 mod 5 = 4; H1/tgbot заблокированы PROPOSAL). РЕАЛЬНАЯ live-gen, 5-й прогон подряд: dogfood-barber-crm-5d864d, mint-token owner 2aa9b1a2, blank→nextjs_entities (BS-4 эскалация сработала ✓), freeform+art-director/deepseek-v4-pro-thinking/preset=beauty-elegant/cost_rub 4.31 | nextjs_entities / freeform(art-director .tsx) / deepseek-v4-pro-thinking | дизайн ~7/10 (лендинг «Chop & Crown» чисто+иерархия, но монохром-grayscale без барбер-характера, «0 ₽» плейсхолдер; app-shell teal чистый, рабочие Select+empty-states) | НОВЫЙ ТУПНЯК **BS-32**: entity-движок без time-of-day — booking-форма «Дата и время визита» = `mm/dd/yyyy` date-пикер, время визита неуловимо (FieldType без datetime/time, виджет type=date, toDateInput slice(0,10)); writer сам хотел время (лейбл+колонка «Время»), но мог взять только `"type":"date"`. Класс: любой scheduling-апп | PROPOSAL P-DATETIME (мульти-поверхностный template+writer фикс, base-rebuild+regen-verify → не вслепую) + acceptance-lock test_entity_datetime_gap.py (3 green + 1 xfail) | PASS (находка+доказана live+source, dev-контейнер удалён после прогона; positive: эскалация/фильтр-статус/ref-колонка/calendar+split-views/auth все рабочие; BS-29 brand-desync повторился) | PASS
- 2026-06-16 23:40 MSK | #30 сценарий-1 (CRM с нуля «вход, список клиентов, добавление/редактирование, заметки, дашборд», skip_clarify=true; ротация 31 mod 5 = 1; H1/tgbot заблокированы PROPOSAL). РЕАЛЬНАЯ live-gen, 6-й прогон подряд: dogfood-crm-klienty-87e683, mint-token owner 2aa9b1a2, blank→nextjs_entities (BS-4 эскалация сработала ✓), freeform+art-director/deepseek-v4-pro-thinking/preset=local-services/cost_rub 4.33; сущности Client+Note+Task + (app)-роуты /dashboard /clients /tasks | nextjs_entities / freeform(art-director entity) / deepseek-v4-pro-thinking | дизайн ~5/10 (лендинг чистая иерархия+типографика, но полностью МОНОХРОМ — forced --primary green не используется на лендинге, нет ниши/глубины/картинок, фейк-цифры 142/68%/3) | **BS-31 RE-CONFIRMED LIVE (2/2 consecutive реальных CRM-гена → НЕ флук, доминирующий failure-mode)**: ЦЕНТРАЛЬНАЯ `entities/Client.json` снова сломана deepseek'ом, но ДРУГИМ glitch-классом, чем #28 — `"name":{ "type":"string",": true }` (проглочено имя ключа `"required"`) → `JSON.parse` «Invalid control character line 6 col 42», Note/Task парсятся ОК; live-proof dev-контейнер: anon `GET /api/entities/Client`→404 unknown entity vs Note/Task→401 → client-срез CRM молча DOA, билд=SUCCESS; ни один гард не валидирует entity-JSON (messages.py:1227-1228, registry.ts:87-89) | НЕ шипить вслепую — P-ENTITYJSON (regen-1-файл / visible-fail / drop-entity = продуктовое+policy-смежное решение, детерм. repair невозможен — corruption съел контент); СЕВЕРНОСТЬ BS-31 ПОВЫШЕНА; evidence в `_routine/runs/2026-06-16T20-31Z/` | FAIL (core entity DOA; BS-29 brand-desync «Omnia.CRM» vs tab «dogfood crm klienty» повторился; positive: BS-4 эскалация+Note/Task+ref-структура рабочие; dev-контейнер+проект удалены)
- 2026-06-16 23:55 MSK | #31 ротация 32 mod 5 = 2 (SPA-калькулятор, закрыт ЗДОРОВЫМ run #8) + H1/tgbot заблокированы PROPOSAL → НЕ долбил BS-31 3-й раз и НЕ шипил рискованную часть вслепую, а доставил БЕЗОПАСНУЮ ПОЛОВИНУ approved-proposal P-ENTITYJSON (detection-only). | api (messages.py guard-chain), деплой api+worker (НЕ base-rebuild) | дизайн n/a (не генерил — code+test+deploy фикс доминирующего failure-mode) | BS-31 = ДОМИНИРУЮЩИЙ failure-mode (2/2 реальных CRM-гена DOA на центральной Client.json, runs #28+#30); слепая зона «нет сигнала НИГДЕ»: `_normalize_entity_filenames` молча пропускает непарсящийся entity-JSON (messages.py:1227-1228), runtime `loadEntity` молча нуллит (registry.ts:87-89) → 404 unknown entity, билд=SUCCESS | **FIX e3c97ad**: `_warn_unparseable_entity_json(files)` логирует `[PP] entity_json_INVALID: <name>.json does not parse (...)` для каждого битого `entities/*.json` сразу после filename-guard (gated CONTAINER_NEXT); чисто аддитивно (не мутирует files, не кидает try/except, конвенция `[PP]`, локальный `import json as _json`). Убрана МОЛЧАЛИВОСТЬ build-лога — доминирующий failure-mode диагностируем в момент возникновения; repair-ACTION (micro-regen/visible-fail/drop-entity) ОСТАЁТСЯ deferred (детерм. repair невозможен — corruption съел контент; policy-смежно rule 15). VERIFY: pytest test_entity_json_validity.py 3 green + 1 strict-xfail (build-time ГАРАНТИЯ остаётся красной — detection≠guarantee); test_invalid_entity_json_is_flagged_in_build_log на ТОЧНОМ live #28-payload `_BROKEN_CLIENT`→bad==['Client'], Deal не флагается; module imports clean, diff 60 insert/0 del; push e3c97ad→main, prod ff-merge, compose up --build api worker, deployed-grep worker=2/api=1, оба контейнера Up clean boot (api «Application startup complete» / worker «Listening», 0 traceback), api internal /health 200. Evidence `_routine/runs/2026-06-16T20-49Z/00_PROOF.md` | PASS (safe detection-half доминирующего failure-mode доставлена+доказана end-to-end; BS-31 ОСТАЁТСЯ CONFIRMED — UX-DOA не починен, только наблюдаемость; max 1 фикс/прогон; рискованная ACTION-половина НЕ шипнута вслепую)
- 2026-06-17 00:20 MSK | #32 ротация 33 mod 5 = 3 (tgbot, заблокирован PROPOSAL P-BS5) + SPA-calc закрыт ЗДОРОВЫМ run #8 + H1 заблокирован + НЕ долбил BS-31 3-й раз → взял НОВЫЙ never-probed функц-угол: ЧИСЛОВЫЕ ОГРАНИЧЕНИЯ ЦЕЛОСТНОСТИ (раньше мерили required/type, relation, clear, pagination, ref-integrity, search, sort, date-cast, export, lost-update, KPI-count, unique — но НЕ что движок принимает как ЗНАЧЕНИЕ числа). РЕАЛЬНАЯ live-gen: dogfood-sklad-uchet-a086b4, mint-token owner 2aa9b1a2, blank→nextjs_entities (BS-4 эскалация ✓), freeform+art-director/deepseek-v4-pro-thinking/preset=retail-product/cost_rub 4.27; Product (name/article/price/quantity/category/updatedAt), (app)-роуты /dashboard /products /settings; entity JSON ЧИСТЫЙ (БЕЗ entity_json_INVALID — BS-31 НЕ выстрелил этот прогон) | nextjs_entities / freeform(art-director entity) / deepseek-v4-pro-thinking | дизайн ~6/10 (storefront-лендинг чистая иерархия+StorefrontHero/FeatureCard, --primary бирюза applied, но фейк-стат «0 ошибок/3 сек/24-7», без картинок/ниши) | **NEW BS-33 (CONFIRMED live)**: number-поле БЕЗ vocabulary ограничений — authed POST `{price:-50000,quantity:-30}`→201 и `{price:1999.999,quantity:2.5}`→201, сервер сортирует -30 первым как валидную; `z.number()` голый (registry.ts:130), FieldDef без min/max/integer (registry.ts:34-43), Input без min/step (entity-form.tsx:246-256); downstream дашборд totalValue минус×минус = +1.5M фантом-актив | НЕ шипить вслепую — P-NUMRANGE (мульти-поверхностный template + writer-prompt quality-sensitive «min для price/qty НЕ для balance/delta» + base-rebuild); acceptance-lock test_entity_number_constraints_gap.py (3 green + 1 xfail); evidence `_routine/runs/2026-06-16T21-00Z/` | FAIL (движок принимает невозможный сток как валидный) — ПОЗИТИВ: BS-4 эскалация + чистый entity JSON + Auth.js v5 + клиентская required-валидация (entity-form.tsx:149-167 inline-errors+focus) рабочие; dev-контейнер+проект подлежат очистке
- 2026-06-17 00:33 MSK | #33 ротация 33 mod 5 = 3 (tgbot, заблокирован PROPOSAL P-BS5) + SPA-calc закрыт ЗДОРОВЫМ run #8 + H1 заблокирован + BS-31 не долбил 3-й раз → взял НОВЫЙ never-probed функц-угол: ФИЛЬТР/СЕГМЕНТ windowing (раньше мерили search #20, sort #21, export #24, KPI #26, pagination #18 — но НЕ что происходит с быстрым фильтром/канбан-колонкой при >50 записях). Code-path proof (live-gen не жёг: дефект латентен на seeder-масштабе, а writer-prescription SYSTEM_PROMPT.md:104-109 + real run #28 уже доказывают, что путь в проде). | n/a (source code-path, windowing-семья — стандарт доказательства как у P-SORT/P-EXPORT/P-KPICOUNT) | дизайн n/a (не генерил) | BS-34: фильтр/сегмент списка работает CLIENT-SIDE по загруженному ≤50-окну (use-entity.ts ОДИН list() без пагинации → engine DEFAULT_LIMIT=50 → data-table.tsx:281-291 segmented.filter(rows) + board-view.tsx:201-211 grouped по окну; crud-resource.tsx:177-184 mergedParams шлёт только expand, фильтр не уходит на сервер); engine.ts:178-183 УЖЕ умеет server-фильтр → фикс достижим. Болезненнее всего для канбана (предписанный default pipeline-CRM, run #28) | **PROPOSAL P-FILTERWINDOW** (фикс архитектурно-связный на каждом сгенерённом аппе → не шипить вслепую, как сиблинги P-SORT/P-SEARCH/P-EXPORT/P-KPICOUNT) + acceptance-lock test_filter_windowing_gap.py (5 green + 1 xfail) | PASS (новая слепая зона code-proven + locked, фикс-направление доказано достижимым server-side)
- 2026-06-17 00:49 MSK | #34 ротация 34 mod 5 = 4 (free / РЕАЛЬНЫЙ prod-промпт) | H1/tgbot заблокированы PROPOSAL, SPA-calc здоров (#8), BS-31 не долбил → взял РЕАЛЬНЫЙ prod-промпт verbatim из live debug-dump (`/tmp/omnia-pp/43ef732e…`): **СКЛАДСКОЙ УЧЁТ / inventory** — ниша, которую ledger НИКОГДА не трогал (прошлые real-промпты = CRM/booking/calc). РЕАЛЬНАЯ live-gen (~4.3₽) → slug dogfood-sklad-uchet-f7caf1, route `freeform / preset retail-product / nextjs_entities / deepseek-v4-pro-thinking`. | stack nextjs_entities (корректно эскалировал), gen_mode=freeform | дизайн **5.5/10** (landing.png: чистая иерархия + serif-цифры, но МОНОХРОМ — ноль акцента вопреки brief PRIMARY #14B8A6 + «синий акцент»; title=«dogfood sklad uchet» не бренд «Склад.Контроль») | **НЕТ НОВОЙ слепой зоны — генератор отработал реальный inventory-запрос ХОРОШО**: entities/Product.json корректен (**enum категория с options + number price/quantity required** — опроверг мою pre-gen гипотезу про free-text), CrudResource с category filterTabs + domain-aware low-stock badge (q<5/≤15/else, варианты+токены реально в шаблоне) + select-dropdown форма; categories page = клиент-rollup (не фантом-сущность → не DOA); все routes 200/compile clean; `GET /api/entities/Product`→401 (live, НЕ DOA), control→404. Наблюдаемые дефекты ВСЕ известны: BS-6b (моно-лендинг, palette-guard chars=0 на .tsx), BS-29 (brand/title desync), BS-26 (category-rollup Σ по ≤50), BS-34 (category filter по окну). | **none** (нет нового clear+safe+proven дефекта; BS-6b/BS-26 — proposal-class, крупнее одной строки → не шипить вслепую; реал-inventory-репро добавлен как corroborating evidence к BS-26/BS-34) | PASS (healthy-niche confirmation: inventory-ниша зрелая, no new blind spot, no fix)
- 2026-06-17 01:06 MSK | #35 ротация 35 mod 5 = 0 (H1, заблокирован PROPOSAL P-H1) → ротация; SPA-calc здоров (#8), tgbot блок (P-BS5), BS-31 не долбил 3-й раз. Реальная live-gen была бы 3-й подряд CRM-класс (→ повтор BS-31/BS-6b, не ново), поэтому взял НОВУЮ never-ledgered ось через чтение кода entity-движка: **МИСХЭНДЛИНГ КОНСТРЕЙНТ-ПРИМИТИВОВ `required`/`default`** (validation-семья BS-19/22/27/33 покрывала ОТСУТСТВУЮЩИЕ примитивы; никто не проверял, верно ли движок применяет те, что ЕСТЬ). | метод: faithful-proof — `node --experimental-strip-types` на DEPLOYED base omnia-template-nextjs-entities:dev импортируя РЕАЛЬНЫЙ registry.ts createSchema/applyDefaults + реплей engine.createRecord порядка parse→default (стандарт runs #19/#22/#33, без LLM/без gen/без денег) | дизайн n/a (не рендерил) · ФУНКЦ FAIL→FIXED (facet 1) | **NEW BS-35**: поле `required:true`+`default` НЕВЫПОЛНИМО при пропуске — createSchema делал required non-optional, safeParse 400-ил «Required» ДО applyDefaults → дефолт МЁРТВ на ровно той омиссии, ради которой существует (engine.ts:223-227 + registry.ts:170). Доказано на РЕАЛЬНОМ коде: A required+default OMITTED→**400**, B same required:false→201+default (control: дефолт работает только не-required), C/D required string ""/«   »→201 (facet 2: required=presence-only). **FIX a92f73d** (purely-permissive `requiredInPayload = f.required && f.default === undefined` → только 400→201-with-default, не может зарегрессить): base пересобран на проде, verify на rebuilt image БЕЗ bind-mount — A→**201 status:"new"**, F required+default='bogus'→**400 Invalid enum** (value-валидация цела), G required-no-default OMIT→**400 Required** (default-less hard-required). Facet 2 (required string accepts ""/whitespace) → PROPOSAL (ужесточение = смена write-семантики всех аппов, ср. BS-33) | **fix a92f73d** (facet 1) + test_entity_required_default_semantics.py (5 green + 1 strict-xfail facet 2); evidence `_routine/runs/2026-06-17T00-53Z/` | PASS (новая слепая зона в never-probed «движок ломает имеющиеся примитивы»-оси: FAIL доказан на DEPLOYED-коде faithful-replay, фикс ДОСТАВЛЕН+пересобран base+проверен на rebuilt image end-to-end 400→201 с controls F/G цельными, заперт тестом; throwaway всё --rm, prod tmp очищен)
- 2026-06-17 01:19 MSK | #36 ротация 36 mod 5 = 1 (CRM с нуля) → но 3-я подряд CRM-класс live-gen лишь повторила бы BS-31/BS-6b (не ново), H1/tgbot заблокированы PROPOSAL, SPA-calc здоров (#8) → взял НОВУЮ never-ledgered ось через чтение кода entity-движка (метод faithful-proof, стандарт #19/#22/#33/#35, без LLM/без gen/без денег): **`default` ПРИМЕНЯЕТСЯ БЕЗ ВАЛИДАЦИИ** (BS-35 покрывал required+default deadlock + required-string-emptiness; никто не проверял, валиден ли САМ дефолт по типу/enum поля). | метод: `node --experimental-strip-types` на DEPLOYED base omnia-template-nextjs-entities:dev (869d31ffd20e) импортируя РЕАЛЬНЫЙ registry.ts createSchema/applyDefaults + реплей createRecord parse→default | дизайн n/a (не рендерил) · ФУНКЦ FAIL→FIXED | **NEW BS-36**: applyDefaults инжектил `f.default` без проверки → enum-дефолт не-из-options / нечисло-в-number / строка-в-boolean молча садились на пропуске; number-вектор компаундит BS-22 (нечисло → 500 numeric-сорта, ::numeric без safe-cast). Доказано на РЕАЛЬНОМ коде: A enum'pending' OMIT→**201 status:"pending"**, C number"none"→**201 qty:"none"**, E bool"yes"→**201 inStock:"yes"**; controls B/D валидный дефолт→201 ок, F provided'pending'→**400** (движок 400-ит то, что сам пишет). **FIX 8745c31** (`zodForField(f).safeParse(f.default).success` gate, пусто-пермиссивен): base пересобран, verify на rebuilt image БЕЗ bind-mount — A/C/E→201 БЕЗ битого поля, B/D→201 с дефолтом, F→400. | **fix 8745c31** + test_entity_default_validation.py (6 green); BS-35 test обновлён под новую форму applyDefaults; evidence `_routine/runs/2026-06-16T22-13Z/` | PASS (новая слепая зона «движок доверяет своему дефолту мимо валидации, что проходит каждое provided-значение»: FAIL доказан на DEPLOYED-коде faithful-replay, фикс ДОСТАВЛЕН+base пересобран+проверен на rebuilt image end-to-end с целыми controls, заперт тестом; throwaway всё --rm, prod tmp очищен)
- 2026-06-17 01:25 MSK | #37 ротация 37 mod 5 = 2 (SPA-tool, закрыт ЗДОРОВЫМ #8) → H1/tgbot заблокированы PROPOSAL, BS-31 не долбил → после streak #32-#36 (всё faithful-proof движка) взял РЕАЛЬНУЮ live-gen как живой юзер на СВЕЖЕЙ never-tried design-forward нише (студия архитектуры/интерьеров) чтобы прощупать DESIGN-ось (под-probed в последних прогонах): dogfood-arch-studio-kontur-b29604, mint-token owner 2aa9b1a2, skip_clarify=true | freeform / template=blank / preset=studio-showreel / deepseek-v4-pro-thinking · cost_rub 9.04 passes 2 · ui_audit 8/10 (typography_count + 4 button-heights) · acceptance struct+resp+vision True · image_resolver 10/10 | дизайн **~8.5/10** (01_landing.png hero: ТЁМНЫЙ luxe-палитра — НЕ BS-6b моно-grey, Playfair-serif hero, AI-картинки портфолио 10/10, before/after compare-слайдер, scroll-анимации, ниша-уместно; **подтверждает: freeform+art-director+landing-section-kit путь ЗДОРОВ и даёт WOW** — BS-6b моно-проблема СПЕЦИФИЧНА для entity .tsx-лендингов, не freeform; минус: ui_audit typography 8 размеров/3 веса + фабрик-стат «12 лет» vs «15 лет») | **NEW BS-37**: явный «контакты с картой» → ФАЛЬШ-плейсхолдер `<div>«Интерактивная карта»` (03_writer_raw.html:368-371, 0×iframe/maps/yandex во всём HTML); корень — 98KB freeform-system-prompt БЕЗ map/iframe-директивы (grep 0), а AD-бриф ЯВНО прописал карту (brief:182); keyless map-iframe бесплатен — возможность есть, директивы нет; класс = любой локальный бизнес «сайт с картой». Re-confirmed на свежей нише: **BS-15** (consultation lead-форма preventDefault→reset→success, 0 action/fetch/mailto → заявка в никуда) + **BS-30** (футер «Политика конфиденциальности»/«Условия»→#hero, фабрик-телефон/соц/«15 минут»). **BS-29 НЕ репро** (freeform `<title>«Контур…»` == hero-бренд «Контур» → BS-29 ENTITY-апп-специфичен, share_meta-путь) | **none** (BS-37 фикс = writer-prompt-quality + fabricated-address-sensitive + regen-verify → PROPOSAL P-MAP, не вслепую; остальное — уже-proposals/fuzzy; max 1 фикс/прогон) | PASS (DESIGN ЗДОРОВ — freeform WOW 8.5/10, refute BS-6b-для-freeform; NEW BS-37 code-proven+locked в PROPOSAL P-MAP; BS-15/BS-30 re-confirmed свежая ниша; BS-29 refute для freeform; live-gen ВОССТАНОВЛЕН — реальная сборка прошла, wallet healthy 9.04₽; sandbox-проект очищен)
- 2026-06-17 02:19 MSK | #38 never-probed ось (follow-up EDIT-путь на УЖЕ рабочем entity-аппе; ротация 38 mod 5 = 3 tgbot заблокирован P-BS5 → взят новый угол ПРЯМО по жалобе владельца «кинул 5 промптов — генератор ничего не делал/ломал»). [Цикл исследования выполнен предыдущей dead-сессией 02:13 MSK — live-gen, evidence, LEDGER, acceptance-lock; сессия умерла ДО commit/RUNS-записи/снятия lock (stale-lock 22:42Z, age 34м). Этот прогон ДОВЁЛ доставку: добавил PROPOSAL P-TSXEDIT-VALIDATE, RUNS-строку, прогнал тест (2 passed + 1 xfail ✓), commit+push, снял lock. Свежий expensive live-gen НЕ переделывал — был бы дубль.] | 1-й билд: blank→**nextjs_entities** (BS-4 эскалация LIVE ✓) / freeform+art-director / deepseek-v4-pro-thinking · preset=local-services · cost_rub 4.65 · 11 файлов, все routes 200/✓Compiled (рабочий CRM clients/appointments/notes/dashboard). FOLLOW-UP: mode=edit/surgical=True/deepseek-chat/gen_mode=plain/cost 0.21/**hot_reload OK written=3** | дизайн n/a (баг функциональный, не визуальный) · ФУНКЦ **СЛОМАН ЖЁСТКО**: рабочий апп 200→**ВСЕ 6 routes 500** от ОДНОЙ дешёвой правки | **NEW BS-38**: follow-up расширения модели данных → CHEAP surgical-edit → битый .tsx без syntax/compile-валидации перед commit+hot_reload → `services/page.tsx` незакрытый `columns={[` + `(app)/layout.tsx` побитый `hrefdashboard/...` (layout оборачивает всю группу) → весь апп 500, build рапортует SUCCESS. Корень intent_triage.py:62-69 + prompt_builder.py:4005-4016 + messages.py:763,2724,3227 (нет .tsx-гейта, NO_NODE). Семья «ложного успеха» BS-12/BS-28/BS-31, но провал ЖЁСТКИЙ | PROPOSAL P-TSXEDIT-VALIDATE (реальный parser-гейт контейнерных .tsx перед commit + revert-to-last-good + regen-verify; Python-баланс-скобок FP-опасен и не ловит attribute-corruption → не шипить флаки-полугейт) + acceptance-lock test_container_edit_tsx_validity.py (2 green: routing→CHEAP + 2 corruption-сигнатуры; 1 strict-xfail: build-time gate) — прогнан локально 2 passed/1 xfailed ✓ | PASS (новая never-ledgered ось «follow-up bricks working container app»: доказана РЕАЛЬНОЙ live-gen + live 6×500 на DEPLOYED dev-контейнере + 2 corruption-сигнатуры + screenshot, локализована file:line, заперта тестом; фикс мульти-поверхностный/NO_NODE → PROPOSAL, не вслепую; контейнер+проект удалены прошлой сессией)
- 2026-06-17 02:34 MSK | skipped: sequential-lock held by concurrent run (#39, lock 2026-06-16T23:21:35Z ≈ 13 min old < 15 min threshold) — yielded per ШАГ 0 guard 3; did not touch lock
- 2026-06-17 03:07 MSK | #39 ротация 39 mod 5 = 4 (free / РЕАЛЬНЫЙ prod-промпт). H1/tgbot заблокированы PROPOSAL, SPA-calc здоров (#8), BS-31 не долбил → взял реальный prod-промпт. ВЗЯЛ «школу №7» (тот же промпт что run #7) → BS-9/P-RBAC уже покрыт, НЕ переоткрывал; вместо этого пробил НОВУЮ ось, которую run #7 пропустил: РЕНДЕРИНГ РАСПИСАНИЯ. Live-gen dogfood-shkola-7-f4cae4 (mint-token owner 2aa9b1a2) | blank→nextjs_entities (BS-4 эскалация ✓) / freeform art-director / deepseek-v4-pro-thinking · preset=education-bright · cost_rub 5.5682 · 8 сущностей (User/Grade/Schedule/Homework/News/Class/Subject/StudentProfile) + 5 app-routes, snapshot 3a67fb03 | дизайн n/a (рендер не снимал — фокус на функц) · ФУНКЦ FAIL→FIXED | **NEW BS-39**: writer выписал `schedule/page.tsx` `view="calendar" dateField="dayOfWeek"`, но dayOfWeek=enum[monday..saturday] (НЕ дата) → `parseLocalDate("monday")`=null → все 12 уроков `continue`-скипнуты из placed[], noRecords=rawCount===0 false → EmptyState тоже не рисуется → ПУСТАЯ сетка недели без объяснения (репро node на РЕАЛЬНОЙ parseLocalDate: 12 уроков→0 placed). table-фолбэк был только при ОТСУТСТВУЮЩЕМ dateField. Попутно BS-9 переподтверждён вживую (Grade/Homework access="owner" → ученик/родитель видят 0 оценок; User access="public" → anon читает ФИО/телефоны) — НЕ переоткрывал, P-RBAC | **FIX 58aef74**: CrudResource `calendarPlaceable` — view=calendar с загруженными строками но 0 placeable → DataTable (writer уже дал columns); parseLocalDate экспортирован; чистый рендер-фолбэк, generalises на любой misprescribed-календарь (enum/missing/malformed); tsc --noEmit clean; base omnia-template-nextjs-entities:dev пересобран на проде + verified (grep calendarPlaceable×2, export parseLocalDate×1); проект+контейнеры удалены; acceptance-lock test_calendar_unplaceable_fallback.py 3 green | PASS (НОВАЯ слепая зона в never-probed «calendar/recurring-schedule rendering»-оси: func FAIL доказан code-trace 3 поверхности + node-репро на РЕАЛЬНОЙ parseLocalDate (12→0 placed) + локализован file:line; малый безопасный фикс доставлен и доказан на DEPLOYED base-образе; BS-9 не переоткрывал т.к. run #7+P-RBAC покрывают)
- 2026-06-17 03:36 MSK | #40 ротация 40 mod 5 = 0 (H1, заблокирован PROPOSAL P-H1) → след.; entity-CRUD оси насыщены (BS-16..39) → взят СВЕЖИЙ design/freeform-архетип, ни разу не проб: портфолио фотографа с галереей (тест: галерея = реальные фото или фальш-плейсхолдеры, отлично от BS-37-карты). Live-gen dogfood-foto-portfolio-40-1ed311 (mint-token owner 2aa9b1a2), skip_clarify=true «just generate» | gen_mode=**freeform** (✓ корректно — портфолио без auth/БД, НЕ переэскалировал в entities) / preset=studio-showreel бренд «VERESK» / deepseek-v4-pro-thinking · art-director ran · cost_rub 8.47 · `image_resolver resolved=8/8` · dead_links 3→0 inline · ui_audit 7/10 [typography_count, interactive_sizes, icon_family] | дизайн **≈8/10** — сильно: кинематографичный full-bleed герой-портрет точно по брифу (deep shadow/navy/85mm/grain), редакторская типографика, нишевая палитра; проблемы: 3 ui_audit-фейла + латентный риск «3 тёмных слоя» в герое (тёмное фото + near-black bg + затемняющий оверлей = верх мутный) · ФУНКЦ: галерея **РЕАЛЬНА** (8/8 фото, 200 ИЗВНЕ) → гипотеза «фальш-галерея» РЕФУТ для freeform; НО снимок-thumbnail продукта показал ПУСТОЙ чёрный герой | **NEW BS-40**: preview-воркер скриншотит freeform через `file://`, фото запечены ПУБЛИЧНЫМ MinIO-URL (image_resolver.py:357), а контейнер НЕ достаёт публичный хост (hairpin: worker→public=http=000, →minio:9000=200/9ms) → chromium не красит фото → thumbnail И дизайн-судья видят без-картиночную страницу, хотя живой сайт ок; `_IMAGE_WAIT_MS=3000` (preview.py:70) — пластырь, не лечит недостижимый URL (коммент мисдиагнозит «медленные фото»). Радиус: все freeform с реальными `<img data-omnia-gen>` | FIX **c8d4160** `_rewrite_minio_to_internal` (репойнт public→`minio:9000` только в in-memory копии для chromium, обе ветки capture()/_render_async; отдаваемые файлы хранят публичный URL → юзеры не затронуты; 3 теста 14/14 green, ruff clean) → deploy api+worker, health 200 → **ДОКАЗАНО** re-render на НОВОМ воркере: thumbnail 118KB→712KB, герой-портрет рендерится (thumb_AFTER_fix.png) | PASS (новая never-ledgered ось «product's own eye is blind to its own images»: доказана РЕАЛЬНОЙ live-gen + worker-curl 000-vs-200 + before/after thumbnails; фикс малый/безопасный/доказан end-to-end; freeform = без контейнера, чисто)
- 2026-06-17 04:02 MSK | #41 ротация 41 mod 5 = 1 (CRM с нуля) НАСЫЩЕН (BS-31/BS-6b, runs #30/#36) → взят СВЕЖИЙ never-probed архетип: **БЛОГ** (один из 3 заявленных стартовых шаблонов MVP «простой блог», ни разу не догфуден). Промпт «блог о путешествиях, несколько статей с фото, каждую можно открыть на отдельной странице», skip_clarify=true. Live-gen dogfood-travelblog-41-57bde6 (mint-token owner 2aa9b1a2) | gen_mode=**freeform** (НЕ переэскалировал в entities — корректно для контент-сайта) / preset=hospitality-escape / deepseek-v4-pro-thinking · cost 9.51₽ · passes 2 · `image_resolver resolved=13/13` · ui_audit 9/10 [typography_count] · acceptance score=9 passed=False (struct=False из-за dead-links) но шипнут (acceptance_score_only) | дизайн n/a (фокус функц) · ФУНКЦ: writer выдал РЕАЛЬНЫЙ мультистраничный блог (index.html + 3 статьи iceland/japan/morocco + assets), навигация на статьи РАБОЧАЯ → гипотеза «блог=одностраничная статика с мёртвыми Читать-далее» ЧАСТИЧНО рефут (статьи реальны), НО `[PP] dead_links initial=28 after_inline=28` — **0 из 28 починено** | **NEW BS-41**: `repair_dead_links_inline` = ПОЛНЫЙ no-op на контент/редакционных сайтах — fallback-словарь `_REPAIR_PREFERRED_TARGETS` зашит под витрины (contacts/cta/order/...), а AD назвал секции `hero/archive/manifesto/subscribe` → `_pick_repair_target`→None → файл без изменений → все плейсхолдеры шипятся (теги ИСЛАНДИЯ/ВУЛКАНЫ, футер Политика/TELEGRAM). Корень link_validator.py:55,106-108 | **FIX 4ab0ff9**: `_pick_dead_link_target(ids,content)` (только полностраничный пасс; edit-orphan фиксер не тронут) всегда даёт цель: CTA → первая nav-referenced секция страницы → `#top`. ДОКАЗАНО на РЕАЛЬНЫХ файлах run #41 28→0 (index→`#hero`, статья→`#top`) + на DEPLOYED prod-бинаре api content-fixture 2→0; deploy api+worker, health 200 ИЗВНЕ; 12/12 test_link_validator green (+4 новых) | PASS (новая never-ledgered ось «dead-link repair niche-blind to content sites»: func FAIL доказан live-логом 28→28 + source file:line + репро на РЕАЛЬНЫХ сген. файлах; малый безопасный детерм фикс доставлен и доказан end-to-end на DEPLOYED бинаре; проект удалён, контейнеров нет)
- 2026-06-17 04:30 MSK | #42 ротация 42 mod 5 = 2 (SPA-tool, закрыт ЗДОРОВЫМ #8) → H1/tgbot заблокированы PROPOSAL; вместо насыщенных осей взял НИКОГДА-не-догфуженную ФЛАГМАНСКУЮ фичу — «вернуться назад»/«откат за 1 sec» (headline-обещание владельца «кнопка вернуться назад для каждого промпта»). | 1-й билд: blank→**nextjs_entities** (BS-4 эскалация LIVE OK) / freeform+art-director / deepseek-v4-pro-thinking · рабочий CRM автосервиса (clients/appointments/notes/dashboard). FOLLOW-UP edit: surgical/deepseek-chat/hot_reload OK. ROLLBACK: POST /rollback к снапу state A | дизайн n/a (баг функциональный) · ФУНКЦ **СЛОМАН**: откат контейнерного аппа = NO-OP на живом превью — git ревертит к A, контейнер `omnia-dev-<slug>` продолжает отдавать B (git-снап «Управляйте ремзоной без хаоса» vs живой контейнер «ДОГФУД-Б-МАРКЕР», 200 OK без hot_reload) | **NEW BS-42** + **FIXED e39931a**: rollback.py на `_CONTAINER_NEXT` теперь `read_files(new_sha)`+`hot_reload(slug,files)` best-effort (паритет style_patch.py:209); задеплоено api/worker; live-verify на DEPLOYED-бинаре: контейнер B→A на повторном откате; acceptance-lock test_rollback_container_hot_reload.py 8 passed +1 strict-xfail | PASS (новая never-ledgered флагман-ось «rollback no-op на контейнерном превью»: доказана РЕАЛЬНОЙ live-gen + git↔контейнер дивергенцией + api-логом без hot_reload; фикс ясный/малый/паритетный → доставлен и ПРОВЕРЕН на деплое B→A; контейнер удалён)
- 2026-06-17 05:10 MSK | #44 ротация 44 mod 5 = 4 (free / РЕАЛЬНЫЙ prod-промпт). H1/tgbot заблокированы PROPOSAL, SPA-calc здоров (#8), CRM-class насыщен (BS-31/BS-6b) → взят свежий prod-промпт из логов прода: **«Сделай генератор QR-кодов: ссылка/текст→QR, скачать PNG, выбор размера»** (project 8dd41e42 — НАСТОЯЩИЙ юзер, НЕ sandbox). Свежую expensive live-gen НЕ запускал — этот реальный prod-проект даёт более сильный evidence, чем дубль dogfood-гена (run #43 уже сделал полную live-gen QR-репро вчера); re-генерация была бы чистым дублем закрытой-как-PROPOSAL оси. | server (_route.md): gen_mode=**freeform** (✓ корректно) / preset=local-services / deepseek-v4-pro-thinking / template=blank / use_art_director_freeform=True | дизайн n/a (тот же лендинг-класс, что #43 ≈8/10; провал функциональный) · ФУНКЦ **СЛОМАН НАСМЕРТЬ, у живого юзера**: `08_post_palette_guard.html:19` `<script src=qrcode@1.5.3/build/qrcode.min.js>` (re-probe **404**) + `:336` `QRCode.toCanvas` → `QRCode undefined` → пустой canvas → QR-инструмент DOA в любом браузере, шипнут реальному пользователю | **BS-43 CONFIRMED на РЕАЛЬНОМ ПРОДЕ** (не догфуд-артефакт): идентичный мёртвый URL + call-site, что в run #43. Re-verify naive-URL-swap не сработает: GH-build node-qrcode тоже 404 (build/ gitignored), jsdelivr flat = только lib/+helper/; резолвящиеся QR-libы (qrcodejs/qrious/qrcode-generator) имеют ДРУГОЙ API ≠ `QRCode.toCanvas`. Фикс (P-CDNDEAD план A: захостить QR-UMD как kit-ассет + prompt-директива) мульти-поверхностный/LLM-compliance-leg-неверифицируем-за-1-прогон → НЕ шипнут вслепую | **action: PROPOSAL P-CDNDEAD промоутнут реал-прод-evidence + acceptance-lock `apps/api/tests/test_freeform_qr_library_resolves.py` (3 green + 1 strict-xfail, прогнан локально ✓), docs-only commit** | PASS (high-value: перевёл BS-43 из dogfood-only в CONFIRMED-prod-impacting на живом юзер-проекте — evidence для гринлайта рискованного мульти-поверхностного фикса; локализован file:line; заперт тестом; фикс рискованный → PROPOSAL, не вслепую; чужой prod-проект НЕ тронут — только read-only греп дампа+CDN-проба; sandbox не создавался)
- 2026-06-17 05:43 MSK | #45 ротация 45 mod 5 = 0 (H1, заблокирован PROPOSAL P-H1) → след.; CRM-class насыщен, SPA-calc здоров (#8), tgbot блок (P-BS5) → взята НИКОГДА-не-пробованная ось: **МОБИЛЬНАЯ НАВИГАЦИЯ + freeform-ИНТЕРАКТИВНОСТЬ end-to-end** (прежние runs мерили дизайн/контент на desktop, но НЕ проверяли, ЖИВЫ ли бургер/FAQ-аккордеон в реальном браузере на телефоне). Live-gen: «Лендинг для фитнес-студии PULSE: расписание, тарифы, отзывы, FAQ, форма записи на пробное; мобильная версия обязательна» (sandbox dogfood-fit-studio-1df6f7, project e60c7e47) | server: gen_mode=**freeform** / template=blank / deepseek-v4-pro-thinking / niche=фитнес-спорт | design **~8/10** (премиум тёмная палитра под нишу, сильная иерархия/типографика, hero «Разогрей свой внутренний пульс»; секции наполнены реальным контентом — тариф START 4 900 ₽/мес, 7 отзыв-блоков, 6 направлений) · ФУНКЦ **HEALTHY (PASS)**: бургер `#menu-toggle`↔`#mobile-menu` тоггл доказан (hidden True→False, is_visible True); FAQ `.faq-question` тоггл `.faq-item.is-open` (max-height 0→600px, oh 20→72); kit-ассеты на `/p/<slug>/assets/` все 200 + `<base href>` инжектится; live `/p/` 200 (76930B) | **НОВАЯ ось вернулась ЗЕЛЁНОЙ** — закрыт вопрос «не DOA ли мобильный freeform-UX»: бургер+аккордеон реально работают, ассеты сервятся, контент наполнен (не theater, кроме известной lead-формы BS-15). Единственный дефект — косметика: закрытый FAQ-ответ оставляет ~20px паддинг-полоску (kit max-height:0 не зануляет writer-pb-5) → **NEW BS-44 / PROPOSAL P-FAQPAD** (low-sev; шаренный kit-ассет, восстановление паддинга хрупко → не blind-ship) | **action: PROPOSAL P-FAQPAD + docs-only commit; sandbox=static (контейнер не создавался, удалять нечего)** | PASS (доказал функционально, не на глаз: бургер/FAQ тоггл + ассет-200 замерены в headless Playwright против внутреннего api:8000; артефакт anime.js-reveal на скрине отличён от реального контент-гэпа через греп HTML; один новый low-sev BS зафиксирован как PROPOSAL, не шипнут вслепую; прод не тронут)
- 2026-06-17 06:07 MSK | #46 ротация 46 mod 5 = 1 (CRM-web-app) → CRM/entity-класс насыщен (BS-16…BS-36, BS-6b), H1/tgbot блок (P-H1/P-BS5), SPA-calc здоров (#8) → взята ось-расширение run #45: **МУЛЬТИ-ВИДЖЕТНАЯ freeform-ИНТЕРАКТИВНОСТЬ на SaaS-лендинге — ДВА НИКОГДА-НЕ-ПРОБОВАННЫХ виджета: ВКЛАДКИ (tabs) + ПЕРЕКЛЮЧАТЕЛЬ ТАРИФОВ Месяц/Год с пересчётом цен** (#45 покрыл бургер+FAQ; калькулятор #8; QR #43 — но tabs и pricing-toggle-с-recompute не проверялись НИКОГДА). Live-gen: «Лендинг для SaaS веб-аналитики «Графика»: hero, возможности в виде переключаемых вкладок (Дашборды/Отчёты/Интеграции), тарифы с переключателем Месяц/Год (цены пересчитываются), отзывы, FAQ, форма заявки на демо; мобильная обязательна» (sandbox dogfood-saas-widgets-e4ac66, project 09abcbaf) | server: gen_mode=**freeform** / template=blank / preset=saas-product / art_director=gemini-3.5-flash-high / writer=deepseek-v4-pro-thinking / cost_rub 9.05 / passes=2 / dead_links 3→0 inline / ui_audit 5/10 (typography_count,interactive_sizes,color_count,button_rules,icon_family) / acceptance verdict=broken score=6 resp=False (ЛОЖНО — мобайл здоров, см.ниже) | design **~7.5/10** (чистая SaaS-палитра bg #FAFAFA / accent #0EA5E9 sky / Instrument Serif+DM Sans; 10 наполненных секций реальным контентом — тарифы 990/2990₽+Индивидуально, 3 отзыва, 4 FAQ; мобайл single-column аккуратный; минус — ui_audit-флаги консистентности + generic feature-скриншоты) · ФУНКЦ **ВСЕ 3 ВИДЖЕТА HEALTHY (PASS), доказано в headless Playwright против внутр. api:8000/p/**: (1) **PRICING-TOGGLE** — `.price-month` vis[true,true]/`.price-year`[false,false] initial → клик «Год» → month[false,false]/year[true,true], видимые годовые цены **790₽ / 2 390₽** (реальная скидка 20% от 990/2990, НЕ театр — значения разные, предрендер+show/hide); (2) **TABS** — 3 кнопки с богатым текстом («Дашборды/Чистые графики…», «Отчёты/Автосводки в Telegram+Slack…», «Интеграции/Совместимость с CMS…»), панели тогглят display block↔none по data-tab, каждая = feature-скриншот (3 img-URL все **HTTP 200** валидный PNG 101K/94K/36K с этого Mac=внешний клиент); (3) **FAQ** — `.faq-item.is-open` тоггл false→true→false, `.faq-answer` offsetHeight 20→72→20 (рабочий аккордеон). Виджет-JS/CSS в шаренном `assets/omnia-kit.js`+`omnia-kit.css` (оба 200), НЕ inline — initial static-read «нет FAQ-хендлера» был ОПРОВЕРГНУТ после чтения внешнего kit | **НОВАЯ ось вернулась ЗЕЛЁНОЙ** — закрыт вопрос «живы ли tabs и pricing-toggle-с-recompute в freeform»: оба работают, цены пересчёта реальны (не fake). НИ ОДНОГО нового слепого пятна. **RE-CONFIRMED BS-44** (20px FAQ-padding sliver) на ВТОРОЙ независимой генерации в ДРУГОЙ нише → усилен PROPOSAL P-FAQPAD. Метод-нота: воркер не достаёт публичный MinIO/CDN (NAT-hairpin) → desktop-фуллпейдж скрин пустоват (reveal opacity:0 + img h=0) = известный BS-40-класс артефакт, НЕ реальный баг (img 200 извне, мобайл-скрин наполнен) | **action: none (фикс не нужен — ось здорова; новых багов нет); docs-only commit (BS-44 repro#2 + этот RUNS). sandbox=freeform static (контейнер не создавался — чистить нечего)** | PASS (доказал функционально, не на глаз: pricing-flip/tabs-display/FAQ-isopen замерены в Playwright + img-200 curl'ом извне; recompute-значения реальны; артефакт reveal/hairpin отличён от реального гэпа через HTML-греп+внешний curl; BS-44 воспроизведён кросс-нишево; прод не тронут, чужие проекты не тронуты)
- 2026-06-17 06:41 MSK | #47 ротация 47 mod 5 = 2 (SPA-tool, закрыт ЗДОРОВЫМ #8) → H1/tgbot блок (P-H1/P-BS5), SPA-calc здоров, freeform-show/hide-виджеты здоровы (#45/#46) → взята НИКОГДА-не-пробованная ось: **LIVE COUNTDOWN TIMER на промо-лендинге** (виджет на ОСНОВЕ ВРЕМЕНИ — нужен setInterval + реальный будущий дедлайн; куда вероятнее DOA, чем show/hide #45/#46). Live-gen: «Лендинг для распродажи "Чёрная пятница" магазина электроники TechnoBum: hero с ТАЙМЕРОМ ОБРАТНОГО ОТСЧЁТА до конца акции (дни/часы/мин/сек), сетка товаров со скидками (старая цена зачёркнута + новая), преимущества, форма подписки, FAQ; мобильная обязательна» (sandbox dogfood-promo-countdown-ed2649, project 4d778e5a) skip_clarify=true | server (_route.md): preset=retail-product / gen_mode=freeform-AD / writer=deepseek-v4-pro-thinking / template=**nextjs_entities** (discovery эскалировал промо-ЛЕНДИНГ → entity CRUD-апп, /signin=200). Писатель: `entities/Product.json` (access:public), `entities/Lead.json` (owner), `entities/Campaign.json` (**access:"admin"**, поля name/countdownEnd/isActive/promoCode) + `PublicLanding`, который `entities.Campaign.list({limit:1})` → `<CountdownChip end={campaign.countdownEnd}>` | design **~8/10** (премиум retail-палитра, сильная иерархия; товары + зачёркнутые цены смотрятся хорошо) · ФУНКЦ **СМЕШАННЫЙ**: ТОВАРЫ **HEALTHY** (Playwright hydrated: grid 6 карточек, 6 img, 6 strikethrough старых цен, 13×₽; «Товары скоро появятся» = лишь pre-hydration SSR-фолбэк, Product=public) — НО **ТАЙМЕР СЛОМАН/ОТСУТСТВУЕТ**: `timerDigits=false`, ровно ОДНА console-ошибка `401 Unauthorized`; публичная главная читает Campaign (access:admin) → **401** (`GET /api/entities/Campaign?limit=1 401 in 441ms` = вызов страницы) → `campaign`=null → `campaign?.countdownEnd ? <CountdownChip/> : null` рендерит ПУСТОТУ → таймера НЕТ для публики, хотя ЮЗЕР ЯВНО ЕГО ПРОСИЛ и КОПИРАЙТ страницы его рекламирует (hero «таймер уже тикает», FAQ «Таймер на сайте показывает точное время») | **NEW BS-45** (admin-writes/public-reads коллизия доступа) — корень: prompt_builder.py:1438 (public-read→public framed ТОЛЬКО под живой каталог, предупреждает лишь про owner) ⟂ prompt_builder.py:1538 (операционные данные оператора→admin); Campaign — ТРЕТИЙ непокрытый паттерн (admin пишет, public читает), ни одно правило не разрешает, ни один гейт не ловит публичный read admin/owner-сущности | **action: PROPOSAL P-PUBCFG** (фикс behavioral-prompt, проверяем только re-gen'ом / широкий blast / НАИВНЫЙ «сделай Campaign public» УТЕЧЁТ секретный promoCode → НЕ clear+safe → не шипить вслепую; в плане — детерминированный post-gen гейт: распарсить page.tsx на entities.X.list/get vs access сущности) + docs-only commit; sandbox-контейнер `omnia-dev-dogfood-promo-countdown-ed2649` удалён | PASS (новая ось дала CONFIRMED-баг high-value: явно-запрошенная флагман-фича промо-лендинга невидима публике, и страница про неё ВРЁТ; локализован file:line+root-collision; доказан функционально в Playwright — products-grid посчитан, 401 пойман в консоли+container-log, timerDigits замерен; фикс несёт риск утечки секрета → PROPOSAL, не вслепую; прод не тронут, чужие проекты не тронуты, тест-контейнер вычищен)
